# Citlali Beammap Handoff - 2026-05-22

## 2026-05-22 16:19 EDT

### Repositories

- `citlali`: `gw_dev`, pushed to `origin/gw_dev` at `3c98aab4 Add empirical template beammap calibration amplitudes`.
- `toltec_beammap`: dashboard/review tooling is being saved separately in that repo on `main`.

### Citlali Changes Saved

- Implemented empirical array-template calibration amplitudes in `include/citlali/core/engine/beammap.h`.
- Gaussian fit amplitude remains in `amp` for morphology/QC.
- New calibration amplitude is written as `cal_amp`.
- `flxscale` now uses `cal_amp` when empirical matching succeeds and falls back to Gaussian `amp` otherwise.
- APT and fit-QC outputs now include:
  - `cal_amp`
  - `cal_amp_method`
  - `template_amp`
  - `template_offset`
  - `template_resid_rms`
  - `template_npix`
  - `template_amp_over_fit_amp`
  - `cal_amp_over_fit_amp`
  - `map_peak_amp`
  - `map_peak_amp_over_fit_amp`
- Local verification passed:
  - `git diff --check`
  - `cmake -S . -B build`
  - `cmake --build build -j 4`
  - local binary after reconfigure reported `v4.0.0-366-g3c98aab4`.

### Current Data/Reduction State

- `redu42` is downloaded and uncompressed under `/Users/gwilson/work_toltec/local_data/beammaps/3c273/redu42`.
- `redu42` is the 6-iteration version inspected against `redu41`.
- Beammap QC dashboard is running locally at `http://127.0.0.1:8053` with:
  - current path: `/Users/gwilson/work_toltec/local_data/beammaps/3c273/redu42/152307/raw`
  - comparison path: `/Users/gwilson/work_toltec/local_data/beammaps/3c273/redu41/152307/raw`
  - raw TOD diagnostics enabled.

### Quick Redu42 Read

- `a1100`: good detectors increased from 2950 to 2959; median `a_fwhm` narrowed from 7.45 arcsec to 7.20 arcsec; median map S/N increased from 37.1 to 38.2.
- `a1400`: good detectors decreased from 1285 to 1279; FWHM largely unchanged; median map S/N slightly increased.
- `a2000`: good detectors decreased from 923 to 917; FWHM largely unchanged; median map S/N slightly decreased.
- `redu42` convergence summary:
  - `a1100`: 3000 / 3166 converged
  - `a1400`: 1197 / 1369 converged
  - `a2000`: 673 / 956 converged

### Next Checks

- Inspect `redu42` interactively for whether additional iterations improve beam shape without creating new edge/pathology failures.
- After the next Unity run with `3c98aab4`, verify that APT `flxscale` reflects `cal_amp` and compare `cal_amp_over_fit_amp` against the offline Python empirical-template audit.
- If empirical-template calibration behaves well, update downstream dashboard summaries to make `cal_amp` and `cal_amp_method` first-class calibration diagnostics.
