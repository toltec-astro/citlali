# Citlali Refactor Handoff - 2026-07-14

## Governing State

- Branch: `codex/structural-refactor`.
- Phase 2 config authority and provenance is active.
- Do not push; the user controls GitHub pushes and Unity work.
- Coadd implementation commit: `c2e053b3c`.
- Coadd point-acceptance commit: `0ba604e58`.

## Coadd Validation Closure

Point `redu46` and science final iteration `redu11` both embed
`v4.0.0-3450-gc2e053b3`. They close the disabled and enabled coadd gates.

Point `redu46` is exact against accepted `redu45`: 489/489 config leaves, all
13 complete product families including RTC/PTC timestreams, zero changed or
skipped records, zero serious log issues, and 53.804 seconds versus 56.176
seconds. Coadd provenance records requested/effective disabled, no execution,
no cardinality, and successful reduction completion; mapmaking independently
records no coadd.

Science `redu08` through `redu11` are the four fruit-loop iterations of one
run; only final `redu11` is the accepted candidate against final `redu07`.
Its 502-leaf config is exact, all 27 products are present with zero skips and
zero serious log issues, and the science-equivalence v2 gate accepts a maximum
map RMS-relative difference of `7.65e-14`. Coadd provenance records requested
and effective enabled, execution complete, three maps, six required logical
writes, and completed outputs. Those values exactly match mapmaking
provenance. Runtime is 719.154 seconds versus 709.597 seconds.

The validation ledger contains 33 valid records. Coadd authority/provenance is
complete. No Beammap run was required because point already covers the same
disabled-coadd path and science covers enabled coaddition.

## Noise Validation Closure

The bounded Phase 2 `noise-products` implementation is accepted on Unity. Its
six frozen paths have one direct typed reader, a requested/effective/realized
execution plan, and a one-way adapter into `omb`/`cmb`. Requested disabled-state
values are preserved while effective activation and count follow mapmaking.
The existing deterministic Boost MT19937 default is explicit as fixed internal
seed `5489`; this is provenance, not a new user knob. Wiener-filter dependency
policy consumes the effective noise accessor rather than the preserved request.

Successful CLI completion writes required atomic
`noise_products_provenance.yaml` using schema
`citlali-noise-products-provenance-v1`. It records final-iteration scientific-
map and realization cardinality, empirical-product count, realization-image
count, RNG identity, and output completion. The reduction auditor validates
the sidecar semantically and cross-checks it against mapmaking v2 provenance.
The frozen boundary audit rejects mirrored-reader, reverse-mutation, implicit-
seed, requested-policy-read, and provenance drift.

Local verification is clean: `citlali_cli` and `citlali_test` build, all 328
CTest cases pass, 48 config-boundary tests pass, all eight compact-
compatibility profiles pass, and full config preflight reports no gaps or
review-required reads. No noise-generation, empirical-product, filtering, or
FITS numerical code changed.

All three Unity gates are accepted at `1faec7cc`.

- Point `redu47` validates disabled noise against `redu46` with exact config
  and all 13 scientific product families exact.
- Point `redu49` validates the final bounded full-output fixture with
  `n_noise_maps: 10`: 30 observation realizations, three empirical products,
  and 30 realization writes. Realization, variance, and empirical-weight
  comparisons against OG `redu08` are roundoff-scale.
- Science final iteration `redu15` validates generation-only noise with two
  observations and coaddition: 60 observation plus 30 coadd realizations,
  zero empirical products, and zero realization writes. It is effectively
  identical to accepted refactor `redu11` and passes the science profile
  against matching OG `redu27`.

Every candidate has valid v1 noise provenance and zero serious log issues.
The validation ledger contains 36 valid records. Noise-products authority and
provenance is complete.

## Active Domain

Pointing is the active bounded Phase 2 domain. Its local implementation is
described below; Unity validation remains. Polarimetry is still blocked on
scientific-policy decisions, and compact-config rollout and Phase 3 remain
paused.

## Pointing Local Checkpoint

The bounded pointing implementation is complete locally but remains pending
Unity acceptance. The five frozen paths now load directly into an immutable
request snapshot. A separate effective plan resolves the map-geometry default
header radius and whether normalized observation maps are available from
mapmaking. Optional filtering and coaddition occur downstream and do not
disable raw pointing fits. Only the three existing PTC source-center fields
receive a one-way adapter; there is no reverse synchronization.

Each final-iteration observation records its identity, scientific map count,
fit attempts, valid fits, and output completion. Successful pointing CLI runs
must atomically publish `pointing_provenance.yaml` using schema
`citlali-pointing-provenance-v1`. The reduction auditor validates the five-key
request and effective policy, all observation/fit cardinalities, and agreement
with mapmaking v2 provenance. Comparison tooling supports
`--require-candidate-pointing-provenance` for older baselines without this
sidecar.

No Gaussian-fit, Ceres, source-finding, filtering, or map numerical code
changed. The sole remaining gate is an unchanged Unity point run with zero
serious log issues, valid mapmaking and pointing sidecars, and exact pre-existing
products against its matching accepted baseline. Do not mark pointing
provenance complete before that validation.

## Pointing Validation Failure And Correction

Unity point `redu50` embeds `v4.0.0-3456-g98d2a5d2` and has the same 489-leaf
merged-config hash as accepted disabled-noise `redu47`. Its complete maps,
RTC/PTC timestreams, diagnostics, source table, and other stable products are
exact, and it has zero serious log issues. The strict gate nevertheless failed:
all 11 fitted columns in the three-row pointing table were zero. The new
effective plan had incorrectly required map filtering to be enabled before
running the existing raw observation-map Gaussian fits.

This is a policy-wiring regression, not a numerical-fitting change. The
corrected plan makes pointing-fit availability depend only on mapmaking. It
retains map-filter and coadd values as provenance context without allowing them
to suppress raw pointing fits. The updated semantic auditor now rejects the
`redu50` sidecar for inconsistent fit activation, availability, and disable
reason. Regression tests cover fitting with filtering disabled and fitting
disabled because mapmaking is unavailable.

Local `citlali_cli` and `citlali_test` builds pass. All 336 CTests, 43 baseline-
tool tests, 54 config tests, all eight compact profiles, and full config
preflight pass. Commit the correction, rebuild on Unity, and rerun the unchanged
standard point fixture. Compare against `redu47`; the fitted pointing table and
all other pre-existing products must be exact, and the candidate pointing
sidecar must record three fit attempts.

After that corrected gate closes, begin the post-processing authority domain.
The project owner proposes matched OG/refactor pointing overlays for validation.
Use small, diagnostic cases that independently exercise map filtering and
source-finding/fitting activation, with identical merged low-level config for
each OG/refactor pair. Do not start that domain before the pointing rerun passes.

## Pointing Domain Accepted

Corrected Unity point `redu51` embeds `v4.0.0-3457-ga9d17fa1`. Its 489-leaf
merged config is exact against accepted disabled-noise `redu47`. The strict
zero-tolerance comparison includes all RTC/PTC timestream arrays and reports 13
common product families, zero changed records, and zero skipped records. The
previously zeroed pointing table is exact, including all 11 fitted columns.

The candidate has zero serious log issues. Version-1 pointing provenance is
semantically valid and records mapmaking enabled, map filtering disabled,
fitting requested and effective, one completed observation, three scientific
maps, three fit attempts, three valid fits, and completed outputs. Runtime is
59.971 seconds versus 58.627 seconds. The validation ledger now has 37 records.
Pointing config authority and provenance is complete.

Post-processing is the active bounded Phase 2 domain. First freeze and audit
the complete `post_processing.*` surface and its execution consumers. Then
separate immutable requested filtering/finding/fitting controls from effective
activation and realized products, retaining only one-way numerical adapters to
the Wiener filter and map fitter. Validate with matched OG/refactor point
overlays that isolate disabled filtering, enabled filtering, and source-
finding/fitting behavior. Keep compact-config rollout, Phase 3, and unsupported
polarimetry work paused.

## Post-Processing Characterization

The bounded surface is frozen without changing production behavior. It
contains 35 leaves: 24 under `post_processing.*` and 11 under top-level
`wiener_filter.*`. The Wiener prefix is part of this domain because it controls
the map-filter template and convergence policy. The machine-readable manifest
is `tools/config/post_processing_legacy_paths.json`; the contract narrative is
`doc/POST_PROCESSING_CONFIG_AUTHORITY.md`; and the exact starting-state audit is
included in config preflight.

The current mixed boundary has one legacy Wiener parser for 21 leaves, 13
other direct or mirrored typed reads, a reverse Wiener-to-typed mirror,
post-load request mutation when mapmaking is disabled, and a source-finding
copy from the observation map buffer to the coadd map buffer. Two request gaps
were explicit: `post_processing.source_fitting.model` was ignored and
`wiener_filter.kernel_template_tail_mode` executed only through legacy state.
Both now have closed-enum representation in a complete direct request reader.
Focused tests prove full parsing, disabled-value preservation, and invalid-enum
diagnostics. The reader now runs during `Engine` config loading as a fail-fast,
read-only shadow. It compares activation and histogram unconditionally and
only compares details when the legacy path loads them, avoiding false
differences from disabled legacy defaults. The legacy parser and reverse
mirrors still drive execution. The CLI/test builds, all 342 CTests, 60 config
tests, all eight compatibility profiles, and full preflight pass. Next validate
this shadow on Unity, then derive a separate effective plan and retain only
one-way numerical adapters.

## Enabled-Filtering Point Failure And Repair

Unity point `redu52` embeds `v4.0.0-3461-gd9db1183`. This first run using the
matched post-processing overlay enabled
`post_processing.map_filtering`, source finding, Gaussian filtering policy,
and low-pass-only Wiener behavior. The refactor completed the raw and filtered
fit work but then failed with `pointing fit results already recorded`. This is
a lifecycle/provenance defect exposed by the broader validation fixture, not a
numerical filter or fitting failure.

Pointing provenance v1 allowed one fit record per observation. Pointing output
actually performs a raw normalized-map fit and, when non-coadded filtered
outputs are enabled, a second fit of the filtered maps. The repair adds explicit
raw-observation and filtered-observation fit stages, permits exactly one record
for each expected stage, rejects missing or unexpected stages at observation
completion, and preserves both cardinalities in
`citlali-pointing-provenance-v2`. The reduction auditor still accepts historical
v1 sidecars and applies stage-aware semantics to v2. No Gaussian fitting,
source finding, filtering, map arrays, or output order changed.

Local verification after the repair is clean: `citlali_cli` and
`citlali_test` build, all 344 CTests pass, 45 provenance-auditor tests pass, 60
config-boundary tests pass, all eight compact profiles pass, and full config
preflight reports no drift. Commit and push the repair, rebuild on Unity, and
rerun the same refactor point overlay. Require successful completion, zero
unexpected error-level messages, valid pointing v2 provenance with three raw
and three filtered fit attempts, and matched scientific products before
continuing post-processing authority migration.

## Enabled-Filtering Point Accepted

Unity point `redu53` embeds `v4.0.0-3462-gc75f079b` and successfully reruns the
same overlay that failed in `redu52`. It completes in 59.772 seconds with zero
serious log issues. `citlali-pointing-provenance-v2` is semantically valid and
records one observation, three raw fit attempts, three filtered fit attempts,
all six fits valid, and completed output.

The merged low-level OG `redu09` and refactor `redu53` configs each contain 490
leaves and differ only in the two expected input/output paths rooted under the
separate reduction directories. All 13 products shared with accepted unfiltered
refactor `redu51` are bit-for-bit exact. The enabled filter adds the expected
eight products. Against matching OG `redu09`, those eight filtered products
have identical inventory, zero skipped or changed records under the standard
numeric gate, exact three-row pointing and 195-row source tables, maximum
filtered signal absolute difference `2.97e-11`, and maximum filtered weight
absolute difference `1.10e-13`. The OG run has its known twelve PTC NetCDF
errors; the refactor candidate has none, so the OG comparison is intentionally
restricted to filtered scientific products while `redu51` supplies the raw
regression gate.

The validation ledger now contains 38 records. This closes the stage-aware
pointing repair and the first enabled-filtering post-processing checkpoint.
The fully transferred directory was re-audited after the initial copy: all 50
files and all required provenance sidecars are present, and the non-PTC OG
comparison still has zero changed or skipped records.

## Post-Processing Execution Plan

The first authority checkpoint after `redu53` is complete locally. A new
`PostProcessingExecutionPlan` stores the immutable 35-leaf request, a separate
effective snapshot, explicit reduction/mapmaking/coadd resolution context, and
reset realized state. It suppresses requested map filtering and source finding
only when mapmaking is unavailable. It derives source-fitting need from
pointing/Beammap mode or requested filtering/finding, preserving the corrected
rule that pointing fits do not depend on optional filtering.

The plan is initialized once during config loading. The parity shadow now
compares legacy state with `plan.requested`. Existing filtering, finding,
fitting, and output consumers still use legacy state, so this checkpoint is
behavior-preserving and does not itself require another Unity run. Local
`citlali_cli` and `citlali_test` builds pass; all 347 CTests, the focused
boundary tests, all eight compact profiles, and full config preflight pass.

Commit this checkpoint, then replace the legacy Wiener parser and reverse
Wiener-to-typed mirror with a one-way adapter from the effective map-filter
snapshot. Inspect both serial and OpenMP Wiener implementations before that
cutover, preserve every conversion and derived default, and rerun the accepted
`redu53` enabled-filtering point overlay after the consumer change. Source
finding and source fitting follow as separate bounded slices. Do not add
post-processing provenance until realized cardinality is wired to actual
execution events.

## Map-Filter Consumer Cutover

The first consumer slice is complete locally. Both serial and OpenMP
`WienerFilter::get_config` YAML parsers are removed, along with the reverse
`mirror_wiener_filter_config` path. `adapt_map_filter_config_one_way` now copies
the effective typed filter snapshot into either Wiener target. It preserves
the old enum strings, every edge-guard and convergence field, conditional
Gaussian/Airy FWHM population, and arcsecond-to-radian conversion. Runtime
kernel/noise checks still run after adaptation.

Filter activation and required filtered-output policy now use the effective
plan. Map-diagnostic edge-guard metadata does as well, avoiding stale defaults
after mirror retirement. Generic policy accessors fall back to legacy config
only for lightweight test doubles or pre-plan use; the real Engine plan is
initialized before these consumers. Source-finding and source-fitting parsing,
adapters, and legacy parity checks are intentionally unchanged.

The CLI and test targets build. All 347 CTests, 60 config-boundary tests, all
eight compact profiles, and full preflight pass. The one local compile repair
added the established plan-availability fallback for generic test engines; no
production error remained. Commit this slice and run the unchanged enabled-
filtering point overlay used by accepted `redu53` on Unity. Require zero serious
log issues, exact merged-config identity, valid pointing v2 provenance, exact
raw products against `redu51`, and filtered products within the accepted OG
gate before migrating source finding.

## Map-Filter Unity Acceptance

Unity point `redu54` embeds `v4.0.0-3465-ga89e0ee5` and accepts the one-way
typed map-filter consumer cutover. The run finishes in 60.041 seconds with zero
serious log issues. All eight required provenance sidecars are present and
semantically valid; pointing v2 again records three valid raw and three valid
filtered fits with completed output.

The 490-leaf merged low-level config is byte-identical to accepted `redu53` and
differs from matching OG `redu09` only in the two expected reduction-root
paths. Against `redu53`, all 21 common products are present, no records are
skipped, and all 2,041 records pass the established tolerance. Sixteen records
are not bitwise identical, all within three filtered a1400 products; there is
no finite-mask mismatch and the largest absolute difference is `8.73e-11`.
Pointing and source tables are exact. Against OG `redu09`, all 639 compared
non-PTC records pass with no missing, extra, changed, or skipped products.

The validation ledger now contains 39 records. Source finding is the next
bounded post-processing consumer cutover. Replace its YAML-to-map-buffer parser
and map-buffer-to-coadd mirror with one-way adapters from the effective plan;
do not combine source fitting or realized provenance into that slice.

## Source-Finding Consumer Cutover

The source-finding slice is complete locally. The deleted
`citlali_config_read_source_finding.h` had parsed threshold, source window, and
mode a second time into the observation map buffer, converted the window to
radians, and then copied that realized observation state into the coadd map
buffer. `source_finding_config_policy.h` now performs one-way projection from
the effective typed plan into both numerical targets, with coadd explicitly
gated and no observation-to-coadd policy transfer.

Source-finding execution and output policy now consume effective activation.
The request/legacy shadow still compares activation, but source-finding details
were removed because the legacy object no longer owns them. Source fitting is
unchanged and remains shadowed on the mixed boundary. No detection loop,
Gaussian fit, map array, source table, or output-order code changed.

The CLI and test targets build. All 349 CTests pass, including focused stale-
target, coadd-gating, conversion, and shadow tests. All 61 config-boundary
tests, eight compact compatibility profiles, 100% compact-surface coverage,
and full config preflight pass. Run the unchanged enabled-filtering point
overlay against accepted `redu54`; require zero serious issues, valid required
provenance, identical config, complete product inventory, and accepted source
and filtered-map equivalence before starting the source-fitting cutover.

## Source-Finding Unity Acceptance

Unity point `redu55` embeds `v4.0.0-3467-gaa593a2b` and accepts the typed
source-finding consumer cutover. It completes in 60.882 seconds with zero
serious log issues, 12 PTC chunks, all 50 expected files, and all eight
required provenance sidecars semantically valid.

Its 490-leaf merged low-level config is byte-identical to `redu54`. All 2,041
records across the 21 common products are bit-for-bit exact at zero tolerance,
including complete RTC/PTC timestreams, all maps and diagnostics, the 195-row
source table, and both raw/filtered pointing tables. Against matching OG
`redu09`, all 639 non-PTC records pass with zero changed or skipped records and
no product-set differences; configs differ only in the two expected paths.

The validation ledger now contains 40 records. This closes source finding.
Source fitting is the next and final consumer cutover before post-processing
realized-state/provenance work.

## Source-Fitting Consumer Cutover

The source-fitting consumer cutover is complete locally and not yet accepted
on Unity. The mixed `read_source_fitting_config` parser and its limit-factor
helpers are deleted. `adapt_source_fitting_config_one_way` now projects the
effective typed fitting request into the mature `mapFitter`, preserving the
bounding-box and fitting-radius arcsecond-to-pixel conversions, rotation-angle
policy, two-element amplitude/FWHM vectors, and nonpositive-factor sentinel
that retains established fitter defaults. The Gaussian fitting algorithm is
unchanged.

The parity shadow no longer compares fitting details because no legacy config
object owns them after this cutover. The remaining activation reader is a
standalone compatibility/shadow boundary. Simplifying that header exposed one
test-only transitive dependency on `PostProcessingExecutionPlan`; the test now
includes its real dependency explicitly. Both `citlali_cli` and
`citlali_test` build, all 350 CTests and 62 config tests pass, all eight compact
profiles pass, compact-surface coverage remains 100%, and full preflight has
no drift. Push and rerun the unchanged enabled-filtering point overlay against
accepted `redu55`. Do not begin realized post-processing provenance until that
gate passes.

## Source-Fitting Unity Acceptance

Unity point `redu56` embeds `v4.0.0-3469-g9f8ad50e` and closes the final
post-processing consumer gate. It completes all 12 PTC chunks with zero serious
log issues, 50 files, and all required provenance sidecars valid. Its merged
490-leaf config has the same SHA-256 as `redu55`. The strict zero-tolerance
comparison finds 21 common products, 2,041 records, zero missing/extra
products, zero skipped records, and zero changed records. Full RTC/PTC
timestreams, both pointing tables, and all 195 source rows are bit-for-bit
identical. The validation ledger now contains 41 records.

All three post-processing numerical consumers are now accepted one-way typed
adapters. The next bounded work is realized state and required atomic
provenance. Instrument successful filtering, source detection/fitting, and
source-table writes at their actual completion points; do not infer completion
from requested activation. Use `redu56` as the immediate exact point baseline.

## Realized Post-Processing Provenance Prepared

Project-owner decisions for this slice are explicit: catalog source fits,
pointing fits, and Beammap detector fits are distinct realized fitter families;
source finding requested without map filtering is a configuration error. The
supported finder runs only on filtered observation or coadd maps, so silently
accepting the unsupported combination would publish a misleading request.

The implementation now records successful observation/coadd filter contexts
and map counts, source-finding contexts, detected candidates, catalog fit
attempts and valid fits, source-table writes and rows, raw/filtered pointing fit
contexts, and Beammap fit contexts. State resets with each reduction iteration,
then completion checks exact routing against final mapmaking cardinality. Source
rows must equal detected candidates, valid fits cannot exceed attempts, and
each enabled filtered context must have exactly one finder/fit/table event.

The required atomic sidecar is `post_processing_provenance.yaml`, schema
`citlali-post-processing-provenance-v1`. It contains requested and effective
35-leaf snapshots, effective-resolution reasons, and realized cardinality. It
is written at the CLI success boundary; incomplete state and write failure
propagate as reduction failure. The reduction auditor now supports
`--require-post-processing-provenance`, validates sidecar semantics, checks
filter counts against mapmaking v2, and checks pointing fit totals against
pointing v2. The frozen post-processing audit enforces the source code wiring.

Local verification is clean: `citlali_cli` and `citlali_test` build, all 357
CTest cases pass, 43 reduction-auditor tests pass, all 63 config/preflight tests
pass, all eight compact profiles pass, and full config preflight has no drift.
The production numerical algorithms were not changed. The initial test-target
build caught and repaired two generic-template assumptions before this result.

Next, commit and push this slice, then run the unchanged enabled-filtering point
overlay. Compare against accepted exact `redu56`; require a valid new sidecar,
one observation filter/source/table context, 195 source rows, separate three-map
raw and filtered pointing fit contexts, zero serious log issues, exact merged
config, and exact pre-existing products. Follow with science for coadd routing
and Beammap for detector-fit cardinality. Do not mark post-processing complete
or retire the remaining activation-only legacy shadow until all three gates
pass.

## Realized Post-Processing Point Acceptance

Unity point `redu57` embeds `v4.0.0-3471-gf8a4a596` and accepts the required
post-processing provenance implementation. The run completes all 12 PTC chunks
with zero serious log issues. Its sidecar is valid and reports one observation
filter/source/table context, three filtered maps, 195 detected and successfully
fitted source rows, three raw pointing fits, and three filtered pointing fits.
All coadd and Beammap counters are correctly zero for this workflow.

The merged low-level config has the same SHA-256 as accepted `redu56`. A strict
zero-tolerance comparison including complete RTC/PTC timestreams finds 21
common products, 2,041 records, zero missing or extra products, zero skipped
records, and zero changed records. The validation ledger now contains 42
records. Use `redu57` as the immediate exact point baseline.

Science coadd and Beammap detector-fit cardinality remain required mode gates,
but the owner has elected to batch those expensive reductions after the local
activation-shadow cleanup. Do not interpret this deferral as domain closure.

## Post-Processing Authority Cleanup

The remaining activation shadow is retired locally after the `redu57`
acceptance commit. `get_citlali_config` now loads the full 35-leaf typed request
once before mapmaking setup and uses it to initialize the effective execution
plan. The duplicate YAML reader for `post_processing.map_histogram_n_bins`, the
activation-only YAML reader, and the temporary shadow comparator are deleted.
The typed request remains immutable after loading: mapmaking-disabled policy is
resolved in the execution plan rather than overwriting requested filtering,
finding, and fitting values. The legacy Beammap no-map iteration reduction is
retained as an isolated normalization.

Local verification is clean: both targets build, all 355 CTests pass, all 63
config-boundary tests pass, all eight compact profiles pass, and full preflight
has no drift. The inventory now describes post-processing as typed authority
with one-way adapters, while provenance remains partial pending science and
Beammap gates. This unpushed cleanup was not present in `redu57`; the next Unity
point must compare exactly against `redu57` before cross-mode closure work.

## Post-Processing Authority Cleanup Accepted

Unity point `redu58` embeds `v4.0.0-3473-gdd6fdfc9` and accepts the local
activation-shadow cleanup. The run has zero serious log issues, all required
provenance is valid, and the merged config plus post-processing provenance
hashes match `redu57`. A strict zero-tolerance comparison including complete
RTC/PTC timestreams finds 21 common products, 2,041 records, and no missing,
extra, skipped, or changed records. The validation ledger now contains 43
records.

The owner has also supplied a three-observation OOF dataset (152385-152387).
Both external `oof/*/70_reduce.yaml` files are replaced locally by matched
modern pointing-engine configurations; they differ only in the executable
path. They select `pointing.source_strategy.mode: psf_preserve`, disable
Gaussian fits, use map-center source support, and keep fruitloops, filtering,
source finding, noise realizations, and large TOD products disabled for the
first bounded baseline. `72_reduce.yaml` remains the identical dataset/APT
overlay. The merged pairs are exact after executable normalization, and both
70 files round-trip all 444 low-level leaves with zero differences.

The workdirs are flattened as `oof/{citlali,refactor}`, so the correct data and
fit-report path is `../data`; the initially prepared `../../data` path was
rejected after it made TolTECA load the unrelated top-level validation data.
APT paths use `/work/toltec/commissioning2025-test/2026-refactor/oof/apts`.
Corrected external config SHA-256 values are: OG 70
`04f143ee265a91a428c6bc15bd7edcd25d0405ffd489394a80be4b9429ee5ea9`,
refactor 70
`0b82e68d66cc116ba606220b49601838089333e230aeeb1795cf36a3ce7200ad`,
and both 72 overlays
`1ce228dfab3d7e431c0eb5548209c2cb4a40efe5c5258a9baa3d6c6f533e73bf`.
This setup can close the deferred OOF runtime baseline without changing
Citlali's reduction-type enum. The generic product comparator now accepts
`--mode oof` and has focused path-resolution coverage.

### OOF Input-Selection Correction

The first OOF launch failure was not ultimately caused by the flattened
workdir path. The input path resolved, but the overlay selected `scannum == 2`
while the requested observations use a different scan number. The owner
confirmed that correcting the scan-number predicate resolves input discovery.

TolTECA masked this ordinary empty-selection error in its metadata-enrichment
path. After applying the user selection, its loader attempted to construct an
Astropy `Table` for `n_tones` from an empty row set and raised `ValueError:
Arguments "names" and "dtype" must match number of columns`. Citlali was never
started. TolTECA should instead detect an empty post-selection dataset and
report the resolved input path and selection expression directly. The paired
OOF configs and hashes above must be refreshed after the corrected selector
and APT paths are synchronized back from Unity.

## First OOF Product Validation

Paired OG/refactor `redu00` reductions completed for observations
152385-152387 after correcting the TolTECA scan-number selector. Both logs end
normally with no error- or critical-level records. The refactor run embeds
`v4.0.0-3473-gdd6fdfc9`, writes all required provenance sidecars, and completes
in 52.94 seconds versus 56.47 seconds between the OG log endpoints.

The full OOF product comparison finds 30 common products, no missing or extra
comparable products, and no skipped records. All three pointing-table data
matrices are exact. FITS maps and complete RTC/PTC diagnostics satisfy the
standard `atol=2e-8`, `rtol=1e-10` gate. Nine non-tolerance records are confined
to inactive RTC-despike config metadata: the typed refactor records requested
local-residual values while OG records legacy defaults after the parent
despiker is disabled. The parent `CONFIG.DESPIKED` value is disabled in both,
and no scientific product difference results.

The run exposed one real multi-observation provenance defect. Refactor ECSV
`date` metadata and FITS `DATEOBS0` headers are rotated by one observation:
152385 receives 152387's date, 152386 receives 152385's, and 152387 receives
152386's. Numeric pointing rows, `obsnum`, and MJD remain correct. The date
factory was evaluated before the current telescope file was loaded, so it read
the telescope state left by the initial geometry pass or previous observation.

The local correction removes the premature date snapshot from
`ReductionObservationContext` and evaluates the date factory immediately after
loading and pointing the current observation's telescope data. A
multi-observation regression test requires sequential values derived from the
newly loaded telescope state. The CLI/test build, all 355 CTests, and full
config preflight pass. This OOF gate remains scientifically clean but is not
accepted until a Unity rerun confirms correct per-observation dates.

## OOF Date Fix Accepted

Refactor OOF `redu01` embeds `v4.0.0-3477-g342a021c` and validates the
multi-observation lifecycle correction against OG `redu00`. The run completes
normally with no error- or critical-level records, 18 completed PTC chunks,
all required provenance sidecars valid, and a 45.503-second log interval.

The per-observation ECSV `date` metadata and FITS `DATEOBS0` values now match
the OG products exactly for all three observations:

- 152385: `2026-02-19.06:18:03`
- 152386: `2026-02-19.06:19:42`
- 152387: `2026-02-19.06:21:11`

The full-depth product comparison finds 30 common products, no missing or
extra products, and no skipped records. The three pointing-table matrices are
exact. All scientific numeric records pass `atol=2e-8`, `rtol=1e-10`; a strict
comparison excluding RTC diagnostic containers has zero changed records over
the remaining 27 products. The nine accepted changes are the same inactive
RTC-despike config metadata described above, repeated across three
observations. They do not affect processing because the parent despiker is
disabled in both configurations.

This accepts OG `redu00` versus refactor `redu01` as the OOF baseline and
closes Phase 1 safety stabilization for all four validation modes. The
machine-readable validation ledger now contains 44 accepted records.

## Post-Processing Mode-Gate Preparation

Post-processing is now recorded accurately as typed execution authority with
one-way numerical adapters. Point `redu58` already accepts removal of the last
activation shadow and duplicate histogram reader. Provenance remains partial
only because the science coadd-filtering and Beammap iterative-fit runtime
gates are still in progress.

The inventory validator now rejects unambiguous migration labels whose
declared execution authority or adapter direction disagrees with the label.
The intentionally unresolved `mixed-adapter` domains remain eligible for
either typed or mixed execution authority because their cross-domain policy
decisions are not yet complete. This preserves the deferred polarimetry and
astrometry/photometry states without weakening typed-authoritative checks.

Reusable matched TolTECA overlays were added under `validation/configs`:

- `science_post_processing_84_reduce.yaml` isolates coadd filtering while
  disabling fruit loops, noise realizations, and source finding;
- `beammap_post_processing_80_reduce.yaml` retains three fitting iterations
  while disabling unrelated map filtering, noise, and source finding.

The owner started current science and Beammap reductions from `342a021c`
before these repository-only templates were committed. Those runs use the
same complete post-processing implementation and are more comprehensive than
the reduced-cost overlays, so they remain valid and must not be restarted.
The full config preflight passes 65 tests, all eight compatibility profiles,
100% compact-surface coverage, and every authority/boundary audit.

## Beammap Authority Characterization

While the post-processing science and Beammap reductions run, the next
Beammap domain was characterized without changing Citlali execution. The new
`tools/config/beammap_legacy_paths.json` freezes all 74 default `beammap.*`
leaves with no known typed gaps. `doc/BEAMMAP_CONFIG_AUTHORITY.md` records the
current and target contracts.

`tools/config/audit_beammap_boundary.py` verifies the frozen/default surface,
typed model, exact config-literal file boundary, one typed installation, and
the single `fit_radius_fwhm` adapter into `map_fitter`. It also requires
dedicated Beammap provenance to remain explicitly `missing` at this
characterization checkpoint. Six focused tests and the audit are integrated
into the full preflight, which now passes 71 tests plus all eight compatibility
profiles and all existing authority audits. No build or Unity rerun is needed
for this tooling-only checkpoint.

## Parallel Review Package Adopted

Three read-only review tasks recorded their results in:

- `handoff/PHASE2_COMPLETION_CENSUS_2026-07-14.md`
- `handoff/BEAMMAP_AUTHORITY_DESIGN_REVIEW_2026-07-14.md`
- `handoff/CONFIG_USABILITY_TOLTECA_REVIEW_2026-07-14.md`

The reviews were reconciled against the living roadmap rather than treated as
independent plans. They agree that Phase 2 is incomplete, the active science
and Beammap post-processing gates must close first, and Beammap is the next
bounded authority migration. The accepted sequence after that is atomic
astrometry/photometry, the minimal KIDs external identity plus global ordered
config-source provenance, a mechanical polarimetry capability disposition,
and a final current four-mode Phase 2 snapshot.

The 74-leaf Beammap surface is accepted as the policy boundary, with
`beammap_source.*` retained as an adjacent observation-calibration authority.
The review found a real stale-state hazard in the latter: missing per-array
flux in a later observation can inherit prior legacy-map state. That behavior
must be replaced by atomic observation-value construction and replacement,
without broadening Beammap's numerical algorithms.

Compact configuration remains an authoring/translation project, not an active
runtime authority. Phase 2 retains matched low-level mode overlays and must
add durable ordered-source provenance. Production compact rollout, approved
profile defaults, and the full hermetic TolTECA merge suite remain explicit
rollout blockers. Existing `*_standard` profiles are prototypes, not approved
scientific defaults.

The complete owner-question inventories remain in the reports. Ask only the
questions needed by the next bounded implementation and do not silently infer
scientific policy. No source or runtime behavior changed in this review
checkpoint.

## Post-Processing Mode Gates Accepted

The final science and Beammap post-processing gates completed from
`v4.0.0-3477-g342a021c`.

Science `redu16` through `redu19` are the four retained fruit-loop iterations;
`redu19` is the final product. It has zero error- or critical-level records and
all required provenance sidecars pass semantic and cross-sidecar validation.
Post-processing records zero observation filter contexts and one coadd filter
context with three filtered maps. Its low-level config SHA-256 is
`cd860b8b712810f9f8651c325a6f1642e8453aa6148410cf7ff7a7058de286a3`,
identical to accepted refactor `redu15`. The strict full-depth comparison finds
27 common products, no missing or extra products, no skipped records, and no
changed records outside tolerance. The log interval is 789.840 seconds versus
710.332 seconds for `redu15`; the increase is concentrated in PTC processing
and is recorded without a performance conclusion.

Beammap `redu02` has zero error- or critical-level records and all required
provenance sidecars pass. Post-processing records three iterative detector-fit
contexts, 15,407 attempts, and 15,407 valid fits. Its low-level config SHA-256
is `aa956b28465eaef8b23763e877857b5b8929e95ca4fbdc976db6d7b2a775636d`,
identical to accepted refactor `redu01`. The strict comparison excludes only
the profiling sidecar and reads the complete detector TOD and split FITS maps:
12 common scientific products, no missing or extra products, no skipped
records, and zero changed records. The 3,758.004-second log interval is 8.95%
above `redu01`; the three-iteration mapmaking interval is 0.15% faster, while
the increase is concentrated in PTC/output-adjacent intervals. It is recorded
without attributing a code regression.

These two gates complete post-processing authority and provenance. The machine
inventory now marks the domain complete. The next active work is the bounded
Beammap effective-plan/provenance migration; compact rollout, polarimetry, and
Phase 3 remain paused.

## Beammap Effective-Plan Preparation

The first bounded Beammap Phase 2 implementation checkpoint is prepared
without changing production execution. A complete component serializer covers
all 74 frozen `beammap.*` leaves. The boundary audit now derives 59 typed-reader
roots and 59 serializer roots mechanically, expands fixed vector roots, and
requires exact 74/74 coverage. It also requires the new
`BeammapExecutionPlan` and serializer to remain absent from production wiring.

The pure plan preserves an immutable requested snapshot and separately records
effective phase correction, per-phase prior inheritance, missing-prior-path
disablement, split-flag default/sort/dedup decisions, measurement and
convergence availability, and mapmaking-disabled iteration reduction. Focused
tests prove reset safety and that effective normalization does not mutate the
request. Cold-boundary validation now rejects non-finite iteration tolerance,
prior SNR, flagging-vector, and sensitivity-band values; it also checks the
vector shapes already required by the readers and enabled detector-TOD output
subdirectory presence.

This checkpoint does not change the current production authority claim. The
typed Beammap object and one-way `fit_radius_fwhm` adapter still drive
execution, and dedicated Beammap provenance is still missing. The next bounded
step is to construct the plan from a genuinely immutable request, then move
one consumer at a time to effective policy before adding realized lifecycle and
required provenance. Do not infer new scientific bounds or redesign Beammap
algorithms during that work.

Local verification is clean: `citlali_cli` and `citlali_test` build, all 363
CTest cases pass, and full config preflight passes 74 boundary tests, all eight
TolTECA compatibility profiles, 100% compact-surface coverage, and every
authority audit. No Unity reduction is needed for this production-unwired
preparation checkpoint.

## Beammap Effective Plan Wired

The follow-up checkpoint constructs `BeammapExecutionPlan` at the sole
Beammap YAML boundary. Family readers return a raw 74-leaf request and explicit
presence for optional per-phase prior and split-flag fields. The plan keeps
that request unchanged and resolves all established effective policy in one
place. Reader-side phase, prior, split, and mapmaking-disabled mutation helpers
are retired.

To keep this cut behavior-preserving, `ReductionConfig::beammap` receives a
one-way copy of the plan's effective snapshot for mature consumers. The shared
map-fitter radius adapter now receives `beammap_plan.effective().fitting`
directly. There is no reverse synchronization. The audit enforces boundary
call counts and order, exact 74/74 reader and serializer coverage, absence of
the retired helpers, and the continued absence of a production provenance
writer.

Local verification passes the `citlali_cli` and `citlali_test` builds, all 364
CTest cases, 74 config tests, all eight compact compatibility profiles, and the
complete preflight with no drift. No numerical algorithm changed. The next
coherent slice is Beammap realized iteration/output cardinality plus required
atomic provenance. Combine that work before requesting the next expensive
Unity Beammap validation; this intermediate authority commit alone does not
warrant an hour-long run.

## Beammap Realized Lifecycle And Required Provenance

The next coherent checkpoint is implemented locally. The Beammap plan now
resets once per outer reduction iteration, begins after timestream processing
has established each observation's scan count, and records every internal
Beammap iteration around the existing stage calls. It captures phase, active
maps, completed mapmaking pass count, source-aware RTC rerun, fit completion,
newly/total converged maps, and terminal reason. The hooks are cold lifecycle
bookkeeping; fitting, convergence, RTC/PTC, map population, and detector
flagging algorithms are unchanged.

Observation completion occurs only after the existing raw/filtered/coadd
output path returns. CLI completion cross-checks Beammap observation identity
and map count against completed mapmaking provenance and requires exact
agreement between completed internal iterations and post-processing Beammap
fit contexts. Existing plans remain authoritative for map writes and aggregate
fit attempts/valid counts.

The required atomic `beammap_provenance.yaml` uses
`citlali-beammap-provenance-v1` and contains requested/effective config,
effective-resolution reasons, observation/iteration lifecycle, and terminal
realized state. Incomplete lifecycle or sidecar publication failure fails the
reduction. The Beammap audit now requires exact lifecycle-hook call counts and
one ordered CLI completion/write path in addition to 74/74 reader and config-
serializer coverage.

Local verification: `citlali_cli` and `citlali_test` build; all 17 focused
Beammap tests and all 372 CTests pass; full preflight passes 75 Python tests,
all eight compatibility profiles, 100% compact coverage, and every authority
audit. The inventory remains `partial` pending a matched Unity Beammap run.
Do not claim the Beammap authority domain complete yet: observation-resolved
prior/reference state, adjacent atomic `beammap_source.fluxes` handling, and any
additional Beammap-specific optional-product cardinalities required by the
design review remain follow-up work.

## Required Detector TOD Cardinality

The next safe Beammap output slice records detector-specific PTC TOD without
touching unresolved prior, reference, or split-output policy. When
`beammap.detector_tod_output.enabled=true`, the observation plan expects exactly
one successful write. The hook runs only after the existing atomic NetCDF
writer returns and records the output iteration plus detector, slot, and
maximum-sample dimensions. Missing and duplicate writes prevent observation
completion; disabled output requires zero writes.

This is behavior-preserving for successful reductions and enforces the
project-owner decision that explicitly enabled products are required. It does
not alter scan selection, fitted/fallback source positions, NetCDF layout, or
TOD samples. Local verification passes both build targets, all 18 focused
Beammap tests, all 373 CTests, 75 Python preflight tests, all eight compatibility
profiles, and every audit. Include this commit in the same pending Unity
Beammap validation candidate as the lifecycle/provenance checkpoint.

## 2026-07-15 Beammap Lifecycle Validation Accepted

Downloaded Beammap `redu03` identifies `v4.0.0-3486-gb530e838`, matching local
commit `b530e8387`. Its low-level config is byte-identical to accepted `redu02`
(SHA-256 `aa956b28465eaef8b23763e877857b5b8929e95ca4fbdc976db6d7b2a775636d`),
the compressed log has zero serious records, and all 198 PTC chunks complete.
The log interval is 3,609.307 seconds.

The new Beammap sidecar is complete and internally consistent: one observation
(`148670`), 5,234 detectors/maps, 198 scans, three contiguous completed
iterations, maximum-iteration termination, and one required detector-TOD write
at iteration two with 5,234 detectors, 20 slots, and 788 maximum samples. The
accepted scientific profile is exact. A strict full-depth `redu02`/`redu03`
comparison excluding only `citlali_profile.ecsv` reads all 12 products,
including detector TOD and split FITS maps, with no missing, extra, skipped, or
changed records.

The baseline audit now has `--require-beammap-provenance`, Beammap semantic
checks, and mapmaking/post-processing cross-checks. This accepts the dedicated
lifecycle/provenance checkpoint. Keep the Beammap authority inventory partial:
observation-resolved prior/reference decisions and atomic
`beammap_source.fluxes` state remain. No prior, reference, split-output, or
source-flux fallback policy
was chosen here.

## Atomic Beammap Photometry State

The next local candidate fixes the review's concrete stale-flux defect without
changing successful Beammap science. Photometry input is now parsed into a
temporary `BeammapPhotometryObservationConfig`, validated for every runtime
array, and installed only after success. Installation replaces typed
photometry and the legacy mJy/beam map and clears derived MJy/sr values, so a
later observation cannot inherit omitted entries. Invalid or missing required
flux still fails the reduction, but through `citlali::error::invalid_config`
rather than a library `exit()`.

Project-owner clarification on 2026-07-15 corrected the initial ownership
model: source identity belongs to telescope data, while TolProj owns calibrator
selection and flux estimation. Citlali therefore does not mirror source name
or coordinates into this config domain. `beammap_provenance.yaml` v2 names
`telescope_data` as the source-identity authority and records only the
per-array flux/uncertainty consumed as photometry input. The validation audit
continues to accept v1 and semantically checks the v2 authority and photometry
record.

Local `citlali_cli` and `citlali_test` builds pass. All 24 focused
Beammap/photometry tests, all 377 CTests, 49 reduction-audit tests, 75 config
tests, all eight compatibility profiles, and every authority audit pass. A
matched Unity Beammap is required for v2 acceptance. The project owner
confirmed that missing or invalid required array flux must fail the reduction
and that no fallback is permitted.

## 2026-07-15 Beammap Photometry Ownership Gate Accepted

The refreshed Unity Beammap `redu04` is complete and identifies
`v4.0.0-3489-g7e577c81`. Its low-level config is byte-identical to accepted
`redu03` (SHA-256
`aa956b28465eaef8b23763e877857b5b8929e95ca4fbdc976db6d7b2a775636d`),
all 198 PTC chunks complete, and the log has zero serious records. The required
`citlali-beammap-provenance-v2` sidecar passes semantic audit and records
telescope data as source-identity authority, TolProj as calibrator-flux
authority, the three required installed array fluxes, one complete
observation, three complete iterations, and one required detector-TOD write.

The strict full-depth `redu03`/`redu04` comparison reads all 12 stable products
including detector TOD and six split FITS files. It reports no missing, extra,
skipped, or changed records. The dedicated Beammap scientific-equivalence
profile likewise reports exact detector identity, flags, APT quantities, and
all good/bad signal, weight, and kernel map layers.

Total log time is 3,661.793 seconds versus 3,609.307 seconds (+1.45%). The
dominant mapmaking interval is slightly faster; variation is concentrated in
PTC and diagnostics timing. This is within the provisional performance budget,
although peak RSS remains unavailable. The Beammap policy/provenance domain is
now complete. Keep generic astrometry separate; the next bounded Phase 2 work
is the minimal external KIDs identity and ordered config-source manifest.
