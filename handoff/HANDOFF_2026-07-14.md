# Citlali Refactor Handoff - 2026-07-14

## Governing State

- Branch: `codex/structural-refactor`.
- Phase 2 config authority and provenance is active.
- Do not push; the user controls GitHub pushes and Unity work.
- Coadd implementation commit: `c2e053b3c`.
- Coadd point-acceptance commit: `0ba604e58`.

## Coadd Validation Closure

Point `redu46` and science final iteration `redu11` both embed
`v4.0.0-3450-gc2e053b3`. They close the disabled and enabled coadd gates.

Point `redu46` is exact against accepted `redu45`: 489/489 config leaves, all
13 complete product families including RTC/PTC timestreams, zero changed or
skipped records, zero serious log issues, and 53.804 seconds versus 56.176
seconds. Coadd provenance records requested/effective disabled, no execution,
no cardinality, and successful reduction completion; mapmaking independently
records no coadd.

Science `redu08` through `redu11` are the four fruit-loop iterations of one
run; only final `redu11` is the accepted candidate against final `redu07`.
Its 502-leaf config is exact, all 27 products are present with zero skips and
zero serious log issues, and the science-equivalence v2 gate accepts a maximum
map RMS-relative difference of `7.65e-14`. Coadd provenance records requested
and effective enabled, execution complete, three maps, six required logical
writes, and completed outputs. Those values exactly match mapmaking
provenance. Runtime is 719.154 seconds versus 709.597 seconds.

The validation ledger contains 33 valid records. Coadd authority/provenance is
complete. No Beammap run was required because point already covers the same
disabled-coadd path and science covers enabled coaddition.

## Next Domain

The next bounded Phase 2 domain is `noise-products`. First freeze and census
its requested keys, then define effective randomization/seed policy and
realized product cardinality without changing noise-generation algorithms.
Polarimetry remains blocked on scientific-policy decisions. Broader
post-processing, compact-config rollout, and Phase 3 compiled-boundary work
remain paused while this bounded domain is active.
