# 2026-07-06 Handoff

## 2026-07-06 15:27:57 EDT - Reduction Product Comparison Tooling

- Branch: `codex/structural-refactor`
- Relevant HEAD after tooling commit: `395faa9a Add reduction product comparison tooling`
- Prior beammap extraction commit: `b49b8bcb Extract beammap processing helpers`
- Known unrelated dirty file left untouched:
  - `tools/local/configure-local-build.sh`

### Context

The current beammap validation run is expensive, so validation tooling was
added while that run churns. The new tool is meant for quick triage of completed
Citlali reductions across pointing, beammap, and science workflows.

This is not a replacement for the strict deterministic manifest gate. Use the
manifest tools when the question is "did this deterministic run match within
the established gate?" Use the product-comparison tool when the question is
"what products differ, which numeric arrays changed most, and are the expected
files present?"

### New Tool

- Tool:
  - `tools/baseline/compare_reduction_products.py`
- Documentation:
  - `tools/baseline/README.md`

Capabilities:

- Resolves `latest` `reduNN` directories from the standard
  `/Users/gwilson/work_toltec/local_data/2026-refactor/<mode>/<label>/reduced`
  layout.
- Supports `--mode point`, `--mode beammap`, and `--mode science`.
- Compares matching FITS, netCDF, CSV, and ECSV products.
- Reports missing and extra comparable products.
- Ranks numeric array/column differences by max/median absolute and fractional
  difference.
- Writes Markdown reports with `--report-out` and machine-readable JSON with
  `--json-out`.

Default filtering:

- Skips logs, configs, `index.yaml`, `learning_iter_*.csv`, and
  `*_timestream.nc`.
- Use `--include-timestream` for deeper TOD diagnostics.
- Use `--include` and `--exclude` globs for targeted comparisons.
- Default numeric tolerance is aligned with the deterministic manifest wrapper:
  `--atol 2e-8 --rtol 1e-10`.

### Standard Commands

Pointing latest OG/refactor comparison:

```bash
$HOME/tolteca/bin/python tools/baseline/compare_reduction_products.py \
  --base-root /Users/gwilson/work_toltec/local_data/2026-refactor \
  --mode point \
  --baseline-redu latest \
  --candidate-redu latest \
  --report-out /tmp/point_product_compare.md
```

Beammap latest OG/refactor comparison after the current long run lands:

```bash
$HOME/tolteca/bin/python tools/baseline/compare_reduction_products.py \
  --base-root /Users/gwilson/work_toltec/local_data/2026-refactor \
  --mode beammap \
  --baseline-redu latest \
  --candidate-redu latest \
  --top 30 \
  --report-out /tmp/beammap_product_compare.md \
  --json-out /tmp/beammap_product_compare.json
```

Science explicit pair comparison, useful when `latest` is not the intended
baseline/candidate pair:

```bash
$HOME/tolteca/bin/python tools/baseline/compare_reduction_products.py \
  /Users/gwilson/work_toltec/local_data/2026-refactor/science/citlali/reduced/redu13 \
  /Users/gwilson/work_toltec/local_data/2026-refactor/science/refactor/reduced/redu09 \
  --mode science \
  --top 30 \
  --report-out /tmp/science_product_compare.md \
  --json-out /tmp/science_product_compare.json
```

Tiny fixture smoke test:

```bash
$HOME/tolteca/bin/python tools/baseline/compare_reduction_products.py \
  tools/baseline/examples/tiny_reduction \
  tools/baseline/examples/tiny_reduction
```

### Verification Already Run

- Syntax check:
  - `$HOME/tolteca/bin/python -m py_compile tools/baseline/compare_reduction_products.py`
- Tiny fixture self-compare:
  - 1 common CSV product.
  - 0 changed records.
- Point latest OG/refactor smoke:
  - Baseline resolved to:
    - `/Users/gwilson/work_toltec/local_data/2026-refactor/point/citlali/reduced/redu06`
  - Candidate resolved to:
    - `/Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu114`
  - 11 common comparable products.
  - No missing or extra comparable products.
  - 0 non-tolerance changes.
  - Largest numeric deltas were within tolerance.
- Beammap smoke against existing downloaded outputs:
  - Baseline and candidate both resolved to `redu00`.
  - Report correctly showed that the existing refactor beammap output is missing
    the final FITS/ECSV map/table products relative to OG.
  - This is expected for the incomplete/stale local beammap snapshot and should
    be rerun after the current Unity beammap validation finishes.
- Science smoke:
  - Compared current latest OG `redu13` against refactor `redu09`.
  - Product sets matched.
  - Report highlighted the known coadded filtered map differences, especially
    `noise_variance_I` and filtered `signal_I`.

### How To Interpret Reports

- `Product Set Differences`:
  - Missing/extra products are often the first failure mode to inspect.
  - For beammap, missing final FITS/ECSV products are a hard validation concern
    unless the run was known to be incomplete.
- `Largest Numeric Differences`:
  - This table is useful for localizing differences by product and HDU/variable.
  - Small absolute changes with large fractional changes can occur where the
    baseline value is near zero; inspect `max_abs_diff` and `median_abs_diff`
    together.
- `Non-Tolerance Changes`:
  - These are the records outside `--atol/--rtol`, shape/header/product
    changes, or read errors.
  - A nonzero count is not automatically a regression, but it should be
    explained before continuing a risky refactor phase.

### Relationship To Existing Manifest Gate

Strict deterministic gate remains:

```bash
$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py \
  --case point_refactor_reduNN \
  --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/reduNN \
  --manifest-out /tmp/citlali_point_refactor_reduNN_manifest.json

tools/baseline/compare_deterministic_manifests.sh \
  /tmp/citlali_point_refactor_reduPREV_manifest.json \
  /tmp/citlali_point_refactor_reduNN_manifest.json
```

Use both when appropriate:

- Manifest comparison: strict pass/fail behavioral gate.
- Product comparison: fast diagnosis and human-readable localization when a
  run differs or when a new beammap/science validation lands.

### Suggested Next Use

When the current Unity beammap run finishes and is downloaded, run:

```bash
$HOME/tolteca/bin/python tools/baseline/compare_reduction_products.py \
  --base-root /Users/gwilson/work_toltec/local_data/2026-refactor \
  --mode beammap \
  --baseline-redu latest \
  --candidate-redu latest \
  --top 40 \
  --report-out /tmp/beammap_product_compare.md \
  --json-out /tmp/beammap_product_compare.json
```

Then inspect:

- whether final map FITS and fit-QC/apt ECSV products are present on both sides,
- largest FITS image deltas by array/flag product,
- fit-QC ECSV numeric deltas,
- PTC/RTC diagnostic netCDF changes if present.

If that beammap report is clean or differences are explainable, the next safe
refactor target is output-only beammap cleanup:

- `Beammap::write_beammap_fit_qc_table`
- `Beammap::write_detector_specific_ptc_tod`

Do not stack deeper beammap algorithm refactors before the current beammap
validation result has been inspected.

## 2026-07-06 - Unity CMake/Build-Time Audit And Helper Optimization

- Branch: `codex/structural-refactor`
- Relevant commits:
  - `d92b7fd7 Optimize Unity refactor build helper`
  - `395faa9a Add reduction product comparison tooling`
  - `7569fc3f Document reduction product comparison workflow`
- Known unrelated dirty file left untouched:
  - `tools/local/configure-local-build.sh`

### Context

This work happened in a side thread while the main refactor/beammap validation
was running. The user explicitly said not to do the deeper architecture tie-in
yet; that remains a later structural-refactor stage. This entry only covers the
workflow/CMake helper changes intended to reduce Unity build latency.

The user observed that Unity `cmake + build` is at least 6x slower than local
builds. The audit found that the normal Unity helper was paying the configure
cost on every update, and configure can invalidate a large part of the build
graph.

### Measured Local Timings

Measured in `/Users/gwilson/GitHub/citlali-refactor`:

- No-op build only:
  - `/usr/bin/time -p cmake --build build --target citlali_cli -j 8`
  - about `1.2 s`
- Configure only:
  - `/usr/bin/time -p cmake -S . -B build`
  - about `23 s`
- Build immediately after configure:
  - about `90 s`
- Configure with `-DFETCHCONTENT_UPDATES_DISCONNECTED=ON`:
  - about `22 s`
- Build immediately after that configure:
  - about `119 s`, including broad third-party dependency rebuilds

The key result is that the expensive path is not the build tool checking a
clean tree. The expensive path is explicitly running configure, then building.

### Likely Root Cause

- `tools/unity/citlali_refactor_bashrc.sh` previously always ran:
  - `cmake -S . -B build ...`
  - then `cmake --build build --target citlali_cli ...`
- `tula_cmake` generates `*_config/gitversion.h` during configure with a
  wall-clock timestamp.
- Those generated headers are included by broad headers such as `engine.h`,
  `kidsproc.h`, and CLI parsing.
- That makes configure invalidate PCH / the large CLI translation unit, and in
  some cases fetched dependency objects.
- A plain configure also tried to update `FetchContent` git checkouts, which
  creates network failure and latency risk.

Do not solve the header architecture issue in this workflow slice. That should
be part of the later "move non-template implementation out of headers" phase.

### Implemented Helper Behavior

`tools/unity/citlali_refactor_bashrc.sh` now defaults to:

1. fetch/switch/pull the refactor branch,
2. decide whether configure is actually needed,
3. skip explicit configure when the build tree is current,
4. run `cmake --build`.

New environment variable:

```bash
CITLALI_REFACTOR_CONFIGURE=auto    # default
CITLALI_REFACTOR_CONFIGURE=always  # force configure
CITLALI_REFACTOR_CONFIGURE=never   # fail instead of configuring
```

Auto mode runs configure when:

- `${build_dir}/CMakeCache.txt` is missing,
- `${build_dir}/CMakeFiles` is missing,
- CMake/dependency inputs changed between the pre-pull and post-pull commits,
- relevant cache options changed, including:
  - `CMAKE_BUILD_TYPE`
  - `CITLALI_USE_WIENER_FILTER_OMP`
  - `USE_INSTALLED_NETCDF`
  - `CONAN_CMD`
  - `FETCHCONTENT_SOURCE_DIR_TULA`
  - `FETCHCONTENT_UPDATES_DISCONNECTED`

The helper now adds this configure option whenever configure is run:

```bash
-DFETCHCONTENT_UPDATES_DISCONNECTED=ON
```

`tools/unity/unity.env.example` now documents
`CITLALI_REFACTOR_CONFIGURE`.

### Expected Unity Behavior

After this is pushed and pulled on Unity, source the updated helper or reload
the shell environment:

```bash
source "${HOME}/work_toltec/citlali_dev/citlali_refactor/tools/unity/citlali_refactor_bashrc.sh"
```

The first `citlali-refactor-update` after this helper lands may still configure
once, because the existing Unity cache likely does not yet have
`FETCHCONTENT_UPDATES_DISCONNECTED=ON`.

Subsequent ordinary refactor updates should print:

```text
Skipping CMake configure; build tree is current. Set CITLALI_REFACTOR_CONFIGURE=always to force it.
```

Then they should go straight to:

```bash
cmake --build "${build_dir}" --target citlali_cli -j "${jobs}"
```

Use forced configure only when needed:

```bash
CITLALI_REFACTOR_CONFIGURE=always citlali-refactor-update
```

Use `never` as a diagnostic guard if we want to prove the fast path is being
used:

```bash
CITLALI_REFACTOR_CONFIGURE=never citlali-refactor-update
```

### Verification Already Run Locally

- `bash -n tools/unity/citlali_refactor_bashrc.sh`
- Helper smoke checks for:
  - boolean normalization,
  - CMake cache value parsing,
  - no-change CMake input detection.
- `git diff --check` for the Unity helper files.

No Unity run has validated the new helper yet. The first Unity test should
check whether the helper configures once, then whether the next run skips
configure as expected.

## 2026-07-06 15:44:56 EDT - Structural Refactor Census

- Branch: `codex/structural-refactor`
- Relevant recent commits:
  - `b49b8bcb Extract beammap processing helpers`
  - `395faa9a Add reduction product comparison tooling`
  - `7569fc3f Document reduction product comparison workflow`
  - `66f560a4 Document Unity build helper optimization`
- Known unrelated dirty file left untouched:
  - `tools/local/configure-local-build.sh`

### Scope Boundary

The current structural-refactor stage should stay focused on:

- `engine/beammap` structure,
- engine orchestration and output helpers,
- validation tooling and build workflow support,
- typed config wiring where it clarifies pipeline ownership.

Do not define this stage as "refactor every large file in Citlali." The very
large legacy headers such as `rtcproc.h`, `ptcproc.h`, `clean.h`,
`wiener_filter*.h`, and `jinc_mm.h` are valid future work, but they should not
be pulled into this stage unless they directly block the engine/beammap cleanup
or config work.

### Remaining Hotspots In Current Scope

| Area | Approx size | Risk | Notes |
| --- | ---: | --- | --- |
| `Beammap::run_loop` | 670 lines | High | Still owns mapmaking pass orchestration, fit initialization, fit execution, convergence, and final iteration output. |
| `Beammap::write_beammap_fit_qc_table` | 430 lines | Low/Medium | Mostly table assembly, metadata, units, and descriptions. Good next extraction target after beammap validation. |
| `Beammap::calc_empirical_template_calibration` | 380 lines | Medium/High | Algorithmic calibration path. Pure sampling helpers have been extracted; deeper solver/candidate extraction should wait for beammap validation. |
| `Beammap::write_detector_specific_ptc_tod` | 340 lines | Medium | Output planning plus netCDF writing; separable into selection/planning and writer layers. |
| `Beammap::update_prior_frame_estimates` | 330 lines | High | Math-sensitive prior alignment logic. Defer until lower-risk output/run-loop splits are validated. |
| `Beammap::set_apt_flags` | 320 lines | Medium | Can split by flag family, but directly affects detector flags and flux scale fields. |
| `Beammap::setup` | 280 lines | Medium | Allocation/config initialization; touches member invariants. |
| `Beammap::write_beammap_map_products` | 275 lines | Low/Medium | Output helper extraction, relatively safe. |
| `Beammap::apply_scan_band_mask` | 250 lines | High | Directly changes beammap masking behavior if mishandled. Defer. |
| `Beammap::process_apt` | 250 lines | Medium | Important final calibration/table logic. |

### Recommended Order

1. Wait for the current Unity beammap validation result before stacking more
   beammap algorithm refactors. The latest beammap extraction touched run-loop
   execution paths and empirical-template sampling helpers.
2. If beammap validation is clean or differences are explainable, start with
   output-only beammap cleanup:
   - `Beammap::write_beammap_fit_qc_table`
   - `Beammap::write_detector_specific_ptc_tod`
   - optionally `Beammap::write_beammap_map_products`
3. Return to `Beammap::run_loop` after the output helpers:
   - extract mapmaking pass orchestration,
   - extract fit diagnostics bookkeeping,
   - avoid changing fit initialization semantics until another beammap
     checkpoint passes.
4. Split `Beammap::set_apt_flags` by responsibility:
   - fit-quality flags,
   - sensitivity flags,
   - position/prior-distance flags,
   - flux scale calculation.
5. Defer the highest-risk math until the lower-risk layers are stable:
   - `update_prior_frame_estimates`,
   - `choose_prior_guided_init`,
   - `apply_scan_band_mask`,
   - deeper empirical-template candidate/solver extraction.

### Definition Of Complete For This Stage

This structural stage can be considered complete when:

- `Beammap::run_loop` no longer owns detailed scan, mapmaking, fit, convergence,
  and output mechanics inline.
- Beammap output writers are schema/helper driven rather than large monolithic
  functions.
- Beammap member functions are organized around named pipeline responsibilities
  that match validation concepts.
- Typed config access is centralized enough that future config simplification
  can happen without spelunking through long execution blocks.
- Point validation remains stable and at least one beammap validation has
  checked the recent beammap extraction and output split work.

This stage does not require refactoring all large timestream/mapmaking legacy
headers. Those belong to a later, separate performance/pipeline-core cleanup
phase.

## 2026-07-06 20:08 EDT - Deferred Map-Write Array Identity Cleanup

- Relevant commit:
  - `c6347324 Fix detector beammap kernel FITS output`
- Known unrelated dirty file left untouched:
  - `tools/local/configure-local-build.sh`

### Context

The first refactor beammap validation after the beammap helper extraction
segfaulted during split-by-flag FITS map output, immediately after:

```text
kernel_map_diag stage='map output ... split-by-flag before write'
output progress (flag=0) [  0%]
```

The focused fix changed the kernel FITS FWHM lookup in
`Engine::write_maps` from `calib.array_fwhms[calib.arrays(i)]` to
`calib.array_fwhms[array_index]`. In detector-grouped beammaps, `i` is a
detector/map index, not an index into the small `calib.arrays` vector. The
writer had already computed the correct map array identity through
`map_write_indices(...)`; the kernel FWHM path was simply not using it.

### Deferred Cleanup

The crash fix is intentionally narrow and should be the right behavior fix.
During a later output-writer refactor, clean up the surrounding naming and
guards so this class of bug is harder to reintroduce:

- Rename `MapWriteIndices::array_index` and local `array_index` variables to
  `array_id` or similar. The value is the TolTEC array id used to key
  `calib.array_fwhms`, not a positional index into `calib.arrays`.
- Add/centralize a checked array-FWHM lookup for FITS map output rather than
  indexing `calib.array_fwhms[...]` directly.
- Keep `map_write_indices(...)` as the single source of truth for
  map-index-to-output-file, Stokes, and array-id routing.
- Revisit this when splitting `Beammap::write_beammap_map_products` and the
  shared map image writer, not during the current beammap validation rerun.

## 2026-07-07 - Beammap Output Refactor Validation Boundary

- Branch: `codex/structural-refactor`
- Known unrelated dirty file left untouched:
  - `tools/local/configure-local-build.sh`

### Unity Validation In Flight

The user pushed the beammap-output refactor set and started a Unity beammap
compile/reduction. Treat that Unity run as validating the pushed state through
the map-product/output helper work, including the split-FITS, fit-QC metadata,
detector TOD metadata, and map-product logging helpers.

While that Unity reduction is running, additional local-only refactor commits
were made and locally built. These are not part of the in-flight Unity
validation unless the user explicitly says they pushed them:

- `0708b5c2 Extract beammap fit QC signal vectors`
- `2ace8525 Extract beammap PTC output metadata helper`
- `af908303 Extract beammap APT table output helper`
- `7eeab46e Extract beammap output target selection`

Each code commit passed:

```bash
cmake --build build --target citlali_cli -j 8
```

### Current Refactor Direction

The latest local work remains output/product focused:

- `Beammap` no longer exposes a fit-QC-only signal-vector helper type/method;
  the calculation now lives with the detector-table vector helpers.
- The PTC product writer delegates the `FRUITLOOPS_ITER` NetCDF scalar update
  to a small helper, keeping the writer focused on output workflow.
- The APT table writer delegates ECSV matrix construction to a helper while
  preserving the original `calib.apt[...]` lookup behavior.
- The beammap output dispatcher delegates map-buffer/FITS-vector target
  selection and raw-observation output side effects to a helper, preserving the
  original output order.

Do not push the local-only commits until deciding how to handle the incoming
Unity beammap result. If the Unity reduction fails against the pushed state,
fix that first or clearly separate the failure from the local-only follow-on
refactors.

## 2026-07-08 - Beammap Validation and Typed Post-Processing Cleanup

- Branch: `codex/structural-refactor`
- Known unrelated dirty file left untouched:
  - `tools/local/configure-local-build.sh`

### Beammap Validation Checkpoint

The user downloaded the latest refactor beammap reduction:

- OG comparison: `beammap/citlali/reduced/redu01`
- Refactor comparison: `beammap/refactor/reduced/redu04`

The low-level `70_reduce.yaml`/`72_reduce.yaml` inputs match except for the
expected executable path. The refactor run completed cleanly and logged:

- citlali `v4.0.0-3013-g7b484eb9`
- kids `04088da`
- tula `f30f81d`

Validation result:

- APT and fit-QC ECSV products have matching row/column counts and identical
  `flag`/`flag2` classifications.
- Six split FITS products have matching file sizes, HDU counts, HDU names, and
  sampled pixel data consistent with floating-point drift.
- Stats NetCDF variable names match. Largest visible differences are in weight
  products where global relative scale is small; high local relative values are
  near-zero denominator effects.
- `source_crossing_tod` has identical file sets and NetCDF variable sets. The
  `index.yaml` difference is only timestamp/version metadata.
- Runtime is close to OG for this validation pair: refactor about `58m38s`
  versus OG about `55m04s`, much closer than the earlier rough 30% slowdown
  concern.

Conclusion: no product-structure or detector-classification red flags from the
latest beammap validation. Continue refactoring.

### Current Direction

The active phase is now typed-config consumer wiring rather than more broad
file splitting. Recent work has moved beammap, pointing, map-filter, output,
and metadata policies toward `typed_config`.

The latest local cleanup centralizes post-processing activation in
`PostProcessingConfig` helpers:

- `set_map_filtering_enabled`
- `map_filtering_active`
- `set_source_finding_enabled`
- `source_finding_active`

This keeps the compatibility mirror booleans and nested typed config fields in
sync without changing current behavior. The code passed:

```bash
cmake --build build --target citlali_cli -j 8
```

Follow-on cleanup added the same style of typed predicates for coadd/noise
activation and output policy:

- `coadd_active`
- `noise_maps_active`
- `noise_realization_outputs_active`
- `noise_product_outputs_active`
- `empirical_noise_weights_active`
