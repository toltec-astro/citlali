# Citlali Refactor Handoff - 2026-06-30

Local timestamp: 2026-06-30 07:35:30 EDT

## Update - Deterministic validation gate

- Codified the first structural-refactor behavior gate around the point 152389
  validation case.
- Added `tools/baseline/compare_deterministic_manifests.sh`, a wrapper around
  `compare_manifests.py` that applies the accepted deterministic comparison
  policy:
  - `--ignore-sha256`
  - `--atol 2e-8`
  - `--rtol 1e-10`
  - ignore run case labels, paths, mtimes, byte sizes, aggregate byte totals,
    and log line-count drift
- Updated `tools/baseline/README.md` with the `seq`/one-thread validation
  workflow and example commands.
- Added `doc/VALIDATION_POINT_152389_SEQ_2026-06-30.md` documenting:
  - local and Unity validation layout
  - deterministic baseline/refactor run-to-run results
  - baseline-vs-refactor tolerance result
  - known OMP/14 nondeterminism that should be handled in a separate branch
- Validation result captured:
  - baseline `redu02` vs `redu03`: deterministic under `parallel_policy: seq`,
    `n_threads: 1`
  - refactor `redu02` vs `redu03`: deterministic under the same mode
  - baseline `redu02` vs refactor `redu02`: matches with `atol=2e-8`,
    `rtol=1e-10`; differences are floating roundoff only

## Update - Typed config/error scaffolding

- Added initial header-only library scaffolding for the next refactor phase:
  - `include/citlali/core/error/error.h`
  - `include/citlali/core/config/config_error.h`
  - `include/citlali/core/config/enum_parser.h`
  - `include/citlali/core/config/runtime_config.h`
  - `include/citlali/core/config/mapmaking_config.h`
  - `include/citlali/core/config/noise_config.h`
  - `include/citlali/core/config/reduction_config.h`
- The config enums mirror existing YAML strings for:
  - `runtime.parallel_policy`: `seq`, `omp`
  - `runtime.reduction_type`: `science`, `pointing`, `beammap`
  - `mapmaking.grouping`: `auto`, `detector`, `nw`, `array`, `fg`
  - `mapmaking.method`: `naive`, `jinc`, `maximum_likelihood`
- Added pure validation helpers and `ValidationReport` diagnostics without
  wiring them into `Engine` or the CLI yet, so runtime behavior is unchanged.
- Added `tests/test_config_scaffold.cpp` and included it in the optional
  `citlali_test` target.

## Update - Runtime config adapter wiring

- Added the first staged typed-config wiring in `Engine`:
  - included `citlali/core/config/runtime_config.h`
  - added `typed_runtime_config` as a typed mirror of existing runtime fields
  - added `Engine::get_runtime_config`
  - changed `Engine::get_citlali_config` to read runtime options through that
    adapter before continuing into timestream and mapmaking config
- The adapter deliberately keeps the existing `get_config_value` calls as the
  authoritative parser for now, so existing `missing_keys` and `invalid_keys`
  behavior is preserved.
- Each typed runtime field is copied only when the corresponding legacy key
  parsed cleanly. Valid configs should populate the same legacy fields as
  before:
  - `verbose_mode`
  - `output_dir`
  - `n_threads`
  - `parallel_policy`
  - `redu_type`
  - `use_subdir`
  - `interp_over_gaps`
- No mapmaking, timestream, pointing, beammap, product writing, or CLI behavior
  was intentionally changed.

## Update - Runtime adapter validation attempt, refactor redu04

- User downloaded
  `/Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu04`
  after the runtime-config adapter build.
- Generated manifest:
  `/private/tmp/point_152389_refactor_runtime_adapter_redu04_manifest.json`
- Manifest generation command completed with 29 files and 0 tool warnings.
- Deterministic comparisons against both:
  - `/private/tmp/point_152389_citlali_seq_redu02_manifest.json`
  - `/private/tmp/point_152389_refactor_seq_redu02_manifest.json`
  reported large numeric differences.
- This is not a valid deterministic-gate comparison because `redu04` was run
  with a different runtime config:
  - `redu02`: `runtime.n_threads: 1`, `runtime.parallel_policy: seq`
  - `redu04`: `runtime.n_threads: 6`, `runtime.parallel_policy: omp`
- The generated YAML diff is limited to those runtime values:
  `n_threads` and `parallel_policy`.
- Next validation run for this adapter should rerun the point case with
  `runtime.parallel_policy: seq` and `runtime.n_threads: 1`.

## Update - Runtime adapter deterministic validation passed

- User replaced the refactor `redu04` output with a deterministic run.
- Confirmed generated runtime config:
  - `runtime.n_threads: 1`
  - `runtime.parallel_policy: seq`
  - `runtime.reduction_type: pointing`
- Generated manifest:
  `/private/tmp/point_152389_refactor_runtime_adapter_seq_redu04_manifest.json`
- Manifest generation completed with 29 files and 0 tool warnings.
- Deterministic comparison against the protected baseline passed:
  - baseline:
    `/private/tmp/point_152389_citlali_seq_redu02_manifest.json`
  - candidate:
    `/private/tmp/point_152389_refactor_runtime_adapter_seq_redu04_manifest.json`
  - result: `manifests match`
- Deterministic comparison against the previous refactor seq run also passed:
  - baseline:
    `/private/tmp/point_152389_refactor_seq_redu02_manifest.json`
  - candidate:
    `/private/tmp/point_152389_refactor_runtime_adapter_seq_redu04_manifest.json`
  - result: `manifests match`
- Conclusion: the runtime-config adapter wiring is behavior-preserving for the
  point 152389 deterministic gate.

## Update - Mapmaking/noise typed mirror wiring

- Added the next staged typed-config mirror fields in `Engine`:
  - `typed_mapmaking_config`
  - `typed_noise_config`
- Included:
  - `citlali/core/config/mapmaking_config.h`
  - `citlali/core/config/noise_config.h`
- `Engine::get_mapmaking_config` now mirrors cleanly parsed legacy values into
  those typed structs while leaving the existing legacy fields and parser calls
  authoritative.
- Mirrored mapmaking values:
  - `mapmaking.enabled`
  - `mapmaking.grouping`
  - `mapmaking.method`
  - `mapmaking.pixel_axes`
  - map buffer values after `omb.get_config` succeeds:
    `coverage_cut`, `pixel_size_arcsec`, `cunit`, `x_size_pix`, `y_size_pix`,
    `crpix1`, `crpix2`, `crval1_J2000`, `crval2_J2000`
- Mirrored noise values:
  - `noise_maps.enabled`
  - `noise_maps.n_noise_maps`
  - `noise_maps.randomize_dets`
  - `noise_maps.write_realizations`
  - `noise_maps.products.enabled`
  - `noise_maps.products.apply_empirical_weights`
- Adjusted standalone `NoiseConfig` validation to match the legacy parser more
  closely: `n_noise_maps` is only validated when noise maps are enabled, and
  the lower bound matches the current legacy `get_config_value(..., {0})`
  behavior.
- Updated the config scaffold unit test accordingly.
- No mapmaking algorithm, map buffer parsing, noise-map generation, product
  writing, or CLI behavior was intentionally changed.

## Update - Mapmaking/noise mirror deterministic validation passed

- User downloaded the latest refactor reduction:
  `/Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu05`
- Confirmed generated runtime config:
  - `runtime.n_threads: 1`
  - `runtime.parallel_policy: seq`
  - `runtime.reduction_type: pointing`
- Confirmed generated config for `redu05` is identical to the previous
  validated `redu04` config.
- Generated manifest:
  `/private/tmp/point_152389_refactor_mapmaking_noise_mirror_seq_redu05_manifest.json`
- Manifest generation completed with 29 files and 0 tool warnings.
- Deterministic comparison against the protected baseline passed:
  - baseline:
    `/private/tmp/point_152389_citlali_seq_redu02_manifest.json`
  - candidate:
    `/private/tmp/point_152389_refactor_mapmaking_noise_mirror_seq_redu05_manifest.json`
  - result: `manifests match`
- Deterministic comparison against the previous runtime-adapter refactor run
  also passed:
  - baseline:
    `/private/tmp/point_152389_refactor_runtime_adapter_seq_redu04_manifest.json`
  - candidate:
    `/private/tmp/point_152389_refactor_mapmaking_noise_mirror_seq_redu05_manifest.json`
  - result: `manifests match`
- Conclusion: the mapmaking/noise typed mirror wiring is behavior-preserving
  for the point 152389 deterministic gate.

## Update - Imported OG determinism fix

- Imported the OG `gw_dev` determinism fix from local checkout
  `/Users/gwilson/GitHub/citlali`.
- Source commit:
  `991428e70c280c4a7d917d9079cd84264c757821`
  (`fixing nondeterminancy`)
- Applied with `git cherry-pick --no-commit`, so the code changes are staged
  but not committed in this worktree.
- Files imported from the OG fix:
  - `include/citlali/core/engine/engine.h`
  - `include/citlali/core/timestream/ptc/ptcproc.h`
  - `include/citlali/core/timestream/rtc/rtcproc.h`
  - `include/citlali/core/timestream/timestream.h`
- High-level behavior:
  - adds mutex-protected snapshots/copies around RTC/PTC diagnostic caches
  - avoids direct unlocked map/vector access while parallel scan work is still
    mutating diagnostic state
  - snapshots RTC source-protection diagnostics before recording learning state
  - clears cached RTC diagnostics under the same mutexes that protect writes
- This is intentionally separate from the typed-config refactor slices.
- Required Unity validation after push:
  - compile
  - deterministic point `seq`/one-thread comparison
  - two OMP point runs to confirm the prior run-to-run drift is gone in this
    refactor branch

## Update - First OMP validation after determinism fix

- User downloaded parallel refactor run:
  `/Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu06`
- Refactor branch commit at local inspection:
  `ee429eca` (`fixing non-determinism a la OG citlali`)
- Confirmed generated config differs from validated `redu05` only in runtime
  parallelism:
  - `redu05`: `runtime.n_threads: 1`,
    `runtime.parallel_policy: seq`
  - `redu06`: `runtime.n_threads: 6`,
    `runtime.parallel_policy: omp`
- Generated manifest:
  `/private/tmp/point_152389_refactor_determinism_fix_omp6_redu06_manifest.json`
- Manifest generation completed with 29 files and 0 tool warnings.
- OMP/6 comparison against latest validated refactor seq run passed:
  - baseline:
    `/private/tmp/point_152389_refactor_mapmaking_noise_mirror_seq_redu05_manifest.json`
  - candidate:
    `/private/tmp/point_152389_refactor_determinism_fix_omp6_redu06_manifest.json`
  - result: `manifests match`
- OMP/6 comparison against protected baseline seq run also passed:
  - baseline:
    `/private/tmp/point_152389_citlali_seq_redu02_manifest.json`
  - candidate:
    `/private/tmp/point_152389_refactor_determinism_fix_omp6_redu06_manifest.json`
  - result: `manifests match`
- Conclusion so far: the first OMP/6 run after importing the OG determinism fix
  matches the deterministic point 152389 gate under the standard comparison
  policy. A second OMP/6 run is still needed to verify OMP run-to-run
  stability directly.

## Update - Second OMP validation after determinism fix passed

- User downloaded second parallel refactor run:
  `/Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu07`
- Confirmed generated config is byte-identical to `redu06`.
- Confirmed runtime settings:
  - `runtime.n_threads: 6`
  - `runtime.parallel_policy: omp`
  - `runtime.reduction_type: pointing`
- Generated manifest:
  `/private/tmp/point_152389_refactor_determinism_fix_omp6_redu07_manifest.json`
- Manifest generation completed with 29 files and 0 tool warnings.
- Direct OMP run-to-run comparison passed:
  - baseline:
    `/private/tmp/point_152389_refactor_determinism_fix_omp6_redu06_manifest.json`
  - candidate:
    `/private/tmp/point_152389_refactor_determinism_fix_omp6_redu07_manifest.json`
  - result: `manifests match`
- OMP/6 comparison against latest validated refactor seq run passed:
  - baseline:
    `/private/tmp/point_152389_refactor_mapmaking_noise_mirror_seq_redu05_manifest.json`
  - candidate:
    `/private/tmp/point_152389_refactor_determinism_fix_omp6_redu07_manifest.json`
  - result: `manifests match`
- OMP/6 comparison against protected baseline seq run passed:
  - baseline:
    `/private/tmp/point_152389_citlali_seq_redu02_manifest.json`
  - candidate:
    `/private/tmp/point_152389_refactor_determinism_fix_omp6_redu07_manifest.json`
  - result: `manifests match`
- Conclusion: the imported OG determinism fix resolves the earlier point 152389
  OMP run-to-run drift in this refactor branch under the tested OMP/6 setup.

## Update - Config mirror phase 2 wiring

- Added the next staged typed-config mirror headers:
  - `include/citlali/core/config/timestream_config.h`
  - `include/citlali/core/config/coadd_config.h`
  - `include/citlali/core/config/post_processing_config.h`
  - `include/citlali/core/config/pointing_config.h`
- Extended `ReductionConfig` to include those structs and validation hooks where
  applicable.
- Added typed engine mirrors:
  - `typed_timestream_config`
  - `typed_coadd_config`
  - `typed_post_processing_config`
  - `typed_pointing_config`
- Wired the mirrors into the existing legacy readers without changing the
  authoritative execution fields:
  - shallow timestream controls: `enabled`, TOD type, RTC/PTC output enables,
    derived output type, output subdir, and stats eigenvalue flag
  - coadd enable
  - post-processing enables for map filtering and source finding
  - source fitting arcsec inputs and Gaussian limit factors before the legacy
    pixel conversion
  - pointing source strategy, Gaussian-fit switch, fruit-loops center mode, and
    header-center controls
- Local checks passed:
  - config header syntax checks for `reduction_config.h`,
    `timestream_config.h`, and `pointing_config.h`
  - `git diff --check`
  - prior OMP/6 deterministic manifests `redu06` vs `redu07` still match
- No Unity compile or new reduction was attempted. Next validation should be a
  Unity compile and one point 152389 OMP/6 run compared against the validated
  `redu07` or protected baseline manifests.

## Update - Config mirror phase 2 validation passed

- User downloaded the post-config-mirror validation run:
  `/Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu08`
- Refactor branch commit at validation inspection:
  `2cf6a537` (`more config mirrors`)
- Confirmed generated Citlali low-level YAML is byte-identical to validated
  `redu07`.
- Confirmed runtime settings:
  - `runtime.n_threads: 6`
  - `runtime.parallel_policy: omp`
  - `runtime.reduction_type: pointing`
- Generated manifest:
  `/private/tmp/point_152389_refactor_config_mirror_phase2_omp6_redu08_manifest.json`
- Manifest generation completed with 29 files and 0 tool warnings.
- Direct comparison against validated OMP/6 refactor run passed:
  - baseline:
    `/private/tmp/point_152389_refactor_determinism_fix_omp6_redu07_manifest.json`
  - candidate:
    `/private/tmp/point_152389_refactor_config_mirror_phase2_omp6_redu08_manifest.json`
  - result: `manifests match`
  - also passed with exact structured numeric tolerances
- OMP/6 comparison against latest validated refactor seq run passed:
  - baseline:
    `/private/tmp/point_152389_refactor_mapmaking_noise_mirror_seq_redu05_manifest.json`
  - candidate:
    `/private/tmp/point_152389_refactor_config_mirror_phase2_omp6_redu08_manifest.json`
  - result: `manifests match`
- OMP/6 comparison against protected baseline seq run passed:
  - baseline:
    `/private/tmp/point_152389_citlali_seq_redu02_manifest.json`
  - candidate:
    `/private/tmp/point_152389_refactor_config_mirror_phase2_omp6_redu08_manifest.json`
  - result: `manifests match`
- Conclusion: config mirror phase 2 is behavior-preserving for the point
  152389 OMP/6 validation gate.

## Update - Beammap typed config mirror scaffold

- Added `include/citlali/core/config/beammap_config.h`.
- Added `BeammapConfig` to `ReductionConfig` validation.
- Added `Engine::typed_beammap_config`.
- `Engine::get_beammap_config` now mirrors the effective legacy beammap values
  into the typed struct after the legacy parser applies defaults and local
  compatibility adjustments.
- Mirrored beammap groups include:
  - beammap source name, sky position, and per-array source fluxes
  - iteration and phase strategy
  - reference detector/subtraction/derotation
  - RFI mask and scan-band mask settings
  - detector weighting mode
  - split-FITS-by-flag settings
  - soft-prior settings
  - detector TOD sidecar settings
  - flagging vectors and sensitivity limits
- Existing legacy fields remain authoritative; no runtime behavior is intended
  to change.
- Updated config scaffold tests for the beammap detector-weighting enum and
  beammap validation bounds.
- Local checks only:
  - direct C++ syntax checks for `beammap_config.h` and
    `reduction_config.h` passed, with only expected `#pragma once` warnings
    from compiling headers directly
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
- No Unity compile or reduction was attempted. Next validation should compile
  on Unity and run at least the existing point gate; a representative beammap
  compile/reduction check is also appropriate because this touches beammap
  config wiring.

## Update - Timestream output/chunking typed mirror scaffold

- Extended `include/citlali/core/config/timestream_config.h` with typed
  mirrors for per-stream TOD output details:
  - raw/processed output mode: `full`, `mini`, `full_outer`, `mini_outer`
  - optional outer context sample count
  - explicit 1-based output chunk indices
  - output selection mode: `indices`, `all`,
    `uniform_plus_source_crossing`
  - uniform/source-dense selection counts
- Added typed mirror coverage for the `timestream.chunking` block:
  `chunk_mode`, `value`, and `force_chunking`.
- `Engine::get_timestream_config` now mirrors these values after the legacy
  parser applies defaults and validation. The existing legacy fields remain
  authoritative, so no runtime behavior is intended to change.
- Updated config scaffold tests for the new TOD output enums and validation
  bounds.
- Local checks only:
  - direct C++ syntax checks for `timestream_config.h` and
    `reduction_config.h` passed, with only expected `#pragma once` warnings
    from compiling headers directly
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
- No Unity compile or reduction was attempted.

## Update - Timestream output/chunking OMP/6 point validation passed

- User downloaded the point 152389 refactor validation run:
  `/Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu09`
- Generated config confirms this was an OMP/6 run, not the seq/one-thread
  deterministic gate:
  - `runtime.n_threads: 6`
  - `runtime.parallel_policy: omp`
  - `runtime.reduction_type: pointing`
- Generated manifest:
  `/private/tmp/point_152389_refactor_timestream_output_omp6_redu09_manifest.json`
- Manifest generation completed with 29 files and 0 tool warnings.
- Direct comparison against the latest validated OMP/6 refactor run passed:
  - baseline:
    `/private/tmp/point_152389_refactor_config_mirror_phase2_omp6_redu08_manifest.json`
  - candidate:
    `/private/tmp/point_152389_refactor_timestream_output_omp6_redu09_manifest.json`
  - result: `manifests match`
- OMP/6 comparison against latest validated refactor seq run passed:
  - baseline:
    `/private/tmp/point_152389_refactor_mapmaking_noise_mirror_seq_redu05_manifest.json`
  - candidate:
    `/private/tmp/point_152389_refactor_timestream_output_omp6_redu09_manifest.json`
  - result: `manifests match`
- OMP/6 comparison against protected baseline seq run passed:
  - baseline:
    `/private/tmp/point_152389_citlali_seq_redu02_manifest.json`
  - candidate:
    `/private/tmp/point_152389_refactor_timestream_output_omp6_redu09_manifest.json`
  - result: `manifests match`
- Conclusion: timestream output/chunking typed mirror is behavior-preserving
  for the point 152389 OMP/6 validation gate.

## Update - Post-processing map-filter typed mirror scaffold

- Extended `include/citlali/core/config/post_processing_config.h` with typed
  mirrors for `post_processing.map_filtering` and `wiener_filter` settings.
- Mirrored map-filter values include:
  - filter type: `wiener_filter`, `convolve`, `destripe`
  - template type: `kernel`, `gaussian`, `airy`, `highpass`
  - low-pass-only and error-normalization flags
  - edge-guard enablement and threshold/fill/taper settings
  - denominator convergence tolerances and iteration controls
  - per-array template FWHM values in arcseconds
- `Engine::get_map_filter_config` now mirrors these values from the legacy
  `wiener_filter` object after its existing parser runs. The legacy parser and
  runtime fields remain authoritative; no runtime behavior is intended to
  change.
- Updated config scaffold tests for the map-filter enums and validation bounds.
- Local checks only:
  - direct C++ syntax checks for `post_processing_config.h` and
    `reduction_config.h` passed, with only expected `#pragma once` warnings
    from compiling headers directly
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
- No Unity compile or reduction was attempted.

## Update - Post-processing source-finding typed mirror scaffold

- Extended `include/citlali/core/config/post_processing_config.h` with
  `SourceFindingConfig`.
- Mirrored source-finding values include:
  - enablement
  - `source_sigma`
  - `source_window_arcsec`
  - `mode`
- `Engine::get_citlali_config` now mirrors these values before the legacy
  `source_window_arcsec` value is converted to radians. The legacy fields
  remain authoritative; no runtime behavior is intended to change.
- The typed mirror keeps `mode` as a string because the legacy parser currently
  does not constrain the allowed values.
- Updated config scaffold tests for source-finding validation bounds.
- Local checks only:
  - direct C++ syntax checks for `post_processing_config.h` and
    `reduction_config.h` passed, with only expected `#pragma once` warnings
    from compiling headers directly
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
- No Unity compile or reduction was attempted.

## Update - Post-processing histogram typed mirror scaffold

- Added `PostProcessingConfig::map_histogram_n_bins`.
- `Engine::get_mapmaking_config` now mirrors the effective
  `post_processing.map_histogram_n_bins` value from `omb.hist_n_bins` after
  `MapBuffer::get_config` parses it.
- Moved the typed post-processing reset to immediately before mapmaking config
  parsing so the histogram mirror survives into the later post-processing
  parser block.
- Existing legacy fields remain authoritative; no runtime behavior is intended
  to change.
- Updated config scaffold validation coverage for negative histogram bin
  counts.
- Local checks only:
  - direct C++ syntax checks for `post_processing_config.h` and
    `reduction_config.h` passed, with only expected `#pragma once` warnings
    from compiling headers directly
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
- No Unity compile or reduction was attempted.

## Update - Timestream learning core typed mirror scaffold

- Extended `include/citlali/core/config/timestream_config.h` with
  `TimestreamLearningConfig`.
- Mirrored core `timestream.learning` lifecycle/apply values include:
  - `enabled`
  - `diagnostics_enabled`
  - `learn_iters`
  - `apply_start_iter`
  - `max_records_per_type`
  - `apply_sample_masks_enabled`
  - `apply_max_new_flagged_fraction`
- `Engine::get_learning_config` now mirrors these values after the existing
  legacy parser accepts each key. The legacy `ReductionLearningState::Options`
  object remains authoritative; no runtime behavior is intended to change.
- This intentionally does not yet mirror the larger map-pixel-outlier,
  busy-detector, or scan-network-pathology option groups.
- Updated config scaffold tests for the new learning validation bounds.
- Local checks only:
  - direct C++ syntax checks for `timestream_config.h` and
    `reduction_config.h` passed, with only expected `#pragma once` warnings
    from compiling headers directly
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
- No Unity compile or reduction was attempted.

## Update - Timestream learning subgroup typed mirror scaffold

- Extended `include/citlali/core/config/timestream_config.h` with typed
  mirrors for the remaining `timestream.learning` option groups:
  - `TimestreamLearningMapPixelOutlierConfig`
  - `TimestreamLearningBusyDetectorConfig`
  - `TimestreamLearningScanNetworkPathologyConfig`
- Mirrored map-pixel-outlier settings include diagnostics flags, contributor
  tracing flags, detector-exclusion controls, retained-pixel limits, robust-z
  thresholds, coverage thresholds, and source-protection radius.
- Mirrored busy-detector settings include scan-local detector exclusion
  enablement.
- Mirrored scan-network-pathology settings include enablement, pre-stage apply
  flags, candidate/severe event thresholds, residual-z thresholds, and the
  maximum newly flagged fraction.
- `Engine::get_learning_config` now mirrors these values after the existing
  legacy parser accepts each key. The legacy `ReductionLearningState::Options`
  object remains authoritative; no runtime behavior is intended to change.
- Updated config scaffold tests for the new learning subgroup validation
  bounds.
- Local checks only:
  - direct C++ syntax checks for `timestream_config.h` and
    `reduction_config.h` passed, with only expected `#pragma once` warnings
    from compiling headers directly
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
- No Unity compile or reduction was attempted.

## Update - Timestream source-protection typed mirror scaffold

- Extended `include/citlali/core/config/timestream_config.h` with typed
  mirrors for source-protection controls in:
  - `timestream.raw_time_chunk.despike.source_protection`
  - `timestream.processed_time_chunk.flagging.second_pass_local.source_protection`
- The mirror records both the configured enable flag and the effective active
  flag after Citlali applies the reduction-type/source-aware gate.
- `Engine::get_rtc_config` mirrors raw-time-chunk despike source-protection
  settings from the legacy `rtcproc` parser state.
- `Engine::get_ptc_config` mirrors processed-time-chunk second-pass local
  source-protection settings from the legacy `ptcproc` parser state.
- The existing legacy parser and runtime fields remain authoritative; no
  runtime behavior is intended to change.
- Updated config scaffold tests for source-protection radius validation.
- Local checks only:
  - direct C++ syntax checks for `timestream_config.h` and
    `reduction_config.h` passed, with only expected `#pragma once` warnings
    from compiling headers directly
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
- No Unity compile or reduction was attempted.

## Update - Raw-time-chunk despike typed mirror scaffold

- Extended `include/citlali/core/config/timestream_config.h` with typed
  mirrors for `timestream.raw_time_chunk.despike` controls:
  - core despike enablement and legacy-pass enablement
  - `min_spike_sigma`, `time_constant_sec`, and `window_size`
  - local-residual compact-event controls
  - compact raw/delta morphology gates
- `Engine::get_rtc_config` now mirrors the effective legacy
  `rtcproc.despiker` state after `RTCProc::get_config` completes. The legacy
  parser and runtime fields remain authoritative; no runtime behavior is
  intended to change.
- The typed mirror only copies the main despike numeric fields when
  `raw_time_chunk.despike.enabled` is true because the legacy `Despiker` object
  only initializes those fields when the parser reads that block.
- Updated config scaffold tests for local-residual and compact-gate validation
  bounds.
- Local checks only:
  - direct C++ syntax checks for `timestream_config.h` and
    `reduction_config.h` passed, with only expected `#pragma once` warnings
    from compiling headers directly
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
- No Unity compile or reduction was attempted.

## Update - Processed-time-chunk second-pass local typed mirror scaffold

- Extended `include/citlali/core/config/timestream_config.h` with typed
  mirrors for
  `timestream.processed_time_chunk.flagging.second_pass_local` controls:
  - enablement and source-protection settings
  - spike/good-fraction/baseline thresholds
  - raw-like and delta-like residual event controls
  - step-shift, event merge, cluster, and auto-flag caps
  - selective busy-network acceptance flag
- `Engine::get_ptc_config` now mirrors the effective legacy
  `ptcproc.second_pass_local` state after `PTCProc::get_config` completes.
  The legacy parser and runtime fields remain authoritative; no runtime
  behavior is intended to change.
- Updated config scaffold tests for second-pass local validation bounds.
- Local checks only:
  - direct C++ syntax checks for `timestream_config.h` and
    `reduction_config.h` passed, with only expected `#pragma once` warnings
    from compiling headers directly
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
- No Unity compile or reduction was attempted.

## Update - Raw despike/PTC second-pass OMP/6 point validation passed

- User reported the latest commit compiled on Unity and downloaded the point
  validation run as
  `/Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu10`.
- Confirmed generated config:
  - `runtime.n_threads: 6`
  - `runtime.parallel_policy: omp`
  - `runtime.reduction_type: pointing`
- Generated manifest:
  `/private/tmp/point_152389_refactor_ptc_second_pass_omp6_redu10_manifest.json`
- Manifest generation completed with 29 files and 0 tool warnings.
- OMP/6 comparison against the previous accepted refactor OMP/6 run passed:
  - baseline:
    `/private/tmp/point_152389_refactor_timestream_output_omp6_redu09_manifest.json`
  - candidate:
    `/private/tmp/point_152389_refactor_ptc_second_pass_omp6_redu10_manifest.json`
  - result: `manifests match`
- OMP/6 comparison against the earlier config-mirror OMP/6 run passed:
  - baseline:
    `/private/tmp/point_152389_refactor_config_mirror_phase2_omp6_redu08_manifest.json`
  - candidate:
    `/private/tmp/point_152389_refactor_ptc_second_pass_omp6_redu10_manifest.json`
  - result: `manifests match`
- OMP/6 comparison against latest validated refactor seq run passed:
  - baseline:
    `/private/tmp/point_152389_refactor_mapmaking_noise_mirror_seq_redu05_manifest.json`
  - candidate:
    `/private/tmp/point_152389_refactor_ptc_second_pass_omp6_redu10_manifest.json`
  - result: `manifests match`
- OMP/6 comparison against protected baseline seq run passed:
  - baseline:
    `/private/tmp/point_152389_citlali_seq_redu02_manifest.json`
  - candidate:
    `/private/tmp/point_152389_refactor_ptc_second_pass_omp6_redu10_manifest.json`
  - result: `manifests match`
- Conclusion: the source-protection, raw despike, and PTC second-pass local
  typed mirrors are behavior-preserving for the point 152389 OMP/6 validation
  gate.

## Update - TolTECA low-level config boundary inventory

- Added `tools/config/tolteca_lowlevel_inventory.py`.
- The tool compares TolTECA authoring YAML files against the generated
  `citlali_*.yaml` file that Citlali ingests.
- It can scan a reduction directory for all `NN*.yaml` files in leading-number
  merge order, matching the TolTECA config workflow where higher numbered files
  override lower numbered files.
- It flattens `reduce.steps.*.config.low_level`, normalizes list indexes, and
  classifies generated keys as:
  - copied from low-level authoring with the same value
  - copied path but rewritten by TolTECA
  - TolTECA-generated input assembly
  - generated but not found in authoring YAML
- Ran the tool on the downloaded point 152389 `redu08` validation case using
  the reduction directory scan.
- Results:
  - generated Citlali leaf keys: 489
  - `40_setup.yaml` low-level leaf keys: 0
  - `70_reduce.yaml` low-level leaf keys: 444
  - `72_reduce.yaml` low-level leaf keys: 0
  - copied from `70_reduce.yaml` low-level with same value: 443
  - TolTECA-generated `inputs` leaves: 45
  - low-level paths rewritten by TolTECA: 1
  - generated non-input keys not found in authoring YAML: 0
- The only rewritten low-level path is `runtime.output_dir`, from `"."` in
  `70_reduce.yaml` to the active TolTECA reduction output directory.
- Added `doc/TOLTECA_CONFIG_BOUNDARY_POINT_152389_2026-06-30.md`.
- Updated `tools/config/README.md` with the new inventory workflow.
- Conclusion: in this point workflow, config simplification should target the
  TolTECA-authored `low_level` block/profile first while keeping the generated
  legacy Citlali YAML valid. `inputs` and `runtime.output_dir` should be
  treated as TolTECA-owned assembly products.

## Update - Four-intent compact config prototype

- Extended `tools/config/expand_compact_config.py` so the compact authoring
  layer supports four first-class reduction intents:
  - `pointing`
  - `oof`
  - `beammap`
  - `science`
- Added `oof` as a first-class compact mode while mapping it to the existing
  legacy Citlali engine setting `runtime.reduction_type: pointing`.
- Added:
  - `tools/config/profiles/oof_standard.yaml`
  - `tools/config/examples/oof_standard_compact.yaml`
- Added output formats to the compact expander:
  - `full`: standalone legacy Citlali-style YAML tree
  - `low_level`: bare low-level block with TolTECA-owned `inputs` omitted
  - `tolteca`: `reduce.steps.0.config.low_level` wrapper suitable for a
    TolTECA `NN*.yaml` file
- `--base-config` now accepts either full Citlali YAML or a TolTECA file
  containing `reduce.steps.*.config.low_level`; the latter is extracted before
  profile and compact overlays are applied.
- Added `tools/config/compare_lowlevel_yaml.py`, a pass/fail low-level YAML
  equivalence helper that accepts bare low-level YAML or TolTECA wrappers.
- Added point compatibility harness files:
  - `tools/config/profiles/pointing_compat_passthrough.yaml`
  - `tools/config/examples/pointing_compat_passthrough_compact.yaml`
- Added compatibility passthrough harnesses for the remaining user-facing
  intents:
  - `tools/config/profiles/oof_compat_passthrough.yaml`
  - `tools/config/profiles/beammap_compat_passthrough.yaml`
  - `tools/config/profiles/science_compat_passthrough.yaml`
  - matching examples under `tools/config/examples/`
- Added `tools/config/examples/pointing_compat_point70_compact.yaml`, which
  expresses the point 152389 runtime, map, product, processing, and
  source-fitting values through compact keys while using the passthrough
  baseline profile.
- Added stronger compact-key fixtures for representative non-point baselines:
  - `tools/config/examples/oof_compat_149056_compact.yaml`
  - `tools/config/examples/beammap_compat_3c273_compact.yaml`
  - `tools/config/examples/science_compat_goodsn_compact.yaml`
- Added docs:
  - `doc/CITLALI_TOLTECA_REDUCTION_INTENTS_2026-06-30.md`
  - `doc/POINTING_COMPACT_EQUIVALENCE_2026-06-30.md`
- First point-equivalence results against the downloaded point 152389
  `70_reduce.yaml` baseline:
  - `pointing_standard`: 178 differences after ignoring `runtime.output_dir`
  - `pointing_compat_passthrough`: 0 differences after ignoring
    `runtime.output_dir`
  - `pointing_compat_point70_compact`: 0 differences after ignoring
    `runtime.output_dir`
- Representative passthrough checks for all four intents returned zero
  low-level differences after ignoring `runtime.output_dir`:
  - point 152389 refactor `70_reduce.yaml`: 444 leaves
  - OOF `149056/70_reduce.yaml`: 164 leaves
  - beammap `3c273/70_reduce.yaml`: 485 leaves
  - science `GOODS-N/70_reduce.yaml`: 219 leaves
- The stronger compact-key fixtures for OOF 149056, beammap 3c273, and science
  GOODS-N also returned zero low-level differences against their selected
  baselines after ignoring `runtime.output_dir`.
- Added `tools/config/compact_compatibility_cases.yaml` and
  `tools/config/run_compact_compatibility.py` to turn those checks into a
  repeatable local gate. Current local run:
  - command:
    `$HOME/tolteca/bin/python tools/config/run_compact_compatibility.py --work-dir /private/tmp/citlali_compact_compat --json-out /private/tmp/citlali_compact_compat/results.json --markdown-out /private/tmp/citlali_compact_compat/results.md`
  - result: 8 passed, 0 failed, 0 skipped
- Added `tools/config/lowlevel_to_compact_config.py`, which bootstraps compact
  compatibility YAML from an existing low-level Citlali or TolTECA
  `70_reduce.yaml` file. OOF requires `--mode oof` because legacy Citlali still
  stores OOF as `runtime.reduction_type: pointing`.
- Generated compact YAML from the representative point, OOF, beammap, and
  science baselines and round-tripped each back through
  `expand_compact_config.py` plus `compare_lowlevel_yaml.py`; all returned zero
  differences after ignoring `runtime.output_dir`.
- Updated `tools/config/README.md`.
- Local checks passed:
  - `py_compile` for config tools
  - `expand_compact_config.py --list-profiles`
  - expanded all compact examples in `full`, `low_level`, and `tolteca`
    formats
  - `compare_lowlevel_yaml.py` returned zero differences for the passthrough
    point compatibility harness
- No Citlali runtime behavior, parser behavior, Unity build, or reduction was
  changed by this prototype.

## Update - 2026-06-30 09:31:16 EDT

- Updated `tools/unity/citlali_refactor_bashrc.sh` so
  `citlali-refactor-update` auto-detects build parallelism when neither
  `CITLALI_REFACTOR_JOBS` nor `CITLALI_BUILD_JOBS` is set.
- Detection order:
  - `nproc`
  - `getconf _NPROCESSORS_ONLN`
  - fallback to 15
- The helper now prints the chosen build job count before invoking
  `cmake --build`.
- `CITLALI_REFACTOR_JOBS=<n>` remains the explicit cap/override.
- Updated:
  - `doc/UNITY_GIT_PULL_WORKFLOW_2026-06-30.md`
  - `tools/unity/README.md`
  - `tools/unity/unity.env.example`
- No ssh, scp, rsync-to-Unity, Citlali build/test/reduction, or Unity access was
  attempted.

## Update - 2026-06-30 09:24:59 EDT

- Read the refreshed Unity configure log copied to `/Users/gwilson/foo/foo.log`.
- The NetCDF selection flags now apply correctly:
  - `USE_INSTALLED_NETCDF=ON`
  - `CONAN_INSTALL_NETCDF=OFF`
  - `FETCH_NETCDF=OFF`
  - `FETCH_NETCDFCXX4=ON`
- The remaining configure failure is still in NetCDF CXX4, but at the HDF5
  plugin check:
  `CHECK_LIBRARY_EXISTS(${HDF5_C_LIBRARIES} H5free_memory "" HAVE_H5FREE_MEMORY)`.
- The failed refactor log shows `FIND_PACKAGE(HDF5)` resolving through Conan's
  autogenerated `FindHDF5.cmake`, while the copied working `citlali/build`
  directory does not contain a Conan `FindHDF5.cmake` and its cache records
  system HDF5 via `/usr/bin/h5cc`.
- Diagnosis: an earlier configure that used Conan NetCDF likely left stale
  generated finder modules in `citlali_refactor/build`. Those modules shadow
  CMake's system HDF5 discovery on the later installed-NetCDF configure.
- Patched `tools/unity/citlali_refactor_bashrc.sh` so, when
  `CITLALI_USE_INSTALLED_NETCDF` is enabled, it removes only these generated
  stale finder files from the refactor build directory before configure:
  - `FindHDF5.cmake`
  - `Findhdf5.cmake`
  - `FindnetCDF.cmake`
  - `FindNetCDF.cmake`
  - `FindCURL.cmake`
- Updated:
  - `doc/UNITY_GIT_PULL_WORKFLOW_2026-06-30.md`
  - `tools/unity/README.md`
- No ssh, scp, rsync-to-Unity, Citlali build/test/reduction, or Unity access was
  attempted.

## Update - 2026-06-30 09:18:17 EDT

- Read the Unity configure log copied to `/Users/gwilson/foo/foo.log`.
- The active failure is:
  `build/_deps/netcdfcxx4-src/CMakeLists.txt:413 (CHECK_LIBRARY_EXISTS)`.
- The refactor configure had selected Conan NetCDF C:
  `USE_INSTALLED_NETCDF=OFF`, `CONAN_INSTALL_NETCDF=ON`.
- The existing working Unity `citlali/build` cache uses installed NetCDF C and
  fetched NetCDF CXX4:
  - `USE_INSTALLED_NETCDF=ON`
  - `CONAN_INSTALL_NETCDF=OFF`
  - `FETCH_NETCDF=OFF`
  - `USE_INSTALLED_NETCDFCXX4=OFF`
  - `CONAN_INSTALL_NETCDFCXX4=OFF`
  - `FETCH_NETCDFCXX4=ON`
- Patched `tools/unity/citlali_refactor_bashrc.sh` to pass those NetCDF
  settings by default and added `CITLALI_USE_INSTALLED_NETCDF=ON` as the
  override knob.
- Updated:
  - `doc/UNITY_GIT_PULL_WORKFLOW_2026-06-30.md`
  - `tools/unity/README.md`
  - `tools/unity/unity.env.example`
- Local lightweight checks only:
  - `bash -n` passed for Unity shell helpers.
  - sourced `tools/unity/citlali_refactor_bashrc.sh` locally and confirmed the
    aliases are defined.
  - `git diff --check` passed.
- No ssh, scp, rsync-to-Unity, Citlali build/test/reduction, or Unity access was
  attempted.

## Update - Unity Git Pull Workflow

- Read the copied Unity `.bashrc` at `/Users/gwilson/foo/.bashrc`.
- Mirrored the existing Unity function/alias workflow for the refactor branch:
  - added `tools/unity/citlali_refactor_bashrc.sh`
  - defines `citlali_refactor_update`
  - aliases `citlali-refactor-update` and `citref-update`
- The helper:
  - uses repo `${HOME}/work_toltec/citlali_dev/citlali_refactor`
  - pulls branch `codex/structural-refactor`
  - builds under `${HOME}/work_toltec/citlali_dev/citlali_refactor/build`
  - uses preset `unity_release`
  - builds target `citlali_cli`
  - defaults to 15 build jobs
  - points `FETCHCONTENT_SOURCE_DIR_TULA` at the sibling `tula` checkout
  - refuses to use the protected gw_dev comparison checkout unless explicitly
    overridden
- Added GitHub-pull setup documentation:
  - `doc/UNITY_GIT_PULL_WORKFLOW_2026-06-30.md`
- Updated Unity helper docs:
  - `tools/unity/README.md`
  - `tools/unity/unity.env.example`
  - `doc/UNITY_SYNC_BUILD_WORKFLOW_2026-06-29.md` now marks rsync as fallback.
- Local lightweight checks only:
  - `bash -n` passed for Unity shell helpers.
  - sourced `tools/unity/citlali_refactor_bashrc.sh` locally and confirmed the
    function and aliases are defined.
  - `git diff --check` passed.
- No ssh, scp, rsync-to-Unity, Citlali build/test/reduction, or Unity access was
  attempted.

## Update - 2026-06-30 09:11:40 EDT

- Patched the Unity refactor helper to pass `CONAN_CMD` explicitly.
- The existing Unity `citlali/build` cache records:
  `/work/toltec/toltec_shared/toltec_astro/extern/pyenv/versions/conan1/bin/conan`
- `tools/unity/citlali_refactor_bashrc.sh` now:
  - uses `CITLALI_CONAN_CMD` or `CONAN_CMD` if set
  - otherwise tries the recorded existing Unity Conan path
  - then tries the TolTEC `toltec_20241023/envs/conan1` fallback path
  - then tries `command -v conan`
  - passes the resolved executable to CMake as `-DCONAN_CMD=...`
  - fails before configure with clear instructions if no executable is found
- Updated:
  - `doc/UNITY_GIT_PULL_WORKFLOW_2026-06-30.md`
  - `tools/unity/README.md`
  - `tools/unity/unity.env.example`
- Local lightweight checks only:
  - `bash -n` passed for Unity shell helpers.
  - sourced `tools/unity/citlali_refactor_bashrc.sh` locally and confirmed the
    aliases are defined.
  - `git diff --check` passed.
- No ssh, scp, rsync-to-Unity, Citlali build/test/reduction, or Unity access was
  attempted.

## Update - 2026-06-30 09:06:54 EDT

- Investigated why Unity's existing `citlali_dev/citlali/build` works without
  a sibling `tula` checkout.
- The copied Unity cache shows:
  - `citlali/build`: `FETCHCONTENT_SOURCE_DIR_TULA` is empty and
    `tula_SOURCE_DIR` is `build/_deps/tula-src`.
  - `citlali/build_unity_release_native_lto`: `FETCHCONTENT_SOURCE_DIR_TULA`
    points at `/home/toltec_umass_edu/work_toltec/citlali_dev/tula`.
- The user's normal `.bashrc` `citbuild` function runs `cmake ..` inside the
  existing `build` directory. It does not use `--preset unity_release` and does
  not force a sibling `tula` checkout.
- Updated `tools/unity/citlali_refactor_bashrc.sh` to mimic the working
  `citlali/build` behavior by default:
  - no configure preset unless `CITLALI_REFACTOR_PRESET` is set
  - `CMAKE_BUILD_TYPE=Release`
  - `CITLALI_USE_WIENER_FILTER_OMP=ON`
  - `-U FETCHCONTENT_SOURCE_DIR_TULA` so stale cache entries do not force a
    missing sibling `tula` checkout
  - CMake can populate `citlali_refactor/build/_deps/tula-src`
- Updated:
  - `doc/UNITY_GIT_PULL_WORKFLOW_2026-06-30.md`
  - `tools/unity/README.md`
  - `tools/unity/unity.env.example`
- Local lightweight checks only:
  - `bash -n` passed for Unity shell helpers.
  - sourced `tools/unity/citlali_refactor_bashrc.sh` locally and confirmed the
    aliases are defined.
  - `git diff --check` passed.
- No ssh, scp, rsync-to-Unity, Citlali build/test/reduction, or Unity access was
  attempted.

## Update - 2026-06-30 07:58:29 EDT

- Clarified the Unity `tula` dependency setup.
- `CMakeUserPresets.json` sets `FETCHCONTENT_SOURCE_DIR_TULA` to the sibling
  checkout `${HOME}/work_toltec/citlali_dev/tula`.
- Updated `tools/unity/citlali_refactor_bashrc.sh` so it fails before CMake
  with an explicit `git clone https://github.com/toltec-astro/tula.git tula`
  instruction when the `tula` checkout is missing.
- Updated `doc/UNITY_GIT_PULL_WORKFLOW_2026-06-30.md` with the one-time `tula`
  clone command and the `CITLALI_TULA_DIR` override.
- Local lightweight checks only:
  - `bash -n` passed for Unity shell helpers.
  - sourced `tools/unity/citlali_refactor_bashrc.sh` locally and confirmed the
    aliases are defined.
  - `git diff --check` passed.
- No ssh, scp, rsync-to-Unity, Citlali build/test/reduction, or Unity access was
  attempted.

## Update - 2026-06-30 07:55:23 EDT

- Fixed `tools/unity/citlali_refactor_bashrc.sh` to support Git worktrees.
- The previous checkout check required `${repo}/.git` to be a directory, but a
  Git worktree stores `.git` as a file that points back to the main repository.
- The helper now uses `git -C "${repo}" rev-parse --is-inside-work-tree` for
  both the refactor checkout and the protected baseline checkout.
- Local lightweight checks only:
  - `bash -n` passed for Unity shell helpers.
  - sourced `tools/unity/citlali_refactor_bashrc.sh` locally and confirmed the
    aliases are defined.
- No ssh, scp, rsync-to-Unity, Citlali build/test/reduction, or Unity access was
  attempted.

## Update - Processed-time-chunk weighting core typed mirror scaffold

- Extended `include/citlali/core/config/timestream_config.h` with typed mirrors
  for the core `timestream.processed_time_chunk.weighting` fields:
  - weighting type: `full`, `approximate`, `hybrid`, `validated`, `const`
  - resolved `source_mask_radius_arcsec`
  - hybrid correction min/max factors
  - median/lower/upper map-weight factors
  - `busy_row_suppression` enablement and thresholds
- Added typed mirrors for the processed-time-chunk flagging inverse-variance
  thresholds:
  - `lower_tod_inv_var_factor`
  - `upper_tod_inv_var_factor`
- `Engine::get_ptc_config` now mirrors these values from the effective
  `ptcproc` state after the existing legacy `PTCProc::get_config` parser runs.
  The legacy parser and runtime fields remain authoritative; no runtime
  behavior is intended to change.
- This intentionally does not yet mirror the larger expert-level
  `weighting.validation` or `weighting.corr_penalty` subtrees.
- Updated config scaffold tests for the processed weighting enum and basic
  weighting/busy-row validation bounds.
- Local checks only:
  - direct C++ syntax checks for `timestream_config.h` and
    `reduction_config.h` passed, with only expected `#pragma once` warnings
    from compiling headers directly
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
  - `git diff --check` passed
- No Unity compile or reduction was attempted.

## Update - PTC expert weighting typed mirror scaffold

- Extended `include/citlali/core/config/timestream_config.h` with typed mirrors
  for the remaining expert-level processed-time-chunk weighting groups:
  - `weighting.validation`
  - `weighting.corr_penalty`
- Added `ProcessedTimeChunkWeightGrouping` for grouping values shared by the
  validation diagnostics:
  - `array`
  - `nw`
  - `all`
- Mirrored validation settings include accumulation/apply controls,
  source-subtraction requirements, transient ratio controls, upward correction
  controls, atmospheric correlation controls, validation sample thresholds, and
  high-weight validation/capping controls.
- Mirrored corr-penalty settings include the scan/network sampling thresholds,
  seed/floor/exponent controls, pair-correlation term, common-mode/elevation
  term, and low/mid-band common-mode ratio term.
- `Engine::get_ptc_config` now mirrors these values from the effective
  `ptcproc.weight_validation` and `ptcproc.weight_corr_penalty` objects after
  the existing legacy `PTCProc::get_config` parser runs. The legacy parser and
  runtime fields remain authoritative; no runtime behavior is intended to
  change.
- Updated config scaffold tests for:
  - processed weight grouping enum parsing
  - validation bounds
  - corr-penalty bounds
- Local checks only:
  - direct C++ syntax checks for `timestream_config.h` and
    `reduction_config.h` passed, with only expected `#pragma once` warnings
    from compiling headers directly
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
  - `git diff --check` passed
- No Unity compile or reduction was attempted.

## Update - PTC clean core typed mirror scaffold

- Extended `include/citlali/core/config/timestream_config.h` with typed mirrors
  for the core `timestream.processed_time_chunk.clean` settings:
  - clean enablement
  - effective active cleaner mode
  - clean grouping vector
  - `mask_radius_arcsec`
  - `tau`
  - standard PCA enablement, `stddev_limit`, `n_calc`, and per-array
    `n_eig_to_cut`
- Added `ProcessedTimeChunkCleanerMode` for effective cleaner labels:
  - `none`
  - `standard_pca`
  - `null_model`
  - `marchenko_pastur`
  - `adaptive_selector`
- `Engine::get_ptc_config` now mirrors these values from the effective
  `ptcproc.cleaner` state after the existing legacy `PTCProc::get_config`
  parser runs. The mirror only copies cleaner internals when
  `clean.enabled=true`, because some legacy `Cleaner` fields are only
  initialized in that parser path.
- This intentionally does not yet mirror the cleaner expert subtrees:
  `null_model`, `marchenko_pastur`, `adaptive_selector`, or `corr_grouping`.
- Updated config scaffold tests for the cleaner-mode enum and standard PCA
  `n_calc` bounds.
- Local checks only:
  - direct C++ syntax checks for `timestream_config.h` and
    `reduction_config.h` passed, with only expected `#pragma once` warnings
    from compiling headers directly
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
  - `git diff --check` passed
- No Unity compile or reduction was attempted.

## Update - PTC clean expert typed mirror scaffold

- Extended `include/citlali/core/config/timestream_config.h` with typed mirrors
  for the remaining expert-level processed-time-chunk cleaner groups:
  - `clean.corr_grouping`
  - `clean.null_model`
  - `clean.marchenko_pastur`
  - `clean.adaptive_selector`
- Added `ProcessedTimeChunkCorrGroupingMetric` for `corr_grouping.metric`:
  - `abs`
  - `signed`
- Mirrored corr-grouping settings include metric, correlation threshold,
  overlap/good-fraction/group-size/sample thresholds, and residual-cleaning
  behavior.
- Mirrored null-model settings include surrogate count, quantile,
  good-fraction threshold, mode/sample limits, seed, and grouping filter.
- Mirrored Marchenko-Pastur settings include good-fraction threshold,
  mode/sample limits, optional frequency band, robust clip, bulk keep fraction,
  q-grid size, and grouping filter.
- Mirrored adaptive-selector settings include detector/sample/pair limits,
  seed, robust clip, score weights, low/mid bands, candidate offsets, grouping
  filter, and candidate logging.
- `Engine::get_ptc_config` now mirrors these values from the effective
  `ptcproc.cleaner` state after the existing legacy parser runs. The legacy
  parser and runtime fields remain authoritative; no runtime behavior is
  intended to change.
- Updated config scaffold tests for:
  - corr-grouping metric enum parsing
  - cleaner expert validation bounds
- Local checks only:
  - direct C++ syntax checks for `timestream_config.h` and
    `reduction_config.h` passed, with only expected `#pragma once` warnings
    from compiling headers directly
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
  - `git diff --check` passed
- No Unity compile or reduction was attempted.

## Update - PTC clean core Unity compile passed

- User reported that the previous commit compiled successfully on Unity.
- Interpreting this as `037686b2` (`mirror ptc clean core config`), because
  the current latest local commit is now `42695e17`
  (`mirror ptc clean expert config`).
- This was a compile-only confirmation in the thread; no reduction output for
  this compile check has been downloaded yet.

## Update - fruit-loops typed mirror scaffold

- Added passive typed mirrors for `timestream.fruit_loops` in
  `include/citlali/core/config/timestream_config.h`.
- New enums cover the legacy parser's finite option sets:
  - `mode`: `upper`, `lower`, `both`
  - `weight_feedback.reference`: `p95`, `p90`, `p99`, `median`, `p50`, `max`,
    `peak`
  - `interp_mode_override`: `auto`, `nearest`, `bilinear`, `jinc`, `trunc`
- `Engine::get_ptc_config` now mirrors effective fruit-loops runtime state
  from `ptcproc` after `PTCProc::get_config` has parsed and normalized the
  low-level YAML. This means legacy aliases remain handled by the existing
  parser:
  - `type: coadded` is mirrored as `coadd`
  - `interp_mode_override: legacy_nearest` is mirrored as `trunc`
  - invalid weight-feedback ordering is mirrored after the legacy parser has
    disabled weight feedback
- The mirror includes enablement, output iteration saving, map path/type, mode,
  S/N and flux limits, adaptive support thresholds, local sigma settings,
  weight-feedback settings, central keep radius, interpolation override, center
  convention, add-back weight recomputation, and max iterations.
- Runtime behavior is intended to remain unchanged; the legacy parser/runtime
  fields remain authoritative.
- Updated config scaffold tests for fruit-loops enum parsing and validation
  bounds.
- Local checks only:
  - direct C++ syntax checks for `timestream_config.h` and
    `reduction_config.h` passed, with only expected `#pragma once` warnings
    from compiling headers directly
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
  - `git diff --check` passed
  - direct syntax probing of `engine.h` is blocked in this local environment by
    missing `omp.h`
- No Unity compile or reduction was attempted.

## Update - raw-time-chunk filter/downsample typed mirror scaffold

- Extended `include/citlali/core/config/timestream_config.h` with typed mirrors
  for core raw-time-chunk RTC processing settings:
  - `timestream.raw_time_chunk.downsample`
  - `timestream.raw_time_chunk.filter`
  - `timestream.raw_time_chunk.filter.notch`
  - `timestream.raw_time_chunk.filter.edge_guard`
  - `timestream.raw_time_chunk.IIR_filter`
  - `timestream.raw_time_chunk.flux_calibration.enabled`
  - `timestream.raw_time_chunk.extinction_correction.enabled`
- Added enums for the filter edge-guard finite option sets:
  - `mode`: `flag`, `none`
  - `combine`: `sum`, `max`
- `Engine::get_rtc_config` now mirrors effective runtime state from `rtcproc`
  after the legacy RTC parser runs. Static notch bandwidths are reconstructed
  from the runtime center-frequency/Q representation so the typed mirror records
  effective `delta_f_Hz` values.
- The existing legacy parser/runtime fields remain authoritative; this slice is
  intended to be behavior-preserving.
- This intentionally leaves the larger raw-time diagnostic trees for separate
  follow-up slices:
  - `raw_time_chunk.flagging.network_step_mask`
  - `raw_time_chunk.flagging.impulsive_capture`
  - `raw_time_chunk.flagging.impulsive_coincidence`
  - `raw_time_chunk.line_audit`
  - `raw_time_chunk.kernel`
  - `raw_time_chunk.altaz_destripe`
- Updated config scaffold tests for:
  - raw filter edge-guard enum parsing
  - raw filter/downsample/IIR validation bounds
  - the legacy runtime rule that downsampling requires RTC filtering
- Local checks only:
  - direct C++ syntax checks for `timestream_config.h` and
    `reduction_config.h` passed, with only expected `#pragma once` warnings
    from compiling headers directly
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
  - `git diff --check` passed
- No Unity compile or reduction was attempted.

## Update - raw RTC filter Unity compile passed

- User reported that the previous commit compiled successfully on Unity.
- Interpreting this as `2f4eee6a` (`mirror raw rtc filter config`).
- This was a compile-only confirmation in the thread; no reduction output for
  this compile check has been downloaded yet.

## Update - raw-time-chunk flagging diagnostic typed mirror scaffold

- Extended `include/citlali/core/config/timestream_config.h` with typed mirrors
  for `timestream.raw_time_chunk.flagging`:
  - `delta_f_min_Hz`
  - `lower_tod_inv_var_factor`
  - `upper_tod_inv_var_factor`
  - `network_step_mask`
  - `impulsive_capture`
  - `impulsive_coincidence`
- `Engine::get_rtc_config` now mirrors these values from the effective
  `rtcproc` state after the existing legacy parser runs.
- Validation mirrors the bounds already enforced by the legacy parser for the
  diagnostic subgroups. The core inverse-variance factors are intentionally not
  range-checked here because the current parser does not bound them.
- This intentionally still leaves the largest raw-time groups for separate
  follow-up slices:
  - `raw_time_chunk.line_audit`
  - `raw_time_chunk.kernel`
  - `raw_time_chunk.altaz_destripe`
- Updated config scaffold tests for raw-time diagnostic subgroup validation.
- Local checks only:
  - direct C++ syntax checks for `timestream_config.h` and
    `reduction_config.h` passed, with only expected `#pragma once` warnings
    from compiling headers directly
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
  - `git diff --check` passed
- No Unity compile or reduction was attempted.

## Update - raw-time-chunk kernel/altaz typed mirror scaffold

- Extended `include/citlali/core/config/timestream_config.h` with typed mirrors
  for:
  - `timestream.raw_time_chunk.kernel`
  - `timestream.raw_time_chunk.altaz_destripe`
- `Engine::get_rtc_config` now mirrors these values from the effective
  `rtcproc` state after the existing legacy parser runs.
- The kernel mirror records enablement, filepath, type, effective FWHM in
  arcsec, and parsed FITS image extension names. The legacy parser still reads
  `kernel.image_ext_names` for FITS kernels; this slice intentionally does not
  alter that behavior.
- The alt-az destripe mirror records enablement, grouping, time-trend/derivative
  fit toggles, and minimum sample count.
- Validation remains intentionally narrow:
  - kernel has no additional typed validation because the legacy parser does
    not currently enforce a finite type set or numeric bounds there
  - alt-az `min_samples` mirrors the existing parser lower bound of 4
- This leaves `raw_time_chunk.line_audit` as the remaining large raw-time typed
  mirror slice.
- Updated config scaffold tests for alt-az destripe validation.
- Local checks only:
  - direct C++ syntax checks for `timestream_config.h` and
    `reduction_config.h` passed, with only expected `#pragma once` warnings
    from compiling headers directly
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
  - `git diff --check` passed
- No Unity compile or reduction was attempted.

## Update - raw-time-chunk line-audit typed mirror scaffold

- Extended `include/citlali/core/config/timestream_config.h` with a typed mirror
  for `timestream.raw_time_chunk.line_audit`.
- `Engine::get_rtc_config` now mirrors the effective `rtcproc.line_audit` state
  after the existing legacy parser runs, including:
  - base line-search thresholds and windowing parameters
  - pre-filter, post-filter, and model-protected PTC toggles
  - fixed-notch frequencies, widths, and exclusion settings
  - shared-notch application thresholds
  - detector-local notch thresholds and context samples
- Fixed-notch widths are mirrored from the runtime state after the legacy parser
  has applied its defaulting and one-width-to-many-frequencies expansion.
- Validation mirrors the legacy parser bounds and cross-field checks for:
  - PTC line frequency range
  - shared-notch apply width range
  - detector-notch width range
  - fixed-notch vector presence, lengths, and finite positive values when fixed
    notches are enabled
- Runtime behavior remains unchanged; the legacy parser/runtime fields are still
  authoritative.
- Updated config scaffold tests for line-audit validation bounds.
- Local checks only:
  - direct C++ syntax checks for `timestream_config.h` and
    `reduction_config.h` passed, with only expected `#pragma once` warnings
    from compiling headers directly
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
  - `git diff --check` passed
- No Unity compile or reduction was attempted.

## Update - astrometry calibration typed mirror scaffold

- Added `include/citlali/core/config/calibration_config.h` with a typed mirror
  for the astrometry calibration-item pointing offsets.
- Added `Engine::typed_astrometry_config`.
- `Engine::get_astrometry_config` now mirrors the effective legacy
  `pointing_offsets` state after the existing parser has accepted and
  normalized it:
  - azimuth offsets in arcsec
  - altitude offsets in arcsec
  - effective modified-Julian-date vector, including the legacy `[0, 0]`
    sentinel state when interpolation is not active
- Runtime behavior remains unchanged; the legacy maps/vectors remain
  authoritative.
- Photometry calibration is still represented through the existing
  `BeammapSourceConfig` mirror populated by `Engine::get_photometry_config`;
  no duplicate photometry type was added in this slice.
- Updated config scaffold tests for astrometry pointing-offset validation.
- Local checks only:
  - direct C++ syntax checks for `calibration_config.h`,
    `timestream_config.h`, and `reduction_config.h` passed, with only expected
    `#pragma once` warnings from compiling headers directly
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
  - `git diff --check` passed
- No Unity compile or reduction was attempted.

## Update - observation preflight helper scaffold

- User reported that the previous Unity compile completed successfully before
  this slice.
- Started the next structural phase by extracting one small observation
  preflight action from the CLI body.
- Added `include/citlali/core/pipeline/observation_preflight.h`.
- Moved the existing `flxscale_correction` logic from the local lambda in
  `src/citlali/cli/main.cpp` into
  `citlali::pipeline::apply_flxscale_correction`.
- The helper is templated over the current engine/raw-observation/logger
  surfaces, so it does not introduce a new owning runner class or require a
  broad include/source-file boundary yet.
- Runtime behavior is intended to be unchanged:
  - absent correction remains a no-op success
  - non-finite or non-positive correction remains a failure
  - missing APT `flxscale` column remains a failure
  - valid correction still multiplies the APT `flxscale` column in place
- Added fake-object scaffold tests for the helper success, no-op, invalid
  correction, and missing-column cases.
- Local checks only:
  - direct C++ syntax check for `observation_preflight.h` passed, with only the
    expected `#pragma once` warning from compiling a header directly
  - a small in-memory C++ template-instantiation probe for
    `apply_flxscale_correction` passed
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
  - `git diff --check` passed
  - direct local syntax checking of `tests/test_config_scaffold.cpp` is blocked
    by missing local `gtest/gtest.h`; Unity remains the compile authority
- No Unity compile or reduction was attempted for this slice.

## Update - config-copy output-layout helper scaffold

- Continued the runner/output-layout extraction by moving the config-file copy
  step after `create_output_dir()` out of `src/citlali/cli/main.cpp`.
- Added `include/citlali/core/pipeline/output_layout.h`.
- New helper preserves the old basename behavior: each configured file is
  copied to `<redu_dir>/<basename>` with overwrite, matching the previous
  manual `find_last_of("/")` logic.
- `main.cpp` now calls
  `citlali::pipeline::copy_config_files_to_reduction_dir(...)`.
- Added scaffold coverage for `config_copy_filename` and
  `config_copy_destination`.
- Runtime behavior is intended to be unchanged; no output directory naming or
  copy policy change intended.
- Local checks only:
  - direct C++ syntax check for `output_layout.h` passed, with only the
    expected `#pragma once` warning from compiling a header directly
  - small in-memory C++ template-instantiation probe for
    `copy_config_files_to_reduction_dir` passed
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
  - `git diff --check` passed
- No Unity compile or reduction was attempted for this slice.

## Update - downsample effective-sample-rate preflight helper

- User reported that the previous pushed slice compiled successfully on Unity
  before this work started.
- Continued the observation preflight extraction by moving the downsample
  validation/factor derivation/effective sample-rate assignment out of
  `src/citlali/cli/main.cpp`.
- Added `citlali::pipeline::configure_effective_sample_rate(...)` to
  `include/citlali/core/pipeline/observation_preflight.h`.
- Runtime behavior is intended to be unchanged:
  - when downsampling is disabled, `telescope.d_fsmp` is still set to
    `telescope.fsmp`
  - when downsampling is enabled with a positive factor, `d_fsmp` is still
    `fsmp / factor`
  - when factor is non-positive, the helper still derives it as
    `floor(fsmp / downsampled_freq_Hz)`
  - the previous invalid downsample frequency, non-positive factor, and
    anti-alias Nyquist error paths/messages are preserved
- Added fake-engine scaffold coverage for disabled downsampling, configured
  factor, derived factor, invalid downsample frequency, downsample frequency
  above sample rate, and filter frequency above downsample Nyquist.
- Local checks only:
  - direct C++ syntax checks for `observation_preflight.h` and
    `output_layout.h` passed, with only expected `#pragma once` warnings from
    compiling headers directly
  - small in-memory C++ template-instantiation probe for
    `configure_effective_sample_rate` passed
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
  - `git diff --check` passed
- No Unity compile or reduction was attempted for this slice.

## Update - observation output-layout helper scaffold

- Continued the runner/output-layout extraction by moving per-observation
  output layout setup out of `src/citlali/cli/main.cpp`.
- Extended `include/citlali/core/pipeline/output_layout.h` with helpers for:
  - legacy six-digit obsnum formatting
  - setting `engine.obsnum` and `engine.obsnum_dir_name`
  - updating OMB/CMB obsnum header vectors
  - creating the obsnum, raw, optional filtered, and optional logs directories
- `main.cpp` still reads the `obsid` from `rawobs_kids_meta` in place, then
  calls `citlali::pipeline::prepare_observation_output_layout(...)`.
- Runtime behavior is intended to be unchanged:
  - obsnums still use `std::setw(6)`/zero-padding semantics
  - `omb.obsnums` is still cleared and set to the current observation
  - `cmb.obsnums` is still appended only when coadding
  - the same per-observation directories are created under
    `engine.obsnum_dir_name`
- Added fake-engine scaffold coverage for obsnum formatting, non-coadd output
  layout state, and coadd obsnum propagation.
- Local checks only:
  - direct C++ syntax checks for `output_layout.h` and
    `observation_preflight.h` passed, with only expected `#pragma once`
    warnings from compiling headers directly
  - small in-memory C++ template-instantiation probe for the observation
    output-layout helpers passed
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
  - `git diff --check` passed
- No Unity compile or reduction was attempted for this slice.

## Update - HWPR observation preflight helper scaffold

- User reported that the previous output-layout slice was pushed to GitHub and
  a Unity compile was running before this work started; a point validation run
  was expected after that compile.
- Continued the observation preflight extraction by moving the HWPR-load branch
  out of `src/citlali/cli/main.cpp`.
- Added `citlali::pipeline::load_hwpr_data_if_requested(...)` to
  `include/citlali/core/pipeline/observation_preflight.h`.
- Runtime behavior is intended to be unchanged:
  - HWPR logic still runs only when `rtcproc.run_polarization` is true
  - `calib.ignore_hwpr == "true"` still disables HWPR loading
  - absent `hwpdata` still disables HWPR loading
  - `hwpdata.filepath() == "null"` still disables HWPR loading
  - non-null HWPR filepaths still call
    `calib.get_hwpr(filepath, telescope.sim_obs)`
  - the existing `"getting hwpr file"` and `"ignoring hwpr"` log paths are
    preserved
- Added fake-engine/raw-observation scaffold coverage for loading HWPR,
  ignoring by config, ignoring a `"null"` filepath, and no-op behavior when the
  reduction is not polarized.
- Local checks only:
  - direct C++ syntax checks for `observation_preflight.h` and
    `output_layout.h` passed, with only expected `#pragma once` warnings from
    compiling headers directly
  - small in-memory C++ template-instantiation probe for
    `load_hwpr_data_if_requested` passed
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
  - `git diff --check` passed
- No Unity compile or reduction was attempted for this slice.

## Update - map-center overwrite preflight helper scaffold

- User reported that the HWPR preflight slice was pushed to GitHub and was
  ready to move forward; Unity compile/point validation from the previous
  output-layout checkpoint was still in flight.
- Continued the observation preflight extraction by moving the duplicated
  map-center overwrite block out of `src/citlali/cli/main.cpp`.
- Added `citlali::pipeline::overwrite_map_center_if_configured(...)` to
  `include/citlali/core/pipeline/observation_preflight.h`.
- Replaced both existing call sites:
  - the single-input observation setup path
  - the multi-input per-observation telescope setup path
- Runtime behavior is intended to be unchanged:
  - the overwrite still only happens when both `omb.crval_config[0]` and
    `omb.crval_config[1]` are nonzero
  - the same telescope header keys are updated:
    `Header.Source.Ra` and `Header.Source.Dec`
  - values are still converted from degrees to radians before assignment
  - the same `"overwriting map center"` log path is preserved
- Added a small local `degrees_to_radians(...)` helper in the pipeline header
  instead of including `constants.h`, because `constants.h` pulls in Eigen and
  breaks the lightweight local header syntax probe without the full Unity build
  include path.
- Added fake-engine scaffold coverage for the no-op case and the configured
  overwrite case.
- Local checks only:
  - direct C++ syntax checks for `observation_preflight.h` and
    `output_layout.h` passed, with only expected `#pragma once` warnings from
    compiling headers directly
  - small in-memory C++ template-instantiation probe for
    `overwrite_map_center_if_configured` passed
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
  - `git diff --check` passed
- No Unity compile or reduction was attempted for this slice.

## Validation - point reduction checkpoint after runner extracts

- User reported that the latest pushed build compiled successfully on Unity and
  the point reduction was downloaded locally.
- Latest downloaded refactor point output:
  `/Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu11`
- Compared against previous refactor point output:
  `/Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu10`
- Refactor `redu11` versus refactor `redu10`:
  - FITS image HDU arrays for a1100/a1400/a2000 are exactly equal
  - NetCDF numeric variables are exactly equal except `VERSION` string
    variables in `ptcdiag`/`rtcdiag`
  - ECSV table data are exactly equal; only `creation_date` metadata differs
  - `learning_iter_0.csv` contains the same rows, but row order differs
  - raw byte-level differences are therefore limited to timestamps/logs/index
    files/version strings/order-only CSV output
- Compared latest refactor `redu11` against latest OG citlali point output:
  `/Users/gwilson/work_toltec/local_data/2026-refactor/point/citlali/reduced/redu05`
- Refactor `redu11` versus OG `redu05`:
  - ECSV table data are equal; only metadata such as creation date and mean
    telescope quantities differ
  - `learning_iter_0.csv` contains the same rows, but row order differs
  - FITS image HDU differences are at floating-point roundoff scale:
    maximum absolute differences are about `1e-9` or smaller
  - NetCDF numeric differences are also at roundoff scale, with the largest
    observed max absolute differences around `1e-8`; `VERSION` string
    variables differ as expected
- No regression was found in this point validation checkpoint.

## Update - timing-gaps output-layout helper scaffold

- User reported that the map-center overwrite slice was pushed and ready to
  continue.
- Continued the output-layout extraction by moving the timing-gaps warning and
  optional `gaps.log` write out of `src/citlali/cli/main.cpp`.
- Added `citlali::pipeline::record_timing_gaps_if_needed(...)` and
  `citlali::pipeline::gaps_log_filepath(...)` to
  `include/citlali/core/pipeline/output_layout.h`.
- Runtime behavior is intended to be unchanged:
  - the warning still fires only when `engine.gaps.size() > 0`
  - the warning text preserves the current `obnsum` spelling
  - `gaps.log` is still written only in verbose mode
  - the log body still starts with `Summary of timing gaps`
  - each gap entry is still written as `-<key> gaps: <value>`
  - the helper preserves the previous `obsnum_dir_name + "/logs/gaps.log"`
    path construction
- Added scaffold coverage for the gaps-log path and the warning-only branch.
- Local checks only:
  - direct C++ syntax checks for `output_layout.h` and
    `observation_preflight.h` passed, with only expected `#pragma once`
    warnings from compiling headers directly
  - small in-memory C++ template-instantiation probe for
    `record_timing_gaps_if_needed` passed
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
  - `git diff --check` passed
- No Unity compile or reduction was attempted for this slice.

## Update - observation exposure-time helper scaffold

- User reported that the timing-gaps output helper slice was pushed and ready
  to continue.
- Continued the observation-loop extraction by moving exposure-time bookkeeping
  out of `src/citlali/cli/main.cpp`.
- Added `citlali::pipeline::update_observation_exposure_time(...)` to
  `include/citlali/core/pipeline/observation_preflight.h`.
- Runtime behavior is intended to be unchanged:
  - exposure still uses the first and last `TelTime` samples
  - `omb.exposure_time` is still assigned `tn - t0`
  - when coadding, `cmb.exposure_time` still accumulates the current
    `omb.exposure_time`
  - when not coadding, `cmb.exposure_time` is unchanged
- Added fake-engine scaffold coverage for the non-coadd assignment and the
  coadd accumulation path.
- Local checks only:
  - direct C++ syntax checks for `observation_preflight.h` and
    `output_layout.h` passed, with only expected `#pragma once` warnings from
    compiling headers directly
  - small in-memory C++ template-instantiation probe for
    `update_observation_exposure_time` passed
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
  - `git diff --check` passed
- No Unity compile or reduction was attempted for this slice.

## Update - fruit-loop iteration policy helper scaffold

- User reported that the observation exposure-time helper slice was pushed and
  ready to continue.
- Continued the preflight extraction by moving the fruit-loop iteration policy
  setup out of `src/citlali/cli/main.cpp`.
- Added `citlali::pipeline::configure_fruit_loop_iteration_policy(...)` to
  `include/citlali/core/pipeline/observation_preflight.h`.
- Runtime behavior is intended to be unchanged:
  - fruit-loop reductions still warn when noise maps are disabled
  - non-fruit-loop reductions still force `fruit_loops_iters = 1`
  - beammap reductions still force `fruit_loops_iters = 1` even if fruit loops
    are configured
  - forced single-iteration reductions still set `save_all_iters = true`
  - science fruit-loop reductions with noise enabled leave the existing
    iteration settings untouched
- Added fake-engine scaffold coverage for:
  - non-fruit-loop single-iteration policy
  - beammap single-iteration policy
  - warning when fruit loops run without noise maps
  - preserving science fruit-loop iteration settings
- Local checks only:
  - direct C++ syntax checks for `observation_preflight.h` and
    `output_layout.h` passed, with only expected `#pragma once` warnings from
    compiling headers directly
  - small in-memory C++ template-instantiation probe for
    `configure_fruit_loop_iteration_policy` passed
  - compact compatibility suite still passed: 8 passed, 0 failed, 0 skipped
  - Python config tools still compile
  - `git diff --check` passed
- No Unity compile or reduction was attempted for this slice.
