# Citlali Handoff — 2026-08-25

## 08:45 EDT — Stage 7 production-JINC local completion candidate

### Scope and disposition

The exact frozen local NGC4449 observation 152390 production-JINC reduction
completed successfully after the compact-v2 native execution repairs. This
record establishes software completion, bounded canonical publication, and
artifact integrity. It deliberately does **not** claim scientific equivalence
to the prior naive or established science reductions.

No new entry was added to `validation/accepted_runs.json`: the actual map
comparison and data-pathology interpretation remain pending.

### Source and build identity

- Branch: `codex/repair-native-consumer-extinction`
- Commit: `8addcce62c79c1a1cad12912c1d16cfef0ed47ea`
- Version: `v4.0.0-3706-g8addcce62`
- Executable SHA-256:
  `5f9fd79168c4f4510b2806ad44d4e4c2c4ef0fa876a983fc222fe3cbb795215a`
- KIDs identity reported by the executable: `04088da-dirty`
- Frozen input:
  `/private/tmp/citlali-stage7-science-replay-27b453f9e/jinc-input.yaml`
- Frozen input SHA-256:
  `13e1fbfee5f4581b90aa1f13073a0137ce42a6b28482ebb91296884ad96a51ad`
- Complete log:
  `/private/tmp/citlali-stage7-science-replay-27b453f9e/replay-8addcce62.log`
- Final output:
  `/private/tmp/citlali-stage7-science-replay-27b453f9e/jinc-output/redu22`
- Root-index SHA-256:
  `17d80a65753a8cfa561aeb83c90e399bdd2e710cbc813870e2a798a81f3b9619`

The `/private/tmp` paths are local transient evidence locations. The identities
and audit results in this handoff are the durable record; those paths are not a
permanent artifact archive.

### Completed production replay

- Four fruit-loop iterations completed: `redu19`, `redu20`, `redu21`, and
  final `redu22`.
- Exactly 496 scan starts and 496 scan completions were logged: all 124 scans
  in each iteration.
- Elapsed Citlali time: 1h45m30.47367275s.
- Error-level records: 0.
- Critical-level records: 0.
- Final products include three observation maps, four observation diagnostic
  products, three raw coadds, three filtered coadds, three raw-coadd
  diagnostics, the restart checkpoint, learning summaries, and the required
  provenance sidecars.

The final root index reports source revision `8addcce62`, version
`v4.0.0-3706-g8addcce62`, and build timestamp `2026-08-25T03:22:40`.

### Transactional publication and bounded provenance

Canonical root-index publication was deferred after every iteration and was
performed only after the final required outputs and provenance validated. The
final raw-timestream sidecar is
`citlali-raw-timestream-provenance-v3` and embeds
`citlali-native-cohort-product-provenance-v3` with:

- `detector_sample_expansion: false`;
- 5,518 detectors reconciled as 4,512 APT-eligible plus 1,006 APT-excluded;
- one observation-scope record for each of the 1,006 APT exclusions;
- exactly 124 scan summaries;
- execution completion true with 124 completed scans; and
- `canonical_publication.status: validated_complete` with
  `bounded_provenance_validated: true`.

The sidecar is 19,189,837 bytes and 760,481 lines. It is large because natural-
scope scan causes retain detector identity lists, but it contains no exhaustive
detector-by-sample transition ledger. The indexed 650,585-byte NetCDF restart
checkpoint remains explicitly a checkpoint/cache product, not canonical
lineage.

An independent recursive audit reread the product tree and verified:

- 6 indexes;
- 5 child indexed directories;
- 33 immutable indexed files;
- 133,869,733 immutable indexed bytes; and
- every recorded size, file SHA-256, and child-index SHA-256.

### Legacy JINC fruit-loop scale repair

Commit `8addcce62` separates any-coadd output behavior from the successor
F009/F010 coadd contract. Any coadd suppresses observation-only empirical-noise
companions and their package joins. A legacy production-JINC coadd retains the
established `MEDERR` and positive `MEDRMS` cards on raw `weight_I`; the next
fruit-loop iteration reads `MEDRMS` from that raw-weight HDU. It does not
publish false `noise_variance_*` HDUs.

The first-iteration raw coadds recorded:

| Array | `MEDRMS` | `MEDERR` |
| --- | ---: | ---: |
| a1100 | 1.48016542856412 | 1.25936162470683 |
| a1400 | 1.04306041665838 | 0.898717269754083 |
| a2000 | 1.64147241151624 | 1.41365471562524 |

All three raw coadds carry only the established
`global_nonprecision_scaled_coefficient` join and no empirical-noise-variance
HDU.

### Validation gates

- Focused `citlali_science_map_fits_products_test`: 35/35 passed. Artifact
  assertions prove legacy JINC raw `weight_I` retains `MEDRMS` while the
  successor coadd omits it and package joins remain unchanged.
- Complete CTest surface: 823/823 runnable tests passed; one established
  disabled test was not run.
- Required config preflight: 129 Python tests, four numbered-config mode kits,
  eight compatibility cases, and all boundary audits passed.

### Warnings and observed pathologies

The successful replay emitted 8,681 warnings in four known classes:

| Count | Warning class |
| ---: | --- |
| 4,092 | `median_rms` has three values for one map and uses the map-index fallback |
| 4,092 | `fruit_loops_flux` has three values for one map and uses array-id indexing |
| 496 | busy-row suppression had no second-pass diagnostics for the scan |
| 1 | telescope input omitted configured optional fields |

These are explicit fallbacks rather than error-level failures, but their volume
is not acceptable as a final operational logging contract and should be
cleaned up independently of the scientific map review.

Two high-value observed-data targets for that review are:

- scan 72 / network 11: dominant step at sample 813, bounded mask [752, 874],
  486 detectors and 54,366 newly flagged samples (10.11%); and
- repeated RTC line-audit recommendations near 11.0063 Hz and 47.7772 Hz,
  plus recurrent bad-detector recommendations including detector UIDs 194,
  4996, and 5101.

These are data-pathology observations, not software-completion failures.

### Interrupted reproducibility replay

A second same-input run used log
`/private/tmp/citlali-stage7-science-replay-27b453f9e/replay-8addcce62-run2.log`.
It completed its first two iterations with exactly matching learning counters
and reproduced the positive `MEDRMS` restart transition. It had no error-level
record when the owner requested a pause during the third iteration. The
process was interrupted and exited 130. No final canonical index was
published, so this is partial supporting evidence rather than a completed
reproducibility result.

### Next gates

1. Compare the completed 152390 observation and coadd FITS products with the
   available established science reductions, separating expected input/config
   differences from implementation differences.
2. Inspect the named 152390 scan/network/detector pathologies and decide which
   represent real data rejection, overly broad policy, or warning-only
   diagnostics.
3. Do not promote the run into `validation/accepted_runs.json` until the
   scientific comparison has an explicit disposition.
4. If the local scientific review succeeds, have the owner push this branch
   and perform one Unity build/reduction from exact source commit
   `8addcce62c79c1a1cad12912c1d16cfef0ed47ea`; record the Unity executable
   SHA-256 separately because it is build-environment specific.
