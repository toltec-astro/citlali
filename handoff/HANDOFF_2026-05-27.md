# Citlali Beammap Handoff - 2026-05-27

## 2026-05-27 15:57 EDT

### Current State

- `citlali` local branch: `gw_dev`, clean against `origin/gw_dev` at note time.
- Beammap dashboard work is inspecting 3C273 reductions in
  `/Users/gwilson/work_toltec/local_data/beammaps/3c273`.

### Next Citlali Change TODO

- Fix the PTC TOD metadata written for beammap fruitloops products:
  the log for `redu46` reports `writing processed time chunk for beammap iteration 5`,
  but `source_crossing_tod/toltec_commissioning_beammap_152307_ptc_timestream.nc`
  still has `FRUITLOOPS_ITER = 0`.
- The next Citlali change should make the NetCDF metadata reflect the actual
  beammap fruitloops iteration used for the written PTC timestream.

### Resolution

- Patched the beammap PTC TOD writer to update the scalar `FRUITLOOPS_ITER`
  in the PTC timestream NetCDF immediately before appending the selected
  beammap iteration chunks.
- Added fit-QC diagnostics for fruitloops feedback support so the next
  beammap run records the source-support center, adaptive thresholds, support
  radius, and final support footprint.
