# Citlali Refactor Handoff - 2026-07-12

## Active phase

Phase 2 config authority and provenance remains active. Raw timestream is the
current bounded domain. Production is still legacy-authoritative: merged YAML
is parsed by `RTCProc::get_config`, then ten mirrors populate the typed raw
snapshot. Do not remove or bypass that path yet.

## Unwired typed-to-RTC checkpoint

The complete one-way raw adapter is implemented but intentionally not wired
into `Engine`. It is split into filtering/corrections, flagging/despiking, and
line-audit adapters, with a separate observation-state overlay. The request
adapter covers all 169 frozen raw paths. Observation state can separately apply
native sample rate, derived downsample factor, filter edge guard/context counts,
source-protection activity, and extinction activity/model.

Focused tests instantiate the real `timestream::RTCProc` and prove:

- complete active request state round-trips through the existing legacy mirrors;
- disabled expert values remain in the request while RTC execution sentinels
  preserve existing behavior;
- observation state overlays do not mutate requested/effective config; and
- beginning another observation clears prior observation-derived state.

The boundary audit now requires 169/169 typed-reader, request-serializer, and
adapter coverage. The named adapters are excluded from the external RTC access
census because they are the compatibility boundary itself; the census remains
40 reviewed downstream access shapes with zero review-required records.

Testing the real RTC object exposed two non-template `Filter` definitions in a
public header without `inline`. Marking them inline repairs the ODR violation
without changing numerical behavior.

## Local gates

- `citlali_cli` build passes.
- All 271 discovered C++ tests pass.
- All 22 focused config-tool tests pass.
- All eight compact compatibility profiles pass.
- Raw coverage is readers 169/169, serializer 169/169, adapter 169/169.
- External RTC census is 40 records, zero unreviewed, with the frozen digest.

## Pure observation resolution complete

The next unwired layer is complete. `raw_timestream_observation_resolution.h`
resolves sample-rate/downsample policy with explicit errors, edge-guard and
outer-context contributions, raw source-protection activity, extinction model,
and the complete per-observation state record. It does not mutate the request.

Filter notch/IIR transient sample formulas and extinction-model selection were
extracted into standalone pure headers. The real legacy `Filter` and
`Calibration` classes delegate to them, avoiding parallel scientific formulas.
Seven focused tests cover valid and invalid sample-rate cases, exact edge-guard
parity for sum and max combination policies, source-protection parity,
extinction parity across representative tau values, and state construction.
Local CLI and test builds pass, all 271 C++ tests pass, and full config preflight
passes with the frozen raw audits unchanged.

## Context-free production shadow active

`Engine::get_rtc_config` now performs one isolated direct typed read before the
legacy parser, resets/stores `RawTimestreamExecutionPlan`, adapts the request
into a temporary `RTCProc`, and compares deterministic raw snapshots after all
ten legacy mirrors. A mismatch throws before reduction execution and logs both
snapshots. The authoritative `engine.rtcproc` is not modified by the shadow and
continues to drive all numerical work.

The generated complete default config matches the real legacy parser. Tests
also prove injected divergence is detected and disabled source-protection or
non-FITS kernel expert values remain in the request while being normalized only
in the temporary execution target. The raw boundary audit now requires exactly
one typed read, parser, ten mirrors, and comparison in that order. All 274 C++
tests and full preflight pass.

## Next work

Populate the typed plan's per-observation state from the pure resolvers and
compare it to the existing legacy sample-rate, edge-guard, source-protection,
and extinction state without driving execution.
Only after local shadow parity passes should production authority flip to the
typed plan. That flip requires a Unity compile and strict point validation,
followed by bundled Beammap/science evidence before retiring the raw parser and
mirrors. Keep polarimetry outside this authority claim.

## Workflow

Use `$HOME/tolteca/bin/python`, build locally with
`cmake --build build --target citlali_cli -j 8`, and run
`$HOME/tolteca/bin/python tools/config/run_config_preflight.py --require-all`.
The user performs Unity builds/reductions and controls pushes. Commit coherent,
locally verified changes; do not push.
