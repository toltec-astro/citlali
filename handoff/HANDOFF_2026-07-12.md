# Citlali Refactor Handoff - 2026-07-12

## Active phase

Phase 2 config authority and provenance remains active. Raw timestream is the
current bounded domain. Production is still legacy-authoritative: merged YAML
is parsed by `RTCProc::get_config`, then ten mirrors populate the typed raw
snapshot. Do not remove or bypass that path yet.

## Unwired typed-to-RTC checkpoint

The complete one-way raw adapter is implemented but intentionally not wired
into `Engine`. It is split into filtering/corrections, flagging/despiking, and
line-audit adapters, with a separate observation-state overlay. The request
adapter covers all 169 frozen raw paths. Observation state can separately apply
native sample rate, derived downsample factor, filter edge guard/context counts,
source-protection activity, and extinction activity/model.

Focused tests instantiate the real `timestream::RTCProc` and prove:

- complete active request state round-trips through the existing legacy mirrors;
- disabled expert values remain in the request while RTC execution sentinels
  preserve existing behavior;
- observation state overlays do not mutate requested/effective config; and
- beginning another observation clears prior observation-derived state.

The boundary audit now requires 169/169 typed-reader, request-serializer, and
adapter coverage. The named adapters are excluded from the external RTC access
census because they are the compatibility boundary itself; the census remains
40 reviewed downstream access shapes with zero review-required records.

Testing the real RTC object exposed two non-template `Filter` definitions in a
public header without `inline`. Marking them inline repairs the ODR violation
without changing numerical behavior.

## Local gates

- `citlali_cli` build passes.
- All 271 discovered C++ tests pass.
- All 22 focused config-tool tests pass.
- All eight compact compatibility profiles pass.
- Raw coverage is readers 169/169, serializer 169/169, adapter 169/169.
- External RTC census is 40 records, zero unreviewed, with the frozen digest.

## Pure observation resolution complete

The next unwired layer is complete. `raw_timestream_observation_resolution.h`
resolves sample-rate/downsample policy with explicit errors, edge-guard and
outer-context contributions, raw source-protection activity, extinction model,
and the complete per-observation state record. It does not mutate the request.

Filter notch/IIR transient sample formulas and extinction-model selection were
extracted into standalone pure headers. The real legacy `Filter` and
`Calibration` classes delegate to them, avoiding parallel scientific formulas.
Seven focused tests cover valid and invalid sample-rate cases, exact edge-guard
parity for sum and max combination policies, source-protection parity,
extinction parity across representative tau values, and state construction.
Local CLI and test builds pass, all 271 C++ tests pass, and full config preflight
passes with the frozen raw audits unchanged.

## Context-free production shadow active

`Engine::get_rtc_config` now performs one isolated direct typed read before the
legacy parser, resets/stores `RawTimestreamExecutionPlan`, adapts the request
into a temporary `RTCProc`, and compares deterministic raw snapshots after all
ten legacy mirrors. A mismatch throws before reduction execution and logs both
snapshots. The authoritative `engine.rtcproc` is not modified by the shadow and
continues to drive all numerical work.

The generated complete default config matches the real legacy parser. Tests
also prove injected divergence is detected and disabled source-protection or
non-FITS kernel expert values remain in the request while being normalized only
in the temporary execution target. The raw boundary audit now requires exactly
one typed read, parser, ten mirrors, and comparison in that order. All 274 C++
tests and full preflight pass.

## Next work

The per-observation shadow is now wired without driving execution.
`prepare_reduction_observation_sample_rate` records and compares sample rate,
downsample factor, edge guard/context, and source-protection activity.
`setup_observation_extinction` completes and compares extinction activity and
model. Any checked mismatch fails with field-level diagnostics. Beginning a
second observation clears the first observation and realized state.

Frequency-derived downsampling has one explicit deferral: legacy configures the
edge guard before the per-observation factor is derived. The typed observation
records its expected factor/guard and marks edge-guard parity deferred; it does
not alter the legacy guard. Resolve that lifecycle ordering deliberately at the
authority flip rather than hiding it or changing behavior in the shadow phase.

The local CLI and test builds pass, all 277 C++ tests pass, all 23 focused
config-tool tests pass, all eight profiles pass, and full preflight passes. The
external RTC census now contains 44 frozen classified records and zero
review-required entries.

Unity compile and strict point validation are pending for this production
shadow checkpoint. Do not flip authority, remove the raw parser/mirrors, or
claim production acceptance until that evidence lands. After point acceptance,
bundle Beammap/science validation before retiring the compatibility path. Keep
polarimetry outside this authority claim.

## Required raw provenance wired

`raw_timestream_provenance.h` defines the versioned
`citlali-raw-timestream-provenance-v1` schema and atomic writer. It serializes
requested/effective config, effective resolutions, optional observation fields,
the edge-guard parity deferral, an explicit execution-completed bit, and
realized counters. Unavailable observation state is represented explicitly.
Uninitialized writes and filesystem failures propagate.

After point `redu37` accepted the production shadow, the sidecar was wired as a
required per-observation output at the successful observation boundary. It is
written only after required TOD writers and observation products complete. The
observation lifecycle owns `completed_scan_count` and
`required_timestream_write_count`; `flagged_sample_count` and
`dynamic_notch_count` remain explicitly unavailable until their natural
execution owners are migrated. Sequential-observation tests prove reset and
independent publication, and a filesystem-failure test proves publication
failure reaches the caller. The reduction audit now supports
`--require-raw-provenance` and
`--require-candidate-raw-provenance`, validates all observation sidecars, and
checks initialized/completed state plus nonnegative realized counts. A follow-up
safety pass makes the writer reject incomplete lifecycle/count state and makes
the audit pair setup-time output provenance with completion-time raw
provenance, reject missing observation sidecars, cross-check scan counts, and
validate resolved sample-rate relationships.

Local CLI/test builds, all 287 C++ tests, 11 focused provenance-audit tests,
and full preflight pass. Unity point `redu38` at `6bbc12ce` accepts the newly
published sidecar. The audit finds complete observation coverage, 12 completed
scans, 48 required writes, and zero serious issues. Against accepted `redu37`,
all 13 scientific product families, including every RTC/PTC array, are exact
with zero skipped records; runtime is 51.459 seconds versus 51.723 seconds.
The validation ledger contains the accepted record. Beammap and science
evidence remains pending. Do not flip raw execution authority or retire
parser/mirrors before those gates pass.

The first science cross-mode attempt at `5d403887` failed before numerical
processing on `downsample.factor expected=1 actual=0`. Science disables
downsampling: the typed observation factor 1 is the correct physical identity,
while legacy RTC initializes/uses its factor only inside the enabled branch.
The shadow now compares factor only when downsampling is enabled, without
changing typed provenance or numerical execution. A focused disabled-science
test covers factor 1 versus legacy sentinel 0; enabled-factor mismatch coverage
remains active. Local builds, all 288 C++ tests, and full preflight pass. Rerun
science and Beammap from the repaired candidate before authority cutover.

The repaired `2d6f80a3` cross-mode runs are accepted. Beammap `redu17` publishes
one valid sidecar with 198 scans/198 required writes; all 12 complete product
families are exact against `redu15`, with zero skipped records and a 3397.522 s
runtime versus 3458.917 s. Science final iteration `redu29` publishes two valid
sidecars with 124 scans/248 required writes each; all 27 complete products pass
against `redu24` with zero changed or skipped records, maximum absolute
difference `4.452e-10`, and runtime 697.572 s versus 705.784 s. Both runs have
zero serious issues and explicit publication log lines. The durable ledger
records both. Point, Beammap, and science gates are now closed for the bounded
raw authority cutover; OOF reuses pointing, while polarimetry stays excluded.

## Raw parser exits removed

All 14 direct `RTCProc::get_config` exits are replaced by exact invalid-key
diagnostics. Malformed filter/notch/IIR/downsample/line-audit relationships now
reach the existing CLI validation boundary instead of terminating from library
code. Mismatched notch vector lengths are bounded before iteration, preventing
out-of-range access while errors accumulate. The raw boundary audit now freezes
the direct-exit count at zero. Valid config behavior and numerical execution are
unchanged. Local builds, all 282 C++ tests, and full preflight pass.

## Unity point shadow gate accepted

Point `redu37` embeds `v4.0.0-3429-gcd8da24f` and validates all raw-shadow work
through the yaml-cpp 0.7 compatibility repair. Against accepted `redu36`, the
merged config hash is identical, all 12 PTC chunks complete with zero issues,
the stable inventory matches, and strict full-product comparison reports 13
common families with zero missing, extra, changed, or skipped records. Runtime
and processed provenance are byte-identical; output provenance differs only in
the expected reduction paths. Logged runtime is 51.723 seconds versus 53.277
seconds. The validation ledger records this accepted checkpoint.

The point production-provenance gate is accepted at `redu38`; do not retire the
raw parser/mirrors or claim typed raw authority until the affected Beammap and
science evidence is accepted. The frequency-derived
downsample edge-guard ordering remains an explicit deferred decision.

## Raw execution authority cutover candidate

After point `redu38`, Beammap `redu17`, and science `redu29` closed the shadow
and required-provenance prerequisites, the bounded raw authority flip was
implemented locally. `Engine::get_rtc_config` now reads the typed request into
the production diagnostics, initializes requested/effective plan state, and
adapts effective policy one way into the production `rtcproc`. The legacy
parser runs only against a temporary `RTCProc`; all ten legacy mirrors populate
only a temporary oracle snapshot. Production and oracle snapshots must match
before execution. Polarimetry remains separate.

`raw_timestream_authority.h` owns the small, testable initialization boundary.
Focused tests prove stale production RTC fields are overwritten, disabled
expert request values remain in requested/effective state while legacy
sentinels reach the processor, and deliberate production/oracle divergence is
detected. The raw boundary audit now requires exactly one typed read, one oracle
parse, ten oracle mirrors, one production authority initialization, and one
parity comparison in that order. The external execution census excludes this
named adapter boundary and remains frozen at 44 classified records.

The CLI and test binaries build locally, all 291 C++ tests pass, all eight real
config profiles pass, and full config preflight reports 169/169 reader,
serializer, and adapter coverage with no audit drift. This is not yet a Unity-
accepted cutover. Run strict point, Beammap, and science validation before
removing `RTCProc::get_config` raw parsing or any of the ten oracle mirrors.
After those gates pass, parser/mirror retirement is the next bounded change.

The first Unity point run from `475bf8e22` failed at 33% map output with an
empty Stokes table and an invalid `maps_to_stokes` index. Root cause: moving
`RTCProc::get_config` to a temporary oracle also moved its adjacent
polarimetry side effects. Even when polarimetry is disabled, legacy parsing
creates `polarization.stokes_params[0] = "I"`; the raw adapter deliberately
does not own that separate domain. The repair adds a narrow
`adapt_legacy_polarimetry_runtime` boundary that transfers only
`run_polarization`, grouping, and Stokes labels from the temporary parser to
the production RTC. A focused test verifies unpolarized Stokes-I initialization
and verifies unrelated raw state is untouched. The raw boundary audit now
requires exactly one such transfer after authority parity. CLI/test builds,
all 292 C++ tests, 25 config-tool tests, all eight profiles, and full preflight
pass. Rerun the Unity point gate before broader validation.

Point `redu40` confirms the Stokes repair: it completes with zero serious
issues, valid required provenance, exact maps/tables, and complete exact RTC/PTC
timestream arrays against accepted `redu38`. The strict comparator found only
two changed scalar metadata records, both `CONFIG.TODIIRHP.FREQ_HZ`: baseline
`0.0`, candidate `0.1`. The IIR is disabled. Raw provenance deliberately keeps
the inactive requested `0.1` plus disabled effective-resolution state, while
the legacy TOD metadata contract records processor-effective sentinels. The
follow-up adds a pure `raw_iir_filter_metadata` projection used by FITS and
NetCDF writers: disabled maps to `0.0`, order `1`, zero-phase `false`; enabled
values pass through. It neither mutates the plan nor reads `RTCProc`. CLI/test
builds, all 293 C++ tests, and full preflight pass. Run one more point before
the expensive Beammap/science gates.

## Workflow

Use `$HOME/tolteca/bin/python`, build locally with
`cmake --build build --target citlali_cli -j 8`, and run
`$HOME/tolteca/bin/python tools/config/run_config_preflight.py --require-all`.
The user performs Unity builds/reductions and controls pushes. Commit coherent,
locally verified changes; do not push.
