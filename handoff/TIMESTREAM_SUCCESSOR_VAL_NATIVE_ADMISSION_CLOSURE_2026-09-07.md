# Timestream Successor VAL Native Admission Closure

Status: exact admission accepted and incorporated locally; documentation
closure candidate requires its own independent review before incorporation.

## Owner disposition and canonical incorporation

On 2026-09-07 the project owner answered "approved." to the explicit request
to admit candidate `74a2727539a5d1812892042a59f4ca9b14ff9c2e` and complete
its bounded documentation closure. That acceptance applies to the exact
reviewed tree `a35fc4f957991f197ee9ecfec0d8f1e21cd6e10a`. The simultaneous
request for a high-level program retrospective does not authorize another
implementation unit or operational activation.

Before acting, live explicit-SSH GitHub and clean local canonical were
reverified at `36176ac14abe23a5c331ba40af2bb86d3e97a7f6`. All 17 files
in the completed admission evidence manifest and the exact independent
review digest were checked. Local `codex/refactor-mainline` was then
fast-forwarded literally to the accepted admission SHA. No history was
rewritten and no GitHub push occurred.

| Role | Exact identity |
| --- | --- |
| Accepted admission and closure parent | `74a2727539a5d1812892042a59f4ca9b14ff9c2e` |
| Accepted admission tree | `a35fc4f957991f197ee9ecfec0d8f1e21cd6e10a` |
| Admission first parent, previous live/local canonical | `36176ac14abe23a5c331ba40af2bb86d3e97a7f6` |
| Admission second parent, reviewed and Unity-tested source | `37e6fb6ce50acba644f04b6b6b546762bf07f384` |
| Tested source tree | `32dea6ffe86b5855ad75a21cd56e6817559fb9f9` |
| Common ancestor and literal implementation base | `080df2a1431487e8cabc255beb9ddc59c0721b59` |

The admission joins the independent uniform scientific authority history and
the VAL source history. All 1600 executable-input mode/blob identities match
the tested source; all 3232 canonical paths outside the eight-path admission
delta are preserved. Both complete parent histories survive in status and
ledger. Original D2 implementation, repair, closure and admission remain
unchanged ancestors. The source branch stays frozen and clean at `37e6fb6ce...`.

## Independent review and completed evidence

The [verbatim admission review](TIMESTREAM_SUCCESSOR_VAL_NATIVE_ADMISSION_001_EXACT_SHA_REVIEW_2026-09-06.md)
passed all three axes with recorded limitations and no actionable findings.
Its SHA-256 is
`90e5eccd4bbc4248b8eacb7a6478774fd2da39079cb1b3bf423cdff7b2d53e44`.
The closed 17-file completion evidence manifest SHA-256 is
`2e436227cbafd9de25f14c632c4b16e46b558817b8246587f16b1a52b4adbe9b`,
at `/private/tmp/citlali-val-native-target-completion-evidence-2026-09-06`.
The original implementation review and its 56-file evidence remain intact.

Owner-run Unity GCC13 job `64035873` completed `0:0` at
`2026-09-06T21:21:59Z`, in 9m34s with six CPUs and the original 64 GiB
request. Batch MaxRSS was 23071116 KiB, approximately 22.00 GiB. All 899
runnable CTests and all 50 focused successor tests passed, including 10 VAL
and eight D2 tests. The sole unchanged disabled test is
`citlali::MapFitterLifecycle.ExactProductSequence`. The receipt confirms
matching clean CLI/source identity, clean pinned dependencies, 21 successful
archive checksum checks and empty Slurm stderr.

Archive:
`/work/toltec/citlali_dev/citlali_acceptance_evidence/TIMESTREAM-SUCCESSOR-VAL-NATIVE-TARGET-001/37e6fb6ce50acba644f04b6b6b546762bf07f384/unity-gcc13-sbatch-64035873`.

- Reviewed runner SHA-256:
  `a0bfbefeea38e994053b3b3d34f9b1f5b44d2da50994141185364ecbc9c8d3f6`.
- Final-status SHA-256:
  `4e08133ee2d5687f7a52d2bfdbfeb51acbb0c0f5c202046f68e56988e063a659`.
- Owner-held archive manifest SHA-256:
  `29162f995b42bf1f95bc0fee1ad1ba8e0c61c236c111e4dde3cd2e4cd2edbb55`.

The full archive remains owner-held; local checks verify the supplied
transcript, runner binding and reconstructed final-status digest. No direct
Unity access occurred. GNU 13.3.0/C++23 Release and the pinned Spack
realization establish source-build/test acceptance. Runtime deployment labels
remain unmanaged/unavailable/unmanaged. No managed-deployment, smaller-memory,
scientific-reduction or production acceptance is inferred.

The unchanged SCI-VAL verifier's historical MAP-unbound failure is preserved
and reproduced on both admission and its canonical base. It is not a pass or
waiver, and its later unexecuted checks are not certified. Layout, both
ledgers, exact preservation and independent review passed separately.

## Bounded closure and verification

This four-path documentation closeout continues on
`codex/timestream-successor-val-native-admission-001`, at
`/private/tmp/citlali-timestream-successor-val-native-admission-001`, from the
accepted admission SHA. It adds this owner-disposition record and the
verbatim review, and prepends status/ledger records without changing prior
history. Applicable AGENTS.md, toltec-context and all three effective
governance documents remain controlling and unchanged.

Required closure checks are exact four-path scope; complete preservation of
every other parent path and all tested executable inputs; verbatim review;
layout/ledger checks; clean committed state; and a fresh-context read-only
independent review of the exact closure SHA on all three governance axes.
The final SHA/tree, digests, review and any subsequent canonical fast-forward
bind externally after commit in
`/private/tmp/citlali-val-native-admission-closure-evidence-2026-09-07`.
The writer does not approve the new closure SHA. After that review passes
and canonical remains clean at the accepted parent, the owner's approval
already authorizes literal incorporation of this bounded closure.

No additional C++ or Unity campaign is triggered by documentation alone;
evidence reuse requires complete executable-input equality, without relabeling
the actual tested SHA. Reassess if canonical moves or any preservation or
review gate fails. The user performs the eventual GitHub push using an
absolute repository path, explicit environment-appropriate URL and full
refs/heads source-to-destination refspec.

## Completed boundary and next planning decision

The VAL native-target unit is accepted and complete. No application unit is
opened by this closure. Numerical producers still own the measurements and
their facts; VAL supplies mechanical identity/snapshot support; named use
owners retain policy. This completion does not establish a full VAL evaluator,
PSD eligibility profile, D2 producer adoption or real-data scientific result.

The next program discussion should select the smallest separately bounded
step connecting these identity and evidence foundations to a named real
measurement/use. Any required PSD-use policy, producer binding or numerical
implementation needs its own recovered authority, explicit owner disposition
and validation. The Monday retrospective is orientation, not such an approval.
Filtering, factor selection, downsampling, substantive RTC/PTC/AST,
common-grid, MAP, CAL, activation, production, cleanup and subsequent
Timestream Successor work remain outside this closeout.
