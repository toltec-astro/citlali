# Citlali Refactor Handoff — 2026-07-11

## Current phase

The project is in Phase 2 of the adopted five-phase roadmap: config authority
and provenance. The immediate subsystem is processed timestream configuration
(`fruit_loops`, cleaning, weighting, and second-pass flagging).

The governing architecture review is
`handoff/EXTERNAL_REFACTOR_ARCHITECTURE_REVIEW_2026-07-10.md`. The finite PTC
transition contract is `doc/processed_timestream_config_transition.md`.

## Last accepted point evidence

- `redu31`, binary `09bafff9`: exact against `redu30`, including complete TOD.
- `redu32`, binary `55bd5218`: exact against `redu31`, including complete TOD.
- Both are recorded in `validation/accepted_runs.json`.
- The local suite currently has 241 passing tests; all eight config preflight
  cases pass.

## Cross-mode validation in progress

Point, beammap, and science reductions are running on Unity from `4c6268d8`.
Do not attribute later local commits to those runs.

Beammap uses its existing numbered config files. Science has a local final
overlay outside the repository:

`science/refactor/83_processed_config_validation.yaml`

It reverses cumulative debug overlays 80–82 by enabling fruit loops, noise
maps, map filtering, and filter error normalization. The run therefore matches
the established full science configuration and can be compared with historical
full runs (`science/refactor/redu07` and `science/citlali/redu11`), subject to a
generated low-level config comparison.

## Processed config work through the running SHA

- Direct typed readers and one-way adapters cover fruit loops, cleaning,
  weighting, validation, correlation penalties, busy-row suppression, and
  second-pass flagging.
- Effective decisions are explicit for cleaner subgroup normalization,
  source-mask inheritance, validated weighting, and busy-row dependencies.
- Focused adapter parity tests exist.
- `PTCProc::get_config` remains as a compatibility seed pending matched point,
  beammap, and science evidence plus versioned processed-config provenance.

## Later local safety commits

These commits are newer than the running Unity SHA and affect invalid config or
transition safeguards, not valid scientific execution:

- `258d90d0` rejects zero or multiple enabled cleaners in typed preflight.
- `59ee9398` rejects malformed cleaner groups and frequency bands.
- `a976931b` removes the cleaner-selection process exit and prevents invalid
  fruit-loop feedback from silently disabling itself before validation.
- `0d211e65` freezes the 171-path legacy PTC YAML boundary and rejects new
  paths or direct exits in the standard config preflight.

## Next actions

1. Audit the downloaded point, beammap, and science products for revision,
   generated config equality, unexpected errors, product completeness, and
   strict numerical parity.
2. Record accepted cross-mode evidence durably.
3. Compile the later invalid-config safety commits on Unity; a point run is
   sufficient because valid execution behavior is unchanged.
4. Do not remove the legacy PTC parser until every gate in
   `doc/processed_timestream_config_transition.md` is satisfied.
5. The next scientific-owner question is whether negative fruit-loop
   `sig2noise_limit` or `array_flux_limit` values are valid sentinels or should
   be fatal. Do not impose a domain without an answer.
