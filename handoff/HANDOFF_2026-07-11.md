# Citlali Refactor Handoff — 2026-07-11

## Current phase

The project is in Phase 2 of the adopted five-phase roadmap: config authority
and provenance. The immediate subsystem is processed timestream configuration
(`fruit_loops`, cleaning, weighting, and second-pass flagging).

The governing architecture review is
`handoff/EXTERNAL_REFACTOR_ARCHITECTURE_REVIEW_2026-07-10.md`. The finite PTC
transition contract is `doc/processed_timestream_config_transition.md`.

## Last accepted point evidence

- `redu31`, binary `09bafff9`: exact against `redu30`, including complete TOD.
- `redu32`, binary `55bd5218`: exact against `redu31`, including complete TOD.
- Both are recorded in `validation/accepted_runs.json`.
- The local suite currently has 241 passing tests; all eight config preflight
  cases pass.

## Cross-mode validation in progress

Point, beammap, and science reductions are running on Unity from `4c6268d8`.
Do not attribute later local commits to those runs.

Beammap uses its existing numbered config files. Science has a local final
overlay outside the repository:

`science/refactor/83_processed_config_validation.yaml`

It reverses cumulative debug overlays 80–82 by enabling fruit loops, noise
maps, map filtering, and filter error normalization. The run therefore matches
the established full science configuration and can be compared with historical
full runs (`science/refactor/redu07` and `science/citlali/redu11`), subject to a
generated low-level config comparison.

## Processed config work through the running SHA

- Direct typed readers and one-way adapters cover fruit loops, cleaning,
  weighting, validation, correlation penalties, busy-row suppression, and
  second-pass flagging.
- Effective decisions are explicit for cleaner subgroup normalization,
  source-mask inheritance, validated weighting, and busy-row dependencies.
- Focused adapter parity tests exist.
- `PTCProc::get_config` remains as a compatibility seed pending matched point,
  beammap, and science evidence plus versioned processed-config provenance.

## Later local safety commits

These commits are newer than the running Unity SHA and affect invalid config or
transition safeguards, not valid scientific execution:

- `258d90d0` rejects zero or multiple enabled cleaners in typed preflight.
- `59ee9398` rejects malformed cleaner groups and frequency bands.
- `a976931b` removes the cleaner-selection process exit and prevents invalid
  fruit-loop feedback from silently disabling itself before validation.
- `0d211e65` freezes the 171-path legacy PTC YAML boundary and rejects new
  paths or direct exits in the standard config preflight.

## Next actions

1. Audit the downloaded point, beammap, and science products for revision,
   generated config equality, unexpected errors, product completeness, and
   strict numerical parity.
2. Record accepted cross-mode evidence durably.
3. Compile the later invalid-config safety commits on Unity; a point run is
   sufficient because valid execution behavior is unchanged.
4. Do not remove the legacy PTC parser until every gate in
   `doc/processed_timestream_config_transition.md` is satisfied.
5. The next scientific-owner question is whether negative fruit-loop
   `sig2noise_limit` or `array_flux_limit` values are valid sentinels or should
   be fatal. Do not impose a domain without an answer.

## Downloaded cross-mode results

The point and science runs from `4c6268d8` completed and were inspected:

- Point `redu33` is strict-exact against `redu32`: 13 common product families,
  complete TOD, zero changed or skipped records, and no unexpected errors. It
  is recorded in `validation/accepted_runs.json`.
- Science `redu16` completed all four fruit-loop iterations with no unexpected
  errors. Its generated low-level config is an exact 502-leaf match to
  `science/refactor/redu07`; all 28 stable comparable product counts match.
- Full science comparison found zero skipped/missing/extra products. Raw
  observation, TOD, and diagnostic products are within tolerance. Twelve
  changed records are confined to three derived coadd noise/weight products:
  filtered a1100 (six records), raw a2000 (three), and map diagnostics (three).
  The largest fractional difference is 0.2977%, and median absolute differences
  are zero. Treat `redu16` as the current full science characterization
  baseline for the later parser-removal comparison, not as strict-exact
  historical equivalence.

Beammap `redu12` is not the requested validation run. Its log embeds
`v4.0.0-3326-g9ef7da8a`, not `4c6268d8`. It completed cleanly but cannot close
the current gate. A replacement beammap is now running from the latest pushed
checkpoint, `4b0126e7`. That checkpoint includes the matched `4c6268d8`
scientific path plus invalid-config safeguards and documentation; those later
changes should not alter valid beammap products. Verify the embedded revision
before comparison when the run lands.

## Later local execution-plan work

The following commits are newer than the running beammap and have not been
pushed:

- `8d7030b5` separates pure processed-weighting dependency resolution from its
  mutating compatibility adapter.
- `aabe0251` makes source-mask inheritance, cleaner grouping, and fruit-loop
  iteration normalization explicit effective-policy results.
- `2d2ab4c1` adds explicit cleaner-mode, interpolation, and source-protection
  resolution records.
- `1e4a2ade` defines a non-wired `ProcessedTimestreamExecutionPlan` with
  independent requested, effective, effective-resolution, and realized state.
- `031c6ce6` upgrades the frozen-path audit to enforce direct typed-reader
  coverage for all 171 legacy PTC paths and adds negative tests to preflight.
- `6f62f7ec` expands focused typed-to-processor parity tests to every field in
  the fruit-loop, cleaning, weighting, validation, correlation-penalty,
  busy-row, and second-pass adapters.
- `86ea2efa` defines atomic plan reset semantics and proves resolution and
  realized state cannot leak between sequential reductions.
- `a29fcbd1` adds deterministic requested/effective snapshot serializers and
  extends the audit to enforce serialization coverage for all 171 paths.
- `8f4a3299` adds explicit-availability serializers for effective-resolution
  and realized-state records.

At the running-beammap checkpoint these changes preserved adapter behavior and
did not yet wire the plan into `Engine` or output. The replacement beammap was
subsequently accepted and the bounded Engine wiring is described below.

Before wiring, the plan defined repeated-run behavior: atomically replace all
requested/effective state from a fresh request, retain supplied values inside
disabled sections, and clear all prior resolution and realized state. A focused
test proves no state leaks between plans. Current legacy reader objects are not
reset piecemeal; the complete wiring applies this contract without creating a
second transitional parser behavior.

## Replacement beammap accepted

Beammap `redu14` completed from `v4.0.0-3392-g4b0126e7` in 3576.607 seconds
with all three beammap phases, all 198 PTC chunks, all 5,234 detector maps,
complete products, and no logged issues. Its merged 529-leaf config is exact
against accepted refactor `redu11`. The complete refactor-to-refactor beammap
comparison is exact, including APT identities, flags, fit metrics, and all
good/bad signal, weight, and kernel map layers.

Against OG `b83c8750` `redu01`, `redu14` passes the versioned
`beammap-scientific-equivalence-v1` profile. Detector identities, flags, and
product sets are exact. The measured maxima remain the previously accepted
bounds: 0.6246% good-signal RMS relative, 0.3082% good-weight RMS relative,
0.2552% sensitivity relative, and sub-microarcsecond position differences.
This accepted checkpoint is recorded in `validation/accepted_runs.json`.

The matched beammap gate is closed. Proceed with the bounded Engine execution-
plan wiring described above; do not interpret beammap acceptance as permission
to remove the compatibility parser before provenance and all remaining removal
gates are complete.

## Engine execution-plan wiring

The bounded authority change is implemented locally after the accepted
beammap checkpoint. `ReductionConfigState` owns the processed execution plan;
`get_ptc_config` resets it from the accepted typed request, records cleaner,
source-mask, and weighting resolutions, and applies the existing one-way
processor adapters from the effective snapshot. Processed runtime accessors
select that snapshot after initialization. Source protection, fruit-loop
interpolation and iteration policy, and successful iteration completion now
populate resolution or realized records. The root typed config remains
synchronized as a temporary compatibility surface.

No legacy parser was removed and no final processed provenance document is
written. Local `citlali_cli` and `citlali_test` builds pass, all 249 C++ tests
pass, all eight config profiles pass, and the frozen boundary remains 171/171
direct typed-reader and serializer coverage. The next gate is a Unity compile
and point reduction. Do not add the final provenance root/writer until that
run is accepted; do not remove the compatibility parser until the broader
cross-mode and provenance gates are complete.

## Engine wiring point gate accepted

Unity point `redu34` embeds `v4.0.0-3406-g86c47fa7` and completed in 59.765
seconds with no serious log issues. Its merged low-level config is an exact
489-leaf match to accepted `redu33`, and stable product inventories match.
The strict complete-product comparison opened every RTC/PTC timestream array:
13 common scientific product families, zero missing or extra products, zero
changed records, and zero skipped records. The accepted checkpoint is recorded
in `validation/accepted_runs.json`.

The Engine execution-plan authority change is therefore accepted. Implement
the versioned processed-config provenance root and required atomic writer next.
Keep the legacy parser as compatibility seed until the emitted requested,
effective, and realized documents have passed cross-mode validation and all
remaining removal gates.

## Processed provenance v1 implemented

The next bounded change adds required reduction-level
`processed_timestream_provenance.yaml` with schema
`citlali-processed-timestream-provenance-v1`. It labels the accepted canonical
typed request, effective config and resolution records, and realized source-
protection/iteration state. The merged low-level YAML remains the record of
source-level key presence and spelling.

The CLI writes the sidecar atomically only after successful reduction
iterations and before logging overall completion. Uninitialized plan state or
any filesystem failure propagates and fails the reduction. Three focused tests
cover schema separation, atomic success, temporary-file cleanup, and failure
propagation. Local CLI/test builds, all 252 C++ tests, all eight config profiles,
and the 171/171 processed boundary audit pass. Unity compile and point output
inspection are the next gate; expect one additional reduction-level YAML file.

Validation tooling is ready for that run. `audit_reduction_run.py` now hashes
and validates runtime, every observation-level timestream-output, and processed
timestream provenance sidecar. Use `--require-processed-provenance` for one
run, or pass `--require-candidate-processed-provenance` to
`compare_reduction_audits.py` when the accepted baseline predates the new file.
Four Python tests cover complete, missing, malformed, and multi-observation
sidecars.

## Point processed provenance accepted

Unity point `redu35` embeds `v4.0.0-3409-g81020d46`. The required processed
sidecar passes the schema-aware audit and records all five effective-resolution
and all three realized-state entries. The merged config is an exact 489-leaf
match to `redu34`. Strict comparison reports 13 common scientific product
families, zero missing/extra products, zero changed records, and zero skipped
RTC/PTC arrays. The intended extra output is the single reduction-level
processed provenance YAML; its SHA-256 is recorded in the validation ledger.

Point provenance is accepted. Do not remove the compatibility parser yet:
beammap and science still need matched runs from a provenance-enabled revision
so their mode-specific resolutions and realized state can be checked. OOF
continues to reuse the explicit pointing gate; polarimetry remains separately
gated.

The provenance audit now also verifies semantics, not only structure. It checks
that cleaner, source-mask, weighting, and fruit-loop resolution records explain
the requested-to-effective transition, and that realized source protection and
iteration counts agree with the effective plan. Point `redu35` passes these
checks. Five Python tests include an intentionally inconsistent resolution.

Parser-removal preparation also now constructs a real value-initialized
`PTCProc`, applies all six PTC-to-typed compatibility mirrors, and requires its
complete serialized processed snapshot to equal fresh typed defaults. The test
passes. Raw default-initialized `PTCProc` scalar memory is not a legitimate
default source; valid legacy runs populate required keys, while the typed model
provides deterministic defaults. The test needs the same FITS/KIDs include
context as `engine.h`, confirming the already-known `ptcproc.h` header-
isolation work remains for Phase 3. All 253 C++ tests and config preflight pass.

## Processed compatibility seed isolated

The six scattered PTC-to-typed mirror calls in `Engine::get_ptc_config` are
now one `seed_processed_timestream_config_from_legacy(...)` call. The helper
retains the exact six mirror operations and is also used by the complete
default-parity test. This is behavior-preserving preparation: the legacy parser
still runs first, typed readers still overwrite accepted request fields,
resolvers still produce the effective plan, and one-way adapters still populate
`PTCProc` for execution.

The eventual removal boundary is now explicit:
`read_processor_config(ptcproc, ...)` plus
`seed_processed_timestream_config_from_legacy(...)`. Do not delete it until
matched provenance-enabled beammap and science runs are accepted. Local CLI
and test builds, all 253 C++ tests, all eight config profiles, 171/171 boundary
coverage, and all five provenance-audit tests pass.

The processed-boundary audit schema is now v3. In addition to frozen path,
typed-reader, and serializer coverage, it requires exactly one ordered parser
and compatibility-seed pair in `Engine::get_ptc_config` and rejects direct
mirror calls there. Six focused boundary-audit tests pass.
