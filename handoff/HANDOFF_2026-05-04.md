# Citlali Handoff: 2026-05-04

## 2026-05-04 10:55:26 EDT

### Current State

- Added a dedicated `handoff/` directory for persistent Citlali handoff notes.
- Moved existing root-level `DEBUG_NOTES_*.md` files into `handoff/` to reduce
  repo-root clutter.
- Updated `CODEX.md` with the standing rule: whenever Citlali is edited, add or
  append to the current day's handoff note with a timestamp.
- Reformatted `doc/citlali_noise_estimation_plan.tex` using the TolTEC
  technical memo template style.
- Set the noise memo author to `G. Wilson \& Codex`.

### Active Test Context

- Unity is back online.
- A single-observation COM-21 test was attempted first with obsnum `150784`,
  but TolTECA failed while loading input metadata before Citlali launched.
- Recommended next smoke-test selection is:

```yaml
select: 'scannum == 2 & obsnum == 148481'
```

- If that succeeds, the next coadd test should use the known AzTEC-C1 set:

```yaml
select: 'scannum == 2 & (obsnum in [129652, 129660, 130021, 130023, 130311, 130313, 131892, 131894, 134162, 134164, 134185, 134187, 134597, 134599, 148479, 148481, 148490, 148492])'
```

### Decisions

- Handoff notes are now first-class repo documentation and should live in
  `handoff/`.
- For TolTEC/Citlali memo-style LaTeX documents, use the template at
  `/Users/gwilson/GitHub/toltec-memoranda/templates/technical_memo_template.tex`.
- Use `G. Wilson \& Codex` as the author when Codex materially drafts or edits
  a memo.

### Next Steps

- Confirm the COM-21 single-observation smoke test with obsnum `148481` gets
  past TolTECA input loading and reaches Citlali.
- If the run reaches Citlali, inspect the output FITS products for formal and
  empirical weights, raw pixel S/N, filtered point-source products, and noise
  summary metadata.
- If TolTECA still fails before Citlali, debug the Unity input datastore/path
  rather than the Citlali noise-products implementation.

## 2026-05-04 12:28:37 EDT

### Current State

- Added FITS extension metadata in `include/citlali/core/engine/engine.h` for
  map products written by `Engine::write_maps`.
- New or clarified extension metadata includes:
  - `BUNIT` on image extensions in addition to the existing `UNIT` keyword.
  - `DESCRIP` text describing the meaning of each HDU image.
  - clearer descriptions for empirical weights, formal weights, jackknife noise
    variance, pixel S/N, and point-source S/N products.
- No local compile was run because this repo's local toolchain is not the
  authoritative validation environment; use Unity for build/runtime checks.

### Next Steps

- Build on Unity and rerun the `noise_products_test` smoke reduction.
- Inspect representative output FITS headers and confirm that `BUNIT` and
  `DESCRIP` appear on the signal, weight, formal-weight, noise-variance,
  S/N, and point-source-product HDUs.
