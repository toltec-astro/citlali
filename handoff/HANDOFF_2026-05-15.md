# Citlali Handoff: 2026-05-15

## 2026-05-15 17:30 EDT - Naive Accumulator Unity Validation

This note records the state after validating the first performance-branch map
accumulation change against `gw_dev`.

## Branch State

- Repository: `/Users/gwilson/GitHub/citlali`
- Branch: `codex/perf-map-accumulation-noise-lifecycle`
- Current branch commit under validation: `5f639e82`
- Compared against: current `gw_dev` at `68093fc4`
- Performance-branch Citlali version on Unity after rebuild:
  `v4.0.0-353-g5f639e82`
- `gw_dev` Citlali version on Unity:
  `v4.0.0-350-g68093fc4`

## Unity Reduction Comparisons

Working directory on Unity:

```text
~/work_toltec/local_data/2025-C1-COM-21/gw_sandbox
```

Validated single-observation reduction setup:

- Naive mapmaking.
- Noise maps disabled.
- OpenMP parallel policy.
- `n_threads: 15`.

Runs compared:

- `redu07`: `gw_dev`, completed in `3m22.039004006s`; scan pipeline time
  `189.261s`; PTC clean interval `10.850s/scan`.
- `redu08`: performance branch rebuilt with native release flags, completed in
  `3m46.542854437s`; scan pipeline time `192.113s`; PTC clean interval
  `10.791s/scan`.

The `redu07` and `redu08` generated Citlali config files were byte-identical.

Checked FITS products:

- obs maps for `a1100`, `a1400`, and `a2000`.
- coadd maps for `a1100`, `a1400`, and `a2000`.
- HDUs/planes checked: `signal_I`, `weight_I`, `coverage_I`,
  `coverage_bool_I`.

Result:

- `signal_I`: RMS and max absolute difference were exactly zero.
- `weight_I`: RMS and max absolute difference were exactly zero.
- `coverage_I`: RMS and max absolute difference were exactly zero.
- `coverage_bool_I`: mismatch count was exactly zero.

This validates the `P-001` tiled naive accumulator for the tested no-noise
standard naive case. It does not yet validate the noise-map path.

## Build Flag Lesson

An earlier performance-branch output, `redu06`, was much slower than `gw_dev`.
That difference was traced to the build, not the accumulator change.

Build metadata copied from Unity showed:

- `gw_dev` release flags included `-march=native`.
- The old performance-branch build did not include `-march=native`.
- The missing flag applied to `citlali`, `citlali_cli`, `kids`, `gramsavgol`,
  `netcdf-cxx4`, and tests in that build tree.

After reconfiguring and rebuilding the performance branch with native release
flags, the performance branch produced byte-identical checked maps against
`gw_dev` and returned close to the `gw_dev` wall time.

Recommended Unity rebuild pattern for this branch:

```sh
cd ~/work_toltec/citlali_dev/citlali_perf_mapaccum
cmake -S . -B build \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_C_FLAGS_RELEASE='-O3 -DNDEBUG -march=native' \
  -DCMAKE_CXX_FLAGS_RELEASE='-O3 -DNDEBUG -march=native'
cmake --build build --target citlali_cli -j 20
grep CMAKE_CXX_FLAGS_RELEASE build/CMakeCache.txt
grep 'CXX_FLAGS =' build/CMakeFiles/citlali.dir/flags.make
./build/bin/citlali --version
```

## Suggested Next Reduction Config

The next validation should exercise `P-002` without changing too many variables
at once. Keep the same observation, naive mapmaking, OpenMP policy, and branch
comparison, but turn on a small number of noise maps first:

```yaml
noise_maps:
  enabled: true
  n_noise_maps: 5
```

Also keep these fixed unless the SLURM allocation requires a different thread
count:

```yaml
mapmaking:
  method: naive

runtime:
  parallel_policy: omp
  n_threads: 15
  interp_over_gaps: true
```

If the `n_noise_maps: 5` comparison matches `gw_dev`, repeat with the standard
production noise-map count, currently `25` in the local `70_reduce.yaml` test
file.

## 2026-05-15 19:13 EDT - Noise-Map Validation And Timers

`redu10` landed as the clean `gw_dev` comparison for the small-noise test.

Runs compared:

- `redu09`: performance branch, Citlali `v4.0.0-353-g5f639e82`, noise maps
  enabled with `n_noise_maps: 5`, wall time `3m39.877138032s`.
- `redu10`: `gw_dev`, Citlali `v4.0.0-350-g68093fc4`, noise maps enabled with
  `n_noise_maps: 5`, wall time `3m28.932295901s`.

The generated Citlali configs were identical.

Comparison result:

- Obs and coadd FITS products were checked for `a1100`, `a1400`, and `a2000`.
- `coverage_bool_I` masks were identical for all checked products.
- Science map and noise-product image differences were roundoff only.
- Largest signal-map max absolute difference: `3.55e-15`.
- Largest checked FITS max absolute difference: `2.84e-14`.
- Histogram netCDF products were exactly equal.
- Mapdiag and PSD netCDF differences were roundoff only.
- Coadd empirical weight scales were identical:
  `0.488468900326375`, `0.509016124292622`, `0.542209831503308`.

Interpretation:

- The tiled naive accumulator is validated for the tested standard
  single-observation naive case with five noise realizations.
- The whole-pipeline runtime did not improve in this run, so the next changes
  should be judged with more targeted timing and memory information.

Repo changes made after this validation, later superseded by the 21:30 cleanup
below:

- Added targeted timer scopes in
  `include/citlali/core/mapmaking/naive_mm.h`.
- Timer labels expected in Unity logs:
  - `populate_maps_naive total`
  - `populate_maps_naive accumulate`
  - `populate_maps_naive merge`
- Updated `doc/PERFORMANCE_AUDIT_2026-05-14.md` with the `redu09`/`redu10`
  validation summary.
- Added `TiledNoiseAccumulator` in
  `include/citlali/core/mapmaking/tiled_accumulator.h`.
- Updated the naive mapmaker so noise realizations accumulate into touched
  tiles and merge into `MapBuffer::noise`, instead of cloning and zeroing a
  full scratch noise cube for each `populate_maps_naive()` call.
- Added focused unit-test coverage for `TiledNoiseAccumulator` in
  `tests/test_tiled_accumulator.cpp`.

At this point the timer and tiled-noise changes had not yet been built or run.
The local workstation remains non-authoritative for Citlali builds per
`CODEX.md`.

## Next Work

This list is historical and has been superseded by the later `redu13`/`redu14`
result.

## 2026-05-15 20:45 EDT - redu11 Tiled-Noise Validation

`redu11` landed from the rebuilt performance branch with the tiled noise
accumulator and timer instrumentation.

Runs compared:

- `redu10`: `gw_dev`, Citlali `v4.0.0-350-g68093fc4`, noise maps enabled with
  `n_noise_maps: 5`, wall time `3m28.932295901s`.
- `redu11`: performance branch, Citlali `v4.0.0-355-g8ee78c30`, same generated
  Citlali config, wall time `3m38.200127022s`.

Comparison result:

- Obs and coadd FITS products were checked for `a1100`, `a1400`, and `a2000`.
- `coverage_bool_I` masks were identical for all checked products.
- Science maps, weights, coverage, formal weights, noise variance, and S/N
  products differed only at roundoff.
- Largest signal-map max absolute difference: `5.33e-15`.
- Largest checked FITS max absolute difference: `2.13e-14`.
- Histogram netCDF products were exactly equal.
- Mapdiag and PSD differences were roundoff-scale; `map_coverage_sum` differed
  by `4.66e-10`, consistent with summed floating-point reduction-order noise.
- Coadd empirical weight scales were identical:
  `0.488468900326375`, `0.509016124292622`, `0.542209831503308`.

Timer aggregate from `redu11`:

- `populate_maps_naive total`: 124 calls, sum `423.871s`, mean `3.418s`.
- `populate_maps_naive accumulate`: 124 calls, sum `422.394s`, mean `3.406s`.
- `populate_maps_naive merge`: 124 calls, sum `1.431s`, mean `0.0115s`.

Interpretation:

- The tiled noise accumulator validates scientifically for the tested
  five-noise-map case.
- This small case does not show a whole-pipeline speedup.
- The merge/full-scratch path is not the dominant time at `n_noise_maps: 5`;
  the detector/sample accumulation loop dominates.

Next queued validation:

- Run the same branch comparison with the standard `n_noise_maps: 25` setting
  to check whether the tiled-noise change helps under larger memory pressure.

Next code target after the queued validation:

- Split naive mapmaking around a precomputed per-detector sample plan so
  detector pointing, rounded pixel indices, validity checks, and valid-sample
  lists can be reused across signal, weight, kernel, coverage, and noise
  accumulation.

## 2026-05-15 21:30 EDT - redu13/redu14 Standard Noise Validation

`redu13` and `redu14` landed with the standard `n_noise_maps: 25` setting.

Runs compared:

- `redu13`: performance branch, Citlali `v4.0.0-355-g8ee78c30`, wall time
  `4m28.969160386s`.
- `redu14`: `gw_dev`, Citlali `v4.0.0-350-g68093fc4`, same generated Citlali
  config, wall time `3m30.785061426s`.

Comparison result:

- Obs and coadd FITS products were checked for `a1100`, `a1400`, and `a2000`.
- `coverage_bool_I` masks were identical for all checked products.
- Science maps, weights, coverage, formal weights, noise variance, S/N,
  mapdiag, histograms, and PSDs differed only at floating-point roundoff.
- Largest signal-map max absolute difference: `5.33e-15`.
- Largest checked FITS max absolute difference: `4.26e-14`.
- Coadd empirical weight scales were identical:
  `0.350383961423778`, `0.370406096095144`, `0.393315916421293`.

Timer aggregate from `redu13`:

- `populate_maps_naive total`: 124 calls, sum `1160.820s`, mean `9.361s`.
- `populate_maps_naive accumulate`: 124 calls, sum `1155.568s`, mean `9.319s`.
- `populate_maps_naive merge`: 124 calls, sum `4.851s`, mean `0.0391s`.

Interpretation:

- The tiled-noise prototype was numerically clean but made the standard
  25-noise-map run about 58 seconds slower, roughly a 28 percent regression.
- Merge time was still tiny compared with detector/sample accumulation time.
- Keeping this as the default naive path would knowingly carry a slowdown.

Repo cleanup made after this validation:

- Removed `TiledNoiseAccumulator` and its tests.
- Restored the prior per-call scratch noise cube in
  `include/citlali/core/mapmaking/naive_mm.h`, including mutex-protected merge
  into the shared noise map.
- Kept the `populate_maps_naive` total/accumulate/merge timers.
- Kept the 2-D `TiledMapAccumulator` for signal, weight, kernel, and coverage
  maps.
- Updated `doc/PERFORMANCE_AUDIT_2026-05-14.md` with the 25-noise-map result
  and the decision to remove the tiled-noise default path.

Next code target:

- Build a precomputed per-detector sample plan so detector pointing, rounded
  pixel indices, validity checks, and valid-sample lists can be reused across
  signal, weight, kernel, coverage, and noise accumulation. That targets the
  measured hot path directly.
