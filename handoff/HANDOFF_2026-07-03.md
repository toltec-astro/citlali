# 2026-07-03 Handoff

## 2026-07-03 14:02:30 EDT

- Branch: `codex/structural-refactor`
- HEAD: `67405b16`
- Changed: `tools/local/configure-local-build.sh`
- Unrelated active worktree changes were present and left untouched, including config tooling and docs.
- Local build-environment setup work:
  - Added `$HOME/tolteca/bin` to `PATH` when its `conan` executable exists, matching the local Python tooling convention.
  - Set the local bootstrap to use Homebrew-installed Boost, CCfits, FFTW, NetCDF, and Eigen.
  - Forced `fmt` FetchContent to use `main` because upstream no longer has a `master` ref.
  - Avoided Conan defaults for Spectra, Ceres, logging, YAML, and testing libs; Ceres/Spectra now use FetchContent in the local bootstrap.
  - Added `BUILD_TESTS` override support, defaulting local bootstrap tests off via `CITLALI_BUILD_TESTS=OFF`.
- Verification so far:
  - `bash -n tools/local/configure-local-build.sh tools/local/configure-build-dir.sh`
  - Dependency check found required Homebrew packages already installed: Boost, CCfits, CFITSIO, CMake, FFTW, HDF5, libomp, NetCDF, Eigen.
  - Earlier configure attempts reached fetched dependency setup, then exposed `fmt` branch and Conan AppleClang 21 issues.
- Not run:
  - No compile/build command after the user noted active development in another thread.
- Next step:
  - When the user gives a quiet window, rerun `make local-bootstrap` and then compile the requested target.

## 2026-07-03 14:22:01 EDT

- Local bootstrap and compile succeeded.
- Additional local bootstrap adjustments:
  - Added `CMAKE_POLICY_VERSION_MINIMUM=3.5` so older fetched dependencies such as `glog`, `ceres`, and `re2` configure under CMake 4.3.
  - Forced `Re2` to FetchContent to avoid Conan 1's unsupported AppleClang 21 setting.
- Commands run:
  - `make local-bootstrap`
  - `cmake --build build --target citlali_cli -j6`
  - `./build/bin/citlali --version`
- Result:
  - Built `build/bin/citlali`.
  - Version smoke test output:
    - `v4.0.0-1287-gacf5e15a-dirty (2026-07-03T14:19:19)`
    - `kids 04088da-dirty (2026-07-03T14:19:17)`
- Notes:
  - `CITLALI_BUILD_TESTS` defaults to `OFF` in the local bootstrap; tests were not configured or run.
  - Build emitted dependency warnings from fetched `glog` about deprecated macOS `syscall`; no build-stopping warnings were observed.

## 2026-07-04 01:30 EDT - Refactor/Validation Workflow

- Branch: `codex/structural-refactor`
- HEAD at the latest local validation check: `a1a969a9`
- The current workflow is working well and should be preserved:
  1. Codex makes behavior-preserving refactor batches, usually 10-15 small commits at a time.
  2. After each batch, Codex runs the local compile gate:
     - `cmake --build build --target citlali_cli -j 8`
  3. Codex fixes any local compile errors before continuing.
  4. Repeat the refactor/build cycle several times before asking for a Unity compile, so Unity's longer build time is amortized over a meaningful batch.
  5. User pushes to GitHub and compiles on Unity from the separate refactor checkout.
  6. User runs the deterministic pointing validation reduction and downloads the resulting `reduNN` directory to:
     - `/Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced`
  7. Codex validates the newest point run against the previous point run with the manifest tools.

- Validation commands used locally:
  - Generate manifests:
    - `$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py --case point_refactor_reduNN --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/reduNN --manifest-out /private/tmp/citlali_point_reduNN_manifest.json --parallel-policy seq --n-threads 1`
  - Compare manifests:
    - `tools/baseline/compare_deterministic_manifests.sh /private/tmp/citlali_point_reduPREV_manifest.json /private/tmp/citlali_point_reduNN_manifest.json --max-diffs 120`

- Latest point validation:
  - Newest run checked: `redu73`
  - Compared against: `redu72`
  - `redu73/citlali.log.gz` ended with `citlali is done`.
  - Both manifests had 29 files and 0 tool warnings.
  - Deterministic manifest comparison result: `manifests match`.

- Operational guardrails:
  - Do not use Unity directly from Codex; the user compiles/runs there.
  - Do not push unless explicitly asked.
  - Leave unrelated dirty files alone. At this handoff, known unrelated local changes were:
    - `tools/local/configure-local-build.sh`
    - this handoff note while it remains untracked
  - Keep using `$HOME/tolteca/bin/python` for Python tools.
  - Prefer local compile repair before handing a batch to Unity.

- Why this workflow is effective:
  - Local builds catch most header/template compile failures quickly.
  - Unity compile time is used only after a substantial local batch.
  - Deterministic point reductions provide a stable behavioral gate.
  - The batch size is large enough to make progress but small enough to isolate regressions.

- Resume guidance:
  - After reboot or usage reset, first run `git status -sb`.
  - If a new point `reduNN` has been downloaded, validate it before new refactor work.
  - If validation is clean, continue the same loop: 10-15 behavior-preserving commits, local `citlali_cli` build, repair compile errors, repeat.

## 2026-07-04 08:44 EDT - Mapdiag Refactor Stage

- Branch: `codex/structural-refactor`
- HEAD at this handoff update: `106d0f0e`
- Local branch status at note time: aligned with `origin/codex/structural-refactor` (`0` commits ahead).
- Current user-side state:
  - User is compiling/running this batch on Unity.
  - Wait for Unity compile and point-reduction result before treating this batch as fully validated.

- Current stage:
  - Working through the `write_mapdiag` / diagnostic-output cleanup.
  - Estimated completion for this stage: about 70%.
  - Estimated broader structural refactor completion: about 75%, excluding the planned config rework phase.

- Recent completed work:
  - Extracted observation contribution table bookkeeping into `include/citlali/core/pipeline/mapdiag_observation_weight.h`.
  - Extracted map-label assignment helpers into `include/citlali/core/pipeline/mapdiag_labels.h`.
  - Added `include/citlali/core/pipeline/mapdiag_edge_guard.h` and moved edge-guard diagnostic field copying there.
  - Added/used mapdiag stats helpers in `include/citlali/core/pipeline/mapdiag_stats.h` for mask counts, weighted sums, median-error lookup, noise-tail summaries, and noise-product scalar grouping.
  - Latest local build after the five-batch run succeeded:
    - `cmake --build build --target citlali_cli -j 8`

- Most recent commits at note time:
  - `106d0f0e reuse mapdiag noise product stats helper`
  - `1026a3c8 extract mapdiag noise product stats helper`
  - `b74763e6 define mapdiag noise product stats`
  - `cc060eb1 reuse mapdiag positive sqrt value helper`
  - `8d351d04 extract mapdiag positive sqrt value helper`

- Remaining work in this stage:
  - The large map-pixel outlier / reduction-learning block inside `write_mapdiag` is still mostly inline and should be handled carefully.
  - Peak/sig2noise and coverage-stat assignment can still be grouped.
  - Do an include hygiene and dead-helper pass after the `write_mapdiag` extraction settles.
  - Continue using Unity point reductions as the behavioral gate after sizeable local build-clean batches.

- Handoff note policy update:
  - The previous "do not touch handoff note unless explicitly asked" instruction is superseded.
  - Update handoff notes at practical refresh points: after large clean refactor runs, after validation results, before risky/high-complexity blocks, or whenever memory/context loss would make recovery costly.

## 2026-07-05 - Engine Implementation Split Batch

- Branch: `codex/structural-refactor`
- HEAD after this batch: `067ccc62`
- User directive for this stage:
  - Validate the latest downloaded point reduction.
  - Continue with large stages of behavior-preserving refactor development between local builds.
  - Complete at least 10 local build checkpoints.
  - Do not push.

- Latest point validation before refactor work:
  - Newest run checked: `redu92`
  - Compared against: `redu91`
  - Manifest generation:
    - `$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py --case point_refactor_redu91 --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu91 --manifest-out /tmp/citlali_point_refactor_redu91_manifest.json`
    - `$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py --case point_refactor_redu92 --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu92 --manifest-out /tmp/citlali_point_refactor_redu92_manifest.json`
  - Both manifests had 29 files and 0 tool warnings.
  - Deterministic comparison:
    - `tools/baseline/compare_deterministic_manifests.sh /tmp/citlali_point_refactor_redu91_manifest.json /tmp/citlali_point_refactor_redu92_manifest.json`
  - Result: `manifests match`.

- Local build gate:
  - Ran `cmake --build build --target citlali_cli -j 8` after each stage.
  - Completed 10 local build checkpoints.
  - All 10 builds passed.

- Local commits made, not pushed:
  - `eab416b9 Split engine implementation details`
  - `a90434ea Split engine output implementations`
  - `b17c8bd5 Split engine config loaders`
  - `a7c25aa8 Split engine timestream config implementations`
  - `aac20beb Split engine learning implementations`
  - `3ec43322 Extract learning APT helpers`
  - `00c693ea Extract TOD output selection helpers`
  - `11980038 Split engine FITS map output implementations`
  - `332bca93 Split engine diagnostic output implementations`
  - `067ccc62 Split engine post-processing implementations`

- Main architectural result:
  - `include/citlali/core/engine/engine.h` is now about 598 lines.
  - The `Engine` declaration remains in `engine.h`.
  - Member-function implementations are now split into focused headers under:
    - `include/citlali/core/engine/detail/`
  - This was intentionally mostly mechanical so future semantic changes are easier to review and bisect.

- New or promoted pipeline helpers:
  - `include/citlali/core/pipeline/learning_apt_helpers.h`
    - Pure APT/UID lookup helpers used by learning apply/collect code.
  - `include/citlali/core/pipeline/tod_output_selection.h`
    - Pure TOD chunk-selection and scan-to-output-row helpers.

- Current structure after the split:
  - Config loader implementations are split by config family.
  - Timestream config implementations are split into RTC, PTC, and PTC line-audit helpers.
  - Learning implementations are split into config, targets, apply, collect, and summary.
  - Output implementations are split into observation maps, TOD files, summaries, FITS PHDU/image writing, spectral outputs, mapdiag, diagnostics, stats, and post-processing wrappers.

- Known local working tree state after this batch:
  - `tools/local/configure-local-build.sh` remains modified and unrelated to this refactor batch.

## 2026-07-05 - Header Wrapper Split Batch

- Branch: `codex/structural-refactor`
- HEAD after code batch: `1cdabe63`
- User directive for this stage:
  - Push onward with another 10 local build checkpoints.
  - Keep two structural stages per build.
  - Continue unless this phase reaches a natural stopping point.
  - Do not push.

- Local build gate:
  - Ran `cmake --build build --target citlali_cli -j 8` after each two-stage checkpoint.
  - Completed 10 successful local build checkpoints.
  - One split-boundary issue was caught during inspection before a build: a multi-line `MapFitter` template declaration in the beammap config split. It was repaired before compiling.
  - No compile repairs were needed after invoking the build command in this batch.

- Local commits made, not pushed:
  - `8212bac4 Split beammap prior and finalization details`
  - `0f4c381d Split beammap pipeline and TOD metadata details`
  - `d85f477b Split mapdiag and FITS image metadata helpers`
  - `422837f8 Split diagnostic NetCDF writer helpers`
  - `07331591 Split mapdiag NetCDF and beammap config helpers`
  - `a8ef0830 Split PHDU config and map filtering helpers`
  - `effcc416 Split source finding and timestream mirror helpers`
  - `7621c425 Split TOD and pointing engine implementations`
  - `0deae4c9 Split IO and KIDs engine implementations`
  - `1cdabe63 Split Lali engine and mapdiag workspace helpers`

- Main architectural result:
  - Many formerly large pipeline headers are now stable wrapper headers that include smaller concept-specific helper files.
  - Beammap implementation details are split across setup, pipeline, prior, finalization, flagging, and process-output helper headers.
  - Map diagnostics are split into stats, NetCDF layout/value definitions, workspace storage, outlier learning, and signal-diagnostic helpers.
  - FITS/PHDU/TOD metadata helpers are separated by output family and keyword/config concern.
  - Engine-facing classes now mostly keep declarations in `todproc.h`, `pointing.h`, `lali.h`, `kidsproc.h`, and `io.h`, with method bodies moved into `include/citlali/core/engine/detail/*`.
  - The timestream config mirror has been split into processed-clean, processed-weighting, raw-flagging, raw-filter, and raw-output groups, which is a useful landing zone for the later typed config mirror phase.

- Recommended next steps:
  1. User pushes this batch and compiles on Unity.
  2. User runs/downloads the next point reduction.
  3. Codex validates newest point `reduNN` against the latest known-clean point run.
  4. If Unity validation is clean, reassess whether this structural split phase is done enough to start the typed config mirror, or whether to first address the remaining large single-method files:
     - `include/citlali/core/engine/detail/beammap_run_loop_impl.h`
     - `include/citlali/core/engine/detail/beammap_detector_table_output_impl.h`
     - large legacy timestream/mapmaking implementation headers outside the engine/pipeline split surface.

- Known local working tree state after this batch:
  - `tools/local/configure-local-build.sh` remains modified and unrelated to this refactor batch.
  - This handoff note has been updated after the code commits.

## 2026-07-05 - Mapdiag And Beammap Detail Split Batch

- Branch: `codex/structural-refactor`
- HEAD after code batch: `9409f3bd`
- User directive for this stage:
  - Push onward with another 10 local build checkpoints.
  - Continue the two-stages-per-build cadence where practical.
  - Keep changes behavior-preserving.
  - Do not push.

- Local build gate:
  - Ran `cmake --build build --target citlali_cli -j 8` after each checkpoint.
  - Completed 10 successful local build checkpoints.
  - No compile repairs were needed in this batch.

- Local commits made, not pushed:
  - `1faac7ef Extract mapdiag workspace allocation`
  - `94269cf2 Extract mapdiag basic map setup`
  - `51b0cfae Extract mapdiag signal setup helpers`
  - `8d37174c Extract mapdiag outlier learning helpers`
  - `43ee20ca Extract mapdiag observation contribution helper`
  - `5384e1a3 Extract mapdiag signal diagnostics helper`
  - `73ed4325 Extract beammap detector TOD writer`
  - `8e9a3b1a Extract beammap detector table outputs`
  - `765d97eb Extract beammap map product output`
  - `9409f3bd Extract beammap calibration and masking details`

- Main architectural result:
  - `write_mapdiag` is now a compact orchestration function. Mapdiag workspace allocation, basic map stats, signal diagnostics, outlier learning, and observation contribution handling are in named helpers.
  - New mapdiag support lives in:
    - `include/citlali/core/pipeline/mapdiag_workspace.h`
    - `include/citlali/core/engine/detail/mapdiag_observation_contribution.h`
  - `beammap.h` dropped from roughly 6900 lines at the start of this batch to 4369 lines.
  - Large beammap output/calibration/masking bodies now live in focused detail headers:
    - `beammap_detector_tod_output_impl.h`
    - `beammap_detector_table_output_impl.h`
    - `beammap_map_product_output_impl.h`
    - `beammap_empirical_template_calibration_impl.h`
    - `beammap_masking_impl.h`

- Recommended next steps:
  1. User pushes this batch and compiles on Unity.
  2. User runs/downloads the next point reduction.
  3. Codex validates newest point `reduNN` against the previous clean point run.
  4. If clean, continue with remaining large beammap members:
     - `run_loop`
     - `set_apt_flags`
     - `update_prior_frame_estimates`
     - `choose_prior_guided_init`
     - `setup`

- Known local working tree state after this batch:
  - `tools/local/configure-local-build.sh` remains modified and unrelated to this refactor batch.
  - This handoff note is intentionally updated and should be committed as the final working-note commit for the cycle.

## 2026-07-05 - Point Validation And Beammap Header Split

- Branch: `codex/structural-refactor`
- HEAD after code batch: `89ee2e4a`
- User directive for this stage:
  - Validate the newest downloaded point reduction.
  - Continue the structural refactor.
  - Do not push.

- Latest point validation before refactor work:
  - Newest run checked: `redu101`
  - Compared against: `redu100`
  - Manifest generation:
    - `$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py --case point_refactor_redu100 --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu100 --manifest-out /tmp/citlali_point_refactor_redu100_manifest.json`
    - `$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py --case point_refactor_redu101 --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu101 --manifest-out /tmp/citlali_point_refactor_redu101_manifest.json`
  - Both manifests had 29 files and 0 tool warnings.
  - Strict hash comparison reported expected churn in logs, yaml, headers, and diagnostic files.
  - Direct numeric comparison passed:
    - FITS image arrays matched exactly.
    - ECSV numeric columns matched exactly.
    - NetCDF numeric variables matched exactly.
  - Validation status: passed for numerical products.

- Local build gate:
  - Ran `cmake --build build --target citlali_cli -j 8` after each structural slice.
  - Completed 3 successful local build checkpoints.
  - No compile repairs were needed after boundary checks.

- Local commits made, not pushed:
  - `e645da27 Extract beammap pipeline and prior details`
  - `08bacda9 Extract beammap run loop and finalization details`
  - `89ee2e4a Extract remaining beammap implementation details`

- Main architectural result:
  - `include/citlali/core/engine/beammap.h` is now 266 lines and is mostly class state, declarations, and detail includes.
  - Beammap implementation bodies now live in detail headers:
    - `beammap_pipeline_impl.h`
    - `beammap_prior_impl.h`
    - `beammap_run_loop_impl.h`
    - `beammap_finalization_impl.h`
    - `beammap_convergence_impl.h`
    - `beammap_output_impl.h`
  - This completes the major Beammap header split. Remaining structural work should focus on splitting oversized detail helpers internally rather than shrinking `beammap.h` further.

- Recommended next steps:
  1. User pushes this batch and compiles on Unity.
  2. User runs/downloads the next point reduction.
  3. Codex validates newest point `reduNN` numerically against `redu101`.
  4. If clean, continue by decomposing large detail files, especially:
     - `beammap_run_loop_impl.h`
     - `beammap_prior_impl.h`
     - `beammap_finalization_impl.h`
     - large NetCDF helper modules such as `rtcdiag_netcdf.h` and `mapdiag_stats.h`.

- Known local working tree state after this batch:
  - `tools/local/configure-local-build.sh` remains modified and unrelated to this refactor batch.
  - This handoff note is intentionally updated and should be committed as the working-note commit for this cycle.

## 2026-07-05 - Point redu100 Validation and Output Helper Batch

- Branch: `codex/structural-refactor`
- HEAD after code batch: `3e5138b9`
- User directive for this stage:
  - Validate the newest downloaded Unity point reduction.
  - Push onward with another 10 local build checkpoints, same cadence as the
    previous batch.
  - Do not push.

- Latest point validation before refactor work:
  - Newest run checked: `redu100`
  - Compared against: `redu99`
  - Manifest generation:
    - `$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py --case point_refactor_redu99 --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu99 --manifest-out /tmp/citlali_point_refactor_redu99_manifest.json`
    - `$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py --case point_refactor_redu100 --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu100 --manifest-out /tmp/citlali_point_refactor_redu100_manifest.json`
  - Both manifests had 29 files and 0 tool warnings.
  - Strict full-file SHA comparison differed in logs/index/full-file hashes, as
    expected for separate runs.
  - Payload-summary comparison matched after ignoring run label, aggregate byte
    count, whole-file SHA, and log size/line-count bookkeeping:
    - `$HOME/tolteca/bin/python tools/baseline/compare_manifests.py /tmp/citlali_point_refactor_redu99_manifest.json /tmp/citlali_point_refactor_redu100_manifest.json --ignore 'run.case' --ignore 'aggregate.total_size_bytes' --ignore 'files.*.sha256' --ignore 'files.citlali.log.gz.size_bytes' --ignore 'files.citlali.log.gz.summary.line_count' --max-diffs 120`
    - Result: `manifests match`

- Local build gate:
  - Ran `cmake --build build --target citlali_cli -j 8` after each checkpoint.
  - Completed 10 successful local build checkpoints.
  - Two failed compile attempts occurred during development and were repaired
    immediately:
    - PHDU unit-conversion helper initially took `Calib` as const, but legacy
      calibration maps use `operator[]`.
    - PHDU beammap/observation helper initially took flux maps as const, but
      beammap PHDU helpers use `operator[]`.

- Local commits made, not pushed:
  - `cc0a1536 Extract primary map image HDU helpers`
  - `1e702e10 Extract coverage map image helpers`
  - `c460121f Extract noise map image helper`
  - `08f228be Extract PHDU unit conversion helper`
  - `dd0b6395 Extract PHDU identity geometry helper`
  - `41b5f90f Extract PHDU extinction OOF helper`
  - `2f827c94 Extract PHDU TOD runtime helper`
  - `fe1aed04 Extract PHDU PTC learning helper`
  - `0c7e3ba7 Extract PHDU pointing header helper`
  - `3e5138b9 Extract PHDU beammap observation helper`

- Main architectural result:
  - `write_maps` in `map_image_output_impl.h` is now mostly orchestration.
    Primary map HDUs, coverage/support HDUs, point-source filtered companions,
    kernel HDUs, and noise-realization HDUs live in
    `include/citlali/core/pipeline/map_image_output_helpers.h`.
  - `add_phdu` in `map_phdu_output_impl.h` is now an ordered set of named PHDU
    sections. Unit conversion, beammap/observation keys, identity/geometry,
    extinction/APT/OOF keys, TOD runtime config, PTC/learning config, and
    pointing/telescope header keys live in
    `include/citlali/core/engine/detail/map_phdu_output_helpers.h`.

- Recommended next steps:
  1. User pushes this batch and compiles on Unity.
  2. User runs/downloads the next point reduction.
  3. Codex validates newest point `reduNN` against `redu100`.
  4. If clean, start the next cleanup pass in `mapdiag_output_impl.h`.
     That file is still large, but should be approached by extracting data
     allocation/reference bundles and write-table groups, not by changing
     mapdiag calculations.
  5. Keep typed config mirror work queued until this output-helper cleanup
     remains stable under Unity validation.

- Known local working tree state after this batch:
  - `tools/local/configure-local-build.sh` remains modified and unrelated to
    this refactor batch.

## 2026-07-05 - Config Loader Helper Ten-Build Batch

- Branch: `codex/structural-refactor`
- HEAD after code batch: `dfed0400`
- User directive for this stage:
  - Push onward with another 10 local build checkpoints, same cadence as the last batch.
  - Stop only if this structural-cleanup phase appears exhausted.
  - Do not push.

- Local build gate:
  - Ran `cmake --build build --target citlali_cli -j 8` after each paired refactor checkpoint.
  - Completed 10 successful local build checkpoints for this run.
  - No compile-repair commits were needed in this run.

- Local commits made, not pushed:
  - `7cd97dac Extract mapmaking policy sync helpers`
  - `4d459948 Extract output map block config read`
  - `0e04a4e1 Extract mapmaking runtime policy helpers`
  - `7cde6517 Extract jinc mapmaker config helpers`
  - `41ce9d28 Extract method-specific mapmaker config`
  - `3203b92a Extract noise map config orchestration`
  - `1f0bfe1c Extract post-processing config reads`
  - `e5872449 Extract source finding config reads`
  - `db5e7d61 Extract interface offset config reads`
  - `dfed0400 Extract map filter config policy`

- Main architectural result:
  - `get_mapmaking_config` is now mostly orchestration; mapmaking activation,
    grouping, output map block, coadd, runtime policy, method-specific,
    jinc, noise-map, and noise-product reads have moved into
    `include/citlali/core/engine/detail/mapmaking_config_read.h` and
    `include/citlali/core/pipeline/mapmaking_config_policy.h`.
  - `get_citlali_config` is now much smaller; interface sync offsets,
    post-processing activation, source-fitting reads, and source-finding reads
    live in `include/citlali/core/engine/detail/citlali_config_read.h`.
  - `get_map_filter_config` now delegates typed Wiener-filter mirroring and
    runtime guard policy to
    `include/citlali/core/engine/detail/map_filter_config_policy.h`.

- Recommended next steps:
  1. User pushes this batch and compiles on Unity.
  2. User runs/downloads the next point reduction.
  3. Codex validates newest point `reduNN` against the previous clean point run.
  4. If clean, continue structural cleanup in output-oriented helpers:
     - `map_image_output_impl.h`
     - `map_phdu_output_impl.h`
     - remaining `mapdiag_output_impl.h` chunks
  5. Keep the typed config mirror as the next major phase after this structural
     cleanup remains stable under Unity validation.

- Known local working tree state after this batch:
  - `tools/local/configure-local-build.sh` remains modified and unrelated to
    this refactor batch.

## 2026-07-05 - Point Validation and Ten-Build Helper Batch

- Branch: `codex/structural-refactor`
- HEAD after code batch: `dc9c6e58`
- User directive for this stage:
  - Validate the latest Unity point reduction.
  - Push onward with another 10 local build checkpoints.
  - Continue building after every other refactor stage.
  - Do not push.

- Latest point validation before refactor work:
  - Newest run checked: `redu99`
  - Compared against: `redu98`
  - Manifest generation:
    - `$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py --case point_refactor_redu98 --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu98 --manifest-out /tmp/citlali_point_refactor_redu98_manifest.json`
    - `$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py --case point_refactor_redu99 --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu99 --manifest-out /tmp/citlali_point_refactor_redu99_manifest.json`
  - Both manifests had 29 files and 0 tool warnings.
  - Structured comparison with `--ignore-sha256` differed only in:
    - `run.case`
    - aggregate byte count
    - `citlali.log.gz` size
    - `citlali.log.gz` line count
  - Validation status: clean under the current structured point-run criterion.

- Local build gate:
  - Ran `cmake --build build --target citlali_cli -j 8`.
  - Completed 10 successful local build checkpoints, each after 2 refactor
    stages.
  - No compile failures occurred in this cycle.

- Local commits made, not pushed:
  - `e221a16f Extract mapdiag noise tail helper`
  - `63f87296 Extract mapdiag observation fraction helper`
  - `53f8a058 Extract mapdiag label storage`
  - `e6e8f7c5 Extract beammap config defaults`
  - `a525b9f5 Extract timestream selection mirror helpers`
  - `29be19a7 Extract mapmaking core config reads`
  - `166039ce Extract mapmaking regime policy reads`
  - `22547156 Extract mapmaking mirror config reads`
  - `b3a99666 Extract noise map detail reads`
  - `dc9c6e58 Extract noise product config reads`

- Main architectural result:
  - `mapdiag_output_impl.h` shed additional noise-tail, observation-fraction,
    and label-storage plumbing.
  - `beammap_config_impl.h` now names the detector TOD output default and typed
    mirror reset.
  - `timestream_top_config_impl.h` delegates TOD selection mirroring and legacy
    state alignment.
  - `mapmaking_config_impl.h` is down to orchestration around helper functions
    for core mapmaking reads, source map regime policy, output-map-block typed
    mirroring, coadd/noise enable flags, and noise product options.

- Recommended next steps:
  1. User pushes this batch and compiles on Unity.
  2. User runs/downloads the next point reduction.
  3. Codex validates newest point `reduNN` against `redu99`.
  4. Continue carefully with `mapdiag_output_impl.h` and `mapmaking_config_impl.h`.
     The mapdiag outlier-learning block remains the highest-risk region.

- Known local working tree state after this batch:
  - `tools/local/configure-local-build.sh` remains modified and unrelated to
    this refactor batch.

## 2026-07-05 - Learning Summary and Mapdiag Helper Batch

- Branch: `codex/structural-refactor`
- HEAD after code batch: `9ab9eaa1`
- User directive for this stage:
  - Push onward with another 10 refactor stages.
  - Build after every other stage.
  - Do not push.

- Local build gate:
  - Ran `cmake --build build --target citlali_cli -j 8`.
  - Completed 5 successful local build checkpoints, each after 2 refactor stages.
  - No compile failures occurred in this cycle.

- Local commits made, not pushed:
  - `2ffadfc5 Extract learning summary application rows`
  - `89977be3 Extract mapdiag edge guard helpers`
  - `2a9a7cdd Extract mapdiag noise stat helpers`
  - `d9a08624 Extract mapdiag coverage helpers`
  - `9ab9eaa1 Extract mapdiag signal stat helper`

- Main architectural result:
  - `learning_summary_impl.h` is now a compact writer that delegates all row
    construction to `learning_summary_csv.h`.
  - `mapdiag_output_impl.h` no longer owns several low-level stat plumbing
    details:
    - edge-guard presence plus assignment
    - map weight-threshold extraction
    - formal-noise and noise-product assign-or-fill logic
    - conditional coverage assignment
    - peak-signal and signal-to-noise/core-tail assignment

- Recommended next steps:
  1. User pushes this batch and compiles on Unity.
  2. User runs/downloads the next point reduction.
  3. Codex validates newest point `reduNN` against the latest clean point run.
  4. Continue `mapdiag_output_impl.h` cleanup carefully; the outlier-learning
     block remains the largest high-risk region and should be split only in
     cohesive, well-built chunks.

- Known local working tree state after this batch:
  - `tools/local/configure-local-build.sh` remains modified and unrelated to
    this refactor batch.

## 2026-07-05 - Two-Stage Build Cadence Batch

- Branch: `codex/structural-refactor`
- HEAD after code batch: `4d357eaa`
- User directive for this stage:
  - Validate the newest downloaded point reduction from the latest Unity run.
  - Continue the refactor with paired stages and build after every 2 stages
    instead of after each individual stage.
  - Complete another local ten-build cycle.
  - Do not push.

- Latest point validation before refactor work:
  - Newest runs checked: `redu97` and `redu98`
  - Manifest generation:
    - `$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py --case point_refactor_redu97 --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu97 --manifest-out /tmp/citlali_point_refactor_redu97_manifest.json`
    - `$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py --case point_refactor_redu98 --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu98 --manifest-out /tmp/citlali_point_refactor_redu98_manifest.json`
  - Both manifests had 29 files and 0 tool warnings.
  - Strict compare had only expected run-local/hash/log differences.
  - Structured compare with `--ignore-sha256` found only:
    - `run.case`
    - aggregate byte count
    - `citlali.log.gz` size
    - `citlali.log.gz` line count
  - Validation status: clean under the current structured point-run criterion.

- Local build gate:
  - Ran `cmake --build build --target citlali_cli -j 8`.
  - Completed 10 successful local build checkpoints.
  - Cadence adjustment:
    - First resumed stage was already committed from the prior cadence.
    - Subsequent work used two-stage paired edits before each local build.
  - No compile failures occurred in this ten-build cycle.

- Local commits made, not pushed:
  - `79c58c43 Extract beammap flagging vector reads`
  - `4f001e5e Extract timestream core config reads`
  - `86482768 Extract timestream output mode reads`
  - `985fdd7c Extract timestream mirrored value reads`
  - `6509aae9 Extract timestream output selection reads`
  - `e633371c Extract timestream output state reads`
  - `1c5ff5d8 Extract learning summary CSV schema helpers`
  - `f24228f1 Extract learning summary event rows`
  - `64f0d9d6 Extract learning summary detector rows`
  - `4d357eaa Extract learning summary map rows`

- Main architectural result:
  - `beammap_config_impl.h` is now mostly orchestration; remaining flagging
    vector reads moved into `beammap_config_loading.h`.
  - `timestream_top_config_impl.h` now delegates core reads, TOD output
    enabled/mode/state reads, mirrored scalar reads, and TOD selection parsing
    to focused helpers.
  - `learning_summary_impl.h` shrank from a field-mapping body into a compact
    writer; CSV schema constants and most row encoders now live with
    `learning_summary_csv.h`.

- Recommended next steps:
  1. User pushes this batch and compiles on Unity.
  2. User runs/downloads the next point reduction.
  3. Codex validates newest point `reduNN` against `redu98`.
  4. Continue extracting the last learning-summary application row, then return
     to `mapdiag_output_impl.h` for larger, carefully staged helper extraction.
  5. Keep typed config mirror implementation as the next major architectural
     phase after this structural cleanup remains stable under Unity validation.

- Known local working tree state after this batch:
  - `tools/local/configure-local-build.sh` remains modified and unrelated to
    this refactor batch.

## 2026-07-05 - Mapmaking/Beammap/TOD Policy Cleanup Batch

- Branch: `codex/structural-refactor`
- HEAD after code batch: `dc5f01b5`
- User directive for this stage:
  - Provide progress status.
  - Validate the latest downloaded Unity point reduction.
  - Continue with another 10 local build checkpoints as in the previous cycle.
  - Do not push.

- Progress status at start of this batch:
  - The refactor is well past the original `engine.h` split.
  - Current work is the post-split cleanup stage: extracting inline config
    policy, typed-mirror assignments, output naming, and validation guards
    into focused helpers.
  - Estimated stage completion: high 70s percent complete for the structural
    cleanup stage.
  - The typed config mirror remains the next major architectural phase after
    these detail files are small and stable under Unity validation.

- Latest point validation before refactor work:
  - Newest downloaded run visible locally: `redu95`
  - Compared against: `redu94`
  - Manifest generation:
    - `$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py --case point_refactor_redu94 --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu94 --manifest-out /tmp/citlali_point_refactor_redu94_manifest.json`
    - `$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py --case point_refactor_redu95 --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu95 --manifest-out /tmp/citlali_point_refactor_redu95_manifest.json`
  - Both manifests had 29 files and 0 tool warnings.
  - Strict hash comparison found expected run-to-run differences in logs,
    indexes, compressed FITS/netCDF containers, and `learning_iter_0.csv`.
  - Structured comparison with `--ignore-sha256` found only:
    - `run.case`
    - aggregate byte count: `47880055` vs `47880047`
    - `citlali.log.gz` byte count: `46620` vs `46612`
    - `citlali.log.gz` line count: `429` vs `413`
  - No structured FITS/netCDF/table numeric-summary differences were reported.

- Local build gate:
  - Ran `cmake --build build --target citlali_cli -j 8` after each stage.
  - Completed 10 successful local build checkpoints.
  - No compile repairs were needed in this batch.

- Local commits made, not pushed:
  - `46804f94 Extract mapmaking noise policy helpers`
  - `661f5851 Extract mapmaker polarization policy`
  - `47d91981 Extract fruit-loop JINC reset policy`
  - `b0c56d46 Extract fruit-loop JINC mirror`
  - `bdafcd51 Extract map regime vocabulary`
  - `38e40ac9 Extract beammap phase strategy policy`
  - `2779d46d Extract beammap priors file policy`
  - `b792b992 Extract beammap split flag parsing`
  - `058e690e Extract TOD output mode flags`
  - `dc5f01b5 Extract TOD selection count validation`

- Main architectural result:
  - Mapmaking noise/coadd synchronization and mapmaker polarization policy now
    live in `include/citlali/core/pipeline/mapmaking_config_policy.h`.
  - Fruit-loop JINC defaults and JINC mapmaker-to-fruit-loop mirroring now live
    in `include/citlali/core/pipeline/fruit_loop_iteration_policy.h`.
  - Beammap phase-strategy normalization, priors-file validation, and split
    flag parsing now live in
    `include/citlali/core/pipeline/beammap_config_loading.h`.
  - TOD output mode flag assignment and selection-count validation now live in
    `include/citlali/core/pipeline/tod_output_selection.h`.

- Recommended next steps:
  1. User pushes this batch and compiles on Unity.
  2. User runs/downloads the next point reduction.
  3. Codex validates newest point `reduNN` against `redu95`.
  4. Continue cleanup of:
     - `mapdiag_output_impl.h`
     - remaining `beammap_config_impl.h` config-read blocks
     - `ptc_config_impl.h`
     - `learning_config_impl.h`
     - `rtc_config_impl.h`

- Known local working tree state after this batch:
  - `tools/local/configure-local-build.sh` remains modified and unrelated to
    this refactor batch.
  - This handoff note should be committed as a working-note artifact.

## 2026-07-05 - Beammap Config Read Cleanup Batch

- Branch: `codex/structural-refactor`
- HEAD after code batch: `f5559eed`
- User directive for this stage:
  - Push onward with another 10 local build checkpoints as in the previous
    cycle.
  - Do not push.

- Validation status before this batch:
  - No newly downloaded Unity point reduction was provided in this turn.
  - Previous validated point comparison remained `redu96` vs `redu95`.

- Local build gate:
  - Ran `cmake --build build --target citlali_cli -j 8` after each stage.
  - Completed 10 successful local build checkpoints.
  - No compile repairs were needed in this batch.

- Local commits made, not pushed:
  - `c73e7a13 Extract beammap iteration config reads`
  - `4eb9bdb4 Extract beammap phase config reads`
  - `a99f0321 Extract beammap reference config reads`
  - `baf8f7e3 Extract beammap RFI mask config reads`
  - `cc4d082e Extract beammap fitting config reads`
  - `aa83aed8 Extract beammap scan band config reads`
  - `6e638718 Extract beammap split FITS reads`
  - `29f0bac0 Extract beammap sensitivity config reads`
  - `84a69407 Extract beammap priors defaults`
  - `f5559eed Extract beammap priors iteration defaults`

- Main architectural result:
  - `beammap_config_impl.h` dropped from 400 lines to 269 lines.
  - Beammap iteration, phase strategy, reference, RFI mask, fitting,
    scan-band mask, split-FITS, sensitivity, and priors default policies now
    live in `include/citlali/core/pipeline/beammap_config_loading.h`.
  - The remaining beammap config method is much closer to orchestration:
    priors optional reads, flagging vector reads, typed mirroring, and final
    helper calls.

- Recommended next steps:
  1. User pushes this batch and compiles on Unity.
  2. User runs/downloads the next point reduction.
  3. Codex validates newest point `reduNN` against `redu96`.
  4. Continue cleanup of:
     - `mapdiag_output_impl.h`
     - remaining `beammap_config_impl.h` priors/config-read blocks
     - `learning_detector_apply_impl.h`
     - `timestream_top_config_impl.h`
     - `learning_summary_impl.h`

- Known local working tree state after this batch:
  - `tools/local/configure-local-build.sh` remains modified and unrelated to
    this refactor batch.
  - This handoff note should be committed as a working-note artifact.

## 2026-07-05 - RTC Finalization and Learning Read Cleanup Batch

- Branch: `codex/structural-refactor`
- HEAD after code batch: `ac6e243b`
- User directive for this stage:
  - Push onward with another 10 local build checkpoints as in the previous
    cycle.
  - Do not push.

- Validation status before this batch:
  - No newly downloaded Unity point reduction was provided in this turn.
  - Previous validated point comparison remained `redu96` vs `redu95`, with
    only run/log byte-count differences in the structured comparison.

- Local build gate:
  - Ran `cmake --build build --target citlali_cli -j 8` after each stage.
  - Completed 10 successful local build checkpoints.
  - No compile repairs were needed in this batch.

- Local commits made, not pushed:
  - `f45e7835 Extract raw filter typed mirror`
  - `1eaef7e7 Extract raw IIR filter mirror`
  - `c69762eb Extract raw filter edge guard mirror`
  - `69ef1f68 Extract raw TOD context policy`
  - `01391b4f Extract raw line audit mirror`
  - `52c19a15 Extract top-level learning config reads`
  - `ee6cb573 Extract map outlier learning reads`
  - `84191945 Extract scan network learning reads`
  - `f5977016 Extract learning diagnostics policy`
  - `ac6e243b Extract beammap detector TOD output reads`

- Main architectural result:
  - `rtc_config_impl.h` is now a 48-line orchestration method.
  - Raw filter, IIR filter, filter edge guard, TOD output context, and line
    audit mirrors now live in
    `include/citlali/core/pipeline/timestream_config_mirror.h`.
  - Added `include/citlali/core/engine/detail/learning_config_read.h` for
    optional learning config reads that mirror typed config only after clean
    parsing.
  - `learning_config_impl.h` is down to 157 lines after applying the generic
    reader to top-level, map-outlier, busy-detector, and scan-network
    pathology options.
  - Beammap detector TOD output reads now live in
    `include/citlali/core/pipeline/beammap_config_loading.h`.

- Recommended next steps:
  1. User pushes this batch and compiles on Unity.
  2. User runs/downloads the next point reduction.
  3. Codex validates newest point `reduNN` against `redu96`.
  4. Continue cleanup of:
     - `mapdiag_output_impl.h`
     - remaining `beammap_config_impl.h` config-read blocks
     - `learning_detector_apply_impl.h`
     - `timestream_top_config_impl.h`
     - `learning_summary_impl.h`

- Known local working tree state after this batch:
  - `tools/local/configure-local-build.sh` remains modified and unrelated to
    this refactor batch.
  - This handoff note should be committed as a working-note artifact.

## 2026-07-05 - RTC Filter and Learning Config Cleanup Batch

- Branch: `codex/structural-refactor`
- HEAD after code batch: `ac6e243b`
- User directive for this stage:
  - Push onward with another 10 local build checkpoints as in the previous
    cycle.
  - Do not push.

- Validation status before this batch:
  - No newly downloaded Unity point reduction was provided in this turn.
  - Previous validated point comparison remained `redu96` vs `redu95`, with
    only run/log byte-count differences in the structured comparison.

- Local build gate:
  - Ran `cmake --build build --target citlali_cli -j 8` after each stage.
  - Completed 10 successful local build checkpoints.
  - No compile repairs were needed in this batch.

- Local commits made, not pushed:
  - `f45e7835 Extract raw filter typed mirror`
  - `1eaef7e7 Extract raw IIR filter mirror`
  - `c69762eb Extract raw filter edge guard mirror`
  - `69ef1f68 Extract raw TOD context policy`
  - `01391b4f Extract raw line audit mirror`
  - `52c19a15 Extract top-level learning config reads`
  - `ee6cb573 Extract map outlier learning reads`
  - `84191945 Extract scan network learning reads`
  - `f5977016 Extract learning diagnostics policy`
  - `ac6e243b Extract beammap detector TOD output reads`

- Main architectural result:
  - `rtc_config_impl.h` is now a 48-line orchestration method.
  - Raw filter, IIR filter, filter edge guard, TOD output context, and line
    audit mirrors now live in
    `include/citlali/core/pipeline/timestream_config_mirror.h`.
  - `learning_config_impl.h` is down to 157 lines after extracting a generic
    optional learning config reader and applying it to top-level, map-outlier,
    busy-detector, and scan-network pathology options.
  - Beammap detector TOD output config reading now lives in
    `include/citlali/core/pipeline/beammap_config_loading.h`.

- Recommended next steps:
  1. User pushes this batch and compiles on Unity.
  2. User runs/downloads the next point reduction.
  3. Codex validates newest point `reduNN` against `redu96`.
  4. Continue cleanup of:
     - `mapdiag_output_impl.h`
     - remaining `beammap_config_impl.h` config-read blocks
     - `learning_detector_apply_impl.h`
     - `learning_summary_impl.h`
     - `timestream_top_config_impl.h`

- Known local working tree state after this batch:
  - `tools/local/configure-local-build.sh` remains modified and unrelated to
    this refactor batch.
  - This handoff note should be committed as a working-note artifact.

## 2026-07-05 - RTC/PTC Typed Mirror Cleanup Batch

- Branch: `codex/structural-refactor`
- HEAD after code batch: `02b8621d`
- User directive for this stage:
  - Validate the newly downloaded Unity point reduction.
  - Continue with another 10 local build checkpoints as in the previous cycle.
  - Do not push.

- Latest point validation before refactor work:
  - Newest downloaded run visible locally: `redu96`
  - Compared against: `redu95`
  - Manifest generation:
    - `$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py --case point_refactor_redu95 --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu95 --manifest-out /tmp/citlali_point_refactor_redu95_manifest.json`
    - `$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py --case point_refactor_redu96 --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu96 --manifest-out /tmp/citlali_point_refactor_redu96_manifest.json`
  - Both manifests had 29 files and 0 tool warnings.
  - Strict hash comparison found expected run-to-run differences in logs,
    indexes, compressed FITS/netCDF containers, and `learning_iter_0.csv`.
  - Structured comparison with `--ignore-sha256` found only:
    - `run.case`
    - aggregate byte count: `47880047` vs `47879678`
    - `citlali.log.gz` byte count: `46612` vs `46243`
    - `citlali.log.gz` line count: `413` vs `411`
  - No structured FITS/netCDF/table numeric-summary differences were reported.

- Local build gate:
  - Ran `cmake --build build --target citlali_cli -j 8` after each stage.
  - Completed 10 successful local build checkpoints.
  - No compile repairs were needed in this batch.

- Local commits made, not pushed:
  - `7a56e89d Extract fruit-loop typed mirror`
  - `89d69bdd Extract processed clean typed mirror`
  - `d4366918 Extract processed weighting typed mirror`
  - `1a2a070d Extract weight validation typed mirror`
  - `b8c22f73 Extract weight correlation penalty mirror`
  - `e8fe9766 Extract second pass local mirror`
  - `3f64f979 Extract raw despike typed mirror`
  - `45d97f5a Extract raw flagging typed mirror`
  - `0df18b1b Extract raw kernel downsample mirrors`
  - `02b8621d Extract raw altaz correction mirrors`

- Main architectural result:
  - Added `include/citlali/core/pipeline/timestream_config_mirror.h` as the
    home for RTC/PTC typed config mirror helpers.
  - `ptc_config_impl.h` is now a thin 40-line orchestration function after
    moving fruit loops, processed clean, weighting, validation, correlation
    penalty, and second-pass local mirrors.
  - `rtc_config_impl.h` is down to 204 lines after moving raw despike,
    raw flagging, raw kernel, downsample, alt-az destriping, and correction
    flag mirrors.
  - This directly supports the upcoming typed-config mirror phase by grouping
    typed assignments outside the legacy engine method.

- Recommended next steps:
  1. User pushes this batch and compiles on Unity.
  2. User runs/downloads the next point reduction.
  3. Codex validates newest point `reduNN` against `redu96`.
  4. Continue cleanup of:
     - `mapdiag_output_impl.h`
     - remaining `beammap_config_impl.h` config-read blocks
     - `learning_config_impl.h`
     - `learning_summary_impl.h`
     - remaining raw line-audit/filter-edge-guard mirrors in
       `rtc_config_impl.h`

- Known local working tree state after this batch:
  - `tools/local/configure-local-build.sh` remains modified and unrelated to
    this refactor batch.
  - This handoff note should be committed as a working-note artifact.

## 2026-07-05 - PHDU/Config Policy Cleanup Batch

- Branch: `codex/structural-refactor`
- HEAD after code batch: `f9bdf75f`
- User directive for this stage:
  - Validate the newest Unity point reduction from the previous pushed batch.
  - Continue with another 10 local build checkpoints.
  - Keep changes behavior-preserving.
  - Commit locally after each successful stage.
  - Do not push.

- Latest point validation before refactor work:
  - Newest run checked: `redu94`
  - Compared against: `redu93`
  - Manifest generation:
    - `$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py --case point_refactor_redu93 --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu93 --manifest-out /tmp/citlali_point_refactor_redu93_manifest.json`
    - `$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py --case point_refactor_redu94 --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu94 --manifest-out /tmp/citlali_point_refactor_redu94_manifest.json`
  - Both manifests had 29 files and 0 tool warnings.
  - Strict SHA comparison found expected content-hash/log/index differences.
  - Structured comparison with `--ignore-sha256` reported only:
    - `run.case`
    - aggregate total byte size
    - `citlali.log.gz` byte size and line count
  - No structured FITS/netCDF/table numeric summary differences were reported.

- Local build gate:
  - Ran `cmake --build build --target citlali_cli -j 8` after each stage.
  - Completed 10 successful local build checkpoints.
  - No compile repairs were needed in this batch.

- Local commits made, not pushed:
  - `8dd0eff3 Extract beammap PHDU key writer`
  - `cd5a1400 Extract JINC PHDU key writer`
  - `63237dbd Extract OOF PHDU conditional writer`
  - `e799fc76 Extract pointing PHDU conditional writer`
  - `7bb6783a Use parse tracking in mapmaking config`
  - `e18684d2 Use parse tracking in citlali config`
  - `5275bb43 Extract source protection activation`
  - `d2eb984b Extract fruit-loop interpolation policy`
  - `4277b875 Extract fruit-loop policy logging`
  - `f9bdf75f Extract mapmaking disabled policy`

- Main architectural result:
  - PHDU reduction-specific branches are now moved behind helper functions:
    beammap, JINC, OOF, and pointing PHDU key writers.
  - Mapmaking and top-level citlali config loaders now share the central
    parse-clean tracking helper.
  - Source-protection activation and disabled-mapmaking policy now live in
    focused engine-detail helpers instead of the top-level config loader body.
  - Fruit-loop interpolation and policy logging now live with the fruit-loop
    policy helpers.

- Recommended next steps:
  1. User pushes this batch and compiles on Unity.
  2. User runs/downloads the next point reduction.
  3. Codex validates newest point `reduNN` against `redu94`.
  4. If clean, continue with larger cleanup slices in:
     - `mapdiag_output_impl.h`
     - remaining `beammap_config_impl.h` load sections
     - `learning_config_impl.h` guarded config-read repetition
     - `ptc_config_impl.h` and `rtc_config_impl.h` config families

- Known local working tree state after this batch:
  - `tools/local/configure-local-build.sh` remains modified and unrelated to
    this refactor batch.

## 2026-07-05 - Runtime/Pointing/Source Config Cleanup Batch

- Branch: `codex/structural-refactor`
- HEAD after code batch: `2217d46a`
- User directive for this stage:
  - Validate the newest Unity point reduction from the previous pushed batch.
  - Continue with another 10 local build checkpoints.
  - Keep changes behavior-preserving.
  - Commit locally after each successful stage.
  - Do not push.

- Latest point validation before refactor work:
  - Newest run checked: `redu95`
  - Compared against: `redu94`
  - Manifest generation:
    - `$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py --case point_refactor_redu94 --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu94 --manifest-out /tmp/citlali_point_refactor_redu94_manifest.json`
    - `$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py --case point_refactor_redu95 --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu95 --manifest-out /tmp/citlali_point_refactor_redu95_manifest.json`
  - Both manifests had 29 files and 0 tool warnings.
  - Strict SHA comparison found expected content-hash/log/index differences.
  - Structured comparison with `--ignore-sha256` reported only:
    - `run.case`
    - aggregate total byte size
    - `citlali.log.gz` byte size and line count
  - No structured FITS/netCDF/table numeric summary differences were reported.

- Local build gate:
  - Ran `cmake --build build --target citlali_cli -j 8` after each stage.
  - Completed 10 successful local build checkpoints.
  - No compile repairs were needed in this batch.

- Local commits made, not pushed:
  - `5512aa68 Use parse tracking in runtime config`
  - `dff6651c Use parse tracking in pointing config`
  - `540aa2d8 Extract pointing config logging`
  - `a26c9fa1 Extract source finding coadd mirror`
  - `aa34d140 Extract source window unit conversion`
  - `acafd957 Extract source fitting unit conversion`
  - `892968c3 Extract source fit limit policy`
  - `1db4802a Extract source fitting activation helper`
  - `574babd5 Extract pointing offset axis normalization`
  - `2217d46a Extract typed pointing offset mirror`

- Main architectural result:
  - Runtime and pointing config loaders now share the central parse-clean
    tracking helper.
  - Pointing config logging/warnings now live in a focused engine-detail helper.
  - Source-finding and source-fitting unit conversions/policies now live with
    the source-finding pipeline helpers.
  - Pointing-offset normalization and typed mirror assignment now live in a
    focused astrometry helper header.

- Recommended next steps:
  1. User pushes this batch and compiles on Unity.
  2. User runs/downloads the next point reduction.
  3. Codex validates newest point `reduNN` against `redu95`.
  4. If clean, continue with larger cleanup slices in:
     - `mapdiag_output_impl.h`
     - remaining `beammap_config_impl.h` load sections
     - `learning_config_impl.h` guarded config-read repetition
     - `ptc_config_impl.h` and `rtc_config_impl.h` config families

- Known local working tree state after this batch:
  - `tools/local/configure-local-build.sh` remains modified and unrelated to
    this refactor batch.

## 2026-07-05 - Config/TOD/Map PHDU Cleanup Batch

- Branch: `codex/structural-refactor`
- HEAD after code batch: `034349ef`
- User directive for this stage:
  - Continue with another 10 local build checkpoints as in the previous cycle.
  - Keep the changes behavior-preserving.
  - Commit locally after each successful stage.
  - Do not push.

- Local build gate:
  - Ran `cmake --build build --target citlali_cli -j 8` after each stage.
  - Completed 10 successful local build checkpoints.
  - No compile repairs were needed in this batch.

- Local commits made, not pushed:
  - `6f698c1f Extract config parse tracking helpers`
  - `145f6bb9 Use parse tracking in timestream config`
  - `1aef9e0c Extract TOD output type selection helper`
  - `6ddd08dd Extract legacy TOD selection alignment`
  - `e76c69b7 Extract map write error formatting`
  - `d6aae92c Extract map write slot validation`
  - `dfdaa68d Extract noise map slot validation`
  - `79889479 Extract PHDU slot validation`
  - `f1ce4ec0 Extract PHDU unit conversion factors`
  - `034349ef Extract PHDU error formatting`

- Main architectural result:
  - Config parse-clean tracking is now centralized in
    `include/citlali/core/engine/detail/config_parse_tracking.h`.
  - TOD output type and legacy selection alignment helpers now live in
    `include/citlali/core/pipeline/tod_output_selection.h`.
  - Map image output validation/error formatting is further centralized in
    `include/citlali/core/pipeline/fits_image_metadata.h`.
  - PHDU slot validation, unit conversion factors, and error formatting now live
    in `include/citlali/core/pipeline/phdu_observation_metadata.h`.

- Recommended next steps:
  1. User pushes this batch and compiles on Unity.
  2. User runs/downloads the next point reduction.
  3. Codex validates newest point `reduNN` against the previous clean point run.
  4. If clean, continue with larger cleanup slices in:
     - `mapdiag_output_impl.h`
     - remaining `beammap_config_impl.h` load sections
     - `learning_config_impl.h` guarded config-read repetition
     - `ptc_config_impl.h` and `rtc_config_impl.h` config families

- Known local working tree state after this batch:
  - `tools/local/configure-local-build.sh` remains modified and unrelated to
    this refactor batch.

## 2026-07-05 - Beammap/TOD/Learning Cleanup Batch

- Branch: `codex/structural-refactor`
- HEAD after code batch: `6eb2efb8`
- User directive for this stage:
  - Continue with another 10 local build checkpoints as in the previous cycle.
  - Keep the changes behavior-preserving.
  - Commit locally after each successful stage.
  - Do not push.

- Local build gate:
  - Ran `cmake --build build --target citlali_cli -j 8` after each stage.
  - Completed 10 successful local build checkpoints.
  - No compile repairs were needed in this batch.

- Local commits made, not pushed:
  - `7a6b8175 Extract beammap typed core mirror`
  - `4bd6e71d Extract beammap typed priors mirror`
  - `de2e25d7 Extract beammap output flagging mirror`
  - `64f7e38e Extract TOD selection typed mirror`
  - `6430c9ce Extract TOD output index config parser`
  - `0e84eb5d Extract TOD selection count reader`
  - `79c11684 Extract CSV output helpers`
  - `23dc4102 Extract learning summary CSV header`
  - `f6eddbea Extract learning summary filename helper`
  - `6eb2efb8 Extract reduction learning config logging`

- Main architectural result:
  - Beammap typed mirror assignment is now grouped in named helpers in
    `include/citlali/core/pipeline/beammap_config_loading.h`.
  - TOD output index parsing and typed selection mirroring now live with the
    TOD output selection helpers in
    `include/citlali/core/pipeline/tod_output_selection.h`.
  - Generic CSV output helpers now live in
    `include/citlali/core/pipeline/csv_output.h`.
  - Learning summary CSV path/header policy now lives in
    `include/citlali/core/pipeline/learning_summary_csv.h`.
  - Reduction-learning config logging is isolated in
    `include/citlali/core/engine/detail/learning_config_logging.h`.

- Recommended next steps:
  1. User pushes this batch and compiles on Unity.
  2. User runs/downloads the next point reduction.
  3. Codex validates newest point `reduNN` against the previous clean point run.
  4. If clean, continue cleanup of:
     - `mapdiag_output_impl.h`
     - remaining `beammap_config_impl.h` load sections
     - `learning_config_impl.h` guarded config-read repetition
     - `timestream_top_config_impl.h` output/chunking config helpers

- Known local working tree state after this batch:
  - `tools/local/configure-local-build.sh` remains modified and unrelated to
    this refactor batch.
  - This handoff note should now be tracked/committed as a working-note artifact.

- Recommended next steps:
  1. User pushes these local commits and compiles on Unity.
  2. User runs/downloads a point reduction and Codex validates newest `reduNN` against the previous run.
  3. If Unity validation is clean, continue with behavior-preserving cleanup in the new smaller implementation files.
  4. The typed config mirror should be the next major architectural phase after the current structural split is stable under Unity validation. The split config loader files are the intended landing zone for that work.

- Current risk notes:
  - This batch is compile-clean locally, but not yet Unity-validated at this note time.
  - The changes are mostly include/file organization plus two pure helper extractions, so expected science-output risk is low.
  - Header-only template movement can still expose Unity-specific include-order issues, so Unity compile remains required before treating the batch as validated.

## 2026-07-05 - Post-Split Cleanup Batch

- Branch: `codex/structural-refactor`
- HEAD after code batch: `d6c959fb`
- User directive for this stage:
  - Validate newest downloaded point reduction.
  - Continue with another 10 local build checkpoints.
  - Keep using larger coherent refactor stages between builds.
  - Do not push.

- Latest point validation before refactor work:
  - Newest run checked: `redu93`
  - Compared against: `redu92`
  - Manifest generation:
    - `$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py --case point_refactor_redu92 --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu92 --manifest-out /tmp/citlali_point_refactor_redu92_manifest.json`
    - `$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py --case point_refactor_redu93 --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu93 --manifest-out /tmp/citlali_point_refactor_redu93_manifest.json`
  - Both manifests had 29 files and 0 tool warnings.
  - Deterministic comparison result: `manifests match`.

- Local build gate:
  - Ran `cmake --build build --target citlali_cli -j 8` after each stage.
  - Completed 10 successful local build checkpoints.
  - Two failed compile attempts occurred during development and were repaired immediately:
    - `tod_stream_output_key` helper declaration order.
    - `add_tod_unit_conversion_vars` constness mismatch with legacy calibration map access.

- Local commits made, not pushed:
  - `59af8e01 Extract beammap config vector helper`
  - `e4907a3f Extract beammap flag vector assignment`
  - `ff1964b4 Extract beammap split flag normalization`
  - `8a79edd5 Extract map HDU WCS helper`
  - `bd5183ff Extract map output scalar guards`
  - `28db5016 Extract map output median guards`
  - `bad8e9af Extract TOD output filename registration`
  - `8a74f574 Extract TOD unit conversion metadata helper`
  - `4e5cd0b5 Split learning apply implementations`
  - `d6c959fb Split learning collect implementations`

- Main architectural result:
  - Continued reducing `Engine` detail-file responsibilities after the large `engine.h` split.
  - Beammap config vector reading/assignment and split-flag normalization now live in `include/citlali/core/pipeline/beammap_config_loading.h`.
  - FITS map HDU/WCS insertion and output guardrails now live in `include/citlali/core/pipeline/fits_image_metadata.h`.
  - TOD output filename registration and unit-conversion metadata now live in `include/citlali/core/pipeline/output_netcdf_metadata.h`.
  - Learning apply/collect implementation files are split by detector/sample and RTC/PTC responsibilities.

- Recommended next steps:
  1. User pushes this batch and compiles on Unity.
  2. User runs/downloads the next point reduction.
  3. Codex validates newest point `reduNN` against `redu93`.
  4. If clean, continue cleanup of the largest detail files, especially:
     - `mapdiag_output_impl.h`
     - `beammap_config_impl.h`
     - learning summary/config helpers
  5. After the current structural cleanup stabilizes under Unity validation, begin the typed config mirror as the next major architectural phase.

- Known local working tree state after this batch:
  - `tools/local/configure-local-build.sh` remains modified and unrelated to this refactor batch.
