# 2026-07-03 Handoff

## 2026-07-03 14:02:30 EDT

- Branch: `codex/structural-refactor`
- HEAD: `67405b16`
- Changed: `tools/local/configure-local-build.sh`
- Unrelated active worktree changes were present and left untouched, including config tooling and docs.
- Local build-environment setup work:
  - Added `$HOME/tolteca/bin` to `PATH` when its `conan` executable exists, matching the local Python tooling convention.
  - Set the local bootstrap to use Homebrew-installed Boost, CCfits, FFTW, NetCDF, and Eigen.
  - Forced `fmt` FetchContent to use `main` because upstream no longer has a `master` ref.
  - Avoided Conan defaults for Spectra, Ceres, logging, YAML, and testing libs; Ceres/Spectra now use FetchContent in the local bootstrap.
  - Added `BUILD_TESTS` override support, defaulting local bootstrap tests off via `CITLALI_BUILD_TESTS=OFF`.
- Verification so far:
  - `bash -n tools/local/configure-local-build.sh tools/local/configure-build-dir.sh`
  - Dependency check found required Homebrew packages already installed: Boost, CCfits, CFITSIO, CMake, FFTW, HDF5, libomp, NetCDF, Eigen.
  - Earlier configure attempts reached fetched dependency setup, then exposed `fmt` branch and Conan AppleClang 21 issues.
- Not run:
  - No compile/build command after the user noted active development in another thread.
- Next step:
  - When the user gives a quiet window, rerun `make local-bootstrap` and then compile the requested target.

## 2026-07-03 14:22:01 EDT

- Local bootstrap and compile succeeded.
- Additional local bootstrap adjustments:
  - Added `CMAKE_POLICY_VERSION_MINIMUM=3.5` so older fetched dependencies such as `glog`, `ceres`, and `re2` configure under CMake 4.3.
  - Forced `Re2` to FetchContent to avoid Conan 1's unsupported AppleClang 21 setting.
- Commands run:
  - `make local-bootstrap`
  - `cmake --build build --target citlali_cli -j6`
  - `./build/bin/citlali --version`
- Result:
  - Built `build/bin/citlali`.
  - Version smoke test output:
    - `v4.0.0-1287-gacf5e15a-dirty (2026-07-03T14:19:19)`
    - `kids 04088da-dirty (2026-07-03T14:19:17)`
- Notes:
  - `CITLALI_BUILD_TESTS` defaults to `OFF` in the local bootstrap; tests were not configured or run.
  - Build emitted dependency warnings from fetched `glog` about deprecated macOS `syscall`; no build-stopping warnings were observed.

## 2026-07-04 01:30 EDT - Refactor/Validation Workflow

- Branch: `codex/structural-refactor`
- HEAD at the latest local validation check: `a1a969a9`
- The current workflow is working well and should be preserved:
  1. Codex makes behavior-preserving refactor batches, usually 10-15 small commits at a time.
  2. After each batch, Codex runs the local compile gate:
     - `cmake --build build --target citlali_cli -j 8`
  3. Codex fixes any local compile errors before continuing.
  4. Repeat the refactor/build cycle several times before asking for a Unity compile, so Unity's longer build time is amortized over a meaningful batch.
  5. User pushes to GitHub and compiles on Unity from the separate refactor checkout.
  6. User runs the deterministic pointing validation reduction and downloads the resulting `reduNN` directory to:
     - `/Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced`
  7. Codex validates the newest point run against the previous point run with the manifest tools.

- Validation commands used locally:
  - Generate manifests:
    - `$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py --case point_refactor_reduNN --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/reduNN --manifest-out /private/tmp/citlali_point_reduNN_manifest.json --parallel-policy seq --n-threads 1`
  - Compare manifests:
    - `tools/baseline/compare_deterministic_manifests.sh /private/tmp/citlali_point_reduPREV_manifest.json /private/tmp/citlali_point_reduNN_manifest.json --max-diffs 120`

- Latest point validation:
  - Newest run checked: `redu73`
  - Compared against: `redu72`
  - `redu73/citlali.log.gz` ended with `citlali is done`.
  - Both manifests had 29 files and 0 tool warnings.
  - Deterministic manifest comparison result: `manifests match`.

- Operational guardrails:
  - Do not use Unity directly from Codex; the user compiles/runs there.
  - Do not push unless explicitly asked.
  - Leave unrelated dirty files alone. At this handoff, known unrelated local changes were:
    - `tools/local/configure-local-build.sh`
    - this handoff note while it remains untracked
  - Keep using `$HOME/tolteca/bin/python` for Python tools.
  - Prefer local compile repair before handing a batch to Unity.

- Why this workflow is effective:
  - Local builds catch most header/template compile failures quickly.
  - Unity compile time is used only after a substantial local batch.
  - Deterministic point reductions provide a stable behavioral gate.
  - The batch size is large enough to make progress but small enough to isolate regressions.

- Resume guidance:
  - After reboot or usage reset, first run `git status -sb`.
  - If a new point `reduNN` has been downloaded, validate it before new refactor work.
  - If validation is clean, continue the same loop: 10-15 behavior-preserving commits, local `citlali_cli` build, repair compile errors, repeat.

## 2026-07-04 08:44 EDT - Mapdiag Refactor Stage

- Branch: `codex/structural-refactor`
- HEAD at this handoff update: `106d0f0e`
- Local branch status at note time: aligned with `origin/codex/structural-refactor` (`0` commits ahead).
- Current user-side state:
  - User is compiling/running this batch on Unity.
  - Wait for Unity compile and point-reduction result before treating this batch as fully validated.

- Current stage:
  - Working through the `write_mapdiag` / diagnostic-output cleanup.
  - Estimated completion for this stage: about 70%.
  - Estimated broader structural refactor completion: about 75%, excluding the planned config rework phase.

- Recent completed work:
  - Extracted observation contribution table bookkeeping into `include/citlali/core/pipeline/mapdiag_observation_weight.h`.
  - Extracted map-label assignment helpers into `include/citlali/core/pipeline/mapdiag_labels.h`.
  - Added `include/citlali/core/pipeline/mapdiag_edge_guard.h` and moved edge-guard diagnostic field copying there.
  - Added/used mapdiag stats helpers in `include/citlali/core/pipeline/mapdiag_stats.h` for mask counts, weighted sums, median-error lookup, noise-tail summaries, and noise-product scalar grouping.
  - Latest local build after the five-batch run succeeded:
    - `cmake --build build --target citlali_cli -j 8`

- Most recent commits at note time:
  - `106d0f0e reuse mapdiag noise product stats helper`
  - `1026a3c8 extract mapdiag noise product stats helper`
  - `b74763e6 define mapdiag noise product stats`
  - `cc060eb1 reuse mapdiag positive sqrt value helper`
  - `8d351d04 extract mapdiag positive sqrt value helper`

- Remaining work in this stage:
  - The large map-pixel outlier / reduction-learning block inside `write_mapdiag` is still mostly inline and should be handled carefully.
  - Peak/sig2noise and coverage-stat assignment can still be grouped.
  - Do an include hygiene and dead-helper pass after the `write_mapdiag` extraction settles.
  - Continue using Unity point reductions as the behavioral gate after sizeable local build-clean batches.

- Handoff note policy update:
  - The previous "do not touch handoff note unless explicitly asked" instruction is superseded.
  - Update handoff notes at practical refresh points: after large clean refactor runs, after validation results, before risky/high-complexity blocks, or whenever memory/context loss would make recovery costly.

## 2026-07-05 - Engine Implementation Split Batch

- Branch: `codex/structural-refactor`
- HEAD after this batch: `067ccc62`
- User directive for this stage:
  - Validate the latest downloaded point reduction.
  - Continue with large stages of behavior-preserving refactor development between local builds.
  - Complete at least 10 local build checkpoints.
  - Do not push.

- Latest point validation before refactor work:
  - Newest run checked: `redu92`
  - Compared against: `redu91`
  - Manifest generation:
    - `$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py --case point_refactor_redu91 --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu91 --manifest-out /tmp/citlali_point_refactor_redu91_manifest.json`
    - `$HOME/tolteca/bin/python tools/baseline/summarize_outputs.py --case point_refactor_redu92 --output-dir /Users/gwilson/work_toltec/local_data/2026-refactor/point/refactor/reduced/redu92 --manifest-out /tmp/citlali_point_refactor_redu92_manifest.json`
  - Both manifests had 29 files and 0 tool warnings.
  - Deterministic comparison:
    - `tools/baseline/compare_deterministic_manifests.sh /tmp/citlali_point_refactor_redu91_manifest.json /tmp/citlali_point_refactor_redu92_manifest.json`
  - Result: `manifests match`.

- Local build gate:
  - Ran `cmake --build build --target citlali_cli -j 8` after each stage.
  - Completed 10 local build checkpoints.
  - All 10 builds passed.

- Local commits made, not pushed:
  - `eab416b9 Split engine implementation details`
  - `a90434ea Split engine output implementations`
  - `b17c8bd5 Split engine config loaders`
  - `a7c25aa8 Split engine timestream config implementations`
  - `aac20beb Split engine learning implementations`
  - `3ec43322 Extract learning APT helpers`
  - `00c693ea Extract TOD output selection helpers`
  - `11980038 Split engine FITS map output implementations`
  - `332bca93 Split engine diagnostic output implementations`
  - `067ccc62 Split engine post-processing implementations`

- Main architectural result:
  - `include/citlali/core/engine/engine.h` is now about 598 lines.
  - The `Engine` declaration remains in `engine.h`.
  - Member-function implementations are now split into focused headers under:
    - `include/citlali/core/engine/detail/`
  - This was intentionally mostly mechanical so future semantic changes are easier to review and bisect.

- New or promoted pipeline helpers:
  - `include/citlali/core/pipeline/learning_apt_helpers.h`
    - Pure APT/UID lookup helpers used by learning apply/collect code.
  - `include/citlali/core/pipeline/tod_output_selection.h`
    - Pure TOD chunk-selection and scan-to-output-row helpers.

- Current structure after the split:
  - Config loader implementations are split by config family.
  - Timestream config implementations are split into RTC, PTC, and PTC line-audit helpers.
  - Learning implementations are split into config, targets, apply, collect, and summary.
  - Output implementations are split into observation maps, TOD files, summaries, FITS PHDU/image writing, spectral outputs, mapdiag, diagnostics, stats, and post-processing wrappers.

- Known local working tree state after this batch:
  - `tools/local/configure-local-build.sh` remains modified and unrelated to this refactor batch.
  - This handoff note should now be tracked/committed as a working-note artifact.

- Recommended next steps:
  1. User pushes these local commits and compiles on Unity.
  2. User runs/downloads a point reduction and Codex validates newest `reduNN` against the previous run.
  3. If Unity validation is clean, continue with behavior-preserving cleanup in the new smaller implementation files.
  4. The typed config mirror should be the next major architectural phase after the current structural split is stable under Unity validation. The split config loader files are the intended landing zone for that work.

- Current risk notes:
  - This batch is compile-clean locally, but not yet Unity-validated at this note time.
  - The changes are mostly include/file organization plus two pure helper extractions, so expected science-output risk is low.
  - Header-only template movement can still expose Unity-specific include-order issues, so Unity compile remains required before treating the batch as validated.
