# Timestream Successor VAL Native Admission 001

Status: admission candidate preparation; canonical owner disposition pending.

## Work order and authority

Work order: `TIMESTREAM-SUCCESSOR-VAL-NATIVE-ADMISSION-001`. Owner: Citlali
project owner. Risk tier: 2. This is bounded closeout preparation for the
owner-approved `TIMESTREAM-SUCCESSOR-VAL-NATIVE-TARGET-001` increment, after
the owner supplied its completed Unity gate. Preparation does not infer
owner acceptance of the source or permission to advance canonical.
This integration operation opens no additional application spine or probe.

Applicable AGENTS.md, the toltec-context skill and Unity evidence boundary,
all three effective governance documents, current status/ledger, original
work order and independent review were recovered. Governance accepted at
`06a3ade51c1b3f38887295433d913811bf25cd14` and incorporated through
`77507836325eff9f469062d5884481ea37599594` remains unchanged. Historical
candidate banners do not supersede that effective incorporation.

Scientific authority remains the exact WP-7.1 baseline and its canonical
router, frozen SCI-VAL Core, accepted prerequisite and D2 owner corrections.
The new canonical uniform-coefficient authority is preserved as described
below; this mechanical VAL increment does not implement or select it.
VAL owns immutable fact-target mechanics. Numerical producers own their
realizations and facts; named consumers retain use policy. No owner,
scientific decision, lifetime or interface changes during admission.

## Exact ancestry and moving-base reassessment

| Role | Exact commit |
| --- | --- |
| Verified live GitHub and local canonical; intended first parent | `36176ac14abe23a5c331ba40af2bb86d3e97a7f6` |
| Reviewed and Unity-tested implementation; intended second parent | `37e6fb6ce50acba644f04b6b6b546762bf07f384` |
| Literal implementation base and common ancestor | `080df2a1431487e8cabc255beb9ddc59c0721b59` |
| Earlier pushed D2 admission closure | `5244e04638db5aa92180feab52acd782229cfa32` |

The tested implementation tree is
`32dea6ffe86b5855ad75a21cd56e6817559fb9f9`.
Branch: `codex/timestream-successor-val-native-admission-001`.
Worktree: `/private/tmp/citlali-timestream-successor-val-native-admission-001`.
The isolated worktree began clean at the verified canonical first parent.
The original source branch stays clean and frozen at its tested SHA.

Trigger: canonical advanced independently while the owner-run job waited.
Observed evidence: live explicit-SSH `ls-remote` and clean local canonical
agree at `36176ac14...`; canonical's 136 changes since the common ancestor
are all under `doc/`. All 1600 tracked mode/blob identities outside `doc/`
and `handoff/` match the implementation base. The only intersection with
the source candidate's six changed paths is status and integration ledger.

Affected authority: canonical now incorporates the reviewed PTC uniform r0.4
scientific freeze, Registry/profile and separate MAP/JINC grants. The
canonical admission receipt and new SCI-VAL profile/source-binding successors
were read. They explicitly require an exact named request and binding;
they do not create numerical publication, a runtime evaluation, a D2 PSD
profile, an implicit policy alias or an operational route. Frozen predecessors
remain unchanged. Mechanical coordinate/realization targeting introduces no
policy evaluator and remains compatible with those ownership boundaries.

Disposition: continue bounded admission preparation. Preserve canonical first
and the exact tested source second in a two-parent merge; do not rebase,
cherry-pick, redesign or drop either history. Reconcile only shared history
records, preserving the complete text from both parents. Fresh independent
review must assess this new exact merge SHA against current authority.
Canonical admission remains an explicit owner decision after that review.

## Completed representative source gate

Owner-supplied Unity inspection reports job `64035873` COMPLETED, exit `0:0`,
in 9m34s, completed `2026-09-06T21:21:59Z`. It retained the original six CPUs
and 64 GiB memory request; the discussed smaller allocation was not used.
Batch MaxRSS is 23071116 KiB (approximately 22.00 GiB), an accounting result
for this run, not certification of a smaller future allocation.

At exact source `37e6fb6ce...` and tree `32dea6ff...`:

- All 899 runnable CTests passed; 900 registered. The sole disabled test is
  the unchanged `citlali::MapFitterLifecycle.ExactProductSequence`.
- All 50 focused successor tests passed, including 10 VAL and 8 D2 tests.
- CLI revision binding matched `37e6fb6ce`; source was clean and detached.
- All 21 archive checksum checks reported OK; Slurm stderr was zero bytes.
- The gate reports PASS with exit status zero. Its exact reviewed runner
  hash matches the owner receipt, and reconstruction of final-status.txt
  from the receipt matches its reported digest locally.

Archive:
`/work/toltec/citlali_dev/citlali_acceptance_evidence/TIMESTREAM-SUCCESSOR-VAL-NATIVE-TARGET-001/37e6fb6ce50acba644f04b6b6b546762bf07f384/unity-gcc13-sbatch-64035873`.

| Evidence | SHA-256 |
| --- | --- |
| gate-script.sbatch | `a0bfbefeea38e994053b3b3d34f9b1f5b44d2da50994141185364ecbc9c8d3f6` |
| final-status.txt | `4e08133ee2d5687f7a52d2bfdbfeb51acbb0c0f5c202046f68e56988e063a659` |
| Owner-held archive SHA256SUMS | `29162f995b42bf1f95bc0fee1ad1ba8e0c61c236c111e4dde3cd2e4cd2edbb55` |
| Independent implementation review | `a3f28d867fa180bbb3d0bb3e3b74ba5ea70a86c5beb123c830592a38c8e0a117` |
| Closed 56-file local implementation evidence SHA256SUMS | `8863b958767f3cbc0868b094fcf8227cdc0b1e27babd5cf04884a8b2000d0646` |

The reviewed runner verifies clean pinned dependencies before and after the
gate. The receipt confirms final clean identities:

- tula_cmake: `6433cdabe7010d0af2d0ba69da7af27510391b80`;
- tula: `aa16c853c6b596c04ccdc90dc3acc4ce2006d947`;
- kidscpp: `498ece1113001ae2d42d96d9fc29152aea3eaaef`.

The CLI reports GNU 13.3.0, C++23 Release, OpenMP and Wiener OpenMP,
Spack DAG `ryu6zhjvp5sh7fsbwrzyykbkgkglzkkx`, Kids 3.1.0. Runtime
deployment labels are profile=unmanaged, lock=unavailable, binding=unmanaged.
This establishes the source-build/test gate, not managed-deployment acceptance.
The full archive remains owner-held. No direct Unity access occurred and no
claim is made that its full contents were locally downloaded or inspected.

## Preservation and final-candidate gates

The [implementation review](TIMESTREAM_SUCCESSOR_VAL_NATIVE_TARGET_001_EXACT_SHA_REVIEW_2026-09-06.md)
is preserved verbatim. It passed all three axes with recorded limitations
and no findings at `37e6fb6ce...`. Its previously pending Unity requirement is
now satisfied by the separate owner receipt; the original report is not edited.
It is not a review of the new integration tree.

Application/test changes on canonical are exactly the three already-reviewed
VAL paths. The implementation handoff is preserved, with bounded new
admission handoff/review/status/ledger records. Required checks before review:

1. Exact parent order, common ancestor, clean state, eight-path first-parent
   inventory and complete source/build/config/test input equivalence to the
   Unity-tested commit for all 1600 paths outside doc/ and handoff/.
2. Exact preservation of all canonical science, registry, governance and
   other paths outside the stated eight-path admission delta; exact source
   candidate preservation outside the inherited canonical documentation and
   new admission records. Complete parent text retained in status and ledger.
3. Scientific layout and both validation ledgers; recheck unchanged SCI-VAL
   verifier on this candidate and canonical, retaining its known historical
   MAP-unbound failure without claiming later checks passed.
4. Fresh-context, read-only independent exact-SHA review of the final merge,
   separately disposing scientific/behavioral, architectural/ownership and
   repository/evidence conformance. Final SHA/tree, digests and verdict bind
   externally after commit, without writer self-approval.

Local implementation evidence already includes 899/899 runnable CTests,
50/50 focused tests, 207/207 baseline tests, full required config preflight,
isolated-header compilation and ledger/layout gates. Its disclosed Mac cached
dependency dirt is supplemental; Unity is the clean representative gate.
No new C++ or Unity run is required for admission only if complete executable
input equivalence is proven. Unity evidence stays bound to its actual source
SHA and is never relabeled as a run at the later merge.

Completion evidence is preserved separately under
`/private/tmp/citlali-val-native-target-completion-evidence-2026-09-06`.
Prior local evidence and submission receipts remain intact.

## Limits, stop conditions and next owner decision

No new scientific values, policy, producer, eligibility evaluator, mask,
serialization, D2 behavior, route or default is added. No filtering, factor
selection, downsampling, substantive RTC/PTC/AST, common-grid, MAP, CAL,
activation, production, cleanup or subsequent implementation is authorized.
The new canonical PTC documentation is preserved, not implemented here.

Stop and reassess if canonical moves, input equality fails, a science or
application conflict appears, or independent review finds a supported defect.
Do not weaken gates or reuse the implementation verdict for the new SHA.

Next owner decision after review: accept the exact admission candidate and
authorize a literal local canonical fast-forward from its verified first
parent. No canonical movement or GitHub push occurs during preparation.
The owner retains every GitHub push; any later command must include the
absolute repository path, environment-appropriate explicit GitHub URL and
complete refs/heads source-to-destination refspec.
