# Saved-product POINT peak-failure diagnosis

2026-09-13 · r0.1 · SCI-FRUIT development

## Program adherence and prior-work recovery

Follow the [charter](../../doc/scientific_contracts/README.md) and
[reviewed prior work](../../doc/scientific_contracts/packages/SCI-FRUIT/v0.1/method_preparation/ordinary_map/method_definition/r0.4/PRIOR_WORK.md).
Adopt the [registered nominal comparison](../fruit_point_nominal_utility_2026-09-13/PROTOCOL.md)
and preserve its [park-for-POINT disposition](../fruit_point_nominal_utility_2026-09-13/DISPOSITION.json).
This is a saved-evidence diagnosis, not scientific authorship or a gate change.
The owner explicitly requested separation of accuracy and availability, exact
withholding reasons, matched/crossed noise ratios, all-pass peak/width/background
traces, and exactly one proposed next experiment.

Use only the retained nominal-comparison receipts, maps, geometry and recorded
truth definitions. Read the existing evaluator code as evidence of the exact
predicate; do not invoke it or the fitter. No pre-PTC input reads, PTC calls,
feedback inference, coefficient reconstruction, optimizers or refits. Do not
load reserved observations. No new threshold or preferred iteration; pass 6
remains the sole registered terminal. Earlier outcomes remain immutable.

Examine all 714 saved array-pass readouts, including null, gross-coma, boundary
and discovery warnings. Report the primary H/D/T peak-pair population separately
from all compact H/shifted-H/D/T cases and challenge cases. Reconstruct the exact
withholding predicate from saved values: base limitations, unavailable fixed
inner-domain probe, peak-domain change >1%, or empirical peak score <=5/missing.
A shape warning alone is not a veto. Check reconstruction against every saved
availability label; do not use truth in that reconstruction.

At each retained pass, form all four ordered seed combinations for H/D, D/H and
T/H per arm/array. Equal seeds are matched; different seeds are crossed. Compare
H(seed2)/H(seed1) and D(seed2)/D(seed1) to quantify common seed response and
differential gain response. These recombinations are dependent diagnostics of
two reused noise realizations, not new trials or independent validation. Truth
normalizes errors, never availability. The 5% comparison is descriptive for the
new crossed ratios, not a retrospective change to registered tests.

Trace saved free-fit peak, centroid, major/minor width, Gaussian integral,
central fitted background plane, outer fitted background plane, inner-domain
fit, score, support, warnings and cumulative wall time through every pass. Keep
the two backgrounds distinct. Compute their values at the recorded fitted
centroid, without changing or fitting any plane. Include recorded source-fit
status and cost; numerical optimizer success is not astronomical accuracy.

As one bounded consistency check, compare saved T maps with their known scalar
multiple of H maps in the identical fixed domains at every pass, for both seeds
and arms. Compare saved fitted peak/width/background and fit-SSE scaling with
that map scaling. This uses array arithmetic and saved parameter readouts only;
it does not create a candidate, refit or change a reduction. It can distinguish
non-homogeneous readout behavior from changes already present in those maps.

Preserve and verify all existing packet/product manifests and opaque archive
statuses. Write a concise diagnosis, numerical tables and one all-pass trace
figure. Recommend exactly one targeted experiment to resolve the strongest
remaining distinction; do not execute it. C remains parked for POINT, the old
image-stability qualification stays failed, and no OOF/production claim follows.
