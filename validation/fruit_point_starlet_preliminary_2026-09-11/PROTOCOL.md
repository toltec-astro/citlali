# Preliminary starlet execution record r0.1

## Program adherence and prior-work recovery

The [approval](OWNER_APPROVAL.md) and its unchanged linked proposal govern this
isolated empirical screen under the [charter](../../doc/scientific_contracts/README.md).
Adopt the [ordinary-MAP prior-work record](../../doc/scientific_contracts/packages/SCI-FRUIT/v0.1/method_preparation/ordinary_map/method_definition/r0.4/PRIOR_WORK.md),
the coherent r0.2 common harness and registered 123424 pre-PTC input. Cite the
RBF r0.1 retained first maps and independent pupil truth; preserve those
products and both rejected candidates. Defer full feedback comparison and
129081. All material here is implementation-informed experiment evidence,
excluded from independent scientific authorship. No new contract is begun.

## Exact execution

`starlet.py` implements the approved four detail bands, coarse band, B-spline
cascade, complete-support masks, outer-only plane, two coverage strata and
five empirical scatter threshold. Q equal to its D median enters the lower
stratum; Q greater than the median enters the upper. MAD uses factor 1.4826.
Only null20260911 calibrates. Every band/stratum needs at least 64 eligible
pixels and finite positive scatter. Missing calibration makes that array's
estimator unavailable; no scale is dropped or merged.

The exact adjoint reverses the smoothing cascade. Solver coordinates divide
both image and coefficient scatter by the measured outer residual MAD, which
preserves the specified weighted least-squares objective. L-BFGS-B starts at
zero with exactly the approved limits/tolerances. Failed solves admit zero and
remain marked unavailable. Empty selected support is an available zero result.
The approved definition has no fallback for a nonpositive outer MAD. For
nonempty support in this condition, record an unavailable numerical solve;
do not substitute a noise level or silently change the method. This matters
for noiseless maps clipped to D. It is a definition limitation, not evidence
that starlets cannot represent the source.

The new truth is the independent frozen pupil PSF multiplied by exactly four,
with center (17,-11), identical sampling and seed 20260911. Save and hash truth
before construction of the parent. Rebuild the same deterministic nuisance
using retained detector scales, add projected truth to that immutable synthetic
parent, and call the frozen common rank-5 cleaning procedure once with model
zero. Retain all learned group states and map. A create-only start receipt
prevents an accidental second bootstrap. No cleaning function is used in any
map-level test.

Use the unchanged audit oracle: three signed realized processed-template
regions <=15, 15–35 and 35–60 arcsec, separate null20260912 spatial placements,
coverage-scaled residuals and 0.9 covariance + 0.1 diagonal. Check the source
score exceeds five empirical scales in every array; report response and paired
null scores separately. These are descriptive oracle diagnostics, never inputs
to the estimator. If this regime check fails, stop without increasing truth.

For each of nine retained first maps and the new bootstrap, report estimator
admission, coefficients, masks, model errors where truth exists, plane, solver
records and latency. Geometry and calibration are setup costs. For each map,
report all three individual inference times, their median and maximum, and
their serial sum. Apply the proposed 0.5 s median / 2 s maximum bounds to the
three array times per map; also display serial sums so operational overhead is
visible. Unavailable calls cannot satisfy the inference-cost gate.

Noiseless tests use compact, coma1, coma0.5 and coma4, at (17,-11) and (19,-10),
on all three coverage masks. Map-level additions use compact with both nulls,
mismatch, coma1, coma0.5 and coma4 with null20260911, all at the original phase.
Pure plane is 7+2x/90-3y/90, with the real S masks. Test the actual estimator
with calibrated thresholds active. Evaluate fitted compact amplitude, widths
and centroid using the frozen free Gaussian-plus-plane external ruler. This
fit never enters feedback. Comatic centroid is the brightness moment within
the fixed r<=60 aperture about each declared truth center. Integrated brightness
is summed on D; L2 uses that fixed aperture; wings use 15<r<=60, consistent
with the prior RBF evaluation. Report region sums, D leakage outside the fixed
aperture, full-model nonnegativity, and all unavailable evaluations.

Every quantitative gate remains exactly as approved in the linked proposal.
Record phase sensitivity of relative brightness and L2 errors when available.
All gates must pass before proposing any full comparison; failure closes this
candidate, with no automatic revision. Retain diagnostic results after a
failure when the already approved map tests can clarify its scope. At most
30 execution minutes, one process, four numerical threads, 8 GiB peak RSS,
2 GiB outputs. Preserve initial failure records. No output overwrites.

129081 has historical July characterization, disclosed in the prior exposure
audit; it remains reserved for new feedback comparisons. This screen reads no
new 129081 scientific input. OG remains the identified operational benchmark
in the coherent packet; this preliminary estimator screen cannot claim a full
FRUIT latency or recovery improvement against OG.
