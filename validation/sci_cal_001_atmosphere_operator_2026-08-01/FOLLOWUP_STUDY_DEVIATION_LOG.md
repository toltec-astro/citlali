# SCI-CAL-001 follow-up study deviation and clarification log

## Record identity

- Date: 2026-08-01
- Scope: copied-AM follow-up Study C and post-discovery diagnostic C1
- Disposition: clarification only; no operator or operational domain is authorized

The frozen `FOLLOWUP_STUDY_PREREGISTRATION.md` and
`FOLLOWUP_STUDY_PROTOCOL_ADDENDUM.md` remain unchanged. This record resolves a
version-label ambiguity without rewriting either post hoc.

## Annual-anchor Study C

The preregistered `piecewise_linear_los_tau_v1` and `pchip_los_tau_v1`
identities belong to Study C's proposed annual-profile anchor training. The
addendum stopped that study before operator selection after the copied AM 12.2
suite was shown not to be the registered generic-q lineage. No annual-anchor
`v1` surface was selected, evaluated as a successor, or assigned an
operational domain.

## Legacy-anchor diagnostic C1

Diagnostic C1 says to evaluate the already defined legacy-anchor surfaces.
Those existing surfaces are `piecewise_linear_los_tau_v0` and
`pchip_los_tau_v0`, as frozen in the original operator-analysis artifacts.
Accordingly, every C1 row, metric, report statement, and manifest identity is
and remains `v0`.

This clarification does not relabel a `v1` result as `v0`, change either C1
candidate definition, reinterpret the C1 numerical results, or revive the
stopped annual-anchor study. C1 continues to use the repair-base generic-q
fitted anchors and the copied AM 12.2 profiles only as a post-discovery,
non-blinded stress diagnostic. Any deterministic regeneration needed to apply
the exact repair-base evaluation order or strengthen provenance does not
constitute a candidate-version or scientific-model change.

## Unchanged stop boundary

The copied suite remains a distinct AM 12.2 calculation family, not the
historical generic-q identity. This clarification supplies no missing q95
bytes, intermediate physical atmosphere runs, observational-performance
evidence, aligned-elevation eligibility decision, operator authorization, or
operational-domain approval.
