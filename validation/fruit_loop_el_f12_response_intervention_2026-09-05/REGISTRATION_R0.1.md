# EL-F12 prospective execution registration r0.1

Date: 2026-09-05. Status: frozen before the first observation trajectory.

The owner-approved Choice A and CAP-001 screen is registered in
`REGISTRATION_R0.1.json` (SHA-256 `fe9ddf306cbe6445fe74913636edd8b407aa5db9180ee01edd716893fdf6e8dd`). Its 6273 artifact
identities cover the exact inputs, native sources, analyzer/controller,
build configuration, resolved dependency sources, linked libraries,
source grids and fixed evaluation masks. Native source is
`10478fa0e1abb3a53ac74cda67c615de5ed7609c`; the controller's final pre-execution repair is
`80e608eb860589bfebffa91c62502a012da7f2b6`. No native source differs between them.
The rebuilt executable reports native commit 10478fa0e and has SHA-256
`1a47fb8fbf958c2df0d2ce49f27d0e3e20db899447d021c4c291d05f96aa0469`.
System shared-cache libraries are bound by the macOS build and linker
identities; accessible dependency files are hashed individually.

All eight primary commands and four conditional restart commands are fixed.
Every start rechecks registered identities and reserves its full iteration
allowance. Both H0/H pairs must pass before alternatives start. A failed
identity, required output, log, resource or scientific compatibility gate
stops dependent work. There is no automatic retry. The standing single
bounded diagnostic-replacement allowance remains reserved; its use would
need a retained defect record and a consistent same-binary matrix within the
original ceilings.

H0 reproduces all four retained alpha-one map planes at each iteration.
H adds only causal audit state and its arm entry to the checkpoint policy;
all pre-existing checkpoint state, including D19, remains exactly compared.
Learning CSV comparisons preserve exact ordered raw fields at the same
absolute iteration for both supported iteration-column names. Map diagnostics
remain exact. No metric, region or numerical tolerance has been relaxed.

The analyzer uses the existing Gaussian fit, processed-kernel normalization,
and fixed source/Neptune/annular coordinates. For uninjected post-source
residuals it retains the same fitted Gaussian component, leaving the fitted
constant background in the residual. Its all-array support and metric
protections remain the approved design. High response does not label an
exclusion useful or harmful. False-action rates remain unavailable.

Successful temporary occurrence spools are retained losslessly as gzip files:
the compressed copy is decompressed and checked against the original byte
count and SHA-256 before only that temporary uncompressed copy is removed.
Failed attempts are retained untouched. Compression and verification cost
counts against the registered time/output limits. All ordinary maps,
kernels, weights, checkpoints, decision ledgers and unsuccessful products
remain preserved. The 12-hour execution clock starts with the first primary
trajectory and includes the matrix, retention, gates and analysis.

No trajectory has begun at registration. Independent-pointing replication,
policy recommendation, full Gate D, qualification, Stage B, production and
Unity remain outside authorization.
