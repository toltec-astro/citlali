# EL-F12 pre-execution gates r0.1

Date: 2026-09-05. Status: local prototype gates pass; no observation run yet.

Choice A and CAP-001 authorize this default-disabled prototype. Its sole new
configuration field is `timestream.learning.fruit_response_arm`, with disabled,
H, Half and Hold values. The reduction owns decision state; the observation
owns the exact final-PTC occurrence ledger. No RTC/PTC replay or target UID
list supplies the selector. Ordinary cap evaluation precedes suppression at
both stages; other reasons retain precedence. Final coefficients use the
approved positive multiplier without stacking, including quadratic, kernel
and noise propagation. Complete census, assignment, application and horizon
state survive checkpoints. H0 keeps the historical learning-policy snapshot.

The native tests exercise signed and clipped JINC kernels, subpixel phases,
non-integer weights, actual pre-RTC/pre-PTC disagreement, independent reasons,
all new keys, duplicate proposals, joint deletion, support loss, unavailable
state, population limits, and active-assignment NetCDF restart through the
fixed horizon. A useful high-influence synthetic exclusion demonstrates why
response alone is not a benefit label.

The CLI builds. All 652 enabled CTests pass; the pre-existing disabled
`MapFitterLifecycle.ExactProductSequence` remains disabled. All 270 baseline
and FRUIT Python tests pass. The full configuration preflight with
`--require-all` and focused Ruff checks pass. The exact original 30-field
learning inventory remains frozen; its audit checks the one explicitly
approved experimental addition separately. No scientific gate was relaxed.

Initial builds caught an outdated test fixture and a missing standalone-header
include; both were repaired. The first configuration preflight caught the
missing experimental field in the boundary audit. A combined make invocation
reported a jobserver error after CTest passed; the separate CLI build passed.
These were pre-execution implementation defects, with no observation replay.
The retained logs include these failures and the successful successor gates.

All 26 original proposal members and 27 external inputs reverify. The 14 CAP-001
members reverify at their reviewed predecessor commit 7327e642e. Authorized
source changes now have successor implementation identities; no reviewed
scientific proposal was edited. Both untracked archives retain their hashes.

Next, freeze source, binary, dependencies, analyzer, inputs, configurations,
grids and commands before running the approved matrix. The first H0 run must
reproduce the retained EL-F2 alpha-one maps bitwise at every iteration. Both
H audit controls must reproduce H0 before alternatives run. Any advancing
candidate still needs its two restart checks and a separately authorized
independent-pointing replication before a policy recommendation. No Unity,
full Gate D, qualification, Stage B or production result is claimed.
