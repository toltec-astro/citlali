# RTC performance and storage repair — 2026-09-18

Owner authorization: “Let's do this RTC-wide,” including the attached
discussion of array indexing, shared axes, allocation lifetimes, and compact
regular relationships. This is one bounded performance repair, not a scientific
algorithm or processing-policy change.

## Preflight

- Governance read: `governance/ENGINEERING_GOVERNANCE.md`,
  `governance/TIMESTREAM_SUCCESSOR_GOVERNANCE.md`, and
  `governance/REVIEW_AND_CONFORMANCE.md`; effectiveness is recorded in the
  integration ledger at accepted `06a3ade51c1b3f38887295433d913811bf25cd14`.
- Exact canonical base: `fe448860741c543b731faaa76c0eea5194e7599f`, verified
  against live GitHub `refs/heads/codex/refactor-mainline` before edits.
- One owned spine branch: `codex/timestream-successor-rtc-performance-001`.
  Reuse the clean completed worktree
  `/private/tmp/citlali-timestream-successor-cal-001`; preserve its previous
  partial-completion branch. Initial staged, unstaged, and untracked state:
  empty. Other worktrees and retained branches are outside scope.
- Tier 2: hot paths, compact representations, cross-stage consumers, and
  diagnostic publication. Fresh-context independent exact-SHA review required.
- Scientific authority remains the accepted WP-7.1 contract baseline and
  subsequent RTC/CAL/PTC owner bindings already recorded on this ancestry,
  including single-opacity CAL and scoped RTC/PTC completion. Architecture:
  `ARCHITECTURE.md`, accepted successor implementation baseline sections on
  compact axes, storage, views and hot paths, and current status.

## Bounded implementation and evidence

Measure the same 152390/0/2 network12, 414 requested detectors (363 with
output), using the unchanged admitted input and frozen filters/masks. Trace
RTC ingress, Learn/Consider/Apply, relearning, terminal publication and its
CAL/PTC consumers. Record actual array element sizes, allocation multiplicity,
logical payload bytes and lifetimes; distinguish these from allocator/RSS and
diagnostic serialization. Preserve the accepted 618.69-second run and outputs.

Correct demonstrated redundant detector searches, repeated identity/support
reconstruction, duplicated shared-axis calculations and avoidable numerical
copies or diagnostic expansion. Validate exact product/axis/VAL bindings at
typed boundaries, then use indices inside those bound products. Keep regular
filter/decimation relations once and exceptions at their actual occurrences.
No generic cache/framework, new scientific policy, reduced evidence, changed
filter arithmetic, thresholds, donor semantics or PTC method/rank is authorized.
Original-input replay, gaps, physical scans, timing, exclusions and all failure
semantics remain binding. MAP and FRUIT remain deferred.

Owners remain RTC for processing/support, ALIGN/AST for timing and pointing,
VAL for exact immutable facts, CAL for multipliers, and application adapters
for serialization. Expected areas: their existing concrete accessors and
consumers, RTC CLI adapter, focused tests and performance evidence tools.
Any changed persistent representation requires an explicit compatibility
disposition and exact semantic comparison; no silent loss of evidence.

Gates: focused boundary/mapping tests (reordered/subset detectors, nonzero
native starts, gaps, decimation and foreign parents), existing scientific
regressions, ordinary default CLI, full CTest/config/Python gates, and matched
full-network output comparison. Record wall time and peak RSS with actual
AppleClang/Homebrew Release environment. Earlier Unity passes remain bound
only to their tested revisions; no Unity connection or qualification is implied.

Stop/reassess on numerical/support changes, missing authority, unexpected
failure or memory growth, another implementation branch, or a need to change
scientific policy. Implementation and local commits are authorized. Canonical
promotion, owner push, and cleanup remain separate exact operations; no push
or deletion is performed by the agent.

Evidence directory: `/private/tmp/citlali-rtc-performance-001-20260918`.
The [completion handoff](../handoff/TIMESTREAM_SUCCESSOR_RTC_PERFORMANCE_001_2026-09-18.md)
records the exact implementation, independent review, same-data comparison,
timing and storage audit. Durable evidence preserves both the original strict
YAML comparator result and the explicit verification of its two changed
derived CAL references. No scientific difference remains unexplained.
