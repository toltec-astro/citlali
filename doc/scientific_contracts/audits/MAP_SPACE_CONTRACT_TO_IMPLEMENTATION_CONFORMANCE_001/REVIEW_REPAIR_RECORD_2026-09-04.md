# MAP-space independent-review repair record

Date: `2026-09-04`

Status: **bounded documentation successor; independent exact-commit review and
scientific-owner disposition required**

## Program adherence and prior-work recovery

This repair follows the [Scientific Contract Library Program](../../README.md),
its recovery-first and anti-repetition rules, and the accepted engineering and
review/conformance governance recorded in `doc/INTEGRATION_LEDGER.md` at
`06a3ade51c1b3f38887295433d913811bf25cd14`. It is a Tier 2 documentation
repair, not a new scientific-contract package, Stage B author task, or
application implementation unit. The charter's author-channel rule is unchanged.

Recovery dispositions are **adopt** the unchanged frozen scientific oracle and
stable product/edge/trace identifiers; **cite** the original source study and
first documentation successor as exact historical evidence; **supersede** only
the three overstated coordinate classifications and exclusive OOF wording;
**exclude** the optional unmanifested test citation from active evidence; and
**defer** application repairs, numerical validation and unresolved owner choices.
No new science is derived and no implementation evidence becomes scientific
authority.

## Owner authorization and exact scope

The independent reviewer returned `REPAIR REQUIRED` for exact candidate
`402b82bc7c38d8a3739d7803f46ccf3f1bbd90f8`, tree
`163b8136066cf56d320cfb24488350118540510f`. The manager recommended one
bounded documentation successor addressing the two major findings and the
minor citation omission, followed by another exact-commit review. The owner
accepted that recommendation on `2026-09-04`. This authorizes the repairs
below, not acceptance of their eventual result or application/integration work.

The independent review record has SHA-256
`5338e9cd8696831fe2da9856789354a265a40104d23eea6066fef6837cab2cb0`;
the findings, source evidence and disposition are recovered below so this
repair does not depend on a temporary report path or conversation memory.

| Identity | Exact value |
| --- | --- |
| Repair base / reviewed predecessor | `402b82bc7c38d8a3739d7803f46ccf3f1bbd90f8` |
| Repair-base tree | `163b8136066cf56d320cfb24488350118540510f` |
| Original candidate, preserved | `93c2b4591bb5d0cf8efe4491975c31e5f8fb5903` |
| Inspected source base, unchanged | `9f42d348298d76c5d5145aaf0c3eace1f3e154c1` |
| Source-base tree | `e51f22760c64454ce7233c45dd740aa710777bae` |
| Canonical ref observed at preflight | `ae953ed4d87d1f693d2bbf42aebbc25ef730c771` |
| Dedicated repair branch | `codex/map-space-conformance-001-review-repair-2026-09-04` |
| Dedicated worktree | `/private/tmp/citlali-map-space-conformance-001-review-repair-2026-09-04` |
| Initial staged / unstaged / untracked state | clean / clean / none |
| Application WIP | no application implementation slot consumed |

The exact changed-path set is this record plus `FINAL_REPORT.md`,
`PRODUCT_AND_BOUNDARY_IMPLEMENTATION_TRACEABILITY.md`,
`ROUTE_AVAILABILITY_CLASSIFICATION.md`, `OOF_ATTACHMENT_ENVELOPE.md` and
`verify_packet.py`, all within this packet. The previous twelve-file packet
and its two candidate commits remain recoverable; this successor adds one
repair record and changes only the five named existing files.

## Finding dispositions

### Coordinate classifications — major

The predecessor classified MSP-P002, MSP-E002 and MSP-E006 as
`IMPLEMENTED_CONFORMANT_AT_SOURCE_LEVEL`. The reviewer found that this
exceeds the demonstrated boundary property.

At the exact inspected source, `timestream_native_science_projection.h:314-326`
checks the committed PTC operation and `439-494` joins values and pointing
through native identity during construction. That useful behavior remains
positive evidence. However, the later `require_compatible_mapmaking_input`
guard at `211-279` compares payload, flags, dimensions and selected detector
inventory; it reads neither incoming occurrence/application generation nor
exact target-WCS identity. Stored operation/scope state is not consulted by
that consumer guard. The MAP and JINC consumers then use the stored pointing
at `naive_mm.h:376-390` and `jinc_mm.h:370-375,607-617`.

An incoming object with a different occurrence/generation but identical tested
payload/inventory therefore cannot be distinguished by that guard. This is a
source-derived counterexample, not an executed numerical test or a claim that
an observed reduction supplied such an object.

SCI-MAP-REQ-003/005, the frozen PTC-to-MAP coordinate join, and
SCI-JINC-REQ-002 require the exact same-processed-sample ancestry and applicable
AST coordinate/WCS binding. Numerical equality does not establish those joins.
The existing executable analogue lacks that demonstrated frozen boundary, so
the three rows now use `IMPLEMENTED_LEGACY_SEMANTICS` under the unchanged
work-order vocabulary. No other product or edge classification changes.

Product counts become 0 conformant and 8 legacy; all other product counts
remain unchanged. Edge counts become 0 conformant and 6 legacy; all other
edge counts remain unchanged and total 32. The zero-complete-route conclusion
is unchanged. This is a documentation finding disposition, not application
repair, new science, or evidence that an operational reduction is unusable.

### OOF envelope exclusivity — major

The predecessor's `OOF_ATTACHMENT_ENVELOPE.md:12-15` restricted future OOF
consideration to an exhaustive parent list despite excluding OOF from the
graph and admitting no OOF authority that establishes that restriction.
The list is now explicitly non-exhaustive. It neither admits the listed
products nor excludes other proposals, including JINC. Parent selection and
exact roles remain with separately reviewed OOF scientific authority. No new
graph ID, parent admission, numerical route or OOF launch follows.

### Optional unmanifested test citation — minor

MSP-P009 cited `tests/test_session_failure_boundaries.cpp:280-342` with
grade `A/C`, although that file is absent from CTI-S001--CTI-S048. The
reviewer found the admitted implementation evidence independently sufficient
for the existing contradictory classification; the omission was not
independently acceptance-blocking. The smallest repair removes that optional
corroborating citation and changes the evidence grade to `A`. The source
manifest and all its identifiers/digests remain unchanged. No test is
reinterpreted as executed validation.

## Verification and preservation boundary

Required gates are the updated durable verifier, exact-base Git diff checks,
and focused in-memory negative checks for summary drift, out-of-scope row
changes and restoration of the removed citation. The verifier binds the two
preserved predecessor commits/branches, exact source base, thirteen-file
artifact set, six changed paths, unchanged admitted-source digests, named
classification changes, stable IDs, untouched remaining rows and derived
summary counts. It performs no scientific interpretation.

Pre-commit verification on `2026-09-04` passed: all 48 admitted-source
digests, 17 products, 32 edges and 16 traces matched. Eleven in-memory
corruption probes were rejected, covering incorrect summary counts/totals,
restored coordinate conformance, unrelated row changes, restoration of the
optional citation/grade, an unauthorized MSP-P009 state change and exclusive
OOF wording. These probes did not modify the real packet. Staged and clean
post-commit verification remain required before independent review.

Application source, configuration, tests, frozen package/shared conventions,
Registry, validation records, manager status/index/ledger, FRUIT and ALIGN
artifacts, original failure findings, trace plan and backlog are unchanged.
The corrected FRUIT envelope and inherited/closed CTI-OD-007 are preserved.
No build, CTest campaign, reduction, numerical trace, performance measurement,
Unity access, network operation or dependency installation is triggered by
this documentation-only scope. No merge, push, canonical movement, branch
deletion, history rewrite or cleanup is authorized.

## Review and owner stop

The new exact commit requires an independent read-only review on the
scientific/behavioral, architecture/ownership and repository/evidence axes.
The predecessor review does not approve the successor. CTI-OD-001--CTI-OD-006
remain open; CTI-OD-007 remains inherited/closed. Any additional finding that
requires wider changes must be dispositioned separately. No implementation
conformity, application validation, achieved performance, readiness,
production authorization or Unity reproducibility is claimed.
