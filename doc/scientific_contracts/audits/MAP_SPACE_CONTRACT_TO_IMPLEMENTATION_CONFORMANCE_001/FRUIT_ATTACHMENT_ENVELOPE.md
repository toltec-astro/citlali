# FRUIT Attachment Envelope

Status: **one non-exhaustive deferred envelope; `NOT_APPLICABLE` to this study**

This file preserves MSP-PX01 and MSP-E030 without opening a FRUIT source or
scientific audit.  The original study excluded FRUIT and cannot decide its
eventual parent admission.  The documentation successor cites only the dated
Stage A scope below to remove an unintended exclusive-input policy; FRUIT
implementation, untracked archives, and empirical evidence remain outside the
study and are unchanged.

## Deferred MATCHED attachment envelope

MSP-E030 retains its original MSP-P009-to-MSP-PX01 endpoints and
`NOT_APPLICABLE` classification.  It represents one possible future
MATCHED-to-FRUIT attachment review, not an exhaustive list of FRUIT inputs.
For this envelope, a separately authorized review would need an exact
immutable MSP-P009 product and the applicable frozen MATCHED-to-FRUIT boundary,
including parent, template, selected MATCHED method, response/covariance and
support states, lifecycle generation, units, frame, missingness, and exact
FRUIT-side consumption authority.  Those bindings alone do not admit a route.

## Independent FRUIT parent-family scope

The active FRUIT Stage A record at exact snapshot
`d39d4685baa0aeda036305031491121ce97008ec` identifies ordinary MAP, JINC,
FLT-FIXED, and FLT-MATCHED as separately reviewable candidate parent families.
The scoped recovery sources under `doc/scientific_contracts/packages/SCI-FRUIT/v0.1/`
at that commit are `CANDIDATE_PARENT_ADMISSION_MATRIX.md`,
`SCOPE_BRIEF.md` section 3, and
`SCIENTIFIC_OWNER_STAGE_A_R0.8_ACCEPTANCE_AND_STAGE_B_REQUEST_2026-09-01.md`.
The last record expressly leaves parent-route selection undecided.  These
sources are cited only for candidate-family scope; their launch-era provisional
MATCHED wording is not a new finding about the independently frozen MATCHED
package and is not reconciled by this study.

Candidate families are distinct and individually gated.  This packet neither
admits nor excludes any of them from eventual FRUIT use, adds graph edges,
nor equates their observation/coadd groupings, estimands, response, covariance,
or support.  Any eventual admission belongs to separately reviewed FRUIT
authority and exact upstream product/route bindings.

## Required rejection cases for the MATCHED envelope

A future MSP-E030 review must reject a legacy Wiener map presented as
MSP-P009, a FIXED product substituted for MATCHED, missing/changed template
identity, inferred zero response/covariance, method fallback, or
detector/catalog output presented as matched amplitude.  Rejecting a
substitution within this envelope does not exclude an independently reviewed
ordinary MAP, JINC, or FLT-FIXED parent family.  This envelope authorizes no
FRUIT algorithm, validation, application, or production work.

## Present disposition

No route-realization, implementation-conformance, validation, performance,
readiness, production, activation, or Unity claim is made.  The only current
classification is MSP-E030 = `NOT_APPLICABLE`.  FRUIT continues independently;
its eventual attachment needs its own owner review.
