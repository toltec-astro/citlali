# Repair sequence and predecessor compatibility: proposed owner disposition

Status: concrete proposal for CTI-OD-003/004 and the next CTI-OD-006 mapping
unit; **not an application work order or an implemented change**.

## Recovered authority and current application checkpoint

The accepted map-space source study supplies the
[repair backlog](../../audits/MAP_SPACE_CONTRACT_TO_IMPLEMENTATION_CONFORMANCE_001/PRIORITIZED_REPAIR_BACKLOG.md)
and [owner decision queue](../../audits/MAP_SPACE_CONTRACT_TO_IMPLEMENTATION_CONFORMANCE_001/OWNER_DECISION_LEDGER.md).
Its original inspected application base remains historical. Read-only
recovery for this proposal used canonical
`080df2a1431487e8cabc255beb9ddc59c0721b59` on 2026-09-06.
The uniform contract supplies coefficient authority; it does not repair the
existing NOI-to-live-coefficient path or realize a conformant MAP parent.

## CTI-OD-003: recommended sequence

Approve the accepted prerequisite order as separate bounded units:

1. Stop empirical NOI results from changing live PTC/MAP coefficients or
   their dependent support, exposure and validity; preserve diagnostics.
2. Establish the immutable coefficient handoff and its exact PTC/VAL
   identities under the newly admitted uniform science, after recovering
   the actual PTC publication and VAL prerequisites.
3. Implement the stable-original coordinate/exposure ledger and placement.
4. Implement the frozen ordinary MAP observation bundle
   (`CTI-RU-MAP-OBS-001`), including separate admission, support and typed
   companion states. Resolve any remaining exact numerical `coverage_cut`
   gate before affected implementation; do not infer a value.
5. Implement equal-observation coaddition, then the five-role JINC bundle.
6. Continue the accepted separate fixed-filter, matched-template/filter,
   NOI-identity and POINT/VAL sequence when each unit's authority is ready.

This makes the backlog's initial coefficient/nonmutation prerequisite
explicit without combining its distinct authority and implementation tasks.
The VAL/D2 prerequisite lane remains separately owned; its existence is not
permission to add eligibility evaluation or modify that lane here.

## CTI-OD-004: recommended compatibility decision

Retain already published predecessor products and exact generating commits
for read-only inspection and reproducibility. Label their scientific status
explicitly as predecessor/legacy in new catalogs and documentation; retain
their original stored identifiers, filenames and headers as immutable
historical facts. Do not mass-rename archives or relabel them as frozen
SCI-MAP, SCI-JINC, SCI-FLT, SCI-NOI or SCI-POINT products.

Use a milestone-based lifetime: retain each predecessor reader until the
corresponding frozen-product replacement has passed owner-accepted
representative validation and the owner has approved a separate retirement
migration. No automatic expiry date or silent deletion. New conformant
generations receive their own exact product/schema identities. One-way
adapters, if needed, require a later bounded mapping and cannot supply absent
scientific facts or turn legacy payloads into conformant products by naming.

For the first nonmutation unit specifically, preserve empirical diagnostics
under their explicit non-authoritative meanings. Reject an explicit request
to promote empirical NOI into a live coefficient before mutation; do not
silently ignore that request. Historical mutating behavior remains recoverable
from its old exact application generation, not a new compatibility switch in
the corrected path. This is the consequential compatibility choice requiring
the owner's disposition before implementation.

## CTI-OD-006: proposed first bounded mapping unit

Proposed identity: **CTI-RU-NOI-COEFFICIENT-NONMUTATION-001**.
Purpose: map and then repair only the empirical-NOI promotion path identified
as MSP-E031 / CTI-FM-005, with exact source/config/output dependencies and
independent review before any implementation commit is accepted.

Read-only source recovery found:

- `include/citlali/core/pipeline/noise_weight_policy.h`: filtered processing
  enables promotion through either empirical weights or `normalize_error`;
  unfiltered processing uses the empirical-weight policy.
- `src/citlali/core/mapmaking/map.cpp`: `calc_noise_products` derives a
  diagnostic scale, assigns `weight_empirical`, then under the promotion flag
  writes the live `weight` plane and refreshes dependent science products.
- `tests/test_science_map_fits_products.cpp` and
  `tests/test_config_scaffold.cpp`: existing fixtures explicitly expect the
  predecessor promotion and both filtered/unfiltered policy paths.

The first mapping must enumerate every caller and configuration route and
identify all mutated parent fields before edits. The bounded candidate may
change that policy/call chain, its request validation, directly affected
diagnostic metadata, focused fixtures and user-facing migration guidance.
It must preserve the empirical diagnostic calculations where independently
valid; remove live-parent promotion; reject affected explicit requests before
any partial mutation; and leave signal, coefficient, support, exposure,
validity and lifecycle identities unchanged by diagnostic calculation.

Required focused evidence: filtered and unfiltered paths, observation and
coadd, both promotion triggers, positive diagnostic-only cases, invalid or
unavailable diagnostics, repeated invocation, and negative mutation snapshots
covering all affected parent state. Existing tests that encode the forbidden
promotion need explicit replacement expectations, not deletion to make a
gate pass. Required broader gates are the repository's build/version check,
relevant CTests and complete config preflight. Local fallback results remain
supplemental; any required Spack/Unity evidence is a later human-run gate.

No uniform producer, new VAL evaluator, mapmaking arithmetic, coadd rule,
JINC/filter mathematics, NOI design law, POINT policy, dependency/build
machinery, route activation, production or archive cleanup is included.
Exactly one implementation slot may be opened after owner disposition and
rechecking the current canonical/parallel-lane state. The mapping work order
must bind that then-current exact canonical SHA, owned branch/worktree,
complete changed-path scope and representative-environment requirements.
Approval of this proposal must not be treated as approval of an unseen
implementation or its eventual canonical integration.

## Decision requested after freeze/admission integration

Adopt CTI-OD-003's sequence and CTI-OD-004's compatibility policy above, and
authorize preparation of the exact first nonmutation mapping/work order under
CTI-OD-006. No scientific substance needs another approval round. Until this
specific disposition arrives, the application tree remains unchanged.
