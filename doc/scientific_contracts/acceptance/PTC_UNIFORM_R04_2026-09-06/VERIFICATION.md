# Freeze/admission verification

Environment: 2026-09-06, macOS, local `/Users/gwilson/tolteca/bin/python -B`.
Documentation-only integrity checks; no application or scientific runtime
evaluator was built or executed.

## Focused results

`verify_admission.py` passes for the prepared working tree. It verifies all
48 approved original artifacts against exact scientific commit
`b38ce251207e4f445d5b421f42f774ae3e4f0477`; 24 promoted byte-identical copies;
the post-owner consistency report and approval records; 347 prior study,
PTC/MAP/JINC/VAL files against exact pre-operation commit
`3cb01ef3672435c0cf9e3fec505abaa33c2f37a0`; the seven new Registry/register and
boundary files; paired consumer boundary equality; exact inherited Registry
bindings; governing digests and ancestry; approved scientific profile/family
fields; and the complete changed-path boundary.

The common source is SHA-256
`8bc42deb69e23c757883834f7cdb82b94e89210015333f483d15c32377a56685`,
using the wrapper and six modules in the explicit manifest order. The
science and engineering PDF bytes retain their approved digests and 5/14-page
rendering. This operation copies them without re-export or rebuild; the
existing all-page manager QA and independent post-owner consistency evidence
remain bound to those exact bytes.

Four read-only in-memory probes reject changed approved PDF bytes, changed
consumer-permission record bytes, a changed frozen predecessor boundary and
an unexpected application-source path. They change no repository file and
do not implement or simulate runtime VAL semantics.

New navigation and direct promoted Scope Brief/PRIOR_WORK links resolve.
Byte-preserved reference excerpts retain quoted links in their original
source context; their source covers and recovered manifests bind the exact
original objects. They are historical quoted material, not newly authored
relative navigation. All approved copies retain their original Markdown hard
breaks. Strict whitespace checking applies to newly authored paths; copied
historical artifacts are checked by exact digest rather than normalized.

## Broader checks and retained limits

Scientific-contract layout passes with the uniform package added to the
complete-package list. The historical classifications of other packages are
unchanged by this one-entry layout update.

The unchanged SCI-VAL r0.3 verifier returns exactly
`FAIL: MAP reserved profile must remain explicitly unbound` both here and
at clean canonical `080df2a1431487e8cabc255beb9ddc59c0721b59`. This is the
pre-existing obsolete assertion already recorded by the separate VAL/D2
prerequisite lane. It has not been repaired, weakened or counted as a pass.
The new bounded admission checker validates this successor independently;
it does not replace a complete future VAL verification work order.

Application/config/build/test trees are unchanged. Build, CTest, config,
reduction, numerical/performance, Spack and Unity gates are not triggered;
no skipped dataset is represented as passed. Existing application validation
and production authority keep their own source/environment identities.

## Exact-candidate and integration gates

After commit, rerun `verify_admission.py --subject <exact-sha>` and layout,
verify the clean tree and acquire fresh independent exact-SHA dispositions
for scientific/behavioral conformance, architecture/ownership, and
repository/evidence hygiene. An external report binds the exact subject and
verdict. The subsequent integration must independently pass
`verify_admission.py --subject <exact-merge-sha> --integration`, preserving
canonical first, reviewed topic second, all topic science bytes and every
canonical application input. Owner-authorized canonical fast-forward follows
that exact review; the owner performs GitHub publication.
