% Canonical SCI-MAP v0.2 predictions. All 25 predecessor identities are
% preserved; successor distinctions are incorporated under their subjects.
\newcommand{\SCIMapPredictions}{%
\SCIMapPrediction{SCI-MAP-PRED-001}{Uniform constant}{For fixed membership, a constant admitted input with uniform positive coefficients produces that constant on every support-authorized MAP-valid output row; numerator and normalization scale together, and no unsupported scientific row is created.}{Algebraic contract}\par
\SCIMapPrediction{SCI-MAP-PRED-002}{Unequally weighted constant}{For fixed membership, unequal positive coefficients still preserve the same constant on every support-authorized output row, while normalization and conditional variance generally change with position; rows outside that set have no normalized scientific value.}{Algebraic contract}\par
\SCIMapPrediction{SCI-MAP-PRED-003}{One-pixel hand calculation}{For an authorized row with admitted \(z_i\) and \(a_i>0\), the exact triplet is \((N,Q,\widehat m)=(\sum a_i z_i,\sum a_i,\sum a_i z_i/\sum a_i)\); an unauthorized row has no normalized scientific value.}{Algebraic contract}\par
\SCIMapPrediction{SCI-MAP-PRED-004}{One-hot boundary}{A finite coordinate inside exactly one half-open cell has one \(G_{pi}=1\), preserves unity over pixels, and contributes only there. A coordinate on an internal upper edge belongs to the adjacent cell; a coordinate on the outer upper extent or outside the grid contributes nowhere. Fractional splitting is not authorized.}{Algebraic contract}\par
\SCIMapPrediction{SCI-MAP-PRED-005}{Delta input}{For a declared fixed state, a basis-pixel sky perturbation produces the corresponding support-row-restricted column of \(R_{{\rm fixed},\Theta}=A_{{\rm MAP},\Pi}H_{{\rm fixed},\Theta}\), including projection and boundary truncation. A PTC full-procedure or PTC+MAP re-resolved numerical difference exists only when its complete authorized comparison-space record supplies exact common domains and both row maps; absent that record it is unavailable or only a typed state transition/discontinuity and need not equal the fixed-state column.}{Representation fidelity}\par
\SCIMapPrediction{SCI-MAP-PRED-006}{Point-source template}{A declared point-source perturbation \(\boldsymbol\tau_b\) produces \(R_{{\rm fixed},\Theta}\boldsymbol\tau_b\) only for the exact fixed response state and authorized rows. PTC full-procedure and PTC+MAP re-resolved families additionally retain baseline/perturbed identities, perturbation side/sign/amplitude/location/source state/normalization, finite-difference convention, gauge/support/bases, exact common domain and row maps, state transition, availability, and cause. The unavailable whole-chain RTC-to-CAL-to-PTC-to-MAP family is not inferred. Peak, integrated, and fitted amplitudes remain distinct functionals.}{Representation fidelity}\par
\SCIMapPrediction{SCI-MAP-PRED-007}{Resolved and Fourier inputs}{At fixed state, resolved disks, gradients, and Fourier modes produce their explicit \(R_{{\rm fixed},\Theta}\boldsymbol\tau\) on the authorized row set; a constant-input pass does not predict preservation of these modes, and procedure-response families are not inferred from the fixed matrix.}{Representation fidelity}\par
\SCIMapPrediction{SCI-MAP-PRED-008}{Variable coverage}{Fixed-coefficient constant preservation can coexist with spatially varying normalization, support-row membership, covariance, and nonconstant-template response; a row that fails support is absent rather than measured zero.}{Algebraic contract}\par
\SCIMapPrediction{SCI-MAP-PRED-009}{Finite boundary}{At an admitted finite edge, no contribution wraps or clamps; local normalization may preserve a constant while compact-source peak or integrated response changes.}{Representation fidelity}\par
\SCIMapPrediction{SCI-MAP-PRED-010}{Coefficient states}{The admitted uniform family yields exact dimensionless \(\gamma_i=1\) only for an explicit selection with complete PTC publication, exact target/payload/occurrence association, MAP permission, and a requested, applicable, eligible, realized named handoff under the exact compatible source binding. Registry presence alone realizes nothing. Missing selection or handoff remains unavailable with no fallback. Structural availability is independent of numerical class; MAP alone classifies finite positive, exact zero, negative finite, non-finite, and unrepresentable. Zero is noncontributing and the other nonpositive/non-finite classes are invalid with exact cause; uniform one is not precision.}{Engineering conformance}\par
\SCIMapPrediction{SCI-MAP-PRED-011}{Use-qualified exclusion and non-finite states}{A candidate whose typed gate record projects to Boolean nonmembership does not contaminate an accumulator. Structural exclusion prevents payload retrieval; structurally admitted payloads are read only for safe MAP numerical classification before membership and arithmetic. Its complete named-field or other typed record, immutable reason, and cause remain preserved; no universal producer-flag action is inferred.}{Engineering conformance}\par
\SCIMapPrediction{SCI-MAP-PRED-012}{Threshold selector}{For each predeclared positive finite coefficient population, the selected zero-based index is exactly \(\lfloor(\lfloor0.75N\rfloor+N)/2\rfloor\); empty input uses the exact boundary convention \(Q_\star=0\) while selecting no population element and asserting no zero-sky measurement. No row gains support without its own finite positive \(Q_p\). Small \(N\) fixtures straddle every integer-index transition. At exact \(c=0\), both relative thresholds are zero and the finite-positive requirement still excludes zero, negative, non-finite, or absent normalization. Values \(0<c\le1\) use inclusive comparisons in the ordinary recommended range. A \(c>1\) value is admitted only with the independently true, same-value/scope \texttt{coverage\_cut\_above\_one\_override\_requested} fact and may yield empty support; the number does not authorize itself. That prospective applied case has state \texttt{applied\_no\_support\_authorized\_output\_rows}, outcome \texttt{not\_produced}, exact like-named cause, no failure, and no realized MAP product. Negative, non-finite, missing, or unrepresentable \(c\) fails before application or required-product mutation. Every reached cut stage is retained, with all five only for successful product realization; pre-application validation checks the observation-resolved value and never requires a realized value.}{Algebraic contract}\par
\SCIMapPrediction{SCI-MAP-PRED-013}{Missing response}{Every \SCIMapBaseObservationRole{} discloses its known MAP-local realized operator response on exact signal rows. Missing upstream response yields a separately typed limited or unavailable state on complete signal membership, never a smaller subset, zero, or identity. \SCIMapResponseObservationRole{} is unavailable for that exact base parent only when its exact selected role request names that exact response-family object as required; absence of a different fixed-state, full-procedure, re-resolved, coadd-procedure, or whole-chain family does not block an otherwise complete named response role. Each numerical full-procedure difference requires its complete \SCIMapComparisonSpaceRecordId{} record. Coadd-full-procedure/re-resolved and whole-chain responses remain separately unavailable unless authorized. The base bundle remains usable for claims that do not require the absent response; any later response estimate or corrected map has a new versioned identity bound to the original.}{Engineering conformance}\par
\SCIMapPrediction{SCI-MAP-PRED-014}{Role-qualified atomic rejection}{After exact activation makes the proposed aggregate profile evaluable, a required companion incompatibility rejects the complete observation before coadd mutation at the selected role scope. Under \SCIMapBaseCoaddRole{}, unavailable or basis-incompatible response does not change signal membership; it yields unavailable coadd response with exact cause and no hidden subset or zero companion. Under \SCIMapResponseCoaddRole{}, a mixed or incompatible response-family identity, missing fixed-state condition, or absent exact composition theorem blocks that role for an existing base parent. A required covariance incompatibility analogously blocks \SCIMapCovarianceCoaddRole{}. Any separately requested admission may reject the observation only before constructing its own explicitly identified base/signal parent. Other required identity, unit, WCS, shape, policy, and parent incompatibilities remain atomic at their declared role scope. The current candidate has no active profile or realized object decision.}{Engineering conformance}\par
\SCIMapPrediction{SCI-MAP-PRED-015}{One-observation coadd}{After exact profile activation and a realized eligible object decision, a \SCIMapBaseCoaddRole{} containing one admitted base observation under the uniform dimensionless coefficient family reproduces its signal on corresponding centered-integer authorized rows. If the valid applied coadd support policy authorizes no rows, outcome is instead \texttt{not\_produced} with exact no-support cause and no zero coadd. Conforming in-memory and persisted bundles give the same admission, identity, and arithmetic. Unavailable or basis-incompatible upstream response leaves nonempty signal membership unchanged and makes coadd response unavailable with exact cause. \SCIMapResponseCoaddRole{} reproduces response only when it binds one exact family \(R\), every \(R_o^{(R)}\) and all fixed-state/composition conditions are compatible, and \(R_{{\rm coadd,out}}^{(R)}=B_{\rm out}T_{\rm obs}^{(R)}\) is authorized.}{Algebraic contract}\par
\SCIMapPrediction{SCI-MAP-PRED-016}{Uniform observation coefficients}{On each authorized coadd row, two compatible observations produce their equal-observation arithmetic mean and count-like normalization under \SCIMapUniformCoaddProfile. Each requested coadd role binds its own exact parent, lifecycle, attempt outcome, and ordered membership; stronger companions do not change the arithmetic membership of their exact base parent. A response-bearing coadd additionally binds one exact compatible response family and the fixed coadd state/composition authority; a finite difference without an exact composition theorem is not propagated as a Jacobian. Coadd-full-procedure/re-resolved and whole-chain response remain unavailable. The normalization is not precision.}{Algebraic contract}\par
\SCIMapPrediction{SCI-MAP-PRED-017}{Missing, invalid, or no-support observation}{An unsupported observation row contributes at no coadd row; an observation rejected by the exact selected role contributes nowhere to that separately identified base/signal parent and does not increment its count. An applied observation attempt with no support-authorized output rows has outcome \texttt{not\_produced}, is not an admitted coadd member, and has no signal, response, covariance, exposure, count, validity, or completion product. A companion unavailable but permitted by a realized base role does not remove the observation and is not attempt failure. No case creates a zero-valued scientific or companion substitute.}{Engineering conformance}\par
\SCIMapPrediction{SCI-MAP-PRED-018}{Centered integer placement}{A canonical target grid requested before construction yields observation maps already bound to that grid. Other compatible shape differences that are nonnegative and even produce the exact half-difference row/column offset; odd, negative, fractional, or WCS-inconsistent relations fail with no mutation. The FITS representation comparison evaluates every preregistered addressable pixel center and outer-boundary cell vertex, including all corners, with the exact spherical metric and invalid-point rule. A later crop is a new row-selected product with its own identity and lineage and cannot relabel its parent as compatible.}{Engineering conformance}\par
\SCIMapPrediction{SCI-MAP-PRED-019}{Cross-observation covariance}{For two maps with variances \(\sigma_1^2,\sigma_2^2\) and covariance \(c\), a complete \SCIMapCovarianceCoaddRole{} over the exact base-coadd membership includes \(c\) through \(\boldsymbol\Sigma^{\rm obs}\). If a required block is missing, that stronger role is unavailable for the parent; the base role retains a partial, symbolic, summarized, lineage-resolvable, or unavailable state. No subset is formed, the block is never zero or independence, and equal-observation normalization is not precision.}{Algebraic contract}\par
\SCIMapPrediction{SCI-MAP-PRED-020}{Fixed noise operator}{A deterministic admitted realization propagated with fixed state yields the same support-row identities and coefficient-by-coefficient \(A_{{\rm MAP},\Pi}\equiv A_{\rm out}\) and \(B_{\rm out}\) as the signal path; recomputed state exposes a named difference.}{Engineering conformance}\par
\SCIMapPrediction{SCI-MAP-PRED-021}{Sequential and parallel reduction}{Integer facts, identities, masks, support, and cardinality agree exactly. Floating sums agree with a preregistered operation-count/conditioning bound that is frozen before results are viewed and is not loosened after failure.}{Engineering conformance}\par
\SCIMapPrediction{SCI-MAP-PRED-022}{No false significance}{A deliberate attempt to label signal times square root of merely formal weight as empirical significance is rejected or remains unmistakably labeled formal standardized signal.}{Engineering conformance}\par
\SCIMapPrediction{SCI-MAP-PRED-023}{No validity promotion}{A downstream product may have its own use-local validity or eligibility but cannot change MAP-local base-product validity or sever the base/unfiltered MAP parent.}{Engineering conformance}\par
\SCIMapPrediction{SCI-MAP-PRED-024}{No ordinary predicate for JINC}{A deliberate attempt to apply positive-coefficient ordinary admission, support, or complete-bundle availability to JINC is rejected as outside SCI-MAP v0.2.}{Engineering conformance}\par
\SCIMapPrediction{SCI-MAP-PRED-025}{Aggregate, index, and no-support outcomes}{An exact-zero, non-finite, overflowed, or unrepresentable aggregate/index never becomes a live valid product; failure occurs at the declared scope before mutation or completion. Separately, a valid applied policy whose support-authorized output-row set is empty produces no product with exact \texttt{not\_produced} outcome and cause \texttt{no\_support\_authorized\_output\_rows}; it is not a failure and creates no dense zero substitute.}{Engineering conformance}\par
}
