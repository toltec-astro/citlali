# SCI-MAP v0.2/r0.2 scientific source/core/view parity report

Status: **candidate author-source parity report and final-packet acceptance
criteria**. Manager-owned final source, build, render, metadata, and
package-seal records report the actual delivered artifact checks. This record
does not replace them or promote document parity into scientific acceptance or
implementation conformance.

## Normative-source invariant

`SCI-MAP-v0.2-SHARED-AUTHORITY/r0.2`, aggregate SHA-256
`6a7e64377e31b8b0e4dac90a27a7662ac825bd59c5a87d8237eb533d7b8d37c0`,
is the sole normative scientific core. Its ordered stream is wrapper,
notation, definitions, equations, assumptions, requirements, edge cases. Each
view shall import that wrapper exactly once.

| View | Exact entry-source SHA-256 | Scientific role |
| --- | --- | --- |
| Formal | `53d0dbf7269ed826a96b96c56e216a53d2ded5d2e3ba2a9a42953bc33d671523` | Renders normative equations, assumptions, 52 requirements, and 25 predictions. |
| Rationale | `0a16a682018a0df4464c3aca4d21ee2ea66336aa2c7fb9153b8f34b3848188d2` | Explains the same science and may not contradict or replace the core. |
| ECS | `ae745f2bd7ec288c1997b24b26720d7d7033dd05939638fac50bd65cf8f175a2` | Imports canonical clauses and adds only prospective evidence procedure/result records. |

On the stable author bytes, a literal input parse found the same r0.2 wrapper
exactly once in each of the three entry sources and the correct distinct
`identity/formal.tex`, `identity/rationale.tex`, or `identity/engineering.tex`
input exactly once. Recomputed raw entry hashes and the ordered core aggregate
match the values above. This author-source result precedes manager insertion
of mechanical identity/build inputs; the final transitive-input record remains
manager-owned.

## Required scientific parity checks

All three views and documentary records shall agree on:

- contract v0.2, document r0.2, scientific owner Grant Wilson, date
  2026-09-07, and status `Candidate for scientific-owner disposition`;
- the five-stage MAP lifecycle and reached-prefix/terminal-failure rules;
- separation from PTC/VAL axes, ECS results, and ECS artifact realization;
- all six exact roles, parent/membership invariants, and aggregate profile
  `SCI-MAP:observation_coadd_admission@2`;
- base-coadd retention of signal membership when response is unavailable or
  basis-incompatible, and role-qualified atomic rejection for selected named
  companions;
- exact full-procedure comparison-space prerequisites and prohibition of
  implicit fill/intersection/association/interpolation/reprojection/gauge;
- restricted NOI ownership/claim terminology;
- exact result and evidence-artifact vocabularies;
- PTC product identity without an undefined MAP-local `q`, `K_p` only as
  response accumulator, `bold Sigma^obs` as stacked covariance, `Pi` as plan,
  and `J_out` as row selector;
- typed above-one cut override, five cut stages, pre-application resolved
  value, applied-event binding, inclusivity, invalid behavior, no default, and
  small-N order statistic; and
- exact finite WCS center/boundary/corner population, frame/generation,
  great-circle metric, binary64 precision, fail-on-invalid rule, and bounded
  claim.

## Cover and transitive-input check

Each view shall include its exact `identity/*.tex` fragment on the physical
cover after the title. Cover values bind shared and entry bytes separately,
avoiding self-digest cycles. A final verifier shall resolve every transitive
input, reproduce every raw and aggregate digest, and confirm that the
rationale/ECS add no independent science.

Any scientific mismatch is a candidate-source defect requiring corrected
bytes, new hashes, clean rebuild, and renewed review. A successful structural
or PDF check alone cannot establish scientific acceptance or implementation
conformance.
