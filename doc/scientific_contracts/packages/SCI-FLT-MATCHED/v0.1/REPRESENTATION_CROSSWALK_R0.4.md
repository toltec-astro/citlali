# SCI-FLT-MATCHED v0.1 r0.4 Science/Representation Crosswalk

Status: second-review authority and availability closure; all concrete routes
remain unselected

| Object | Scientific-owner decision | Engineering declaration after science | Identity rule |
| --- | --- | --- | --- |
| Weight `W_p` | Select concrete `AO-001-A` or C, commission a concrete successor under B/D, or declare unavailable. C-route effective-sample, anisotropy/dispersion, and leakage quantities are diagnostics, not v0.1 admission gates. | Implement only after exact science; preregister diagnostic estimators, evidence, and numerical profile. | Diagnostics cannot select or invalidate the science; B/D remain authorship triggers. |
| Numerical producer | Exact `n_p/d_p` and any intentionally distinct successor science. | `AO-002-A` exact-operator profile or `AO-002-C` unavailability; optional `tilde d_p` only when a separable denominator is declared. | `F_g` is actual; a linear operator or numerical denominator is recorded only when established. |
| Named covariance role | `AO-003-A` complete, B named projected, or C unavailable; separately authorize U2/joint scope. Every signal bundle carries role identity/status. | `AO-003-D` resident/structured or E lineage/on-demand for an available selected scope. | `AO-003-C` leaves the signal complete unless a named-use policy requires the companion. Representation cannot alter role, scope, population, conditioning, fixed codomain, or query answers. |
| Resolved immutable state | Fix finite scientific state queries within `Q_FLT^0.1` and each named use. | `AO-004-A` full, B compact exact, or C exact lineage reconstruction. | Every supported query is exact; queries outside the vocabulary require a successor. |
| Typed response | Fix fixed/FP/operational-realized/reference type, domain, validity, units, consumers, finite queries, and operational derivative/finite-difference specification. | `AO-005-A` full, B exact structured, or C exact lineage/on-demand. | Representation preserves response identity and common-domain query answers; unsupported queries are unavailable. |
| SCI-VAL verdict policy | Seven PA/SA/SP/CU/NU/RU/FH meanings, axes, dependencies, actions, and versions. | `AO-006-A` separate records, B grouped records, or C seven-role vector. | Layout is lossless only and cannot authorize an actionable scalar. |

Storage footprint remains scientific nonauthority. Engineering representation
may not convert typed absence into zero, success-conditioned covariance, or a
different response experiment.
