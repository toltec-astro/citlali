# SCI-FLT-MATCHED v0.1 r0.4 Owner-Decision and View-Parity Report

Date: `2026-09-01`

Result: `PASS` for source/view parity, second-review repair coverage, and the
two owner-approved policy dispositions; this is not scientific freeze,
implementation conformity, response/covariance fidelity, validation, or route
authorization

## Review and firewall basis

The eight Stage A packet objects remain byte-identical to owner-approved
manifest SHA-256
`255c66da880fc7664a57635b28a98d874fc024490d04528f802635c0382a57c8`.
The r0.4 repair uses only the r0.3 shared-core package, the independent Pro
review recorded in `CHATGPT_PRO_INDEPENDENT_REVIEW_R0.3_2026-09-01.md`, and
the owner directive in `SCIENTIFIC_OWNER_R0.4_DIRECTIVE_2026-09-01.md`. No
implementation-informed object entered the repair channel.

## C01--C08 coverage

| Finding | r0.4 closure |
| --- | --- |
| `C01` | Complete frozen condition `h=(g,theta)` is consistent across fixed-state expectation, variance, reference covariance, and realized covariance; U2 owns variation over theta. |
| `C02` | Fixed `P_C` and almost-sure finite numerical production define operational covariance; response finite differences use one fixed common domain; selection/censoring alternatives require separate roles. |
| `C03` | Owner selected covariance-scope policy: every signal carries role/status; available scopes are companions; typed unavailable scope does not invalidate the signal unless required by named use. |
| `C04` | Owner selected diagnostic-only policy for AO-001-C effective-sample, anisotropy/dispersion, and leakage quantities; no v0.1 threshold gates remain. |
| `C05` | Numerical normalization is optional and exact when declared; `e_d` is otherwise unavailable. |
| `C06` | Template-rescaling consequence is restricted to pure fixed-state amplitude-coordinate reparameterization; reruns are separate realizations. |
| `C07` | PRED assumption mappings now follow actual response/covariance premises rather than GLS-only premises. |
| `C08` | General operational response records reference parent, step, and derivative/sidedness scheme; matrix shorthand requires established linearity. |

## Stable identities and parity

- Requirements: `REQ-001`--`REQ-050` preserved.
- Contract consequences: `PRED-001`--`PRED-024` preserved.
- Options: all 21 alternative IDs across six families preserved; none
  selected.
- Owner ledger: all 17 SODL IDs retained: 14 open, 2 decided, 1 superseded.
  The two r0.4 policy dispositions are additional binding owner records and do
  not close the concrete `SODL-004` or `SODL-008` selections.
- Crosswalk: all 95 stable REQ/PRED/AO IDs retained.
- Both views import the same shared modules in the same order.

The current boundary records are the r0.4 MAP, template, NOI, and finite
one-way FRUIT drafts. `ROUTE_STATUS_R0.4.md` separates generic definition
from realized availability.
