# SCI-FLT-MATCHED v0.1 r0.4 Semantic Change Map

Status: owner-authorized minor repair after the second independent r0.3
document review; all Stage A bytes and stable REQ/PRED/AO/SODL identities
preserved

| Review finding | Stable contract locations repaired | r0.4 result |
| --- | --- | --- |
| `C01` | notation; equations; `ASM-007`--`ASM-009`; `REQ-020/022`; `PRED-003/008/024`; uncertainty budget | `h=(g,theta)` is the complete fixed condition; U2 alone owns variation over `theta`. |
| `C02` | notation/definitions; response/covariance equations; `ASM-009`; validity and edge tables; `REQ-020/045`; `PRED-008/024`; `CT-008/010` | `P_C` fixes the covariance codomain; failure-bearing or draw-dependent domains are unavailable unless separately named; response finite differences use one fixed common domain. |
| `C03` | product roles; `REQ-020/046`; `AO-003`; owner guide/ledger; `CT-010/011` | Every bundle carries covariance role/status; selected available scopes are companions; `AO-003-C` does not invalidate the signal unless named-use policy requires it. |
| `C04` | `AO-001-C`; edge table; owner guide/ledger; `CT-006` | Effective-sample, anisotropy/dispersion, and leakage are mandatory diagnostics but not v0.1 admission thresholds. |
| `C05` | notation; engineering observables; `CT-007` | `tilde d_p` is an optional declared numerical normalization; `e_d` is unavailable when a general `F_g` exposes no separable denominator. |
| `C06` | `PRED-014`; edge table; `CT-003`; evidence patterns | The inverse scaling law is restricted to a pure fixed-state amplitude-coordinate reparameterization; any Learn/Resolve rerun is a new realization. |
| `C07` | `PRED-008`, `PRED-019`--`PRED-022`, `PRED-024`; crosswalk | Universal response/covariance consequences no longer depend on GLS-only assumptions. |
| `C08` | notation; response definitions/equations; `REQ-045`; `CT-008`; engineering observables | General operational response is `R_realized^{delta,sigma}(m;p,s)`; a parent-independent matrix shorthand requires established linearity. |

The nonnormative `six modules` wording was changed to `shared modules`. No
estimator, package identity, anchor lattice, support rule, FRUIT boundary,
concrete AO option, or numerical route changed.

No implementation, conformity, covariance/response fidelity, observational
validation, performance, readiness, production, scientific-freeze, or Unity
claim follows.
