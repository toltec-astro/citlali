# Optimality/title scientific-owner disposition — r0.6

Status: owner selected option 1 on `2026-09-01`; incorporated into the frozen
v0.1/r0.6 scientific authority

Method ID remains `SCI-FLT-MATCHED`. Every public product and boundary carries
exactly one `optimality_status`: `established_exact_local_GLS`, `not_claimed`,
or `unavailable`. Title, filename, and method ID are never evidence.

## Option 1 — Matched-template map amplitude estimation (selected)

Use this human title. Reserve “optimal” for an `AO-001-A` realization for which
every exact local constrained-GLS theorem premise is established.

## Option 2 — Normalized matched-template map filtering

Use this human title while retaining the one-parameter template-amplitude
estimand in the contract body.

## Option 3 — Retain the historical title

Retain “Optimal matched-template map filtering,” but state prominently that the
title is historical terminology and require `optimality_status` on every
product and boundary. `AO-001-C` always carries `not_claimed`.

The final r0.6 rendering uses the selected option 1 title. The selection does
not itself establish optimality; the realization-level rule above remains
binding.
