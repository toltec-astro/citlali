# SCI-FRUIT EL-F3 delegated owner authorization

Date: `2026-09-02`

Authorized activity:
`SCI-FRUIT-EL-F3-LATE-PENALTY-COUNTERFACTUAL-R0.1`

After the causal-diagnostic result stated that a counterfactual required
separate owner authorization before any prototype or reduction, the owner
reported commit `5e9c5f965` pushed and replied:

> pushed, let's proceed. I'm encouraging you to run with this one.

This is recorded as authorization to execute the bounded causal test proposed
in `CAUSAL_DIAGNOSTIC_RESULT_R0.1.md`, with implementation details delegated
to the manager. It is not represented as approval against a pre-existing
content-bound manifest because no EL-F3 manifest existed when the statement
was made.

Before execution, the manager narrowed the intervention from a new Citlali
prototype to an audited copied-checkpoint perturbation. The question is
unchanged: does withholding the newly learned UID 4460 zero-factor penalty
from the state used by EL-F2 alpha-1.25 iteration 5 prevent or reduce the
a1400 collapse? The narrower approach changes no Citlali algorithm or
executable and preserves every original product. The exact intervention,
comparisons, classifications, inputs, bounds, and stop rules are frozen in
the validation `TEST_DEFINITION.md` and
`COUNTERFACTUAL_REGISTRATION_R0.1.yaml` before a checkpoint copy is made or a
reduction is run.

This authorization does not permit parameter tuning, another recurrence,
more iterations, additional observations, unfavorable-result reruns, Gate D,
qualification, Stage B authoring, production changes, fallback selection, or
Unity activity.
