# SCI-FRUIT EL-F8 authorization

Date: 2026-09-03

Status: **approved bounded local empirical-lane execution**

The scientific owner approved Choice A with the exact statement:

> I approve
> `SCI-FRUIT-EL-F8-PENALTY-PLACEMENT-DECOMPOSITION-R0.1` against the exact
> `EL_F8_BUNDLE_MANIFEST_R0.1.md`.

This authorizes only the opt-in development setting, verification, local
build, four ordered one-iteration replays, analysis, and result record
specified by that exact manifest. The two `pre_cleaning` compatibility replays
must pass before either `pre_mapmaking` result may be interpreted.

This approval does not authorize a production default, soft penalty,
confirmation counter, threshold change, detector judgment, recurrence or
method selection, qualification, Gate D, Stage B, another source or
observation, an extra iteration, or Unity activity.
