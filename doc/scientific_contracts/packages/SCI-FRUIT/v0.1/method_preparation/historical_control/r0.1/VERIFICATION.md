# Preparation verification — r0.1

Date: 2026-09-08. Manager: Codex. Result: document/source checks passed.
These checks establish preparation completeness and identity, not scientific
approval, numerical availability or implementation conformity.

## Substantive scope review

- The required opening and all eleven Scope Brief sections are present; inputs,
  outputs, operation boundaries, upstream/downstream ownership and intended use
  are stated. Scientific outputs are distinct from documents to be authored.
- All twenty-one method classes separate recovered facts, pending proposals and
  absent binding/blocked claims. Five stable owner decisions and eight explicit
  dependencies cover the remaining route, method, input and authority questions.
- The proposed selector has a causal information boundary. No numerical gate,
  predictor, mode count or data-derived benefit rule was selected. Historical
  template values are confined to the internal dossier and source copies.
- The current repaired RF-01/02 defining-map/application-coordinate distinction,
  exact PTC coefficient-family boundary, additive/support/null-space meanings,
  NOI conditioning, continuation/termination and universal completion limits
  are retained. Program-order, conditional composition and numerical authority
  are distinct. Generic core science was not reauthored.
- The proposed scope narrows to observation/raw JINC but acknowledges rejoined
  signal admission, numerical producer permissions and companion gaps. JINC's
  historical candidate-registration prose is covered by its later exact freeze;
  registration is not mistakenly listed as absent.
- The complete owner archive contains implementation-informed manager evidence
  and is explicitly prohibited as an independent author packet. Newly authored
  links in the sanitized scope/recovery/reference control reach only the
  proposed scientific/status/process allowlist. Links within copied immutable
  sources retain source context and grant no transitive access.

## Mechanical verification

- 48 copied sources matched their exact Git source bytes, size and SHA-256.
- Historical source tree identity matched. The preparation base descends from
  required empirical starting commit `d39d4685b`.
- Frozen FRUIT source inventory and all seven entries matched; freeze sidecar
  matched; original frozen Stage B archive still hashes to
  `ad3bff3ace6b740ed6c79f37717e48ad549700bd1a5a66a96ae49757c618bd1a`.
- Six JINC canonical modules and four covered boundary/product files matched
  their exact freeze-manifest digests.
- Scope headings, method-class sequence, decision/dependency identifiers,
  newly authored packet links and scientific-only reference identity list passed.
- Only this new preparation directory and three current navigation/status files
  changed. No frozen source/delivery, application/configuration, test, reduction
  or empirical product changed. `git diff --check` passed.
- Earlier manager worktree remains on its original branch/HEAD with exactly
  the two known untracked review archives; their contents were not opened,
  hashed, unpacked or changed. The Stage B worktree remains tracked-clean at
  `6c997638d3e554db089790e8113b7ee03ac50a13`.
- Local packaging verified each archived regular member byte-for-byte against
  the payload, manifest or manifest sidecar; no extra, missing, duplicate,
  symlink or unsafe-path member is accepted. The external archive sidecar binds
  the complete new manager review tar.gz.

No code build, algorithm test, replay, numerical evaluation, author launch,
Unity query or push was run. Those activities would not verify this document-only
preparation and are outside its authorization.
