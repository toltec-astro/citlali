% Sole normative prediction register: stable IDs, owner-directed clarifications.
\newcommand{\Pred}[3]{\par\medskip\textbf{SCI-FRUIT-PRED-#1: #2}\par#3\par}


\newcommand{\NormPredictions}{%
\Unit{Conditional predictions 1--7}{core:pred1}{%
Conditional deductions only. Arithmetic requires ASM-001; complete linearity requires ASM-003, and covariance ASM-004. Missing premises block the affected claim.
\Pred{001}{Zero applied feedback}{If $\ma=0$ and $\Pi_k(0)=B_k(0)=0$ with identical compatible state/support/reference/gauge/null-space facts, substitution gives $z_k=F_k(\yin)$. A zero candidate/accepted model or coordinate zero does not establish this premise; non-zero-preserving offsets defeat the reduction. No unavailable measured mode becomes observed zero.}
\Pred{002}{Identity residual procedure}{If $F_k$ is identity on compatible $Y_k=Z_k$ and $\Pi_k=B_k$ on the applied operand with identical support/reference/gauge arithmetic, removal and rejoin cancel to $z_k=\yin$. Missing compatibility blocks cancellation. Neither equality of model stages nor recovery of an unavailable measured mode follows.}
\Pred{003}{Zero net linear model path}{Under complete fixed linearity and $B_k=A_k\circ\Pi_k$ on the applied-model domain, $D_k=0$ and $z_k=A_k\yin$ is independent of that operand within the fixed conditions. This follows directly from the operator difference; changing state or rules changes the premise.}
\Pred{004}{Perfect applied operand}{If $\yin=\Pi_k(m_{\rm ref}^{\ap})$, $\ma=m_{\rm ref}^{\ap}$ and $F_k(0)=0$ in the declared reference/gauge, then $\rho_k=0$ and $z_k=B_k(m_{\rm ref}^{\ap})$. A perfect accepted model alone is insufficient. Rejoin proves no true sky or unit response; unavailable-mode content remains model/prior supported.}
\Pred{005}{Transfer, output shift and bias}{With identical input/operators under complete fixed linearity, subtracting results at $m_{\rm ref}^{\ap}+\delta m_k^{\ap}$ and $m_{\rm ref}^{\ap}$ yields $D_k\delta m_k^{\ap}$. $D_k$ is the transfer operator; the resulting difference is the distinct output-shift product. Model mismatch is separate, not covariance or bias. Bias is the law-qualified expected shift only with exact estimand/reference/error-law/conditioning authority; otherwise it is unavailable while transfer/shift may exist. Accepted error first undergoes the exact accepted-to-applied transformation/dependence.}
\Pred{006}{Fixed-subspace projection and frozen-component subtraction}{For exact frozen PTC operator-defining state on the admitted finite/rank/support domain, substitution gives $z_{g,t}=x_{g,t}P^{\rm PTC}_{g,t,\Theta}$ and $\delta z_{g,t}=\delta x_{g,t}P^{\rm PTC}_{g,t,\Theta}$. The internal modal coordinate changes algebraically with $x$ under the same map, without RF-03 or operator-generation change. Fixed centering gives the affine map $(Y^{\rm CAL}_{g,t}-\lambda_g)P$ with derivative $H P$, retaining its offset. Frozen-component subtraction instead has identity input derivative. Relearning/re-resolving a defining fact is a separately scoped procedure query; incidental equal outputs do not equate these families or authorize a route.}
\Pred{007}{Model/prior origin is not exposure}{Where a method permits model-only output or model contribution in an unavailable measured additive mode, that content retains model-or-prior-supported origin. It is not measured, recovered from data, acquired exposure or unit-response truth. Its ancestry is model/prior; neither rejoin nor a gauge choice changes that authority. Model-only permission itself must be supplied.}
}
\Unit{Conditional predictions 8--13}{core:pred2}{%
\Pred{008}{Support and additive incompatibility}{Unequal support or incompatible reference/gauge/null-space facts permit a narrower occurrence class only with exact method permission and compatible arithmetic; otherwise affected composition is unavailable. A partial or incompatible map has no licensed sum there. Implicit filling, renormalization, offsets or gauge alignment do not repair this premise.}
\Pred{009}{Fixed-map derivative and defining-state reruns}{On compatible differentiable fixed-state tangent spaces the chain rule gives $J_{z\leftarrow y}^{\rm fixed}=\mathrm DF|_\rho$ and $J_{z\leftarrow m}^{\rm fixed}=\mathrm DB|_m-\mathrm DF|_\rho\circ\mathrm D\Pi|_m$. Internal fixed-map coordinates may change while this derivative applies. Complete fixed linearity reduces it to $A_k,D_k$; affine offsets otherwise remain. Prescribed reruns that can change defining maps/state belong to RF-03; fixed-extent propagation to RF-04, stopping/selection to RF-05 and complete parent-domain composition to RF-07. A fixed derivative omits those causal reruns and cannot replace their query.}
\Pred{010}{Fixed-domain failure and response boundary crossing}{A perturbation leaving the exact finite/rank/support domain, or crossing a branch/reference/selection boundary, may make a fixed response unavailable. It does not silently become RF-03. A finite-difference/state-transition object requires its own compatible comparison and convention. Re-resolving an operator-defining fact is a declared procedure response; evaluating an internal coordinate under an unchanged fixed map is not. Freezing a prescribed defining-state rerun or rerunning a frozen defining fact changes the query. Failed differentiability/comparison cannot be repaired by substituting a fixed-branch derivative.}
\Pred{011}{Law-relative joint covariance}{Complete fixed linearity and finite compatible joint moments yield all four covariance terms under the exact declared law. A physical/random-parent target retains random parent/model values. An explicitly fixed realized-model target has zero model/cross blocks only within its narrower law and omits learning variation. A conditional NOI-assignment law may instead condition on realized parent, fixed model/operators and exact successful design resolution; it is not the physical-parent target. Accepted uncertainty transforms through exact dependence, including transformation uncertainty. Deterministic full-product offsets change the center, not the corresponding zero-target-law noise covariance.}
\Pred{012}{Repeated information remains dependent}{Reusing measured parents or shared model/calibration/reference information preserves ancestry and dependence. Iterations do not become independent measurements and imply no $1/\sqrt{\text{iteration count}}$ uncertainty scaling. Such a scaling would require an independently authorized population and stochastic premise, which iteration count does not provide.}
\Pred{013}{Exact continuation}{After completed absolute zero-based iteration $N$, continuation-sufficient $S_{N+1}$ and identical future controls/compatible method/profile imply continuation at absolute index $N+1$ agrees with uninterrupted scientific execution. Both consume the same causal state, including reference/gauge and stochastic state. $N$ is an index, not a count; omitting a consequential fact defeats the premise. This deduction performs no replay.}
}
\Unit{Conditional predictions 14--17}{core:pred3}{%
\Pred{014}{Request generations, restart and early outcomes}{A request generation has immutable not-requested or requested state; later request creates a new generation. Restart creates a new lineage. New realized values under unchanged learning rules create state/application generations; changed rules require method identity. Retry preserves parent state, attempt/randomness and earlier failure. Availability and producer outcome attach to the stage actually reached. An early stop fabricates no later applied, realized, completion or publication record. An allowed unavailable optional companion may coexist with a narrower core-and-method-permitted complete product.}
\Pred{015}{Resource exhaustion is not convergence}{Reaching a declared iteration/resource maximum establishes its accounting and stopping state only. It supplies none of the separate convergence proposition, metric, persistence or adequacy premises. Completion, limit, convergence and consumer fitness therefore cannot replace each other.}
\Pred{016}{Comparable immutable terminal selection}{Selecting completed absolute iteration $N$ requires an exact rule and all nine fields. Candidates share common quantity/units/frame/domain/gauge/response/support/method-generation/metric meaning or exact selector-owned conversions. Shape/scalar agreement is insufficient; missing hard comparisons block affected selection. Retain every candidate, conversion, inclusion/exclusion, tie, unavailable comparison/cause and terminal response/uncertainty. Field 9 contains only requested/linked use decisions with exact statuses or a no-request statement; no exhaustive future-use list is needed. Selection creates an immutable relation and never mutates the selected bundle.}
\Pred{017}{Grouping does not commute by implication}{Observation-local, coadd-level and jointly learned groupings do not imply $\mathrm{FRUIT}(\mathrm{Coadd}(y_o))=\mathrm{Coadd}(\mathrm{FRUIT}(y_o))$. Their learning, support and dependence may differ. Equality requires exact compatible operators/state/support and learning premises; this is absence of implied equivalence, not universal inequality or a numerical coaddition rule. Here $y_o$ denotes only the compared observation inputs.}
}
\Unit{Conditional predictions 18--20}{core:pred4}{%
\Pred{018}{NOI target-law zero and full-parent members}{Under complete fixed linearity with fixed model/state, full members produce $A_k y_{k,b}^{\mathrm{full}}+D_k\ma$ and their exact law-qualified center. A member with $\mathbb E_{\Lnoi}[\delta y_{k,b}^{\mathrm{NOI}}\mid\Onoi]=0$ produces $A_k\delta y_{k,b}^{\mathrm{NOI}}$, without deterministic rejoin. This zero is a target-law expectation: source/residual structure and nonzero spatial/member sample means may remain; no finite-ensemble centering or $B-1$ correction follows. Full-affine center/estimator and exact source law need separate authority. Fixed, dependent-successor and relearned methods/ensembles retain distinct bindings; no numerical route or pooling is implied.}
\Pred{019}{No numerical admission from a family}{Without a complete exact separately owner-approved method-specific FRUIT-FEEDBACK-METHOD record and required upstream numerical authority, no numerical route/result is admitted. Creating the family, authoring/freezing the conditional core or verifying documents supplies neither approval nor conformance. All numerical routes remain \NA.}
\Pred{020}{Bundle-bound completion and downstream accounting}{A bare ITERATION-RESULT payload is not a scientific handoff without its exact ITERATION-BUNDLE or immutable reference. Base local completion survives absent/failed downstream use; only an explicitly requested product role may require a specified child. A not-requested SCI-VAL use creates no eligibility proposition. Missing profile/scope can produce a realized accounting record with applicability\_unknown and decision\_unavailable, distinct from incomplete/failed/not-produced artifact realization. Requested/linked decisions preserve exact profile/source/status bindings and immutable FRUIT parents without a new iteration generation. No actual numerical completeness or fitness is established here.}
}
}
