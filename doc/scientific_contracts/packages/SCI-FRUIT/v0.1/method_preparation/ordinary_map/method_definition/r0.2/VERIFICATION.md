# Residual-relearning method-definition r0.2 verification

Date: 2026-09-08. Manager: Codex. Result: document/source checks passed.
This review establishes packet consistency, not numerical validation or
approval of the complete method or upstream amendments.

## Scientific review

The owner assent is recorded as residual-relearning direction and targeted
paper-revision authority. Scope D001–D005 remains approved; the learning
schedule is not requested again. Other method choices, exact upstream
amendments and execution remain separate. The prior packet is preserved.

M04 now learns centering and correlated subspace from CAL at bootstrap and
from the newly constructed immutable residual at every feedback iteration.
The fixed recipe uses ordinary configured positive rank, prescribed centering,
identity internal scaling, one fit per pass/group and no support refinement.
The current resolved map is fixed for its Apply step; repeated fitting may
return the same subspace. The model affects the fit through the residual even
though its value path bypasses direct residual processing. Prior cleaned
residuals and bootstrap-state fallback are excluded.

BC-IN binds residual learning, resolution and application; BC-OUT retains
distinct rejoined-product admission. Current centering/reference state and
all parent/model/learning influence are carried across those boundaries.
RF-01/02 stay conditional on current Theta_k; RF-03/04/05 carry prescribed
learning, recursion and terminal dependence. RF-06/07 retain exact distinct
upstream versus parent-domain queries. The uncertainty and persistence records
include every pass's learned state. No new response/NOI product is claimed.

The proposed model selector, gridding prescription, projection, fixed required
populations, bootstrap/index/extent/failure and final-required-iteration rules
remain at their existing method gates. Relearning does not validate a noisy
all-support model or authorize another threshold, adaptive rank or reweighting.
Status prose separates method direction, contract admission, effective settings
and execution; no mathematical inability or implementation readiness is inferred.

## Integrity and preservation checks

- All 38 copied inputs match their exact source bytes, sizes and SHA-256.
  The 37 inherited source rows are unchanged. The one addition is the manager
  discussion. The 24-entry proposed-reference inventory is byte-identical;
  all three process-only flags agree with the full source records.
- Frozen FRUIT inventory/seven sources, MAP r0.7.1 freeze/manifest/modules/
  aggregate/boundaries, and PTC/CAL final status-clean source hashes match.
  No candidate upstream revision or future author input was imported.
- All nine method sections, twenty-one classes, seven crossings, eight U-items
  and six Q-items remain. Bootstrap/model selection, successful extent rule
  and coefficient-family definition match their predecessor text. Every
  historical-behavior table cell remains exact; only candidate dispositions
  change. Newly authored links, whitespace and navigation checks passed.
- Method-definition r0.1, scope r0.1/r0.2 and Stage B r0.4 archives retain
  exact hashes. Earlier packets and frozen core/PTC/CAL/MAP/JINC have no diff.
  Prior manager and Stage B worktrees retain their HEADs and tracked states.
  Both known untracked review archives remain present and opaque; neither
  was read, hashed or unpacked.
- The tar.gz contains unique safe regular members, each byte-identical to
  the payload or manifest/sidecar. Manifest and external archive SHA-256
  sidecars match. No reduction product or opaque archive is included.

Only this r0.2 paper packet and three navigation/status files change. No generic
contract/PDF set, author dispatch, algorithm test, experiment/replay, upstream
amendment adoption, implementation, qualification, Unity or push occurred.
