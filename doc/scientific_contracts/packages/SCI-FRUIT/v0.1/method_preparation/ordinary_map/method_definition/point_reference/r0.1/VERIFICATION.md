# POINT reference paper verification

2026-09-10. Base commit `1a9438f954ab081f4785037d91a97ba54914a0b8`.
Scope: paper recovery and draft consistency, not numerical validation or
implementation conformity. Local Python used `/Users/gwilson/tolteca/bin/python -B`.

## Identity and preservation checks

- All 57 payloads in the frozen ordinary-MAP r0.4 manifest match their recorded
  size and SHA-256. The accepted method and unadopted annex remain unchanged.
- Both earlier minimum-reference briefs, the review inventory and POINT
  acceptance match their four existing SHA-256 sidecars. The accepted r0.2
  object remains exactly `e4eff7ac42155bd14d58337cacc49ccbe24f49bd348e21ddf8b60f8e87e69b86`.
- The 54 repository/source entries in SOURCE_IDENTITIES.json match their named
  Git commit, size and SHA-256. Mutable navigation is identified at the recovered
  base, not silently rebound to this delivery's later edits.
- The preserved C31 binary and four external configuration/provenance files
  were read-only rehashed and match earlier recorded identities. No binary was
  executed, dependency environment claimed recovered, or scientific array read.
- The protected manager worktree remains at
  `6d7eec2f47c0af8b47907a8a446f225c3685915a`, tracked clean, with exactly the two
  known untracked archives present. Their contents were not read, hashed or
  unpacked. The protected author worktree remains clean at
  `6c997638d3e554db089790e8113b7ee03ac50a13`.
- The change is confined to this new paper-record directory and three current
  navigation files. No tracked frozen source, registry, scientific core,
  validation result or earlier review packet is modified. Existing reductions
  remain in place; 129081 scientific values remain unopened.

## Record and boundary review

- The method record has all 21 binding classes exactly once, with accepted,
  proposed and unavailable states. Rank 5, coverage_cut=0.1 and L=6 remain
  proposals; exact S/N and full effective-plan/input/resource bindings remain
  unavailable. No historical setting is silently selected.
- S1 is explicit in the B05 replacement and B09–B11 additions. The five residual
  PTC @2 candidates retain distinct named-use purposes and failure behavior.
  Ordinary @1 decisions are not residual-route permissions. Fresh QC binds
  retained coefficient values to actual current retention/product generations.
- Seven candidate keys are unique and absent from the selected registry. Each
  supplies owner, action, domain, restrictions/permissions, exceptions,
  response/uncertainty roles, missing behavior, lifecycle, aggregation and
  supersession fields. Every adopted-owner-source slot remains unavailable.
  MAP @3 preserves exact ordinary @2 predicates alongside the scoped new branch.
- The unchanged common named-use fragment supplies proposition semantics only;
  it does not authorize a changed parent or transfer permissions between uses.
- The historical control note keeps f70701ad separate from C31 and distinguishes
  occurrence-domain OR admission from the proposed pixel-domain S/N adaptation.
  It invents no significance, compatible noise scalar or new noise experiment.
- Frozen MAP support uses its declared sorted-index statistic and separate
  normalization/science predicates. Gamma, normalization Q_p, the total Q_k
  bundle and S/N scale are not conflated.
- Current navigation now points to this draft and records weighting closure
  and the accepted limited POINT substance. Historical review-time wording
  remains under its historical disposition.

Local Markdown links resolve, JSON parses, review payload sizes/hashes match
the manifest, and `git diff --check` passes. These focused checks are adequate
for this documentation-only change; no compilation, numerical test, replay,
qualification, Unity action, source adoption or independent-author dispatch
was performed. Final release/source identities and actual POINT applicability
remain prerequisites for numerical use, not presumed results of these checks.
