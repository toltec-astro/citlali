# Paper method-definition r0.1 verification

Date: 2026-09-08. Manager: Codex. Result: document/source checks passed.
This is proposal review and packet integrity, not scientific approval or
numerical validation. No algorithm test, experiment or replay was performed.

## Scientific review

The exact owner direction is recorded as scope approval D001–D005. The current
deliverable proposes M01–M09 and covers all twenty-one method classes, seven
boundary crossings, U01–U08 and six remaining questions. It does not request
another standalone scope cycle or reopen generic FRUIT.

The two-sided review checks CAL's calibrated parent, PTC's CAL-only primary
parent/ordinary fit, fixed affine application and coefficient/QC ownership,
and MAP's exact PTC-product input, normalization, support and coordinate roles.
It identifies BC-IN and BC-OUT as missing controlled permissions. External-
residual vocabulary and fixed-operator equations do not close those gates.
Reference/gauge embedding, coefficient family/QC, exact PTC plan and numerical
MAP support policy remain explicit missing bindings. No successor is adopted.

Concrete model copies, support-only selection, projection, state schedules,
coefficient populations/generations, current QC, missing values and failure
scope are proposed explicitly. The no-feedback bootstrap uses the frozen
narrower-composition provision with role records retained. L includes bootstrap;
success selects final required k=L-1, with no earlier-map rescue after failure.
The conditional normalized-MAP round trip concerns applied-model values only;
PTC affine centering and shared model/parent uncertainty remain visible. No
sky-response, precision, convergence or benefit inference is made.

The historical comparison marks new scientific choices and preserves U08's
mapmaker-versus-feedback distinction. It is not a comparison design. Manager
recovery remains outside future independent authorship; no author packet,
dispatch or new generic contract/PDF set was generated.

## Mechanical checks

- All 37 copied sources match their exact Git or attachment bytes, sizes and
  SHA-256. The proposed-reference inventory equals the 24 true-flag source rows.
  All three program references are true in both records and process-only.
- Frozen MAP r0.7.1 freeze, manifest/sidecar, wrapper, six modules, aggregate
  and three boundary/profile digests match. No candidate MAP source was added.
- FRUIT's seven-source inventory and canonical files match the conditional
  freeze. PTC/CAL reviewed modules match the final status-clean hashes in their
  freeze controls, including the recorded promotion wording changes.
- Scope/method/class/boundary/dependency/question counts, newly authored links,
  reference separation, authored whitespace and navigation checks passed.
  Copied-source context links are non-transitive; original whitespace is kept.
- Previous r0.1/r0.2 scope archives and Stage B r0.4 archive retain exact hashes.
  Frozen upstream/core and preceding packets have no diff. Earlier manager and
  Stage B worktrees retain their HEADs and tracked states. Both known untracked
  review archives remain present and opaque, without reading or hashing them.
- Archive member names are unique, relative, safe and regular; every member
  exactly matches its payload or manifest/sidecar. The external SHA-256 sidecar
  binds the final tar.gz. No reduction product or opaque archive is included.

Only this new paper packet and three current navigation/status files change.
No implementation/configuration, numerical route, owner-selected value,
qualification, production, Unity or push action was performed.
