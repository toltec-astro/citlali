# SCI-POINT v0.1 r0.3 Scientific-Owner Closure Directive

Date: `2026-09-02`

Scientific owner: Grant Wilson

Status: binding targeted Stage A repair; exact successor packet approval and
Stage B launch pending

The `r0.2` architecture is accepted subject to the following final repairs:

- retain `diagnostic_only` as a use-specific concept but encode it as
  prescribed consumer action `diagnostic_display_only`, never as a producer
  state or SCI-VAL eligibility value;
- preserve separate request, applicability, eligibility, and realization
  fields for every named use;
- add an exact asymmetric product-and-claim dependency matrix;
- create distinct `POINT-FULL-MAP-RMS-METHOD v0.1` with state
  `unavailable_pending_separate_owner_approval`, blocking only the descriptive
  dynamic-range diagnostic and legacy alias;
- require source association independently for every central, global, restart,
  retry, or other search branch;
- type known, isolated, bright, and approximately centered as applicability
  facts whose numerical tests may remain unavailable;
- mark MAP/JINC/FLT files as draft boundary requirements rather than bound
  numerical instances and supply exact parent signal-role names;
- split source association, fixed-branch response, full-procedure response,
  and observational bias/accuracy roles;
- make POINT amplitude only a candidate input to CAL/TolProj admission and use
  `fitted_amplitude_over_full_map_rms` as the canonical descriptive ratio;
- remove wording that implies unavailable numerical method details have been
  adopted; and
- add falsifiable asymmetric-gate predictions.

The repair does not recover or invent the compatibility, formal-error, or RMS
method; bind an exact parent-route instance; register a named-use profile; or
authorize Stage B. All existing ownership boundaries and nonclaims remain.
