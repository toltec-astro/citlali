# SCI-MAP To SCI-NOI Boundary

Boundary identity: `SCI-MAP_TO_SCI-NOI v0.1/r0.2`

Canonical spelling is exactly `SCI-MAP_TO_SCI-NOI v0.1/r0.2`.

Status: proposed sanitized Stage A boundary; exact bytes await owner approval;
no numerical parent is created or declared available

Scientific owner: Grant Wilson

## Exact Source Binding

This boundary is a compact scientific extract from frozen `SCI-MAP
v0.1/r0.7.1`, candidate commit
`bd010e20eb8a7901aa677810aa7a5c982a436e07`:

- freeze record SHA-256
  `91801005ba2f2bce6471a9f6f4ed0b79806c893f498b4f3cca9e81e26df39ce1`;
- source manifest SHA-256
  `bd3f172f8bb1e17bf95fde034ad76632439c766ef89b0a992361b9e6d79ada0a`;
- exact PTC-to-MAP boundary SHA-256
  `a499c59afb69eefda74a5b131ad37afd165213b99324c8b67a6de1d20793c9b7`;
- MAP upstream-admission profile SHA-256
  `0717476c0a1d177074ee8702c18308f093d45a4913b22933f3fda3d33090a883`;
- MAP coadd profiles SHA-256
  `d93c04488925931676b02dff433774ff2cda9846fdd1d3f34bff29d76efdd702`;
  and
- shared MAP requirements SHA-256
  `68acf81d6c27788495cc680a1819da5f269616493b9aba5a6e0a1d1058ba5fa7`.

These sources remain MAP authority. This file is the only MAP extract proposed
for the future NOI author packet.

## Admissible Parent Classes

The boundary may describe either:

1. one exact realized base/unfiltered observation MAP bundle; or
2. one exact realized centered-integer common-grid observation coadd bundle.

The parent binds exact MAP package/version, estimator and application
generation; observation/coadd membership; stable array/group; signal product;
normalization and accumulator identities; target WCS/frame; support-authorized
rows; coefficient, projection, grouping, boundary, and coadd policies; response
and covariance states; validity, causes, exposure roles, lifecycle, failure,
and immutable provenance.

The exact signal is the calibrated-`x`-derived nonpolarimetric
total-intensity-equivalent MAP quantity in `mJy/beam`, with its originating
fixed nominal beam/template and calibration lineage. A STOKES token does not
establish Stokes I.

The exact original-footprint exposure product names are
`upstream_eligible_original_footprint_exposure` and
`retained_original_footprint_exposure`; their frozen MAP aliases remain
unchanged. Neither product is precision or effective integration time.

## NOI Uses And Immutable Parentage

The realized-MAP GEN route has complete candidate method identity
`NOI-GEN/REALIZED-MAP-CONDITIONAL-SIGN@1`. It randomizes the exact realized MAP
quantity at the boundary named by that method. It is distinct from randomizing
PTC occurrences before a frozen MAP operator, even if an output happens to be
numerically equal. ODQ-102A selects the separate PTC-to-frozen-MAP route, not
this realized-MAP route. This route remains unselected and unavailable.

- GEN may attach to an exact MAP observation bundle, or may apply assignments
  to exact observation-map members before the frozen MAP coadd operator, only
  when its complete DAG names the earliest parent and insertion point.
- UNC may attach a versioned empirical uncertainty companion to the exact MAP
  parent or to a declared GEN ensemble. It does not rewrite MAP covariance,
  response, validity, support, or coefficient claims.
- STD may use the exact MAP normalized signal product as numerator only with an
  authorized compatible positive signal-unit UNC scale.

For a fixed-state claim, signal and realization paths use the same exact
eligibility, membership, projection, grouping, coefficient family/generation,
normalization, support-authorized row selection, boundary, atomic coadd
admission, WCS, response handling, and lifecycle generation. Any re-estimation
or reselection creates a different GEN method.

## Current Numerical Availability

Frozen MAP r0.7.1 does not presently authorize a general numerical ordinary
MAP route. It requires both:

- an exact PTC-owner-selected MAP-facing coefficient family/value/QC state; and
- the exact owner-admitted numerical `coverage_cut` state/value and failure
  policy.

Until those gates and exact runtime parent/evaluation states are realized, a
numerical MAP parent is typed unavailable for NOI. This boundary does not infer
unity, loading, sensitivity, scatter, inverse variance, precision, or a
`coverage_cut` value, and it does not synthesize a map.

The realized-MAP GEN route remains unavailable as an unselected, scientifically
incomplete route independently of parent availability.

## Fail-Closed Compatibility

Missing or conflicting product generation, membership, coefficient,
projection, support-row, WCS/frame, signal quantity/unit/beam, response,
covariance, validity, lifecycle, or provenance identity makes the affected NOI
route unavailable. A finite plane, filename, equal shape, or approximate WCS
does not establish a parent join. A later NOI product is a new immutable
companion and never modifies the MAP product or its claims.

This boundary establishes no implementation conformity, empirical calibration,
physical-noise validity, significance, performance, readiness, or production
authorization.
