# SCI-RTC-001 learned-sampling Stage A successor-3 repair handoff

Date: 2026-08-11

Handoff ID: `SCI-RTC-001-LEARNED-SAMPLING-STAGE-A-SUCCESSOR-3-REPAIR-001`

Status: owner-authorized handoff; role-separated repair launch pending; no
repair task created or launched by this record

## Exact repair authority

- Repair base: `cbb2fd767e0676906d1413ae84022270bee1a667`.
- Base parent: `66c96757164af2c83ee1449d00fea30d131a7e3f`.
- Base tree: `4727864c7ca4f078649fcf6473a7225d5d3aa9f8`.
- Base binary-patch SHA-256:
  `d1521fbc0a5afdfcfa61b41c57ba483b1d69969a45115829d2f8d973a51c9c39`.
- Verified pushed base ref:
  `origin/codex/repair-rtc-learned-sampling-stage-a-successor-2`.
- Proposed successor branch:
  `codex/repair-rtc-learned-sampling-stage-a-successor-3`.
- Independent return-for-repair audit:
  `f935f25c48b80861eb81a61133b20fc8e4fa4cf0`, tree
  `be21aff93642a6a419bf6a1d1b21f8ceff015257`.
- Owner acceptance:
  `SCI-RTC-001_LEARNED_SAMPLING_STAGE_A_SUCCESSOR_2_OWNER_ACCEPTANCE_2026-08-11.md`.

The exact report, evidence, and 15-row finding register are
`RTC_LEARNED_SAMPLING_STAGE_A_SUCCESSOR_2_REAUDIT_2026-08-11.md`,
`RTC_LEARNED_SAMPLING_STAGE_A_SUCCESSOR_2_REAUDIT_EVIDENCE_2026-08-11.md`,
and
`RTC_LEARNED_SAMPLING_STAGE_A_SUCCESSOR_2_REAUDIT_FINDINGS_2026-08-11.csv`.

The frozen predecessor handoffs and ledgers remain immutable. This handoff is
a narrow successor overlay and does not rewrite their bytes.

## Bounded objective

Close exactly the 15 audited findings `S2RA-001` through `S2RA-015`, which map
only to SRA-001 through SRA-009. Preserve every accepted Stage A scientific
decision and positive control. No new science decision is required or
authorized.

| Finding | Required bounded closure |
| --- | --- |
| `S2RA-001` | Capture the source carrier before detector-grid interpolation and bind it to the production observation lifecycle. |
| `S2RA-002` | Remove stale legacy-HWPR dependence from supported total-intensity preparation and make the unsupported-HWPR seam deterministic. |
| `S2RA-003` | Build every counterfactual from the exact native total-intensity RTC domain, realized filter, and phase rather than already coarsened PTC data. |
| `S2RA-004` | Apply the frozen guard/category precedence and serialize complete mutually exclusive exclusion counts and fractions. |
| `S2RA-005` | Preflight the exact invoked response/context work, transient allocation, serialized state, and manifest bytes with overflow-safe guards. |
| `S2RA-006` | Enforce full conditional shapes, cardinalities, compact state, and relational identity/digest bindings in the successor executable contract. |
| `S2RA-007` | Publish exact mode-specific successor contract and preparing-profile identities. |
| `S2RA-008` | Bind the product to canonical raw-input membership/identity bytes and validate the digest relation. |
| `S2RA-009` | Make required publication atomic across the rtcdiag generation and any unavoidable generation-specific manifest/pointer. |
| `S2RA-010` | Supply independent numerical oracles plus production A/B, B/A, failure, repeat, and permitted-parallel lifecycle/non-interference evidence. |
| `S2RA-011` | Derive realized cadence with an epoch-aware numerical method that admits normal native grids. |
| `S2RA-012` | Reset and report requested/effective/realized cadence and both consistency states independently. |
| `S2RA-013` | Refresh or fail exact 40-hex source identity for every ordinary build and validate it after write/reopen. |
| `S2RA-014` | Retain exact finite `Mmax` in a sufficient representation and return truthful resource-limit status without truncation. |
| `S2RA-015` | Validate observation index, obsnum, and telescope-path identity at the production consumer across lifecycle permutations. |

## Frozen Stage A boundaries

The repair must preserve:

- fixed diffraction FWHM authority and non-HWPR motion eligibility;
- the existing-filter counterfactual, factor-1 floor, phase convention, and no
  candidate-specific FIR synthesis;
- analytical or deterministic bounded/adaptive characterization with
  independent metric validity;
- no ranking, recommendation, factor acceptance, selection, or application;
- no Stage A use of an absolute stopband threshold or the provisional future
  `epsilon_alias` target as a gate;
- total-intensity-only capability and deterministic unsupported-HWPR handling;
- exact complete-context and cause-specific exclusion reporting;
- observe-only behavior with no science sample, flag, time, RTC/PTC/map input,
  weight, map, filter, cadence, factor, or execution mutation; and
- no `ptcdiag`, Stage B, production, Unity, reduction, BEAM, or downstream
  authority.

## Mandatory role-separated READY checkpoint

A separate application-repair task must start from the exact pushed base in a
fresh clean worktree. Before editing it must return:

- exact origin/local base, parent, tree, binary-patch digest, branch, worktree,
  and clean state;
- proof that the successor-3 branch did not pre-exist;
- an exact changed-path ceiling with each path and test mapped to one or more
  of `S2RA-001` through `S2RA-015`;
- the exact pre-interpolation capture and full observation-identity lifecycle;
- the native total-intensity validity, guard, factor, phase, cadence, and
  existing-filter authorities;
- overflow-safe work, allocation, serialization, and manifest-byte formulas;
- exact successor schema, contract, validation-profile, reason-vocabulary,
  canonical raw-input join, and 40-hex source-identity design;
- the complete generation-atomic publication and failure matrix; and
- independent numerical and production-boundary evidence traceability.

Silence prohibits a capability. Any new scientific choice, architecture
broadening, mature RTC numerical redesign, cross-cutting public `Engine` state,
or path not justified by the 15 findings is a stop for coordinator/owner
review.

## Path and validation boundary

The prior candidate's 23-path repair surface is not automatically reapproved.
The successor-3 READY checkpoint must choose the smallest exact subset and may
add a path only when one of the 15 findings cannot otherwise close. The repair
may touch application/configuration/test/validation files only after that
separate checkpoint is accepted; this documentation integration authorizes no
such edit.

After an accepted READY checkpoint, deterministic local implementation,
writer/reopen, contract/profile, resource, lifecycle, non-interference, full
CTest, configuration-preflight, baseline, ledger, authority, and diff gates may
run. No Unity or science reduction is an implementation gate.

Return one exact repair commit/parent/tree/patch digest, exact changed paths,
all commands/results, finding-to-change/test closure, remaining unavailable
states, and clean state. Stop for independent re-audit; do not merge, push,
change production, launch Stage B, or launch downstream work.

## Separation from core RTC and integration sequence

This Stage A repair is separate from the phase-independent core RTC repair.
The core phase-zero downsampling authority is recorded in
`SCI-RTC-001_CORE_PHASE_ZERO_DOWNSAMPLING_OWNER_AMENDMENT_2026-08-11.md` and
must not be imported into Stage A as a new repair finding.

Application integration remains MAP followed by accepted CAL consolidation.
Stage A remains separate until a successor is independently re-audited and
accepted.
