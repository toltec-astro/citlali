# SCI-MAP-001 MAP-UNITY-ED2 full/all PTC successor handoff — 2026-08-02

Status: owner-authorized continuation of the existing local task; no push or
Unity action authorized

Package: `SCI-MAP-001`

Evidence request: `SCI-MAP-001-UNITY-001`

Governing decision: `MAP-UNITY-ED2`

Predecessor protocol decision: `MAP-UNITY-ED1`

## Continuation identity

Resume the existing task, branch, and worktree only:

- task thread: `019fc2c5-ca8c-7270-96b9-d9c612c934fb`;
- task host: `local`;
- branch: `codex/map-unity-ed1`;
- worktree: `/Users/gwilson/.codex/worktrees/aa31/citlali-refactor`;
- exact clean resume commit:
  `3e014f11decbcf17ad372391e5e960e6c0c54461`;
- resume tree: `6db187a3f0f976cbd16dafbe17078438c0af1733`;
- resume parent:
  `1b824f138754eeb1856ae5f102027db4b31598be`.

Do not create a new audit, task, branch, worktree, package path, request ID, or
application candidate. Preserve the four returned stop artifacts as immutable
history inside the reserved successor package.

The exact application candidate remains
`ed28dafb37f9113c0d3c95297148157129a90886`, tree
`cf75c36557178f351fb62781108a6f4b41b19225`. The existing frozen predecessor
package remains tree `dbf486e30c9b78ca16e05bccafc2d027562d0746` with
checksum-list digest
`ecf080cce98ad3aef6d6dbf52e72dd53be5d659a40285ec6c9bfbb0aee185a69`.

The dispatch prompt supplies the exact coordination content and
identity-binding commits after this handoff is committed. Bind those identities
before implementation.

## Narrow supersession of the prior stop clauses

`MAP-UNITY-ED2` changes only three boundaries in the original ED1 handoff:

1. the two named segregated captures may make the exact output-only
   `enabled=true`, `mode=full`, `indices=all` configuration/product changes;
2. full PTC storage proportional to the captured primitive population is
   permitted only as temporary, retained input to the compact producer; and
3. the prior prohibition on inventing a ceiling is replaced by the explicit
   owner-approved hard ceiling of 200 GiB.

Every other `MAP-UNITY-ED1` invariant, acceptance gate, stop condition, and
non-authorization remains in force.

## Objective

Complete the existing sibling package at
`validation/campaigns/SCI-MAP-001-UNITY-001/repair-ed28dafb-ed1` so a future
human can, using one ordinary exact-candidate binary:

1. create fresh lightweight Point and Science TolProj source projects;
2. stage verified individual raw-file links and copied small ECSV authorities;
3. execute one segregated Point and one segregated ordered Science full/all
   PTC capture within the resource contract;
4. stream those captures into the nine compact observation/array evidence
   groups and preregistered every-active-network traces;
5. run the unchanged seven acceptance reductions; and
6. verify, package, and return the bounded external evidence without returning
   the retained full PTC products themselves unless focused expansion is
   requested.

This task prepares and locally tests the package. It does not perform any of
those future Unity actions.

## Required project and input staging package

Provide digest-bound JSON specifications and fail-closed human-run commands for
fresh TolProj projects:

- `SCI-MAP-001-POINT-SOURCE`: observation 152389, grouped under `1146+399`;
- `SCI-MAP-001-SCIENCE-SOURCE`: science observations 152390/152392 grouped
  under `NGC4449`, and pointing support 152389/152391/152393 grouped under
  `1146+399`.

Use the exact JSON content in `MAP-UNITY-ED2`. Commands and examples for remote
work must use the SSH alias `unity_toltec`. Keep the site config, Unity test
root, canonical raw root, allocation/account, and other genuine owner values
typed and unresolved until the owner supplies them.

Provide a bounded raw-link manifest generator/stager that:

- accepts an explicit owner-verified canonical raw root;
- resolves each required source to one canonical regular file;
- records observation, basename, resolved absolute target, size, identity, and
  SHA-256;
- rejects missing, ambiguous, non-regular, unresolved, duplicate-basename,
  pre-existing-destination, or identity-mismatched state;
- creates only individual file symlinks inside fresh project data directories;
  and
- never invokes `tolproj copy-raw` after link staging.

Do not use directory-wide symlinks or silently copy data from NESE. A missing
canonical file is a coordinator/owner stop, not permission for an alternate
transfer path.

Copy and digest-bind only:

- `apt_152389_matched.ecsv` for Point;
- `apt_152390_matched.ecsv` and `apt_152392_matched.ecsv` for Science; and
- exactly one `ppt_*.ecsv` for each pointing-support observation 152389,
  152391, and 152393.

The package must explain the authority and destination of each file and reject
ambiguous matches. Existing reductions remain reference material only.
In particular, the local validation suite at
`/Users/gwilson/work_toltec/local_data/citlali-validation/v1` may be inspected
read-only for source/configuration discovery. Any apparent Unity counterpart,
including `/work/toltec/citlali-validation/v1`, is a hypothesis the owner must
verify, not a fact the package may embed. Do not wholesale-rsync either old
Point or Science reductions into the evidence lane.

## Required capture lane

Prepare two auxiliary capture plans, not replacements for the seven fixed
cases:

| Capture | Observations | Role | PTC policy |
| --- | --- | --- | --- |
| `CAP-POINT` | 152389 | Point primitive authority | full/all |
| `CAP-SCIENCE` | ordered 152390, 152392 | Science primitive authority | full/all |

The science source project may contain pointing support 152389/152391/152393,
but those observations are not additional science captures or evidence groups.

Generate the capture overlays mechanically and prove the fully merged capture
config differs from its named fixed reference only at:

- `...processed_time_chunk.output.enabled` -> `true`;
- `...processed_time_chunk.output.mode` -> `full`; and
- `...processed_time_chunk.output.indices` -> `all`.

The comparison must cover all realized leaves, numbered-config order, and
included fragments; a text-only comparison of the small overlay is
insufficient. Any other effective difference stops the workflow.

Compile the ordinary exact-candidate binary once. Bind its candidate SHA,
build inputs, realized build identity, executable SHA-256, and version output,
then reuse the same binary for both captures and all seven cases. A second or
instrumented executable is forbidden.

## Effective-rate gate

Preserve and label processed-TOD `SAMPRATE` as native `telescope.fsmp`. Add an
independent, full-precision realized binding for finite positive
`telescope.d_fsmp` and use that effective rate for sample duration, exposure,
and mapmaking reconstruction. Record decimal and hexadecimal floating-point
representations and cross-check them against scan/sample cardinalities and the
candidate's realized plan. Do not patch the candidate or relabel the native
field.

If full PTC lacks another primitive required by ED1 or if effective rate cannot
be established package-locally, stop with exact evidence; do not infer the
fact from final FITS products.

## Compact producer and evidence contract

Consume the full PTC capture as transient primitive authority and retain the
ED1 compact output design:

- nine separately identified Point/Science observation-by-array groups;
- domain-separated complete-order/population streaming digests;
- per-scan/per-pixel binary64 sufficient statistics for signal, weight,
  kernel, upstream eligibility, retained exposure, and geometric/contributing
  hits;
- pinned 64-realization signs;
- deterministic first/middle/last-scan, every-active-network valid/flagged
  detector traces with typed absence facts;
- fixed trace budget and schema rejection of an accidental full-term axis;
- separately invoked, named, bounded focused expansion; and
- independent reconstruction of signal, formal weight, kernel, all retained
  F010 products and aliases, realizations, and centered coadds without final
  FITS payloads as authority.

Preserve every original seven-case inventory, WCS, coadd, product,
support-floor, provenance, sequential/OpenMP, tolerance, and exhaustive local
F011 gate.

## Hard 200-GiB resource gate

Define one aggregate capture/output root set and account for all generated full
PTC products, capture intermediates, logs, and compact successor artifacts
beneath it. Pre-existing canonical raw targets reached only via symlink are not
part of that generated footprint.

Before any future capture, fail closed unless:

1. current governed usage plus the metadata-derived projected incremental
   output of the next stage is at or below `214748364800` bytes;
2. the selected filesystem reports enough available space for that
   incremental output; and
3. all prior generated files under the governed roots are inventoried.

Measure logical apparent size and allocated usage after each stage, bind both
into the result manifest, and stop before the next stage if either exceeds
200 GiB (`214748364800` bytes). Do not delete evidence to remain below the
ceiling without returning to the coordinator.

Retain both full captures through fresh MAP re-audit and any requested focused
expansion. No script may automatically delete them. Provide only a guarded,
exact-target, human-run future cleanup plan whose eligibility requires a
recorded coordinator acceptance of the re-audit.

## Package, runbook, and verification requirements

Complete internally consistent successor versions of the campaign manifest,
schemas, evidence contract, capture/producer/analyzer tools, driver, wrappers,
verifier, TolProj specs, raw-link manifest/stager, owner template, runbook,
launch checklist, provenance, result collector, resource report, independent
review, and `SHA256SUMS`.

The package must provide explicit future commands for:

- owner push from the local repository;
- human `ssh unity_toltec` login and exact pull/fetch/checkout;
- one candidate compilation and identity capture;
- fresh project creation and verified link/ECSV staging;
- the two captures, seven cases, compact production, verification, and result
  collection; and
- returning only the bounded evidence bundle.

These are instructions, not permission to execute them in this task. Do not
embed actual secrets or guess owner values.

At minimum, local synthetic/metadata validation must:

1. verify the original frozen package remains unchanged;
2. verify all returned stop artifacts remain unchanged;
3. verify the successor package and checksum list;
4. prove application/build surfaces and candidate identity are unchanged;
5. prove the seven-case contract is unchanged;
6. prove the capture-config realized-diff allowlist;
7. test fresh-project, raw-link, APT/PPT, missing/ambiguous/pre-existing, and
   no-`copy-raw` failure paths;
8. test effective/native rate separation and invalid-rate failures;
9. test 200-GiB projection, capacity, cumulative-usage, and retention gates;
10. test compact parity, deterministic selection, every-network coverage,
    class absence, tamper/reorder/non-finite/chunk-invariance, and bounded
    focused expansion;
11. prove the driver cannot contact Unity or submit work; and
12. obtain an independent read-only review of claims, identities, resource
    accounting, runbook safety, and stop conditions.

Use `$HOME/tolteca/bin/python` for local Python. Local results are not Unity
evidence.

## Stop conditions and forbidden actions

Stop and return to the coordinator if implementation requires:

- an application or build-configuration edit;
- a different candidate or second/instrumented binary;
- any fixed-case, observation, array, product, tolerance, numerical, or
  scientific-contract change;
- an effective capture-config difference outside the three-leaf allowlist;
- inference of missing primitive authority from final products;
- a footprint above 200 GiB or unproved capacity;
- missing or ambiguous raw/APT/pointing/effective-rate authority;
- an alternate raw-data transfer path;
- automatic or pre-acceptance cleanup; or
- another scientific or operational owner choice.

Do not push, contact Unity, transfer files, build or reduce remotely, submit
Slurm work, fill owner values, integrate the repair, claim external evidence,
close findings/dependencies, launch re-audit, expand production, or execute
cleanup.

## Handback

Return one coherent implementation commit and, if required, one final
identity-binding commit with:

- exact branch, parent, commit, and tree identities;
- successor package tree, member count, checksum-list digest, and changed-path
  inventory;
- unchanged-candidate, application/build-surface, frozen-predecessor,
  stop-artifact, and seven-case proofs;
- exact TolProj specifications, capture-config diff, effective-rate, storage,
  retention, and cleanup-plan contracts;
- verifier, self-check, focused-test, and independent-review results;
- measured local cardinality/storage/runtime evidence and projected Unity
  resource envelope;
- complete human-only `unity_toltec` runbook; and
- all negative confirmations.

Stop after handback. The coordinator reviews it before asking the owner to push
or supply any Unity operational action.
