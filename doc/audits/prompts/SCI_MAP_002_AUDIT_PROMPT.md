# SCI-MAP-002 scientific-contract audit dispatch

## Execution profile

- MODEL: `Sol`
- EFFORT: `XHigh`
- TASK SHAPE: bounded scientific audit
- MISSION: Independently audit JINC gridding, normalization, response,
  support, array-specific parameter behavior, and shared-map-product
  conformity at the frozen Citlali source identity.
- SCIENTIFIC AUTHORITY: The task may derive and assess the JINC contract. The
  completed naive-only MAP result is post-core context only; it is not JINC
  authority.
- ULTRA TRIGGER: none. Return before any decomposable independent
  multi-workstream study or expensive parameter campaign.
- PARALLELISM: none.
- STOP RULE: stop at the required scope checkpoint before substantive audit
  work, then again at the first viable artifact and before any costly/local
  execution, external evidence request, new helper/schema/validator, or
  scientific decision brief.
- EXPECTED OUTPUT: first, the scope checkpoint only; later work requires a
  separate coordinator direction.
- DOWNSTREAM RESTRICTIONS: SCI-NOI-001/002 and SCI-FLT-001/002 remain
  conditional on a future JINC contract. No JINC conclusion expands
  production use.

## Mandatory scope checkpoint

Before substantive work, return these seven items and stop:

1. exact allowed paths and the smallest deliverable;
2. whether local Citlali reductions are prohibited (default: prohibited);
3. whether Unity evidence is required now (default: not at this checkpoint);
4. named static tests/evidence only;
5. permitted delegation/review (default: none);
6. the first viable artifact; and
7. the next coordinator return point.

Do not inspect application source or the post-core handoff at this checkpoint.
Do not create a branch, worktree, file, helper, schema, validator, campaign,
or evidence request. The coordinator will explicitly authorize the next
phase after reviewing this return.

## Frozen assignment

- Package: `SCI-MAP-002` — JINC gridding interface, normalization, and
  response.
- Tier: B.
- Canonical repository: `/Users/gwilson/GitHub/citlali-refactor`.
- Authority branch/source SHA: `codex/refactor-mainline` at
  `9aae0e669384c5c0c0dda93debc194d6b8dac787`.
- Required future audit branch: `codex/audit-sci-map-002`.
- Frozen inbox manifest:
  `doc/audits/handoffs/SCI-MAP-002/SCI-MAP-002_INBOX_MANIFEST_2026-08-03.yaml`.
- Pre-core authority handoffs: none.
- Post-core evidence handoffs: `SCI-MAP-002-XAUD-001` only.

Included future audit scope: the JINC deposition interface; footprint and
subpixel response; normalization; signed/zero/non-finite coefficient and
support semantics; edge behavior; sequential/parallel equivalence;
array-specific parameter identity and selection; realized configuration; and
the product/provenance interface to downstream noise and filtering consumers.

Explicit exclusions: naive-mapmaker internals, the general MAP coadd repair,
RTC/PTC internals, noise-realization and empirical-covariance algorithms,
post-map convolution/Wiener filtering, calibration/astrometry/flagging
science, source fitting, Beammap inference, and parameter optimization as an
execution campaign. An observational or computational parameter study must
return for a separately scoped `FRAMEWORK-NUM-001` decision.

Future known implementation paths, quarantined until the independent core is
frozen: `include/citlali/core/mapmaking/jinc_mm.h`,
`include/citlali/core/pipeline/mapmaking_dispatch.h`, and
`include/citlali/core/pipeline/mapmaking_method_config.h`.

## Hard boundaries for every later phase

Read and obey the repository `AGENTS.md` and the TolTEC context skill. The
user performs all Unity work; do not connect to Unity. Do not edit Citlali
application code, tests, build files, production documentation, candidate
branches, or another task's worktree; do not push, merge, rebase, or use the
network. Preserve mature JINC numerical behavior unless a future,
evidence-backed repair is separately authorized.

After the checkpoint is approved, the independent core must be frozen before
opening JINC implementation paths or `SCI-MAP-002-XAUD-001`. It must define
the estimator, units, indexing, flags/validity, coefficient/support rules,
response, uncertainty limits, requested/effective/realized state,
array-parameter identity, and falsification cases. The naive-only MAP
handoff may then be opened as post-core evidence; it cannot be promoted to a
JINC result.
