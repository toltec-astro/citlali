# SCI-NOI-002 bounded successor repair dispatch prompt — 2026-08-06

This is a bounded implementation-repair task, not a re-audit, application
integration, evidence request, numerical reduction, Unity task, production
change, recipient-audit dispatch, or default/count-selection task.

## Execution profile

- MODEL: `gpt-5.6-sol`
- EFFORT: `ultra`
- TASK SHAPE: `bounded_cross_path_contract_repair_and_exact_fixture_synthesis`
- MISSION: Implement only the owner-decided SCI-NOI-002 F001/F002/F003/F004/
  F005/F007 contracts and proportional F008 local fixtures at exact application
  base `d5015fe716971bf8ea617e8a187311bf5af05185`.
- ULTRA TRIGGER: Finite-stack identity, global scale, package provenance,
  product aliases, filter projections, and count-completion behavior cross map,
  pipeline, output, and configuration paths and require one reconciled bounded
  repair; Ultra ends at the repair/test commit and coordinator return.
- PARALLELISM: serial; no delegation or subagents.
- STOP RULE: Commit only the bounded repair and proportional local tests, report
  exact commit/digests/gates/clean state, and stop for coordinator review before
  fresh re-audit.

## Frozen authority and entry gate

1. The coordinator prepared this dispatch but has **not** launched it. On
   launch, create a fresh isolated repair worktree at exact
   `d5015fe716971bf8ea617e8a187311bf5af05185`
   (`origin/codex/refactor-mainline`), never from an audit or coordination
   branch. Create `codex/repair-sci-noi-002` only if absent and only in the
   clean new worktree. Stop on any mismatch.
2. Verify every authority entry and digest in
   `doc/audits/handoffs/SCI-NOI-002/SCI-NOI-002_REPAIR_AUTHORITY_MANIFEST_2026-08-06.yaml`,
   SHA-256 `6f4b84995c8cb118bdb182b9189c4b95464c4fe7e414b762debb8786e825ce79`,
   directly from the named Git objects. The coordinator authority is
   `64ba81795110d89d8baf0ad7d645d16472c254c5`; do not assume those dispatch
   documents are present in the application-base checkout.
3. Read `AGENTS.md`, the TolTEC context skill and routed repository
   authorities, `doc/REFACTOR_STATUS.md`, `doc/audits/README.md`,
   `doc/audits/AUDIT_MANAGER_INSTRUCTIONS.md`, and the frozen authority
   manifest before code or test inspection.
4. Return a concise scope checkpoint before the first code or test edit. It
   must give worktree path, starting HEAD, branch state, clean state, verified
   authority digests, allowed findings/paths, prohibited scope, first focused
   tests, and planned return point. Do not proceed past that checkpoint without
   explicit coordinator continuation.

## Allowed repair surface

The included findings are F001, F002, F003, F004, F005, F007, and F008 only.
The only initially allowed components are:

- `src/citlali/core/mapmaking/map.cpp`;
- `include/citlali/core/pipeline/timestream_scan_generation.h`;
- `include/citlali/core/pipeline/noise_execution_plan.h`;
- `include/citlali/core/pipeline/map_filtering_template_noise.h`;
- `include/citlali/core/pipeline/map_image_output_helpers.h`;
- `include/citlali/core/pipeline/map_source_table_output.h`; and
- `include/citlali/core/timestream/timestream.h`.

Use only the existing directly associated tests/fixtures, including the named
manifest paths, and only direct references required to complete this contract.
Do not add a helper, schema, wrapper, verifier, framework, or new product class.
Stop and return if work would broaden FLT, SRC, FRUIT, MODE, MAP, JINC, RTC, or
PTC mathematics, or change a configured count/default.

## Included repair contract

- **F001 / D001:** Implement the approved conditional finite-stack scatter
  identity for the completed `source_imprinted_current` stack. Bind exact
  target, centering, divisor, joint design, support, requested/effective/
  completed count, and validity facts. R=1 may have zero descriptive scatter
  but is invalid for uncertainty/S/N use. Never present it as physical-noise
  variance/covariance, inverse variance, precision, significance, aperture
  uncertainty, or calibration.
- **F002 / D002:** Preserve reciprocal-median scaling only as
  `global_nonprecision_scale_diagnostic`. Empty/nonfinite/invalid calibration
  is unavailable, never successful `alpha=1`; never call the scaled coefficient
  precision. Preserve overwrite behavior only where explicitly
  `existing_use_only`, retaining MAP/FLT-dependent claims as restricted.
- **F003 / D003:** Implement only the qualified package-level provenance
  contract. The reduction package/manifest carries full semantics once; each
  product carries stable package/provenance and product-identity/digest/scope
  joins plus minimal detached-product validity/restriction safeguards. Do not
  duplicate full metadata or persist dense covariance, sign streams, or
  per-sample IDs.
- **F004 / D004:** Give coefficient-standardized signal, conditional
  stack-scatter ratio, pooled stack-scale diagnostic, source-finder score, and
  fitted-amplitude/map-RMS ratio distinct internal/product identities. Legacy
  `sig2noise` aliases may remain only with explicit identity, deprecation, and
  not-significance metadata. Invalid/zero denominators are unavailable, never
  numeric zero. Preserve existing quicklook/engineering heuristics without
  expensive calculations. Do not claim significance, false-positive
  probability, catalog completeness, or universal N-sigma behavior; SCI-SRC-001
  owns those claims.
- **F005 / D005:** Identify current `point_source_uncertainty` truthfully as
  `filtered_pixel_stack_scatter`. Apply the identical realized filter/operator
  and edge treatment to signal, every realization, and kernel/response. Add
  only fixed aperture-plus-background/template projection hooks or small exact
  fixtures actually feasible in this surface. Realization-level scalar
  projections, not diagonal summation or dense covariance, are the admitted
  route. These remain conditional finite-stack diagnostics pending physical-
  noise validation and FLT closure.
- **F007 / D007:** Treat counts as configuration requests, not
  production/scientific values. Preserve requested/effective/completed and
  actual-completion validity; disabled modes and standard Beammap retain
  effective-zero/no-work behavior. Do not select or change Science, Pointing,
  OOF, Beammap, 10/5/64 defaults, or counts.
- **F008 / D008:** Add only exact/analytic local fixtures for R=1 invalidity,
  R=2 normalization, duplicate/complementary/simple-independent designs,
  count-state handling, empty calibration regions, invalid denominators,
  statistic identities, filter/operator parity, a two-pixel correlated
  aperture case, fixed aperture/template projection, and the FRUIT
  configuration dependency where in scope. Do not invent empirical tolerances
  or run an astronomical campaign.

## Explicit F006 and adjacent-package boundary

F006 algorithmic/statistical closure belongs to SCI-FRUIT-001. If safely direct
in the allowed files, this repair may preserve only the interface prerequisite:
internal scale calculation independent of product persistence and explicit
failure when a requested nonzero gate lacks a valid scalar. Do not alter FRUIT
algorithm, threshold, default, iteration, bright-source add-back, stopping
law, or choose a successor estimator. MEDRMS remains the naive baseline; the
future bright-source map-uncertainty requirement remains SCI-FRUIT-001-owned.

Do not alter FLT mathematics, filter algorithms, MAP/JINC/RTC/PTC/MODE
algorithms, any current configuration default, or any held recipient contract.
No local/Unity reduction, astronomical evidence, re-audit, integration, push,
or production action is authorized.

## Required local gates and return

Use exact/analytic assertions where possible. Run only the manifest's
proportional gates: focused R/design/count/invalidity fixtures; package join and
alias tests; filter/operator/kernel parity; two-pixel and fixed-projection
fixtures; the bounded FRUIT configuration dependency test; focused CTest;
required config preflight; and a proportionate `citlali_cli` build. Record an
omitted named gate as `not_applicable` with a scope reason.

Commit application and focused-test changes only on `codex/repair-sci-noi-002`.
Report exact repair commit and parent, changed paths, test commands/results,
product-package/provenance fields, F006 interface disposition, open findings
and exclusions, any held evidence, and clean state. Stop before re-audit,
evidence, push, integration, or production authorization. A fresh re-audit
branch `codex/reaudit-sci-noi-002` requires separate coordinator authorization
after this repair returns.
