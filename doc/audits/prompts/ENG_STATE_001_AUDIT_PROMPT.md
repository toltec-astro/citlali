# ENG-STATE-001 scope-checkpoint dispatch

This starts only the required `FRAMEWORK-SCOPE-001` scope checkpoint for a
future Tier C audit. It does not authorize audit artifact creation,
application/source inspection, a local Citlali reduction, Unity work,
delegation, an independent review, repair, integration, or a push.

## Execution profile

- MODEL: Terra
- EFFORT: High
- TASK SHAPE: bounded engineering
- MISSION: Define the smallest no-code audit plan for Citlali lifecycle,
  provenance, required-product publication, and structured failure flow.
- SCIENTIFIC AUTHORITY: The package may audit requested/effective/
  observation-resolved/realized state and failure propagation; it may not
  redefine an estimator, scientific contract, units, response, validity, or
  production authorization.
- ULTRA TRIGGER: none
- PARALLELISM: none
- STOP RULE: stop immediately after the scope checkpoint.
- EXPECTED OUTPUT: a concise scope-checkpoint handback only.
- DOWNSTREAM RESTRICTIONS: every scientific package retains its current
  consumer restrictions and status axes.

## Immutable dispatch facts

- package: `ENG-STATE-001`, Tier C;
- governing application SHA:
  `9aae0e669384c5c0c0dda93debc194d6b8dac787`;
- authority branch: `codex/refactor-mainline`;
- audit branch reserved for a later authorized audit:
  `codex/audit-eng-state-001`;
- coordination baseline:
  `a445f25c3ef85f0785864e7e53ec52a81b5a8dee`;
- frozen empty inbound manifest:
  `doc/audits/handoffs/ENG-STATE-001/ENG-STATE-001_INBOX_MANIFEST_2026-08-03.yaml`;
- manifest SHA-256:
  `4c0a6cfb049e9b3ac0a2ed2884dce226e2a5599572714c12fb63cd52a5f96b3b`.

The included future audit scope is requested/effective/observation-resolved/
realized authority, lifecycle ownership, exact-SHA provenance, required
product publication, reproducibility, and structured failure propagation. It
excludes numerical estimators, calibration, astrometry, timing, validity
semantics, MAP/ALIGN/CAL repairs, all reductions, and any new framework or
validation harness.

## Required checkpoint

Before opening Citlali source or creating any file, verify the repository and
the immutable facts above; read `AGENTS.md`, the TolTEC context skill, the
audit README, Audit Manager instructions, the ENG-STATE ledger record, and the
frozen manifest. Then return exactly:

1. a proposed list of already-existing paths to inspect in a later audit;
2. the one final report and one proposed ledger-update artifact a later audit
   would create;
3. named no-code inspection commands and any focused header/config/test gates;
4. confirmation that local Citlali reductions are prohibited and Unity
   evidence is not required for this initial audit; and
5. the first viable artifact and the next coordinator return point.

Do not inspect implementation source, create a worktree or branch, edit,
compile, test, use the network, contact Unity, delegate, or begin the audit.
Stop after this handback. If the package cannot be bounded without touching an
adjacent scientific package, name that exact dependency rather than expanding
scope.
