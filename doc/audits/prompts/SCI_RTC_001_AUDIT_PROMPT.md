# SCI-RTC-001 mature-conditioning interface audit dispatch

This dispatch starts one bounded, independent Tier B scientific-interface
audit. It does not authorize implementation, repair, integration, production,
external contact, or another audit.

## Execution profile

- MODEL: `sol`
- EFFORT: `ultra`
- TASK SHAPE: `decomposable_scientific`
- MISSION: independently derive and then audit the exact mature RTC
  conditioning interface and temporal response at application SHA
  `46ad23888a40f5102cdfd50c06e49a549bdf8a20`.
- SCIENTIFIC AUTHORITY: only the approved bounded ALIGN, CAL, and AST objects
  in the frozen authority manifest may inform the pre-source derivation;
  historical behavior and the three submitted cross-audit records are evidence,
  not mathematical authority.
- ULTRA TRIGGER: the audit must reconcile four genuinely independent hard
  investigations—mathematical temporal/response derivation, exact
  source-to-contract tracing, product/validity/provenance tracing, and
  PTC/VAL/BEAM consumer synthesis. XHigh or Max is insufficient because no one
  of these workstreams can substitute for the other three, yet one final
  interface verdict must reconcile them without silently widening mature RTC
  numerical internals.
- PARALLELISM: `none`. Delegation and subagents are prohibited. The auditor
  keeps the four workstreams logically separate, completes the independent
  mathematical core first, and is the sole reconciliation owner.
- STOP RULE: stop at every scope checkpoint, after the final owner-decision
  brief, and before any repair, re-audit, broad test execution, costly study,
  external request, or other package launch.
- EXPECTED OUTPUT: the exact eight artifact paths listed below, their SHA-256
  digests, the independent-core freeze commit, the final audit commit, and one
  concise owner-decision brief.
- DOWNSTREAM RESTRICTIONS: PTC follows RTC; VAL follows frozen RTC/PTC
  interfaces; BEAM follows frozen PTC/VAL interfaces. All remain
  `existing_use_only` or otherwise fail closed under their existing records;
  this audit cannot expand them.

This Ultra choice is recorded under `FRAMEWORK-EFFORT-001`. Ultra ends when
the four workstreams have been reconciled into the final audit and owner brief.

## Mandatory scope checkpoints

Before any substantive work, return a concise checkpoint and stop for
coordinator direction. It must state:

- allowed paths: only the seven deliverable paths below, plus an untracked
  temporary PDF rendering and ordinary Git metadata;
- local Citlali reduction: prohibited;
- Unity evidence: prohibited and not required for this dispatch;
- application, test, build, validation, configuration, and production edits:
  prohibited;
- allowed tests: digest/YAML/link checks, offline LaTeX compilation, static
  source probes after the independent-core freeze, and focused deterministic
  existing RTC tests after the freeze if they require no application edit or
  broad build/test campaign;
- permitted delegation or external review: none;
- first viable artifact: the frozen and committed
  `SCI-RTC-001_INDEPENDENT_CORE.tex`; and
- next return point: immediately after that freeze and before opening any
  quarantined source or post-core handoff.

The three mandatory coordinator returns are:

1. before source inspection, following the independent-core freeze;
2. at the first viable artifact, which is that same frozen core and its exact
   digest/commit; and
3. before any broad, costly, local-reduction, or external execution. Broad or
   costly execution is not authorized by this dispatch.

Return before adding any new artifact class, executable helper, schema,
wrapper, verifier, validation campaign, delegation, independent review,
reduction, external request, or scientific/operational interpretation not
named here. Silence prohibits a capability.

## Assignment and exact identities

- package ID: `SCI-RTC-001`
- package name: raw-timestream conditioning interface and temporal response
- tier: `B`
- canonical repository: `/Users/gwilson/GitHub/citlali-refactor`
- canonical pushed application ref: `origin/codex/refactor-mainline`
- governing application SHA: `46ad23888a40f5102cdfd50c06e49a549bdf8a20`
- governing application parent: `4846fa4db39bd2f7d4ddc41f693836834cbc5ff4`
- governing application tree: `ab230a93b8fb310d58aefd7ac5da92e5d5e0f408`
- audit branch: `codex/audit-sci-rtc-001`
- suggested fresh worktree: `/private/tmp/citlali-audit-sci-rtc-001`
- coordinator ledger and registry snapshot:
  `dd5894679bf12bf4a5fb551e871b3c6010ef9b9b`
- frozen inbox/authority manifest:
  `doc/audits/handoffs/SCI-RTC-001/SCI-RTC-001_INBOX_AUTHORITY_MANIFEST_2026-08-08.yaml`
- manifest SHA-256:
  `9f75e5df0184d73e90acec10cbf8c052fe7baad571a131ba8834f2acbbbccb1c`
- pre-core authority IDs: `ALIGN-OD1-OD8-C001`, `ALIGN-P0-D001`,
  `ALIGN-P0-D002`, `ALIGN-P0-D003`, `ALIGN-P0-D004`, `ALIGN-P0-D005`,
  `CAL-D001-D005`, `CAL-D001-OPACITY-001`,
  `CAL-D002-IDENTITY-001`, `CAL-D001-ACCURACY-001`, and
  `SCI-AST-001-D001-D008`
- post-core evidence IDs: `SCI-RTC-001-XAUD-002`,
  `SCI-RTC-001-XAUD-001`, and `SCI-RTC-001-XAUD-003`
- planned numerical/external studies: none. Static inspection and focused
  deterministic tests are not costly. Any proposed costly study is held until
  a separate `FRAMEWORK-NUM-001` register, guard census, model-free preflight,
  salvage plan, independent review, readiness certificate, launch-gate pass,
  and owner authorization exist.

The current application line contains only the bounded integrated states of
SCI-MAP-001 and SCI-NOI-002 recorded on that line; both remain
`existing_use_only`. CAL and MAP-002 repair tasks are active from the exact
application SHA above. ALIGN/3C273 evidence is owner-managed and active; AST
repair waits for an accepted and re-audited ALIGN successor. No result from an
active task is supplied or may be inferred here.

## Included scope

Audit the mature RTC interface from aligned raw detector samples to the exact
conditioned outputs presented to PTC, VAL, and BEAM. The audit must cover:

- calibration invocation order, factor identity, units, response, validity,
  and provenance at RTC entry;
- despiking, donor selection, target replacement, responsivity conversion,
  replacement validity, flags, and consequential covariance/response;
- FIR, IIR, and notch selection and ordering, coefficient/normalization
  identity, initial/final conditions, edge guards, masking, and finite policy;
- source-mask construction and its coordinate, frame, detector identity,
  validity, and temporal-support contract;
- downsampling/decimation, anti-alias requirements, sample-time/support
  identity, phase, rate, indexing, flags, kernels, and transfer response;
- the combined temporal transfer/response, including passband, phase/delay,
  impulse support, masked/replaced samples, edge loss, and any data-dependent
  or non-LTI behavior;
- requested, effective, observation-resolved, and realized filter/mask/state
  identity and exact provenance;
- output TOD/kernel/flag/diagnostic identity, shapes, units, indexing,
  validity, non-finite and missing policy; and
- exact downstream PTC cleaning/weight, VAL eligibility, and BEAM detector-map
  interface contracts.

## Explicit exclusions

- Do not audit or redesign mature RTC kernel internals unless a concrete
  interface/response contradiction supplies a named Tier-A reopen trigger.
- Do not audit PTC correlated-mode/PCA mathematics, VAL package-wide policy,
  BEAM fitting/calibration, mapmaking, JINC, noise estimation, filters, source
  fitting, mode estimators, or fruit-loop feedback.
- Do not audit application science in the coordination task that prepared this
  prompt; the fresh auditor owns this bounded RTC audit.
- Do not modify application code, tests, build files, validation artifacts,
  configuration, production documentation, or canonical audit coordination.
- Do not repair, re-audit, integrate, push, merge, rebase, cherry-pick,
  install/download software, contact external systems, use Unity, launch local
  Citlali reductions, contact people, or delegate.

## Upstream dependency facts and consequences

- `SCI-ALIGN-001` supplies only the approved bounded common-grid, timing,
  mapping, origin/validity, gap, scan, synthesized-sample, and support
  abstractions listed in the manifest. Its implementation/evidence remains
  active and `existing_use_only`; RTC conformity and new timing/support claims
  remain conditioned.
- `SCI-CAL-001` supplies only the approved bounded calibration, extinction,
  responsivity, factor-identity, unit, and validity abstractions listed in the
  manifest. Its new repair is active from `46ad2388...`; RTC absolute response,
  replacement, and calibration claims remain conditioned until an accepted,
  re-audited, integrated successor exists.
- `SCI-AST-001` supplies only the approved bounded coordinate, frame,
  detector-association, topology, and validity abstractions listed in the
  manifest. AST repair is held behind ALIGN; coordinate-dependent RTC claims
  remain conditioned.

The three `SCI-RTC-001-XAUD-*` records govern historical/bounded observations
at their recorded evidence SHAs. They are not evidence of behavior at
`46ad2388...` and remain closed until the independent core is frozen.

## Quarantined implementation paths

These paths are listed only to establish the quarantine and were verified to
exist at the governing application SHA:

- `include/citlali/core/timestream/rtc/rtcproc.h`
- `include/citlali/core/pipeline/raw_timestream_execution_plan.h`
- `include/citlali/core/pipeline/raw_timestream_provenance.h`

Do not open their contents, diffs, blame, callers, tests, or direct references
before the independent-core freeze. After the freeze, expand only through
direct static references needed to close the listed RTC interface, and record
every additional path plus why it was opened.

## Phase 1 — genuinely independent mathematical core

First verify every path, digest, object commit, status, and phase in the frozen
manifest. Open only the listed pre-core authority objects. Record prior RTC
source or post-core exposure from any context; do not claim independence if
the exposure record cannot support it.

Create exactly:

`doc/audits/packages/SCI-RTC-001_INDEPENDENT_CORE.tex`

Before source inspection, derive a complete interface model. It may use a
stateful operator such as

```text
s_(k+1) = G_k(s_k, x, m, f, theta)
y = D F_K ... F_1 R C A x
```

but must define each operator rather than inherit code names. At minimum:

1. define acquired, aligned, calibrated, replaced, filtered, downsampled, and
   output sample identities, shapes, order, time support, units, frames,
   indexing, validity, and missing/non-finite policy;
2. classify each calibration factor, donor/target response, mask, flag,
   coefficient, initial condition, state, sample rate, phase, and source
   coordinate as fixed, selected, fitted, data-derived, random, or conditioned
   upon;
3. derive the signal operator, offset term, expectation, formal variance and
   full covariance, systematic/calibration and response uncertainty, including
   what is unavailable rather than silently diagonal or zero;
4. derive the impulse, frequency, amplitude, phase/group-delay, edge,
   mask/replacement, downsampling, and combined temporal response, with explicit
   conditions under which an LTI transfer function does or does not exist;
5. distinguish sample validity, detector validity, flags, masks, replacement,
   numerical computability, temporal support/exposure, confidence, variance,
   inverse variance, response, and provenance;
6. define requested, effective, observation-resolved, and realized state;
7. define provisional PTC, VAL, and BEAM input contracts and fail-closed
   restrictions; and
8. preregister analytic limits and deterministic falsification cases for
   identity/no-filter, impulse, constant/ramp, sinusoid/passband, notch,
   despike donor/target, invalid-coordinate mask, edge, short scan,
   non-finite, flag, odd/even downsample, phase, sequential/OpenMP, and
   chained-filter cases. Give a specific rationale for every omitted standard
   method.

Freeze the exact bytes, compute SHA-256, commit them alone, report the path,
digest, freeze commit, timestamp, prior-exposure statement, and next intended
open, then stop. Corrections after freeze are successor revisions; do not
rewrite the frozen bytes.

## Phase 2 — exact source-to-contract and evidence review

Proceed only after explicit coordinator approval of the frozen core. Record
the first opening time for source and post-core evidence. Verify all three
handoff digests again and disposition every record without promoting it beyond
its bounded claim.

Trace exact source at `46ad2388...` against numbered core equations through:

- calibration call ordering and per-sample/per-detector factor application;
- despike detection, donor selection, replacement arithmetic and flags;
- every enabled FIR/IIR/notch path, combined ordering, state, edge guard,
  masking, sequential/OpenMP alternative, and kernel/provenance writer;
- source masks and coordinate/validity admission;
- downsample rate, phase, support, flag aggregation, kernel propagation, and
  anti-alias/transfer implications;
- simulation, diagnostic, full/mini/raw/processed output paths that materially
  exercise the RTC interface;
- configuration resolution and requested/effective/observation-resolved/
  realized provenance; and
- PTC, VAL, and BEAM callers and fail-closed boundaries.

Use focused deterministic static probes and existing tests only after the
freeze. Record command, exact SHA, scope, and result in the local-evidence
YAML. Stop before any broad build/test suite; the coordinator must decide
whether it is necessary and, if costly, require the full
`FRAMEWORK-NUM-001` launch gate. A test pass cannot replace the contract or
authorize production.

Classify each finding as `implementation_defect`, `contract_gap`,
`scientific_policy_decision`, `evidence_gap`, or `dependency_gap`, with P0-P3,
basis, confidence, owner, dependency, and falsifiable closure gate. A known
mismatch is `nonconformant`; a conditional conclusion names the exact stable
dependency fact and test.

## Exact deliverables

Create only these paths:

1. `doc/audits/packages/SCI-RTC-001_INDEPENDENT_CORE.tex`
2. `doc/audits/packages/SCI-RTC-001_SCIENTIFIC_CONTRACT_AUDIT.tex`
3. `doc/audits/evidence/SCI-RTC-001_LOCAL_EVIDENCE_2026-08-08.yaml`
4. `doc/audits/proposals/SCI-RTC-001_LEDGER_PROPOSAL_2026-08-08.yaml`
5. `doc/audits/handoffs/SCI-PTC-001/SCI-PTC-001-XAUD-005.yaml`
6. `doc/audits/handoffs/SCI-VAL-001/SCI-VAL-001-XAUD-007.yaml`
7. `doc/audits/handoffs/SCI-BEAM-001/SCI-BEAM-001-XAUD-003.yaml`
8. `doc/audits/packages/SCI-RTC-001_OWNER_DECISION_BRIEF_2026-08-08.md`

The PTC handoff must state RTC output sample/kernel/flag/transfer facts needed
by cleaning and weights. The VAL handoff must state replacement, flag, mask,
edge, non-finite, and eligibility facts. The BEAM handoff must state the RTC
response, detector/scan validity, and provenance facts needed by detector-map
consumers. Keep each claim bounded; if source evidence cannot support one,
write a truthful pending/limitation record rather than inventing a conclusion.
These are proposals on the audit branch; the auditor does not update the
canonical registry.

The final audit must include the frozen manifest and exposure log, independent
equations, source/equation table, product/consumer matrix, local evidence,
findings/decisions/dependencies, status axes/verdict proposal, restrictions,
incoming dispositions, outgoing handoffs, and a proposed future exact-SHA
evidence protocol only where static evidence cannot decide a question. It must
not request or execute that evidence.

Compile the LaTeX with already available offline tools, render to a temporary
PDF, and inspect readability. Do not add software. Validate YAML, links,
digests, controlled vocabulary, Git objects, diff scope, and clean state.

## Final return and stop

Return:

1. exact audit branch, commit(s), parent(s), tree(s), worktree, and clean state;
2. core path, SHA-256, freeze commit/timestamp, prior-exposure record, and first
   source/post-core opening events;
3. final report path/digest and compile/render result;
4. compact claimed operator and exact implemented operator;
5. all findings, decisions, dependencies, axes, verdict, and restrictions;
6. local evidence and any proposed—but not requested—future evidence;
7. all inbound dispositions and outgoing handoff IDs/digests;
8. exact owner-decision brief path/digest;
9. cost classification and confirmation that no broad/costly execution was
   launched; and
10. confirmation that no application code, test, build, validation, Unity,
    external, repair, re-audit, production, canonical ledger, or other package
    audit was changed or launched.

Stop for coordinator/owner review. Do not repair, re-audit, integrate, push,
contact Unity, launch PTC/VAL/BEAM, or claim production authorization.
