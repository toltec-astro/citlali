# SCI-NOI-002 Cycle 4 bounded repair prompt — 2026-08-07

Continue the existing SCI-NOI-002 repair task. This is a bounded engineering
successor repair of the three Cycle 3 re-audit P1 findings, not a new
scientific derivation, audit, application integration, Unity/local
astronomical reduction, evidence request, production change, or
count/default-selection task.

## Execution profile

- MODEL/EFFORT: current resource-balanced repair profile; increase effort only
  for a genuinely ambiguous cross-mode package-correctness issue.
- PARALLELISM: serial; no delegation or subagents.
- EXACT START: clean `codex/repair-sci-noi-002` at
  `390edf4f8c696551921c615f2439e956d240ec1d`.
- STOP RULE: Stage 1 scope checkpoint, then one bounded Cycle 4 commit and
  proportional/full local gates after coordinator continuation; stop before
  push, integration, evidence, production action, or re-audit.

## Stage 1 entry gate

1. Read `AGENTS.md`, the TolTEC context skill and routed authorities,
   `doc/REFACTOR_STATUS.md`, `doc/audits/README.md`, and
   `doc/audits/AUDIT_MANAGER_INSTRUCTIONS.md`.
2. Retrieve this prompt, the Cycle 4 coordinator disposition, the Cycle 4
   frozen authority manifest, the Cycle 4 dispatch-readiness record, and the
   three Cycle 3 re-audit artifacts from their exact supplied Git objects.
   Verify every SHA-256 before using them.
3. Verify the repair worktree is clean, the branch is
   `codex/repair-sci-noi-002`, HEAD and the locally stored origin ref are
   exactly `390edf4f8c696551921c615f2439e956d240ec1d`, and its parent is
   `63efd8b08a599d2d56a1716e3cbb2d3686d62b9f`. Stop on mismatch.
4. Inspect the current production writers, package finalizer, active Python
   auditor, and existing fixtures. Return a concise scope checkpoint before
   editing: exact authority/entry identities, finding-to-component mapping,
   proposed exact path set, fixtures/gates, prohibited scope, any required
   reduction, and confirmation that no edit/build/test/delegation/external
   action occurred.
5. Wait for coordinator continuation.

## Authorized implementation after continuation

Implement only C4-R001–R004 from
`SCI-NOI-002_CYCLE4_COORDINATOR_DISPOSITION_AND_REPAIR_HANDOFF_2026-08-07.md`:

- exact compact ECSV/NetCDF missingness with C++/Python parity;
- mode-aware successor-coadd expected/observed/member reconciliation that
  preserves the approved no-companion output policy and treats an existing
  scaled coefficient only as a standalone nonprecision diagnostic;
- package reconciliation for the existing split detector-group Beammap
  multi-map-per-array-file layout, with logical per-map grouping and unused
  split files excluded from NOI membership; and
- deterministic production-writer-to-final-package positive/negative fixtures
  for these actual shapes.

Prefer narrow changes to existing writers, `NoiseExecutionPlan`,
`noise_provenance`, the active auditor, and existing direct fixtures. Do not
create a generic publication/join framework, new product class, helper
subsystem, or alternate schema. If an exact required production path is not in
the frozen allowed list, stop before editing and report the narrow need.

## Frozen scientific and operational boundary

Preserve all estimator equations, normalization, realization generation,
sign assignment, coadd and Beammap calculations, output selection/order,
filenames/file partitioning, configuration defaults, and realization counts.
Do not add companion products to make validation pass. Do not change FLT,
SRC, FRUIT, MODE, MAP, JINC, RTC, PTC, Wiener, or auxiliary-channel behavior.
F005 remains conditioned on SCI-FLT; F006 remains held external and
SCI-FRUIT-001-owned. No Unity or astronomical evidence is needed or authorized.

## Required verification and return

Run focused compact-join, coadd, split-Beammap, package-finalization, and
C++/Python auditor fixtures; the active baseline auditor/contract/ledger test
suites; a proportional CLI and relevant C++ target build; full CTest; required
config preflight; JSON/YAML validation; `git diff --check`; and exact path and
prohibited-scope accounting. A successful run has no unexpected error-level
messages.

Commit one coherent Cycle 4 application/test repair. Return exact parent,
commit/tree, changed paths, commands/results, finding dispositions, exclusions,
performance/storage implications, and clean state. Stop before push,
integration, Unity, astronomical evidence, production action, canonical-ledger
mutation, or fresh re-audit.
