# RTC fixed-filter speed support — owner decision, 2026-09-16

The owner explicitly selects original paired measurements as numerical support
through speed-only exclusions, while retaining the existing speed exclusions
at scheduled output centers. This is a bounded correction to the RTC finite
notch/low-pass comparison, not production activation or a new scientific
contract package. Original directive and preflight are preserved at
`/private/tmp/citlali-rtc-speed-support-2026-09-16/OWNER_DIRECTIVE.md` and
`preflight.json` in that evidence directory.

## Authority and prior-work recovery

This narrowly supersedes the lower/upper-speed **complete-footprint** clause
in [the August 30 owner authority](WP7_RTC_OCCURRENCE_SPEED_ADMISSION_OWNER_AUTHORITY_2026-08-30.md),
section “Support and output validity”, for the specified fixed finite filters.
It does not supersede the occurrence-local speed thresholds, AST authority,
beam/cadence/motion margins, filter domain, paired operator, physical runs,
missing-support rules or other exclusion categories. Frozen SCI-RTC r0.12
and its x-only donor exception are not edited or broadened. The proposed
100 ms speed-gap donor bridge is superseded; no interpolation or duration
ceiling applies when the original measurements already exist.

Retain the accepted twelve-detector 152390 connection at d92cd3a51 and the
retention audit at e96a8c3e2. The latter predicts recovery of 411,104 otherwise
eligible positions, but this is an accounting target, not an acceptance cut.
Preserve the existing processing-window endpoint limitation (712 positions in
the prior audit); it is outside this repair. The r-continuity experiment at
4899a6527 is evidence only and does not authorize synthetic r here.

## Runtime responsibilities

- Learn preserves original x/r, AST motion and timing, producer validity and
  exact VAL identities. Speed evidence survives coincident non-speed causes.
- RTC Consider freezes separate original support and direct-center
  dispositions in the existing recovery plan. A low/high speed restriction
  alone ceases to block the fixed finite filters. Other restrictions, including
  the optical-design speed domain, remain support blockers.
- Apply executes the complete frozen plan afresh on original x/r. Finite
  intermediate results at excluded centers can supply the next filter. Direct
  replacement, paired availability, timing and scheduled phase remain enforced.
  Numerical output rows are not permission for map hits. The separate
  `map_center_admitted` result reports necessary restrictions and explicitly
  does not authorize a production map route.
- The conditioned native product retains numerical availability, speed
  evidence and its immutable result owner. Its existing named spectral-review
  use has a separate admission plane preserving its previous population.
  Donor fit and background support likewise retain the prior restrictions.
  Permission for fixed filtering does not authorize new adaptive fitting,
  noise, PCA or donor populations.

The runtime decision is separate from the engineering Learn/Consider/Apply
workflow used to develop and test it. No parallel framework is introduced.

## Bounded validation and retained limitations

Four diagnostic treatments hold data, coefficients, event decisions, timing,
phase, no-spatial-mask selection and direct-center rules fixed: old rejection,
low-only support, high-only support, and both. Only the last is the selected
bounded finite-filter policy; these comparisons are not production modes.

Tests cover exact cascaded support, registered output centers, speed/non-speed
overlaps, unchanged original measurements and unchanged adaptive-review use.
Paired injections run after plan freezing, including astronomical signal during
speed excursions. Fine trajectory truth uses original telescope knots and the
existing provisional uniform readout average; hardware averaging remains
unknown. Existing source, line/noise and limited naive-map diagnostics provide
measurements, not new qualification thresholds, PCA validation or production
map qualification. FRUIT stays deferred. No canonical integration, push,
cleanup, coefficient changes, natural-event admission or donor-policy changes
are authorized by this work order.

This owner-selected correction remains bounded to the named experiment until
separate production integration/qualification. Results, exact source, gates,
limitations and fresh independent review are bound in the work-order handoff
and external evidence manifest. The writer cannot self-approve the review.
