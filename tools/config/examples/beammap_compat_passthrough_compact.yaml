schema: citlali-reduction-v2
mode: beammap
profile: beammap_compat_passthrough

# Use with --base-config /path/to/70_reduce.yaml to validate compact overlays
# against an existing TolTECA beammap low-level baseline.
expert: {}
