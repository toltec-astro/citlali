schema: citlali-reduction-v2
mode: science
profile: science_compat_passthrough

# Use with --base-config /path/to/70_reduce.yaml to validate compact overlays
# against an existing TolTECA science low-level baseline.
expert: {}
